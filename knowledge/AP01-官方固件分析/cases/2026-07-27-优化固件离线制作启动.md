# 2026-07-27 AP01 优化固件离线制作启动记录

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 本次范围

本次按
[`AP01 1.0.2_0031 优化固件 DESIGN`](../../../reference/DESIGN/AP01-1.0.2_0031-opt.bin.md)
开始制作，只执行安全规范允许的静态分析、离线制作基础实现和原厂基线检查。

没有生成优化固件，没有上传文件，没有向设备下发仅下载或安装命令，也没有执行拆机、
短接、调试器连接、擦除或写入。

## 2. 制作前复核

2026-07-27 再次通过参考项目的 `mi_cloud.py firmware` 查询小米云，返回：

| 项目 | 结果 |
| --- | --- |
| 当前版本 | `1.0.2_0031` |
| 云端 MD5 | `469a7329d496f81ae8625c4d76ccf56d` |
| 与固定基线 | 一致 |

原厂文件使用仓库外只读副本：

`/Users/mac/Desktop/cuktech-ap01-firmware-artifacts/original/ap01-1.0.2_0031.bin`

该副本未进入当前制作分支或远端分支。

## 3. 首批实现

首批代码提交：`13a7a9d`。

| 模块 | 本次结果 |
| --- | --- |
| `core/firmware_image/` | 实现精确字节数、双文件指纹、文件头固定标记、型号位置、恢复标记位置、文件尾长度和恢复校验复核 |
| `features/offline_firmware_build/` | 实现只读输入、仓库外输入、修改前旧字节断言、等长修改、证据路径、修改区间、禁止区、文件尾恢复校验、写后回读和构建清单门禁 |
| `app/ap01_firmware.py` | 提供原厂基线检查和离线制作薄入口 |

离线制作入口强制要求：

1. 制作代码已经提交；
2. 小米云版本与 MD5 在本次制作前一小时内重新查询；
3. 修改清单明确批准进入离线构建；
4. 每个修改区间具有 `knowledge/` 下的直接证据；
5. 原厂输入位于仓库外且为只读；
6. 输入与输出路径不同；
7. 输出名固定为 `ap01-1.0.2_0031-opt.bin`；
8. 修改区间外逐字节一致；
9. 文件头和文件尾恢复记录结构保持不变；
10. 冻结成品写后回读的完整文件指纹一致。

## 4. 自动测试

执行：

```bash
python3 -m compileall -q app core features
python3 -m unittest discover -s . -p 'test_*.py' -v
```

结果：9 项测试全部通过。测试覆盖精确基线、错误文件指纹、修改范围越界、文件头保持、
空修改清单拒绝、旧字节断言失败、冻结成品和检查报告。

## 5. 真实原厂基线检查

真实原厂文件检查结果：

| 检查项 | 结果 |
| --- | --- |
| 设备型号 | `njcuk.enstor.ap01` |
| 固件版本 | `1.0.2_0031` |
| 文件字节数 | 6,804,520 |
| MD5 | `469a7329d496f81ae8625c4d76ccf56d` |
| SHA-256 | `8a721fc8ef25458d415b2460e4a251e0503a82f7743fdff85b12612190e5c1cb` |
| 型号位置 | `0x4d48`、`0x1c2698` |
| 文件尾恢复记录 | `0x67d400` |
| 记录长度 | 6,804,520 |
| 记录恢复校验 | `0x7ffd605b` |
| 重算恢复校验 | `0x7ffd605b` |

忽略目录中的机器可读报告为
`artifacts/build/baseline-inspection.json`，不进入代码仓库。

## 6. 当前门禁

| 阶段 | 状态 |
| --- | --- |
| 静态分析 | 原厂基线通过，可以继续 |
| 功能修改清单 | 页面、设置、事件、持久化和载荷地址仍缺直接证据 |
| 优化固件离线构建 | 未开始，禁止生成伪成品 |
| 实验成品仅下载 | 禁止 |
| 真机安装 | 禁止 |

下一步必须先定位并记录首个最小可见变化的文件偏移、运行地址、原字节、调用关系、载荷空间
和失败时原厂行为，再提交修改清单。
