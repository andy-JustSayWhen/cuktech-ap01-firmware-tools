# 2026-07-27 AP01 系统设置菜单首尾循环静态定位

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 范围与能力边界

本记录只分析官方 `1.0.2_0031` 固件中的系统设置列表、左右旋转处理和最小等长修改位置。
原厂基线身份见 [`固件制作规范`](../../../reference/固件制作规范.md) 第 4 节。

本次取得的是静态证据，不是真机运行结果。没有生成优化固件，没有上传文件，也没有向设备
发送仅下载或安装命令。

## 2. 复查方法

1. 校验原厂文件的字节数、两个完整文件指纹和文件尾恢复校验，固定值只见
   [`固件制作规范`](../../../reference/固件制作规范.md) 第 4 节；
2. 从文件偏移 `0x1000` 起按主程序指令反汇编；
3. 对设置列表创建、项目数量读取、项目查找、左右事件和选中切换的调用关系交叉复查；
4. 用 [`requirements.md`](../../../reference/requirements.md) 中固定版本的汇编与链接工具生成
   替换字节；
5. 把生成结果再次反汇编，核对每个跳转目标、调用目标、区间长度和原字节断言；
6. 核对新增执行区前后函数边界、完整零填充范围、全固件直接跳转目标和地址指针。

运行地址等于文件偏移加 `0x9ffff000`。该关系只适用于本记录的官方版本。

## 3. 原厂调用关系

| 事实 | 文件位置或运行位置 | 直接证据 |
| --- | --- | --- |
| 设置列表创建 | 运行地址 `0xa01988d4` | 创建循环固定处理 7 个原厂设置项目 |
| 左右事件处理函数 | 文件 `0x0f9096`，运行地址 `0xa00f8096` | 事件类型为 `17` 时读取按键；右为 `19`，左为 `20` |
| 系统设置列表类型 | 左右处理函数内 | 对象类型字节等于 `26` 时进入系统设置列表分支 |
| 子控件数量 | 运行地址 `0xa00c5fe4` | 返回传入界面控件的直接子控件数量；原厂左右分支传入页面根控件，用该数量遍历并查找系统设置列表 |
| 按序号取得列表对象 | 运行地址 `0xa00c5d84` | 循环查找类型为 `26` 的列表对象 |
| 实际设置列表控件 | 列表数据偏移 `+8` | 列表创建入口 `0xa01988d4` 的返回值保存在这里；原厂选择切换也把该值作为列表控件传入 |
| 选择切换 | 运行地址 `0xa00f3d5a` | 原厂相邻项目切换均传入列表对象、原序号和新序号 |
| 当前项目状态 | 列表对象数据偏移 `+1` | 值 `48` 至 `54` 分别对应序号 `0` 至 `6` |

右旋原厂逻辑依次处理 `0→1` 至 `5→6`，当前序号为 `6` 时返回。左旋原厂逻辑依次处理
`6→5` 至 `1→0`，当前序号为 `0` 时返回。部分内部相邻切换还会调用运行地址
`0xa00c6dfe` 调整原厂列表位置，因此修改方案保留全部内部相邻切换，只接管当次有效首尾。

## 4. 新执行区证据

文件 `0x01c008..0x01c100`、运行地址 `0xa001b008..0xa001b100` 是连续 248 字节零填充区：

- 前一条指令位于运行地址 `0xa001b006`，是函数返回；
- 后一函数从运行地址 `0xa001b100` 开始；
- 对完整原厂文件反汇编后，没有任何直接跳转目标落入该零填充区；
- 对完整原厂文件逐字节检查四字节地址值后，没有指向该区偶数地址的指针；
- 参考项目已经把文件 `0x01c008..0x01c010` 作为近距离跳转区，并在写入前断言 8 字节全零；
- 左右两个状态读取位置与该区互相跳转均处于处理器单条跳转允许范围内。

首次候选只使用文件 `0x01c008..0x01c07e` 的前 118 字节。文件
`0x01c07e..0x01c100` 的后 130 字节保持全零。原厂版本、完整文件指纹、前后边界或零填充
任一不匹配时，清单生成必须停止。修正版的实际使用范围见第 15 节。

## 5. 首次候选的等长修改方案

下表区间均为左闭右开。修改前后字节数完全相同。

| 名称 | 文件区间 | 运行区间 | 字节数 | 修改前 | 修改后 |
| --- | --- | --- | ---: | --- | --- |
| 首尾循环处理程序 | `0x01c008..0x01c07e` | `0xa001b008..0xa001b07e` | 118 | 118 个 `00` 字节 | `83c71400094763f4ea006fd06d0c938617fd63f4da006fd0ad0b638456016fd0cd1793070003a380f40003a589009385faff0146ef80fd516fd08d0983c71400094763f4ea006fd0ad08938607fd63e456016fd0ed0799c26fd0ed709387fa02a380f40003a5890081451386faffef805d4e6fd0ed05` |
| 右旋状态挂接 | `0x0f919e..0x0f91a2` | `0xa00f819e..0xa00f81a2` | 4 | `83c71400` | `6f20b2e6` |
| 左旋状态挂接 | `0x0f976a..0x0f976e` | `0xa00f876a..0xa00f876e` | 4 | `83c71400` | `6f20b28d` |

右旋挂接跳到运行地址 `0xa001b008`，左旋挂接跳到运行地址 `0xa001b044`。处理程序对内部
项目分别跳回原厂 `0xa00f81a2` 和 `0xa00f876e`，首尾切换调用原厂 `0xa00f3d5a`，全部
退出路径回到原厂 `0xa00f80d8`。

## 6. 首次候选的控制流

首次候选把原厂页面根控件的直接子控件数量误作设置项目数，随后执行：

1. 项目数小于 2 时直接返回；
2. 当前序号越过当次项目数时直接返回；
3. 当前序号是当次最后一项时，把状态切换到 `48`，调用原厂选择切换执行
   `项目数减一→0`；
4. 其余有效序号回到原厂分支，保留原厂相邻切换和列表位置调整。

左旋执行对称检查：

1. 项目数小于 2 时直接返回；
2. 当前序号越过当次项目数时直接返回；
3. 当前序号为 `0` 时，把状态切换到 `项目数加 47`，调用原厂选择切换执行
   `0→项目数减一`；
4. 其余有效序号回到原厂分支。

官方创建循环最多产生 7 项，因此本版本的有效项目数范围为 `0..7`。首次候选没有把最后一项
写死为序号 `6`，但读取数量时传入了错误层级的控件。第 14 节真机结果已经否定该候选。

## 7. 原厂日志保持

下列原厂日志代码区全部位于修改清单之外，并要求候选固件与原厂逐字节一致：

- 右旋通用日志：`0x0f912c..0x0f914e`；
- 右旋系统设置日志：`0x0f9172..0x0f918c`；
- 左旋通用日志：`0x0f96f8..0x0f971a`；
- 左旋系统设置日志：`0x0f973e..0x0f9758`。

其中通用日志正文
`ui setting dtl custom handler, key right`（系统设置详情处理函数收到右旋）和
`ui setting dtl custom handler, key left`（系统设置详情处理函数收到左旋）保持原厂字节。
设置功能所需的项目数量读取、对象查找、内部相邻切换和列表位置调整也全部保留。

## 8. 首次候选的修改边界

- 只修改上述 3 个主程序区间；
- 新增 118 字节版本限定处理程序，不修改图形资源、持久化文件或运行时内存分配；
- 不修改文件头、文件尾恢复记录、启动、分区、槽、联网、米家通信和官方更新路径；
- 固件总长度保持不变，构建时仍需重算文件尾恢复校验；
- 原厂左右旋转日志、其他对象类型的寄存器初始化和类型专用处理保持原厂字节；
- 静态检查不能证明界面动画、声音、焦点和长时间运行结果，真机安装仍受恢复门禁阻塞。

## 9. 首次候选的实现与静态验证

实现位于 `features/settings_menu_wrap/`：

- 汇编源文件和链接边界断言固定新增处理程序、两个挂接点、目标地址和区间长度；
- 清单生成器要求真实原厂文件只读、完整零填充区、前后边界、全部旧字节、工具版本和汇编
  结果逐项一致；
- 测试必须把 4 个日志代码区与候选字节逐字节比较；
- 待批准入口的状态固定为等待用户批准，批准入口不接收批准开关，并要求批准范围指纹匹配；
- 离线制作门禁必须确认待批准清单不能生成优化固件。

固定实现提交为 `6557e7da31be3b4687696911181d8243f627de46`。2026-07-28 的验证结果：

- 共运行 15 项自动测试，全部通过；
- 逐条核对 12 条跨区跳转或调用关系，全部与审查地址一致；
- 待批准清单位于 `artifacts/build/settings-menu-wrap-draft.json`，只包含
  `0x01c008`、`0x0f919e`、`0x0f976a` 三个起始位置；
- 清单记录的制作代码无未提交功能改动，日志变化字段为否；
- 本次只读查询小米云后，版本与文件指纹仍和固定原厂基线一致；
- 把该清单交给离线制作入口后，入口以“修改清单尚未明确批准进入离线构建，禁止生成优化
  固件”停止，固件文件和制作记录均不存在；
- 构建目录中旧方案及迁移试验留下的 12 个临时产物已删除，只保留新清单和基线检查记录。

## 10. 首次候选的批准记录

2026-07-28，用户在查看第 5 节全部 3 个精确区间、修改前后字节、作用、风险和第 7 节日志
保持范围后，明确回复“批准”。批准范围仅为第 5 节的 3 个区间进入离线构建，不包含任何新增、
删除或变化后的区间，也不允许下载或安装。

制作工具必须把本记录、目标型号、原厂版本、原厂完整文件指纹、3 个修改区间、全部修改前后
字节、代码间隙和日志保持范围绑定为批准范围。重新生成结果与批准范围完全一致时，才能形成
允许离线构建的清单；任何字段不一致时停止，并重新提交用户审批。

## 11. 首次候选的离线构建结果

批准范围绑定实现提交为 `544f2201234fa1bcb5de5e648cd3ffa8502281b4`。2026-07-28
重新只读查询小米云后，最新版本和文件校验值仍与固定原厂基线一致。随后使用该提交生成批准
清单、冻结成品和冻结构建清单：

- 成品位于被版本控制忽略的
  `artifacts/firmware/ap01-1.0.2_0031-opt.bin`，共 6,804,520 字节；
- 成品 SHA-256 为
  `f000dc91ba5cf7dd0f327b33f1d4a08bfc87bd8118965facf3765b083f0b05f2`，
  MD5 为 `fc51cd3de0b28b9132600f5a60fc38b3`；
- 文件尾恢复校验重算结果为 `0xf0e269ed`；
- 构建只包含第 5 节 3 个修改区间以及文件尾恢复校验字段，修改范围外没有意外差异；
- 四个原厂日志区与原厂文件逐字节一致，右旋和左旋挂接分别为 `6f20b2e6`、`6f20b28d`；
- 共运行 33 项自动测试，全部通过；冻结成品写后回读的完整文件指纹一致；
- 构建清单继续把实验成品仅下载和安装标记为禁止。

该结果只完成离线构建与静态验证。恢复门禁、目标槽证据和独立恢复演练仍未满足，不得上传、
仅下载或安装。

## 12. 首次设置菜单阶段成品

2026-07-28，用户要求把只包含本功能的阶段成品固定命名为 `opt-setting.bin`，并提出真机
安装。产物合同先写入 `brief.md`、`SPEC.md`、本版 DESIGN 和刷机操作规范；制作器随后改为
拒绝使用完整优化版名称生成本阶段成品。

使用提交 `b8134f56463fff8740d942e514c11cfce48e06e7` 重新查询小米云并确认原厂版本和文件
校验值未变后，从仓库外只读原厂基线重新生成：

- 成品：`artifacts/firmware/opt-setting.bin`；
- 构建清单：`artifacts/build/opt-setting-manifest.json`；
- 总字节数、两个完整文件指纹、文件尾恢复校验和实际 120 个变化字节均与第 11 节成品一致；
- 两份成品逐字节一致；
- 成品与构建清单均为只读，并由版本控制忽略；
- 34 项自动测试全部通过；
- 构建清单仍把实验成品仅下载和安装标记为禁止。

生成后只读复核设备，确认唯一匹配 AP01 在线、型号为 `njcuk.enstor.ap01`、固件版本为
`1.0.2_0031`、硬件版本为 `BL618-BL4`，在线更新状态为空闲。设备信息仍未返回当前槽、
更新槽、分区或本次写入目标。恢复工具、同机双份全闪存备份和独立恢复演练也仍未取得，因此
在上传前停止，没有让设备下载或安装本成品。

## 13. 单次真机安装结果

用户随后明确指定本次按参考项目的原厂普通在线更新路径处理写入位置，并只对本次安装跳过
其他恢复门禁。完整上传对象回读与冻结成品逐字节一致后，设备完成接受、下载、安装、重启和
重新上线，运行时间重新计数，短时观察没有立即重启循环。

设备仍报告固件版本 `1.0.2_0031`，这与本阶段保留原厂版本字段的设计一致，不能单独证明
功能是否生效。自动安装链路已经通过。

## 14. 首次真机交互结果与根因

用户在设备重启并重新上线后完成了物理交互：

- 按下、松开能够进入第一项的二级菜单；
- 系统设置一级列表左右旋转均无响应；
- 进入第一项二级菜单后左右旋转正常；
- 二级菜单保持原厂不能首尾循环的行为，用户明确要求本阶段不修改。

这组结果直接证明旋钮硬件、左右事件、按键事件和二级菜单原厂处理仍能工作，失败范围只在
系统设置一级列表的新增处理路径。

重新反汇编确认，运行地址 `0xa00c5fe4` 读取的是“传入控件的直接子控件数量”。原厂在
`0xa00f818c` 和 `0xa00f8758` 传入页面根控件，是为了遍历根控件并找到类型为 `26` 的设置
列表；保存在 `s5` 中的是根控件子控件数，不是设置列表行数。首次候选在两处挂接后首先判断
`s5 < 2`，因此会在一级设置页提前返回。这与左右旋转均无响应、其他旋钮路径正常的真机结果
一致。

原厂调用关系同时给出修正路径：

1. 设置列表创建入口 `0xa01988d4` 的返回值被保存到列表数据偏移 `+8`；
2. 该创建入口固定循环 7 次，每次把新项目直接创建在这个列表控件下；
3. 原厂选择切换也从列表数据偏移 `+8` 取得同一个列表控件；
4. 修正版在两处挂接后，从已经找到的列表数据读取 `+8`，把实际列表控件传给
   `0xa00c5fe4`，再以返回值完成数量和状态边界检查。

修正继续动态读取实际显示项目数，不写死首尾序号；只处理系统设置一级列表，二级菜单不进入
修改范围。当前安装的首次候选及其同字节本地产物均判定为不合格，不得复用、重新上传或再次
安装。修正后任何一个字节发生变化，都必须重新生成修改清单并重新取得用户批准。

## 15. 修正版待批准字节

修正版仍只使用原厂三个等长区间，下表区间均为左闭右开：

| 名称 | 文件区间 | 运行区间 | 字节数 | 修改前 | 修改后 |
| --- | --- | --- | ---: | --- | --- |
| 首尾循环处理程序 | `0x01c008..0x01c092` | `0xa001b008..0xa001b092` | 138 | 138 个 `00` 字节 | `` |
| 右旋状态挂接 | `0x0f919e..0x0f91a2` | `0xa00f819e..0xa00f81a2` | 4 | `83c71400` | `6f20b2e6` |
| 左旋状态挂接 | `0x0f976a..0x0f976e` | `0xa00f876a..0xa00f876e` | 4 | `83c71400` | `6f20528e` |

新增处理程序在左右入口分别执行：

1. 从 `s3 + 8` 读取实际设置列表控件；
2. 调用原厂 `0xa00c5fe4` 读取该列表的直接子控件数量；
3. 把返回值保存为本次项目数，再读取当前状态并执行范围检查；
4. 内部相邻项回到原厂分支，只有首尾切换调用原厂选择切换。

右旋入口仍为 `0xa001b008`；左旋入口因新增的右旋计数指令后移到 `0xa001b04e`。处理程序
使用零填充区前 138 字节，`0x01c092..0x01c100` 的后 110 字节继续保持全零。四个原厂日志
代码区、按键路径和二级菜单路径均不在修改范围。

修正版待批准清单位于
`artifacts/build/settings-menu-wrap-corrected-draft.json`，批准范围指纹为
`e61d2e2235b64172008d5e07085fbb2e89b1d0c653a6049a175860d4f28a4a6e`。当前验证结果：

- 固定工具能够逐字节复现 138 字节处理程序和两个 4 字节挂接；
- 14 条跨区调用或跳转目标均与本节地址一致；
- 4 个关键数据流位置均确认先读取 `s3 + 8`，再把原厂计数结果保存为本次项目数；
- 9 项功能专项测试全部通过；
- 原批准记录因范围指纹不一致被明确拒绝，无法生成允许离线构建的清单；
- 待批准清单的固件输出许可为否，修正版固件尚未生成。

本节三个区间是新的完整批准范围。第 5 节首次候选的批准不适用于本节。

## 16. 修正版批准

2026-07-28 13:07（北京时间），用户在收到第 15 节三个精确区间、完整修改前后字节、范围
指纹和验证结果后，明确回复“批准，生成固件并刷机”。

离线构建批准只绑定第 15 节范围指纹
`e61d2e2235b64172008d5e07085fbb2e89b1d0c653a6049a175860d4f28a4a6e`。本次安装授权只绑定：

- 由该批准范围从固定原厂基线生成的新 `opt-setting.bin`；
- 构建后取得并写入设备案例的新成品完整文件指纹；
- 当前账号内唯一匹配型号、版本仍为 `1.0.2_0031` 且在线更新状态为空闲的 AP01；
- 完整上传对象回读与冻结成品逐字节一致；
- 参考项目已经验证的原厂普通在线更新路径。

旧失败成品及其完整文件指纹不在本次授权内。生成、上传回读、设备状态、接受、下载、安装、
重启或重新上线任一环节异常时停止，不重复下发。本次授权不改变项目通用恢复门禁，也不自动
授权其他设备、其他成品或后续安装。

## 17. 修正版离线构建结果

批准记录固定在提交 `93c38534608dfb4619301a092abfe01849256df7`。构建前重新查询小米云，
官方最新版本仍为 `1.0.2_0031`，官方 MD5 仍为
`469a7329d496f81ae8625c4d76ccf56d`。随后从仓库外只读原厂基线生成：

- 成品：`artifacts/firmware/opt-setting.bin`；
- 构建清单：`artifacts/build/opt-setting-manifest.json`；
- 总字节数：6,804,520；
- SHA-256：`07263981488bfafa18d34b2922297c6bc0bf179960e8ab684ee9d8e65fe87018`；
- MD5：`b285b078b2ac67ddb37e7f30d87f945a`；
- 文件尾恢复校验：`0xf221982b`。

独立复核确认三个批准程序区间及文件尾恢复校验之外逐字节一致，实际变化 138 字节；四个原厂
日志区逐字节一致，文件头、总长度和恢复记录结构不变。34 项自动测试全部通过，成品与构建
清单均为只读。

旧失败成品及其构建清单已经原样保存在 `artifacts/rejected/`，没有删除。修正版成品尚未
上传，设备尚未收到新命令。
