# 2026-07-28 AP01 专用测试设备恢复门禁只读核对

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 范围与操作边界

用户已经明确把当前 AP01 指定为固件、原厂回刷和独立恢复演练使用的专用测试设备。本次只做
云端只读查询、本机日志匹配和本地材料盘点，没有拆机、短接、连接调试器、上传固件、仅下载
或安装。

恢复层级和证据要求只见
[`AP01 离线恢复与救砖证据`](../离线恢复与救砖证据.md)，操作顺序只见
[`AP01 分级恢复与救砖操作规程`](../../../reference/SOP/AP01-分级恢复与救砖.md)。

## 2. 设备唯一性与运行身份

2026-07-28 06:08:36（北京时间）通过当前米家账号只读查询：

| 项目 | 结果 |
| --- | --- |
| 账号内匹配型号的设备数量 | 1 |
| 在线设备数量 | 1 |
| 型号 | `njcuk.enstor.ap01` |
| 当前报告版本 | `1.0.2_0031` |
| 硬件版本 | `BL618-BL4` |
| 当次运行时间 | 396 秒 |
| 在线更新状态 | 空闲 |
| 本地恢复禁用字段 | `0` |

文档不保存设备编号、网络地址、无线网络名称、硬件网络地址或登录信息。本地恢复禁用字段只记录
设备返回值，不能据此判定厂家恢复方法、触发条件或实际可恢复范围。

## 3. 当前运行程序边界

本机画面服务日志在 2026-07-28 06:07:37 记录到一次 `/screen.gif` 请求并返回成功。程序化
比较确认请求来源与第 2 节唯一 AP01 的当前局域网地址一致，但文档不保存该地址。

这证明当前设备仍运行此前安装的实时画面加载器。该加载器保留原厂版本字符串，因此设备报告
`1.0.2_0031` 只能证明版本字段一致，不能证明当前应用槽与官方文件逐字节相同。恢复门禁中的
“原厂状态下同机回刷”尚未完成。

## 4. 本机恢复材料

| 材料 | 当前状态 |
| --- | --- |
| 原厂应用更新包 | 仓库外只读副本存在，6,804,520 字节，身份与固定基线一致 |
| 系统设置菜单最小候选固件 | 本机只读冻结成品存在，尚未上传或发送给设备 |
| 候选固件构建清单 | 本机只读冻结清单存在，继续禁止仅下载和安装 |
| 同机全闪存备份 | 未取得 |
| 第二份独立全闪存副本 | 未取得 |
| 真实分区图 | 未取得 |
| AP01 专属独立恢复记录 | 未取得 |

原厂应用更新包的固定文件值只见
[`固件制作规范`](../../../reference/固件制作规范.md) 第 4 节，候选固件的离线构建结果只见
[`系统设置菜单首尾循环静态定位`](2026-07-27-系统设置菜单首尾循环静态定位.md) 第 11 节。

## 5. 槽与恢复接口核对

原厂文件包含 `ro.boot.current_slot` 和 `ro.boot.update_slot` 字段，但当次只读设备信息和米家
属性没有返回当前槽、更新槽或下一次写入目标。字段存在不能替代真机槽值，因此目标槽门禁仍
判定失败。

本机当次检查没有发现已连接的串口适配器、芯片调试器或逻辑分析仪，也没有发现已经安装的
对应只读识别程序。用户随后确认，当前没有准备本规程所需的限流直流电源、高阻数字万用表、
逻辑分析仪与高阻探头、串口适配器或芯片调试器，以及拍摄与绝缘拆机工具。

## 6. 当前门禁

| 门禁项 | 状态 | 结论 |
| --- | --- | --- |
| 专用测试设备 | 已确认 | 可以继续准备恢复验证 |
| 设备型号、版本、在线状态 | 已只读确认 | 通过本阶段检查 |
| 当前纯原厂应用状态 | 未成立 | 当前运行设置菜单阶段固件 |
| 原厂应用更新包 | 已准备 | 只能用于受控在线原厂回刷 |
| 当前槽、更新槽、写入目标 | 未取得 | 禁止仅下载和安装 |
| AP01 硬件恢复接口 | 未取得 | 禁止猜测接线 |
| 恢复测量与拆机工具 | 当前不可用 | 停止拆机、接口识别和独立恢复演练 |
| 同机双份全闪存备份 | 未取得 | 禁止恢复写入演练 |
| 独立恢复演练 | 未执行 | 禁止优化固件安装 |

## 7. 下一步

设置菜单阶段固件继续保持本机只读冻结。若要执行后续固件安装，先补齐本规程要求的测量与拆机条件，或由
具备电子测量和芯片恢复条件的专业人员协助；在 AP01 焊盘信号和电平取得直接证据前，不选购
也不连接具体串口适配器或芯片调试器。以后增加或确定工具时，先更新
[`项目依赖清单`](../../../reference/requirements.md)。

第 9 节记录的单次用户指定路径不适用于后续操作。在工具和人员条件满足前，不拆机、不识别
接口、不执行独立恢复演练，也不执行其他原厂包或实验成品的仅下载、回刷或安装。

## 8. 设置菜单阶段成品安装前复核

2026-07-28 12:11（北京时间），`opt-setting.bin` 完成离线制作后再次只读查询。账号内仍只有
一台匹配 AP01，设备在线，型号为 `njcuk.enstor.ap01`，固件版本为 `1.0.2_0031`，硬件版本
为 `BL618-BL4`，在线更新状态为空闲。设备信息仍未返回当前槽、更新槽、分区或本次写入
目标。

用户已经提出安装该阶段成品，但第 6 节的恢复工具、同机双份全闪存备份、独立恢复演练和
目标槽门禁没有变化。安装授权不能补足这些客观证据，因此操作在上传前停止；没有上传、仅
下载或安装固件。

## 9. 用户指定的本次参考项目安装路径

用户随后明确要求本次按参考项目处理写入位置，并仅对本次安装跳过第 6 节其他恢复门禁。该
指令只绑定以下对象：

- 账号内唯一匹配型号的 AP01；
- 当前设备版本 `1.0.2_0031`；
- 设置菜单阶段成品 `opt-setting.bin`；
- 成品完整文件指纹
  `f000dc91ba5cf7dd0f327b33f1d4a08bfc87bd8118965facf3765b083f0b05f2`。

参考项目不读取、不指定当前槽或目标槽。它上传完整应用更新包，使用原厂普通在线更新模式，
由 AP01 原厂更新程序决定写入位置。本次先完整回读上传对象并与冻结成品逐字节比较，通过后
才使用同一地址下发安装；任何上传、回读、设备接受、下载、安装、重启或重新上线异常都停止
重复下发。

本次用户指定路径不修改项目通用恢复门禁，也不自动授权其他设备、其他成品或后续安装。

## 10. 本次安装结果

上传对象完整回读为 6,804,520 字节，与本机冻结的 `opt-setting.bin` 逐字节一致。随后使用
同一临时授权地址向唯一 AP01 下发原厂普通在线更新安装：

1. 设备接受安装命令；
2. 下载状态从 `0%` 连续增长，在第 128 秒进入下载完成；
3. 第 133 秒进入安装完成；
4. 第 136 秒设备暂时离线；
5. 第 144 秒重新上线，更新状态回到空闲，运行时间重新从 10 秒计数；
6. 安装后运行时间连续增长到 107 秒，期间没有立即重启循环；
7. 设备仍报告型号 `njcuk.enstor.ap01`、固件版本 `1.0.2_0031`、硬件版本 `BL618-BL4`；
8. 米家属性只读查询返回 13 项，其中 12 项成功，1 项为设备不支持的属性。

自动可验证的上传、下载、安装、重启、重新上线和短时稳定性已经通过。系统设置菜单首尾循环
仍需用户在设备上分别验证“第一项向左”和“最后一项向右”；该物理交互未验收前，不宣布本
功能真机通过。

## 11. 修正版单次安装授权

首次候选真机验收发现系统设置一级列表左右无响应后，修正版改为从实际设置列表控件读取项目
数。完整根因、精确字节和批准范围只见
[`系统设置菜单首尾循环静态定位`](2026-07-27-系统设置菜单首尾循环静态定位.md) 第 14 至
16 节。

2026-07-28 13:07（北京时间），用户明确回复“批准，生成固件并刷机”。本次继续采用第 9 节
相同的参考项目路径，但授权对象改为第 16 节批准范围生成的新 `opt-setting.bin`。生成后必须
先把新成品完整文件指纹补入本节，再上传、完整回读并逐字节比较；设备型号、版本、唯一性、
在线状态或更新状态任一不匹配时停止。

旧失败成品不在本次授权内。修正版已经从固定原厂基线生成并只读冻结：

- 成品：`artifacts/firmware/opt-setting.bin`；
- 总字节数：6,804,520；
- SHA-256：`07263981488bfafa18d34b2922297c6bc0bf179960e8ab684ee9d8e65fe87018`；
- MD5：`b285b078b2ac67ddb37e7f30d87f945a`；
- 文件尾恢复校验：`0xf221982b`。

构建前实时核对确认官方版本、官方文件校验、唯一 AP01、设备型号、设备版本、在线状态和更新
空闲状态均符合授权条件。三个批准程序区间和文件尾恢复校验之外逐字节一致，四个原厂日志区
保持不变，34 项自动测试全部通过。后续上传、回读、安装和自动稳定性结果见第 12 节。

## 12. 修正版单次安装结果

2026-07-28，修正版按第 11 节绑定的精确成品和单次授权执行：

1. 上传对象完整回读为 6,804,520 字节，与本机冻结成品逐字节一致，双方 SHA-256 均为
   `07263981488bfafa18d34b2922297c6bc0bf179960e8ab684ee9d8e65fe87018`；
2. 下发前再次确认账号内只有一台匹配 AP01，设备在线，型号、版本、硬件版本与授权记录一致，
   更新状态为空闲；
3. 设备明确接受安装命令，下载进度从 `0%` 连续增长到 `49%`，第 119 秒进入安装且进度为
   `100%`，第 124 秒报告安装完成；
4. 第 127 秒设备重新上线，运行时间从 8 秒重新计数，证明本次发生了重启；
5. 重启后的 126 秒连续检查中，运行时间依次为 36、57、78、99、120、141、161 秒，更新状态
   始终为空闲，固件版本始终为 `1.0.2_0031`；
6. 本次时间窗口内没有更新错误记录，也没有发现反复重启。

上传对象完整性、设备接受、下载、安装、重启、重新上线和短时稳定性均已通过。设备保留原厂
版本字段，因此版本号不能单独证明设置菜单功能。物理验收固定为：一级列表内部相邻移动左右
均正常、第一项向左跳到最后一项、最后一项向右跳到第一项、按下松开进入二级菜单后保持原厂
旋转行为。用户完成这些动作前，修正版功能状态仍为“待真机交互验收”。

精确修改范围、构建结果和功能验收状态只见
[`系统设置菜单首尾循环静态定位`](2026-07-27-系统设置菜单首尾循环静态定位.md) 第 15～18 节。

## 13. 当前安装版物理交互补充

用户继续操作后确认，当前安装版首尾选中切换基本可用，但首尾循环后的列表可视位置不能立即
跟随目标项；此前怀疑的方向反转更符合列表延迟跳转造成的视觉误判。根因、原厂位置参数和
首尾立即定位修正版待批准范围只见
[`系统设置菜单首尾循环静态定位`](2026-07-27-系统设置菜单首尾循环静态定位.md) 第 19、21 节。

首尾立即定位修正版当前只完成待批准清单，没有生成、上传或安装新固件。设备仍运行第 12 节记录的
已安装版。

## 14. 首尾立即定位修正版单次安装授权

2026-07-28 14:50（北京时间），用户在“生成并刷入”的精确操作范围上明确批注“批准”。
本次授权只绑定：

- 第 21 节三个精确修改区间；
- 批准范围指纹 `6b21793dd700dd1f51af9e14e3261809c092a41c2198a4aa60db36401f690b1f`；
- 固定原厂基线 `njcuk.enstor.ap01`、`1.0.2_0031`；
- 当前账号内唯一匹配 AP01；
- 由本批准范围生成并完成上传对象完整回读校验的 `opt-setting.bin`；
- 第 9 节记录的原厂普通在线更新路径。

生成后必须先把新成品完整文件指纹补入本节。官方版本、原厂基线、目标设备唯一性、设备型号、
设备版本、在线状态、更新空闲状态、上传对象完整回读或批准范围任一不一致时立即停止。本次
授权不适用于其他固件、设备或后续安装。

首尾立即定位修正版已从固定原厂基线生成并只读冻结：

- 成品：`artifacts/firmware/opt-setting.bin`；
- 总字节数：6,804,520；
- SHA-256：`24e50a65c633748f4680a59cc7e97d40582727cf27af06662fb29a6d182afa13`；
- MD5：`b585c5be48b141283e72ed254b84446a`；
- 文件尾恢复校验：`0x215dec65`；
- 构建清单 SHA-256：`b04c9233147e06b634929ad40632975f521bbc040c6b32bbcf31132d2e64ca7d`。

构建前实时核对确认官方版本与原厂文件指纹没有变化，账号内只有一台匹配 AP01；设备在线，
报告型号 `njcuk.enstor.ap01`、版本 `1.0.2_0031`、硬件版本 `BL618-BL4`，更新状态为空闲，
近 24 小时没有更新错误。新成品相对固定原厂基线共变化 161 字节，其中 157 字节位于三个
批准程序区间，4 字节为文件尾恢复校验；批准程序区间和文件尾校验之外没有差异。34 项自动
测试全部通过。

冻结成品上传后，云端对象完整回读为 6,804,520 字节，与本机冻结成品逐字节一致，双方
SHA-256 均为 `24e50a65c633748f4680a59cc7e97d40582727cf27af06662fb29a6d182afa13`。
临时地址只保存在权限为 `600` 的本机私有文件中，完整回读副本权限为只读；回读速度约为
16.07 MiB/s。完成该项校验前没有向设备下发安装命令。

随后使用同一临时地址执行本次批准的原厂普通在线更新：

1. 安装前再次确认账号内只有一台匹配 AP01，设备在线，型号和版本正确，更新状态为空闲；
2. 设备明确接受安装命令，下载进度从 `0%` 连续增长，第 118 秒进入下载完成；
3. 第 125 秒报告安装完成并重新上线，运行时间由约 6,174 秒重置为 10 秒，证明发生重启；
4. 安装后 120 秒连续检查的运行时间为 35、57、78、99、121、143、163 秒；
5. 连续检查期间更新状态始终为空闲，近 10 分钟没有更新错误，也没有再次重启迹象。

上传对象完整性、设备接受、下载、安装、重启、重新上线和短时稳定性均已通过。设备继续报告
版本 `1.0.2_0031`，因此版本号不能单独证明新功能。物理验收固定为：第一项向左后立即显示并
选中最后一项，最后一项向右后立即显示并选中第一项，两个方向都不需要再次旋转，也不播放
跨越中间条目的滚动过渡；内部相邻移动和二级菜单继续保持原厂行为。

## 15. 第 21 节成品物理验收失败

2026-07-28 15:22（北京时间），用户确认：位于第一项“屏幕常亮”时左旋，有概率最终落到
第二项“屏幕保护”。自动安装与稳定性结果继续成立，但系统设置菜单功能验收失败；当前设备
仍运行第 14 节成品，本次记录不授权任何后续生成或安装。

能够写入第二项的原厂路径、旋钮输入解码的中途方向变化处理、证据等级和待实现的 2 字节最小
方向过滤只见
[`系统设置菜单首尾循环静态定位`](2026-07-27-系统设置菜单首尾循环静态定位.md) 第 22 节。

方向过滤随后完成实现和静态验证，新的四区间待批准范围只见同一记录第 23 节。旧批准已被
工具拒绝；没有生成、上传、仅下载或安装新固件，设备状态不变。

## 16. 方向过滤修正版单次安装授权

2026-07-28 15:42（北京时间），用户在上一条回复所列“批准第 23 节四区间，生成并刷入当前
唯一 AP01”的精确范围后回复“批准”。本次授权只绑定：

- 第 23 节四个精确修改区间；
- 批准范围指纹 `cde5f35a4aec8bc914ed7454b0c9dd957bd629dd750d2528b721724dc161a7e9`；
- 固定原厂基线 `njcuk.enstor.ap01`、`1.0.2_0031`；
- 当前账号内唯一匹配 AP01；
- 由本批准范围重新生成并完成上传对象完整回读校验的 `opt-setting.bin`；
- 第 9 节记录的原厂普通在线更新路径。

官方版本、原厂基线、目标设备唯一性、设备型号、设备版本、在线状态、更新空闲状态、上传对象
完整回读或批准范围任一不一致时立即停止。本次授权不适用于其他固件、设备或后续安装。

## 17. 方向过滤修正版离线构建

2026-07-28 15:47（北京时间），重新只读查询官方版本与文件校验值，结果仍为
`1.0.2_0031`、`469a7329d496f81ae8625c4d76ccf56d`。账号内仅有一台型号匹配设备，
设备在线、报告版本 `1.0.2_0031`、更新状态空闲，近 24 小时没有更新错误记录。

批准范围绑定清单文件指纹为
`bc0d7efd49291194c36cf105c0df24d23bfa7ce51bd3a6f8485f244357b5196c`。
由固定原厂基线重新生成的冻结成品为 `artifacts/firmware/opt-setting.bin`：

| 项目 | 结果 |
| --- | --- |
| 文件长度 | `6804520` 字节 |
| 文件 SHA-256 | `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` |
| 文件 MD5 | `13a7286f4824b1ad87d9bc32f1d3d39c` |
| 文件尾恢复校验 | `0xe57875a7`，存储值与重新计算值一致 |
| 相对原厂实际变化 | 四个批准程序区间内 `159` 字节，文件尾恢复校验 `4` 字节，共 `163` 字节 |
| 范围外一致性 | 四个批准程序区间和文件尾恢复校验之外逐字节一致 |
| 自动测试 | 全项目 `36` 项通过 |
| 只读冻结 | 固件与制作记录均为只读 |

上一成品按文件指纹移入 `installed-history` 目录保留。本节尚未上传或安装；后续只有上传对象
完整回读与本节冻结成品逐字节一致，且安装前设备复核继续满足第 16 节停止条件，才能执行该次
授权。

## 18. 上传对象回读、安装与自动稳定性结果

2026-07-28 15:50～15:56（北京时间），参考项目通过原厂普通在线更新路径执行第 16 节单次
授权。安装前完成以下复核：

- 远端上传对象完整回读 `6804520` 字节，文件指纹与第 17 节冻结成品一致；
- 回读副本与冻结成品逐字节一致，下载速度约 `3.318 MB/s`；
- 账号内仍仅有一台目标型号设备，设备在线、版本为 `1.0.2_0031`、更新状态空闲；
- 安装前近 24 小时没有更新错误记录。

设备接受更新后，下载进度由 `0` 连续推进到 `100`，约第 `119` 秒进入下载完成，第 `124`
秒进入安装完成，第 `127` 秒短暂离线，第 `135` 秒重新上线且在线计时由 `3197` 降至 `10`。
这证明设备实际发生重启。重新上线后连续观察 `140` 秒，共采样 `7` 次；设备始终在线、更新
状态空闲、在线计时由 `37` 单调增加到 `172`，没有二次重启或新增更新错误。

自动安装与稳定性检查通过。在线更新接口没有提供设备闪存内容的完整回读能力，因此成品身份
证据由安装前远端对象完整回读、同一临时地址投递、设备更新状态序列和重启结果共同构成，不
写成设备端全闪存逐字节回读。临时地址和临时回读副本已删除。顶部左旋、底部右旋、列表内部
旋转和中途快速反转仍需用户在真机执行物理验收。

## 19. 物理验收通过与成品保留

2026-07-28 16:03（北京时间），用户在真机操作后确认“修复成功”。第 17、18 节对应成品的
设置一级列表旋钮功能验收通过。用户功能范围及首尾循环唯一功能定义只见
[`SPEC`](../../../reference/SPEC.md) 第 1.1 节；本节不重复定义。

验收通过固件保留为 `artifacts/firmware/opt-setting.bin`，完整文件指纹为
`348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1`，与安装成品逐字节
一致。对应制作记录为 `artifacts/build/opt-setting-manifest.json`。

## 20. AGENTS 第二次观察安装与物理结果

2026-07-28 18:10～18:16（北京时间），事件路由修正版按已记录的单次授权进入原厂普通在线
更新路径。冻结成品文件指纹为
`dfc834d8cf72a45c90f1e9dc73a9552ede840b33d53bcb1efcff5c42d1c9520e`。
远端对象完整回读 `6804520` 字节，与本机成品逐字节一致。

设备在第 124 秒下载完成、第 129 秒安装完成、第 132 秒完成重启并重新上线，运行时间从
`1272` 秒重置为 `11` 秒。随后七次采样均在线、更新状态空闲，运行时间由 `26` 秒单调增长
至 `153` 秒，没有新增更新错误。

用户真机确认 AGENTS 页面仍显示正常，但左右旋转无反应，确认键进入原厂设置页。本次物理
验收失败；该次安装授权已经消耗。

## 21. 当前对象身份路由候选

失败根因与修正约束只见
[`一级页面注册与导航静态定位`](2026-07-28-一级页面注册与导航静态定位.md) 第 4、6 节。
修正载荷不再写死 AGENTS 序号，而是取得当前对象并检查其状态标记；左右目标由实际当前序号
计算。

载荷长度为 `27480` 字节，载荷文件指纹为
`6c3d5fdc460753ba4d1e4c806be594bbde7e13be95b0d8aa8c1f261955a55fad`。按固定输入计算的
候选成品文件指纹为
`25c94c9114876b97b07eb7d41f104fdcf496abc59862a33f6964ed28f4b3a200`。该候选尚未冻结、
上传或安装，等待新的逐次授权。

## 22. 当前对象身份路由修正版安装

用户明确批准第三次刷入后，第 21 节候选完成冻结。旧的失败观察成品被同名修正版替换，没有
保留重复固件。冻结成品文件指纹为
`25c94c9114876b97b07eb7d41f104fdcf496abc59862a33f6964ed28f4b3a200`，文件尾恢复校验为
`0x709c4460`，全项目 `46` 项测试通过。

远端对象完整回读 `6804520` 字节，与本机成品逐字节一致，回读速度约 `2.441 MB/s`。设备
接受安装后，第 `126` 秒下载完成、第 `129` 秒安装完成，第 `141` 秒重新上线，运行时间由
`766` 秒重置为 `10` 秒。

重新上线后的七次采样均在线、更新状态空闲，运行时间由 `21` 秒单调增长至 `146` 秒，没有
新增更新错误。自动安装与稳定性检查通过；左右旋转和确认键仍需用户真机验收。

## 23. 第三次物理验收失败

用户真机确认当前对象身份路由修正版的左右旋转与确认键均无可见反应。自动安装与稳定性结果
继续成立，但物理交互验收失败。

静态复查确认原厂初始化只对九个写死保存的原厂页面对象设置初始横向坐标 `320`，没有遍历
直接子对象。新增 AGENTS 根对象保持默认坐标 `0`，持续覆盖底层页面；因此底层切换和详情
变化不能形成正确的可见结果。根因与修正约束只见
[`一级页面注册与导航静态定位`](2026-07-28-一级页面注册与导航静态定位.md) 第 3、6 节。

## 24. 初始横向坐标修正候选

新增载荷在 AGENTS 根对象创建成功后调用原厂横向坐标入口 `0xa00bf942`，把初始横向坐标设为
`320`，使其与九个原厂页面采用相同的隐藏起点，后续交由原厂按序号切换入口移动。

载荷长度为 `27496` 字节，载荷文件指纹为
`92f4b03fc6882d2d72fe91750eeaddc8d43245a2ddc696f1e5e9d59abe3027d0`。按固定输入计算的
候选成品文件指纹为
`da380e4069124de05632dbf52fe3d8ccddf2a2929cbd76c32110b3ce2d0a1d6d`。该候选尚未冻结、
上传或安装，等待新的逐次授权。

## 25. 初始横向坐标修正版安装

用户明确批准第四次刷入后，第 24 节候选完成冻结并替换同名失败固件。冻结成品文件指纹为
`da380e4069124de05632dbf52fe3d8ccddf2a2929cbd76c32110b3ce2d0a1d6d`，文件尾恢复校验为
`0xda152938`，全项目 `46` 项测试通过。

远端对象完整回读 `6804520` 字节，与本机成品逐字节一致，回读速度约 `3.621 MB/s`。设备
在第 `132` 秒下载完成、第 `136` 秒安装完成，第 `148` 秒重新上线，运行时间由 `951` 秒
重置为 `10` 秒。

重新上线后的七次采样均在线、更新状态空闲，运行时间由 `21` 秒单调增长至 `145` 秒，没有
新增更新错误。自动安装与稳定性检查通过；左右旋转和确认键等待用户真机验收。

## 26. 第四次物理验收失败

用户真机确认无法在一级页面中找到 AGENTS。第 25 节的自动安装与稳定性结果继续成立，但
物理交互验收失败，该次安装授权已经消耗。

根因与修正约束只见
[`一级页面注册与导航静态定位`](2026-07-28-一级页面注册与导航静态定位.md) 第 4、6 节。

## 27. 动态一级导航修正候选

载荷长度为 `27560` 字节，载荷文件指纹为
`664ea5210f2755a9555e3700371d0e4d5d7a0556a9208616aefc613450c97f9f`。按固定输入计算的
候选成品长度为 `6804520` 字节，文件指纹为
`3f113ef1f8d121dae767754ac7ab9418b81c82ac31d26ce2f213a11032b41986`，
MD5 为 `7c2eeda3debd4ccfb4df8b5c0629ada6`，文件尾恢复校验为 `0x42452fbf`。

批准记录仍为上次安装已消耗；本候选没有覆盖仓库中的观察固件，没有上传或安装，等待新的
逐次授权。

## 28. 动态一级导航修正版安装

用户明确批准第五次刷入后，第 27 节候选完成冻结并替换同名失败固件。冻结成品文件指纹为
`3f113ef1f8d121dae767754ac7ab9418b81c82ac31d26ce2f213a11032b41986`，文件尾恢复校验为
`0x42452fbf`，全项目 `46` 项测试通过。

安装前确认账号中只有一台目标型号设备，设备在线、版本为 `1.0.2_0031`、更新状态空闲，
近 24 小时没有更新错误。远端上传对象完整回读 `6804520` 字节，与本机成品逐字节一致，
回读速度约 `2.029 MiB/s`。

设备接受安装后，第 `122` 秒下载完成，第 `127` 秒安装完成，第 `130` 秒重新上线，运行时间
由 `1477` 秒重置为 `8` 秒。重新上线后的七次采样均为更新空闲，运行时间由 `29` 秒单调增长
至 `154` 秒，没有新增更新错误。自动安装与稳定性检查通过；一级页面左右旋转、AGENTS 可达性、
AGENTS 概览确认键和详情切换等待用户真机验收。

## 29. 第五次物理验收结果

用户通过真机操作和五张照片确认：AGENTS 一级页面可达，概览确认键进入周剩余额度详情，
详情左右旋转正常，详情确认键返回概览，一级页面左右旋转正常。

同次验收发现一级循环错误出现原厂 C1、C2 条件页。根因与修正序列只见
[`一级页面注册与导航静态定位`](2026-07-28-一级页面注册与导航静态定位.md) 第 2、4 节。
因此第 28 节成品的 AGENTS 交互通过，但整体物理验收不通过。
