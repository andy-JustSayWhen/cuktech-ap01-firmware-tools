# 2026-07-29 AGENTS 九对象覆盖层静态设计

## 1. 目的

为 AGENTS 看板保留独立一级导航位置，同时不改变原厂一级窗口容器的九个直接子对象。

## 2. 直接证据

1. 原厂萌宠根对象位于一级窗口序号 7，原厂米家详情位于序号 8；
2. 参考项目已经通过序号 7 取得萌宠根对象，并沿对象关系取得其图片控件；
3. 两份增加第十个一级直接子对象的观察成品均出现原厂确认操作回归；
4. 回刷 `opt-setting.bin` 后，用户确认原厂交互恢复正常。

## 3. 静态方案

AGENTS 根对象改为萌宠根对象的 320×240 子覆盖层。覆盖层初始横向坐标为 320，进入
AGENTS 虚拟导航状态时移到 0，离开时移回 320。萌宠原有对象、图片和状态不修改。

导航包装仅在覆盖层固定标记有效且虚拟显示状态开启时处理 AGENTS 事件；其他事件原样交回
原厂。功率页左旋和萌宠页右旋进入虚拟状态，虚拟状态左旋回到萌宠，右旋进入功率。

## 4. 失败关闭边界

- 找不到萌宠根对象时不创建覆盖层；
- 创建、状态申请或图片创建失败时清理已创建对象；
- 找不到覆盖层或状态标记不符时不接管任何原厂事件；
- 不读取或改写一级窗口容器偏移 52；
- 不在载荷中保存可写全局状态。

## 5. 关联

- 技术实现唯一原文：[`AP01 1.0.2_0031 优化固件 DESIGN`](../../../reference/DESIGN/AP01-1.0.2_0031-opt.bin.md#108-九对象覆盖层观察成品)
- 前序失败证据：[`新增对象导致原厂确认键越界回归`](2026-07-29-新增对象导致原厂确认键越界回归.md)

## 6. 构建结果

- 成品：`artifacts/firmware/ap01-1.0.2_0031-agents-pet-overlay-observation.bin`
- 输入：已验收的 `opt-setting.bin`
- 载荷：32,420 字节，剩余 28,514 字节
- 单函数最大静态栈：304 字节
- 成品 SHA-256：`e5e1f39db1491307f5639e993c6d6507fe46ee91f5799adbcd19c04464e0e909`
- 恢复校验：存储值与计算值均为 `0xd8d65661`
- 离线回归：62 项通过
- 当前状态：尚未安装
