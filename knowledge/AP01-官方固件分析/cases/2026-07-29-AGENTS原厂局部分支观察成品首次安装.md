# 2026-07-29 AGENTS 原厂局部分支观察成品首次安装

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

技术实现只见
[`AP01 1.0.2_0031 优化固件 DESIGN`](../../../reference/DESIGN/AP01-1.0.2_0031-opt.bin.md)
第 10.13 节。

## 1. 安装对象

| 项目 | 结果 |
| --- | --- |
| 设备 | 唯一在线的 `njcuk.enstor.ap01`，地址 `192.168.31.231` |
| 安装前状态 | `opt-setting.bin`，版本 `1.0.2_0031`，更新状态为空闲 |
| 新固件 | `artifacts/firmware/ap01-1.0.2_0031-agents-stock-local-branches-observation.bin` |
| 文件大小 | 6,804,520 字节 |
| SHA-256 | `1958bf4796635684c5e1b004472665daf11404faab2f204184169b7138c4dc48` |
| MD5 | `a6935dac9d0d8fcc2dd320a0e5391636` |
| 本机数据服务 | `192.168.31.139:18765` |

## 2. 离线与传输门禁

1. 64 项自动测试通过；
2. 两个全局回调地址、功率确认区间和四个局部目标以外的原厂键值回调逐字节不变；
3. 小米云对象完整回读为 6,804,520 字节，速度为每秒 3,199,961 字节，文件指纹与本机冻结
   成品一致，逐字节比较通过；
4. 设备接受仅下载校验，进度达到 100；运行计时从 1,186 秒增长到 1,315 秒；
5. 仅下载结束与正式安装开始之间设备运行计时归零，本记录只保留该事实，不据此判断当时
   激活了哪个镜像。

## 3. 正式安装自动验收

1. 正式安装依次报告下载进度 0、下载完成、安装完成和进度 101；
2. 安装阶段运行计时增长到 134 秒，随后归零到 9 秒；
3. 设备重新上线，型号、地址和版本保持一致，更新状态回到空闲；
4. 安装后运行计时从 21 秒增长到 27 秒，近十五分钟更新错误为空；
5. 局域网三次探测无丢包；
6. 本机数据服务请求数从 11 增至 13，刷新状态正常且错误为空。

## 4. 真机验收边界

自动构建、传输、安装、重启、重新上线和主动取数已经通过。物理验收必须按以下顺序执行：

1. 功率一级页按下并松开；
2. 日历详情返回；
3. 设置菜单返回；
4. 原厂萌宠确认；
5. AGENTS 左右切换、进入详情和返回。

当前等待第 1 项结果。任一异常立即回刷 SHA-256 为
`348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` 的
`opt-setting.bin`。
