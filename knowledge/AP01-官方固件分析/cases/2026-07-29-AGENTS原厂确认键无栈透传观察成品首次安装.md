# 2026-07-29 AGENTS 原厂确认键无栈透传观察成品首次安装

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

技术实现只见
[`AP01 1.0.2_0031 优化固件 DESIGN`](../../../reference/DESIGN/AP01-1.0.2_0031-opt.bin.md)
第 10.12 节。故障证据只见
[`功率页确认键重启回归定位`](2026-07-29-功率页确认键重启回归定位.md)。

## 1. 安装对象

| 项目 | 结果 |
| --- | --- |
| 设备 | 唯一在线的 `njcuk.enstor.ap01`，地址 `192.168.31.231` |
| 安装前状态 | 已回刷 `opt-setting.bin`，版本 `1.0.2_0031`，更新状态为空闲 |
| 新固件 | `artifacts/firmware/ap01-1.0.2_0031-agents-stock-enter-gate-observation.bin` |
| 文件大小 | 6,804,520 字节 |
| SHA-256 | `dd3ded32015e722464e434ca89e19bff3cfc47b585b4a2ce73a6d9a9ff95a036` |
| MD5 | `16a3d579ac76db401c09e3d5ba7ed3eb` |
| 本机数据服务 | `192.168.31.139:18765` |

## 2. 离线与传输门禁

1. 64 项自动测试通过；
2. 无栈入口、完整包装入口、两指令原厂跳板和 27 组交互路由均通过反汇编门禁；
3. 小米云对象完整回读为 6,804,520 字节，文件指纹与本机冻结成品一致，逐字节比较通过；
4. 设备接受仅下载校验，进度达到 100，期间没有更新错误；
5. 仅下载结束后设备运行计时由 976 秒归零到 8 秒，本记录不据此推断新固件是否已经激活。

## 3. 正式安装自动验收

1. 正式安装从下载进度 0 开始，依次报告下载完成和安装完成；
2. 安装阶段运行计时由 24 秒增长到 148 秒，随后归零到 9 秒；
3. 设备重新上线，型号、地址和版本保持一致，更新状态回到空闲；
4. 近十五分钟更新错误为空，局域网三次探测无丢包；
5. 本机数据服务请求数由 6 增至 8，刷新状态正常且错误为空。

## 4. 当前验收边界

自动构建、传输、安装、重启、重新上线和主动取数已经通过。功率一级页确认键是否不再触发
整机重启，仍以本次安装后的物理旋钮操作和运行计时连续增长为准。
