# 2026-07-29 AGENTS 原厂精确调用链观察成品首次安装

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

技术实现只见
[`AP01 1.0.2_0031 优化固件 DESIGN`](../../../reference/DESIGN/AP01-1.0.2_0031-opt.bin.md)
第 10.11 节。

## 1. 安装对象

| 项目 | 结果 |
| --- | --- |
| 设备 | 酷态科桌面充电站显示屏，型号 `njcuk.enstor.ap01` |
| 安装前状态 | 唯一目标设备在线，地址 `192.168.31.231`，版本 `1.0.2_0031`，更新状态为空闲 |
| 安装前固件 | `artifacts/firmware/opt-setting.bin` |
| 新固件 | `artifacts/firmware/ap01-1.0.2_0031-agents-stock-callchain-observation.bin` |
| 文件大小 | 6,804,520 字节 |
| SHA-256 | `e57b8d7a91d69b038004cb8ac5d1620bd0351248c46333d329ceac3e446be40d` |
| MD5 | `20ab4fe1c80720d09c090c4b545223d2` |
| 本机数据服务 | `192.168.31.139:18765` |

## 2. 上传与仅下载校验

1. 小米云上传对象完整回读为 6,804,520 字节，文件指纹与本地冻结成品一致，逐字节比较通过；
2. 设备接受仅下载指令，状态从下载中变为已下载，进度由 0 到 100；
3. 仅下载结束时运行计时为 5943 秒，未下发安装指令；
4. 正式安装命令开始时运行计时为 11 秒，证明两条命令之间设备发生过一次重启；本次记录不
   推断该次重启的内部原因，也不把它作为新固件已激活的证据。

## 3. 正式安装自动验收

1. 设备接受正式安装指令，下载进度达到 100，并依次报告安装中和安装完成；
2. 安装完成后运行计时由 140 秒归零到 7 秒；
3. 设备重新上线，型号仍为 `njcuk.enstor.ap01`，地址仍为 `192.168.31.231`，版本仍为
   `1.0.2_0031`；
4. 更新状态回到空闲，近 10 分钟更新错误记录为空；
5. 本机数据服务请求数由 0 增至 2，刷新状态正常且错误为空。

## 4. 当前验收边界

自动安装、重启、重新上线和主动取数已经通过。系统设置菜单“返回”、日历详情返回、
AGENTS 一级切换和详情交互仍须在设备上完成物理验收；物理验收前不形成完整功能通过或
发布结论。
