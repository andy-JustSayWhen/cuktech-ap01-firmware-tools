# 2026-07-31 FW-AGENTS-008 重新安装

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 固定成品

| 项目 | 结果 |
| --- | --- |
| 成品文件 | `artifacts/firmware/ap01-1.0.2_0031-agents-local-ui-stock-resume.bin` |
| 长度 | 6,804,520 字节 |
| SHA-256 | `8c5641c57eb5ade918450b535f11d8ab5f2a0d4527d9d7015c43a5fbc069ff6a` |
| MD5 | `ae4f9b764a5afd0908d9fe292739b91a` |
| 冻结提交 | `a5c11f2b272a5a1e9a56d12e7ec7c3794c9a62d5` |

## 2. 安装前恢复与工具复核

1. 用户完成人工断电重启后，账号内恰好一台目标设备在线；设备自报型号与版本匹配，更新
   状态为空闲，运行时间从 128 秒重新计数；
2. 用户远端参考项目 `main` 与本地提交
   `56b64082b1aed9404856fcb940ccde6fb7fd3d34` 一致；
3. 参考项目的二维码登录与刷机两个测试文件共 8 项通过；
4. 冻结成品保持只读，长度和两个完整文件指纹与第 1 节一致。

## 3. 上传对象全量回读

直连上传超过三分钟仍未返回，已终止该次云端上传进程；该进程没有向设备下发安装。随后不
切换用户当前代理配置，只复用本机正在监听的 7897 端口重新上传同一冻结成品。

代理上传成功后，从小米文件存储取得官方兼容临时地址并全量回读 6,804,520 字节。回读速度
为每秒 3,044,932 字节；回读文件与冻结成品逐字节一致，SHA-256 与 MD5 均与第 1 节相同。

## 4. 安装、重启与重新上线

使用第 3 节同一个已回读对象，只向账号内唯一目标设备下发一次安装。可观察状态依次为：

1. 更新状态进入下载，进度从 0 连续增长；
2. 116 秒时进入安装中，进度为 100，运行时间为 773 秒；
3. 121 秒时进入已安装，进度为 100，运行时间为 777 秒；
4. 124 秒时进度字段恢复为 101，运行时间重置为 8 秒；
5. 设备重新响应云端调用，安装程序判定已经完成安装、重启并重新上线。

因此，`FW-AGENTS-008` 已刷入指定测试设备。该结论只覆盖上传对象、安装、重启和重新上线，
不替代启动画面、功率确认、功率详情返回及其余物理旋钮验收。下一步必须立即由用户肉眼确认
设备已经进入一级页面；通过后先只测试功率确认，失败时停止其他页面测试。

## 5. 启动画面真机结果

2026-08-01，用户肉眼确认设备已经离开开机动画并进入一级页面，默认显示时钟页。至此，
`FW-AGENTS-008` 的安装后启动画面验收通过。

该结果不证明功率确认已经恢复。下一步只执行：从当前一级页面切到功率页，按下一次旋钮，
观察是否进入功率详情；在结果返回前不继续测试其他页面。
