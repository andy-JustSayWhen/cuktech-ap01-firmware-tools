# 2026-07-31 FW-AGENTS-008 首次安装未完成

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 固定输入

| 项目 | 结果 |
| --- | --- |
| 成品文件 | `artifacts/firmware/ap01-1.0.2_0031-agents-local-ui-stock-resume.bin` |
| 长度 | 6,804,520 字节 |
| SHA-256 | `8c5641c57eb5ade918450b535f11d8ab5f2a0d4527d9d7015c43a5fbc069ff6a` |
| MD5 | `ae4f9b764a5afd0908d9fe292739b91a` |
| 冻结提交 | `a5c11f2b272a5a1e9a56d12e7ec7c3794c9a62d5` |

## 2. 上传与下发结果

1. 上传对象完整回读 6,804,520 字节；回读文件与冻结成品的两个完整文件指纹一致，逐字节
   比较通过。
2. 指定测试设备接受一次安装命令，初始更新状态为下载中、进度为 0。
3. 后续更新状态和进度先后返回 0 与空值，没有出现下载完成、安装、运行计时重置或重新上线
   证据；本次时间窗内也没有取得更新错误事件。
4. 安装程序结束后，连续 10 次设备调用均返回空值。设备列表仍标记在线，局域网地址仍能响应
   标准 32 字节发现包，但发现包时间值约 2,745 秒，与下发前运行计时 1,831 秒连续增长，
   证明设备没有因本次动作重启。

## 3. 当前结论与停止条件

`FW-AGENTS-008` 没有取得下载完成、安装或重启证据，不能称为已经刷入。当前设备仍按
`FW-AGENTS-007` 处理；其界面已卡住、云端设备调用不响应，但局域网只读发现仍有回复。

在设备恢复云端调用前，不重复上传、不重复下发安装，也不尝试未经验证的本地写命令。下一步
需要先对指定设备执行一次人工断电重启，恢复后重新读取运行计时、更新状态和当前页面；只有
设备重新响应，才能重新上传同一冻结成品并从全量回读门禁开始一次新的安装动作。

## 4. 人工重启后的恢复证据

2026-07-31 20:52（北京时间），用户确认已经完成人工重启。随后只读复核得到：

1. 当前账号内恰好一台 `njcuk.enstor.ap01`，设备在线；
2. 设备自报型号为 `njcuk.enstor.ap01`、版本为 `1.0.2_0031`；
3. 更新状态为 `idle`，进度字段为 `101`；
4. 运行时间为 128 秒，已经从人工重启后重新计数；
5. 冻结成品仍为 6,804,520 字节，SHA-256 与 MD5 均与第 1 节一致；
6. 参考项目两个刷机前自动测试共 8 项通过，用户远端 `main` 与本地提交
   `56b64082b1aed9404856fcb940ccde6fb7fd3d34` 一致。

本次恢复只解除“云端调用未恢复”的停止条件，不代表 `FW-AGENTS-008` 已经刷入。下一动作仍
必须重新上传同一冻结成品、完整回读并逐字节比较；全部通过后只下发一次安装。
