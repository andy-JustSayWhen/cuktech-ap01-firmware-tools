# 2026-08-01 FW-AGENTS-009 回退到 opt-setting

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 回退原因与对象

`FW-AGENTS-009` 真机物理导航仍找不到功率页，因此停止其他验收并回退到已
通过物理旋钮验收的设置阶段成品：

| 项目 | 结果 |
| --- | --- |
| 回退文件 | `artifacts/firmware/opt-setting.bin` |
| 长度 | `6,804,520` 字节 |
| SHA-256 | `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` |
| MD5 | `13a7286f4824b1ad87d9bc32f1d3d39c` |

## 2. 门禁与完整回读

- 全项目 `87` 项自动测试通过；
- 参考项目授权与安装 `8` 项测试通过；
- 参考项目本地提交与用户远端 `main` 一致；
- 复用当前正在监听的 `7897` 端口上传，不切换网络配置；
- 云端完整回读 `6,804,520` 字节，每秒 `2,336,834` 字节；
- 回读文件与本机回退文件逐字节一致，两个文件指纹也一致。

## 3. 安装、重启与重新上线

只向账号内唯一目标设备下发一次回退安装：

1. 设备接受安装指令，下载进度连续增长；
2. `115` 秒时进入已下载状态；
3. `120` 秒时进入已安装，进度为 `100`；
4. `123` 秒时运行时间从 `1,251` 秒重置到 `10` 秒，设备重启并重新上线；
5. 紧接着的一次只读核对确认设备在线、更新空闲、运行时间为 `23`
   秒，自报版本为 `1.0.2_0031`。

回退已完成。当前只等待用户在该对照成品上确认功率页是否恢复；在取得该结果前
不构建、不上传、不安装下一个 AGENTS 成品。
