# 2026-08-01 FW-AGENTS-009 安装

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 安装对象

| 项目 | 结果 |
| --- | --- |
| 成品 | `artifacts/firmware/ap01-1.0.2_0031-agents-local-ui-stock-safe.bin` |
| 长度 | `6,804,520` 字节 |
| SHA-256 | `6cfd3036eec22237a1bc91f11996ec6f28d098c8822be19c6c9ecf3f10d99038` |
| MD5 | `179f43636d39ae330e60eeaed357f1b6` |
| 冻结提交 | `6014ead` |

离线构建和模拟证据只见
[`FW-AGENTS-009 离线构建`](2026-08-01-FW-AGENTS-009离线构建.md)。

## 2. 安装前门禁

1. 用户远端参考项目 `main` 与本地提交
   `56b64082b1aed9404856fcb940ccde6fb7fd3d34` 一致；
2. 二维码登录和固件安装共 `8` 项测试通过；
3. 账号内恰好一台 `njcuk.enstor.ap01` 在线，设备自报版本为
   `1.0.2_0031`，更新状态为空闲；
4. 本次冻结成品与离线构建记录的长度和两个完整文件指纹一致。

## 3. 上传和完整回读

不切换用户网络配置，只复用当前正在监听的 `7897` 端口上传冻结
成品。随后从同一云端对象完整回读 `6,804,520` 字节，回读速度为
每秒 `3,096,813` 字节。回读文件与冻结成品逐字节一致，两个文件指纹也与第
1 节一致。

## 4. 安装、重启与重新上线

只向账号内唯一目标设备下发一次正式安装，可观察状态依次为：

1. 设备接受安装指令，进入下载，进度从 `0` 连续增长；
2. `118` 秒时下载完成，进度为 `100`；
3. `122` 秒时进入已安装，进度为 `100`；
4. `125` 秒时运行时间从 `6246` 秒重置为 `8` 秒，设备完成重启并
   重新响应云端调用；
5. 紧接着的一次只读核对确认设备在线、更新空闲、运行时间为 `19`
   秒，版本仍为 `1.0.2_0031`。

因此，`FW-AGENTS-009` 已完成上传、完整回读、一次安装、重启和重新
上线。该结论不替代屏幕、功率页确认和 AGENTS 双向导航的物理验收。
