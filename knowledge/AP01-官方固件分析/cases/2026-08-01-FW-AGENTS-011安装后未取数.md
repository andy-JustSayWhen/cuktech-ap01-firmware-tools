# 2026-08-01 FW-AGENTS-011 安装后未取数

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 安装证据

安装对象及离线证据只见
[`FW-AGENTS-011 离线构建`](2026-08-01-FW-AGENTS-011离线构建.md)。安装前重新查询小米
云，官方最新版本仍为 `1.0.2_0031`，账号中恰好一台目标设备在线、自报版本匹配且更新空闲。

上传后从官方兼容分发地址完整回读 `6,804,520` 字节，耗时 `2.695` 秒，速度
`2,524,531` 字节每秒。回读 SHA-256 为
`660842121da4464667a5253dedad50db92b2dfc9c761d053ba3a35c8f84aa7dd`，MD5 为
`3f0875de0aa64022a4decc86fcf377ad`，与冻结成品逐字节一致。

只下发一次安装：

1. `0` 秒进入下载；
2. `122` 秒下载完成，进度为 `100`；
3. `127` 秒显示已安装；
4. `130` 秒运行时间由 `4,201` 秒重新计为 `8` 秒，设备重新上线；
5. 紧接着核对设备在线、自报 `1.0.2_0031` 且更新空闲。

## 2. 真实数据失败证据

安装前服务请求计数为 `1`，该请求来自本机局域网验证。安装后设备运行时间达到 `369` 秒，
已经超过固件固定的首个 `300` 秒周期；服务请求计数仍为 `1`，最后请求时间没有变化，服务日志
也没有来自 AP01 地址的新 `/a` 请求。四个数据源始终健康，因此失败边界在设备端后台请求没有
到达服务，不在数据采集或四页生成。

该结果否定 `FW-AGENTS-011`，禁止再次安装。安装、重启和在线只证明更新通道成功，不得写成
真实数据功能成功。

## 3. DESIGN 纠偏

`FW-AGENTS-011` 没有完整复用参考项目的成功修改：两个天气格式区用了扩展格式，而且保持原厂
界面定时回调地址不变。下一版 `FW-AGENTS-012` 必须完整复用参考项目：

1. 两个天气格式区都只写 `%s`；
2. 把原厂界面定时回调地址改到 AGENTS 包装入口；
3. 包装入口必须先调用原厂定时回调，再只在共享页面序号、萌宠对象和 AGENTS 状态都有效时
   应用已经提交的四页结果；
4. 保留 `FW-AGENTS-010` 的功率确认保护和全部旋钮局部分支；
5. 使用新文件名和新清单类型，禁止覆盖或混淆失败成品。

上述两处作为一个已成功参考方案整体复用；当前设备没有设备端日志，不能把其中单独一处写成
已证实根因。`FW-AGENTS-012` 仍需重新经过文档、代码、测试、两次构建、全量回读和单次安装。
