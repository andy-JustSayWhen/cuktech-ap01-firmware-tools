# 2026-08-01 FW-AGENTS-012 安装后未取数

## 1. 安装对象

| 项目 | 结果 |
| --- | --- |
| 固件 | `ap01-1.0.2_0031-agents-live-data-reference-complete.bin` |
| 方案 | `FW-AGENTS-012` |
| 字节数 | `6,804,520` |
| SHA-256 | `e9ab51d573b17299a35c7817f0c91ece0d1fc5242c72890db531117c8c4a2e29` |
| MD5 | `df7a461a5fbc8ca743fc7def6fab70a4` |

云端对象完整回读为 `6,804,520` 字节，耗时 `1.696675` 秒，回读文件与冻结成品逐字节一致。
设备只接收一次安装命令。

## 2. 安装时间线

| 相对时间 | 状态 | 进度 | 运行时间 |
| ---: | --- | ---: | ---: |
| `0` 秒 | 下载中 | `0` | `813` 秒 |
| `121` 秒 | 下载完成 | `50` | `935` 秒 |
| `125` 秒 | 已安装 | `100` | `938` 秒 |
| `128` 秒 | 更新字段复位 | `101` | `10` 秒 |

运行时间归零、设备重新在线且更新状态恢复空闲，证明本次冻结成品已完成安装和重启。

## 3. 固定取数门禁

安装前 home02 服务健康结果中的请求计数为 `1`。设备重启后按 DESIGN 只等待一个固定
`300` 秒周期；设备运行到 `328` 秒时：

- 服务仍健康，四个数据源均可用；
- 请求计数仍为 `1`；
- 最后请求时间没有变化；
- 服务日志没有来自 AP01 地址 `192.168.31.231` 的 `/a` 请求。

因此 `FW-AGENTS-012` 没有接通设备真实取数，必须否定，不能增加等待时间或再次安装。

## 4. 局域网排除证据

设备云端信息报告地址为 `192.168.31.231`，电脑地址为 `192.168.31.174`。安装后的只读与
受控探测得到：

- AP01 对本地 `54321` 端口单播和广播均有响应；
- 电脑取得目标设备的局域网地址映射；
- 通过小米云要求 AP01 向电脑发送一个 `64` 字节数据包，电脑实际收到来自
  `192.168.31.231` 的一个数据包；
- macOS 入站防火墙关闭，服务监听全部本机地址的 `18765` 端口。

这些证据证明当次 AP01 到电脑的局域网出站路径可用。服务日志中
`192.168.31.174 GET /a` 是电脑访问自身，不是设备取数证据。

## 5. 与成功参考实现的剩余差异

重新使用参考项目当前代码，从相同官方版本、相同局域网地址和相同刷新周期生成传输对照：

- 两个请求格式、网络调用跳板、成功周期和失败重试修改与 `FW-AGENTS-012` 同义；
- 参考实现的后台包装入口静态栈为 `96` 字节；
- `FW-AGENTS-012` 的后台包装入口静态栈为 `208` 字节，其中四页下载状态为 `136` 字节。

当前没有设备端异常地址或任务栈余量记录，不能把栈差异写成已经证实的唯一根因。下一方案
只允许先把四页下载状态移出原厂天气任务栈，保持服务协议、页面交互和原厂调用点不变，再用
同一取数门禁验证该差异。

