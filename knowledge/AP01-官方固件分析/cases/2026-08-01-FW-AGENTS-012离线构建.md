# 2026-08-01 FW-AGENTS-012 离线构建

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 构建身份

| 项目 | 结果 |
| --- | --- |
| 制作提交 | `d8d463cdc53ef47c1c2fbb3d0bd8d9ae28d05128` |
| 远端分支 | `origin/opt-bin` 与制作提交一致 |
| 阶段输入 SHA-256 | `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` |
| 输出文件 | `artifacts/firmware/ap01-1.0.2_0031-agents-live-data-reference-complete.bin` |
| 输出长度 | `6,804,520` 字节 |
| 输出 SHA-256 | `e9ab51d573b17299a35c7817f0c91ece0d1fc5242c72890db531117c8c4a2e29` |
| 输出 MD5 | `df7a461a5fbc8ca743fc7def6fab70a4` |
| 文件尾恢复校验 | `0xed98fb8c`，保存值与重算值一致 |

构建清单位于 `artifacts/build/agents-live-data-reference-complete/manifest.json`，独立刷前模拟报告
位于同目录 `interaction-simulation.json`。

## 2. 相对失败版的唯一纠偏

本版完整复用参考项目的成功路径：

1. 两个原厂天气格式区的有效内容均严格为 `%s`；
2. 原厂界面定时回调地址改到 `ap01_agents_ui_timer_wrapper`；
3. 反汇编证明包装入口先调用原厂界面定时回调，再检查共享序号、萌宠对象和 AGENTS 状态；
4. 保留 `FW-AGENTS-010` 的三个萌宠局部分支、共享序号过滤和功率确认保护；
5. 失败的 `FW-AGENTS-011` 文件名由构建器直接拒绝。

载荷为 `30,772` 字节，固定空间剩余 `30,162` 字节，单函数最大静态栈为 `208` 字节。

## 3. 验证结果

1. 全项目 `90` 项自动测试全部通过；
2. 独立模拟合同为 `FW-AGENTS-012`，覆盖 `18` 个定向场景、`110` 个轨迹步骤和深度 5 的
   `4,356` 条穷举序列，失败为 `0`；
3. 两个独立目录构建的成品逐字节一致，两个完整文件指纹相同；
4. 两个天气格式区、界面定时包装、后台传输、功率确认保护和共享序号过滤同时通过门禁；
5. 一级键值总回调与页面注册保持阶段输入原字节；所有差异都在允许区间内；
6. 冻结成品和清单只读。

## 4. 下一步

重新核对小米云最新版本、唯一设备在线和更新空闲；上传同一只读成品并全量回读。逐字节一致后
自动安装一次。安装后等待一个固定 300 秒取包周期；服务请求计数没有增加即否定，不追加等待。
