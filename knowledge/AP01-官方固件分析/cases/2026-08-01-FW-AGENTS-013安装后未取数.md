# 2026-08-01 FW-AGENTS-013 安装后未取数

## 1. 安装对象与回读

| 项目 | 结果 |
| --- | --- |
| 固件 | `ap01-1.0.2_0031-agents-live-data-low-stack.bin` |
| 方案 | `FW-AGENTS-013` |
| 字节数 | `6,804,520` |
| SHA-256 | `e17f491f909ee4a77436b7cd83cc233f45056316d4cca546a7286995509e82e9` |
| MD5 | `02e9b1cd1920e4a97cce23c9bf5e4d5e` |

云端全量回读耗时 `2.384697` 秒，回读文件与冻结成品逐字节一致。账号内恰好一台目标
AP01，设备在线、自报版本与官方当前版本均为 `1.0.2_0031`，安装前更新空闲。

## 2. 安装时间线

| 相对时间 | 状态 | 进度 | 运行时间 |
| ---: | --- | ---: | ---: |
| `0` 秒 | 下载中 | `0` | `1,360` 秒 |
| `118` 秒 | 下载完成 | `50` | `1,477` 秒 |
| `124` 秒 | 已安装 | `100` | `1,482` 秒 |
| `126` 秒 | 更新字段复位 | `101` | `10` 秒 |

设备只接收一次安装命令。运行时间归零、重新在线且更新状态恢复空闲，安装证据成立。

## 3. 固定取数门禁

安装前人工核对后的服务请求计数为 `2`。设备重启后只等待一个固定 `300` 秒周期；设备运行
到 `307` 秒时：

- 服务健康且四个数据源可用；
- 请求计数仍为 `2`，最后请求时间未变化；
- 服务日志没有来自 AP01 地址 `192.168.31.231` 的 `/a` 请求；
- 设备在线、更新空闲且没有再次重启。

因此 `FW-AGENTS-013` 没有接通设备取数，必须否定并禁止再次安装。后台包装入口静态栈已经
从 `208` 字节降到 `64` 字节，结果仍相同，所以当前证据否定“只降低该函数静态栈即可恢复
请求”，但没有设备端异常记录，仍不能指定唯一根因。

## 4. 下一隔离边界

第 10.22 节已经复用参考项目的请求格式和调用点，第 10.23 节又排除了当前静态栈差异。下一步
不再继续改四页解析器；改为从参考项目当前远端提交直接生成其单页实时加载器，在同一设备、
同一电脑和独立端口上验证原厂天气任务是否实际发起请求。该探针不承担 AGENTS 页面功能，
只隔离传输触发路径。

