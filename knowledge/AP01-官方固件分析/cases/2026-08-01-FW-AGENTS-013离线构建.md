# 2026-08-01 FW-AGENTS-013 离线构建

## 1. 冻结结果

| 项目 | 结果 |
| --- | --- |
| 制作提交 | `71137f51d579412597a19bafb4972e0122bfaf64` |
| 输出文件 | `artifacts/firmware/ap01-1.0.2_0031-agents-live-data-low-stack.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `e17f491f909ee4a77436b7cd83cc233f45056316d4cca546a7286995509e82e9` |
| MD5 | `02e9b1cd1920e4a97cce23c9bf5e4d5e` |
| 恢复校验 | `0x73c1e128` |

构建清单位于 `artifacts/build/agents-live-data-low-stack/manifest.json`。构建器确认制作提交的
相关代码干净，输出文件为只读。

## 2. 第 10.23 节门禁

- 四页下载状态保持 `136` 字节，但改由原厂内存申请与释放入口承载；
- 后台包装入口静态栈从 `208` 字节降到 `64` 字节，低于成功参考实现的 `96` 字节；
- 反汇编门禁证明申请与释放只由后台下载包装路径各调用一次，其他页面和旋钮路径没有调用；
- 主机替身实测申请失败不发起请求，成功取包后清空原上下文参数并释放一次；
- 两个请求格式仍为 `%s`，原厂网络调用、固定周期、失败重试和界面定时包装保持第 10.22 节方案；
- 功率确认保护、共享序号切页过滤和三个萌宠局部分支继续保留。

## 3. 自动验证

全项目 `93` 项自动测试全部通过。独立刷前模拟得到：

| 项目 | 结果 |
| --- | ---: |
| 定向场景 | `18` |
| 轨迹步骤 | `110` |
| 穷举序列 | `4,356` |
| 失败 | `0` |

两次独立正式构建的字节数、SHA-256 和 MD5 完全相同，文件逐字节比较结果为一致。

## 4. 服务端核对

home02 服务直接取得的包为 `116,103` 字节，解码为四页，页面字节数依次为 `26,104`、
`29,102`、`25,738`、`35,095`。服务健康、未降级，额度、重置卡、个人统计和本地会话四个
来源均可用。安装前人工核对后的服务请求计数为 `2`；安装后必须以大于该值且来源为 AP01
地址的请求作为设备取数证据。

本记录只证明离线构建、模拟和服务端输入符合第 10.23 节，不替代设备实际请求和屏幕验收。

