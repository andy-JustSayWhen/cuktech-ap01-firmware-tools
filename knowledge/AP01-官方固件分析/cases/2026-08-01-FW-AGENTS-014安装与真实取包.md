# 2026-08-01 FW-AGENTS-014 安装与真实取包

## 1. 安装前门禁

| 项目 | 结果 |
| --- | --- |
| 成品 | `ap01-1.0.2_0031-agents-live-data-location-independent.bin` |
| SHA-256 | `8827881cf094b846a4966f45985115d810676eefd34bb1659fd4fd9c399d6203` |
| MD5 | `49b4005f1e6a21fc53ddb7d30eebafaf` |
| 云端全量回读 | `6,804,520` 字节，与本地成品逐字节一致 |
| 回读速度 | `1,414,996` 字节/秒 |
| 设备 | 小米云中唯一 AP01，型号与目标一致 |
| 安装前状态 | 在线，更新状态为空闲 |
| 服务请求基线 | `3`，最后一次是安装前的本机包校验 |
| 数据源 | 四个全部正常 |

## 2. 单次安装

| 相对时间 | 设备状态 |
| ---: | --- |
| `0` 秒 | 开始下载，运行时间 `1315` 秒 |
| `117` 秒 | 下载完成，进度 `100` |
| `121` 秒 | 安装完成，进度 `100` |
| `124` 秒 | 重启后重新上线，运行时间重置为 `8` 秒 |

没有重复下发安装命令。

## 3. AP01 真实取包

2026-08-01 `08:12:27` CST，主服务日志记录：

```text
192.168.31.231 GET /a
```

同时健康状态为：

- 请求总数从 `3` 增加到 `4`；
- 额度、重置卡、个人统计和本地会话四个数据源全部为真；
- 服务无降级、无错误。

`192.168.31.231` 是当次 AP01 的局域网地址；安装前本机包校验的来源地址为
`192.168.31.174`。因此该记录是设备真实请求，不是本机自读。

`08:17:27` CST，服务再次记录 `192.168.31.231 GET /a`，请求总数从 `4`
增加到 `5`。两次设备请求精确相隔 `300` 秒，证明首次取包和原厂定时刷新链均已接通。

## 4. 结论与未替代项

`FW-AGENTS-014` 已证明位置占位入口解除了 AGENTS 取包对原厂天气位置的依赖，
且设备可以从四页真实数据服务取包。这一证据完成了后台传输验收，不替代用户对
屏幕显示、旋钮进出、三张详情和功率页的物理验收。

## 5. 2026-08-03 用户实机补记

用户确认当前固件已经刷入且“没问题”。该反馈完成当前成品的总体实机确认；由于反馈没有逐项
列出基座吸附状态、功率详情和每张详情页路径，不用这一句替代仍需单列的基座专项验收记录。
