# 2026-08-01 FW-AGENTS-014 离线构建

## 1. 代码与输入

| 项目 | 结果 |
| --- | --- |
| 固定提交 | `b3a8968` |
| 远端分支 | `origin/opt-bin` 已包含该提交 |
| 阶段输入 | `opt-setting.bin` |
| 输入 SHA-256 | `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` |
| 输出文件 | `ap01-1.0.2_0031-agents-live-data-location-independent.bin` |
| 清单类型 | `agents-live-data-location-independent-firmware` |
| 交互合同 | `FW-AGENTS-014` |

## 2. 固结结果

| 项目 | 结果 |
| --- | --- |
| 字节数 | `6,804,520` |
| SHA-256 | `8827881cf094b846a4966f45985115d810676eefd34bb1659fd4fd9c399d6203` |
| MD5 | `49b4005f1e6a21fc53ddb7d30eebafaf` |
| 恢复校验 | `0x81a929c7` |
| 设备端载荷 | `30,876` 字节 |
| 固定空间剩余 | `30,058` 字节 |
| 最大单函数静态栈 | `64` 字节 |
| 两次独立构建 | 逐字节一致 |

与 `FW-AGENTS-013` 的 `30,828` 字节载荷相比，新的位置占位入口和实际代码排布
合计增加 `48` 字节，最大单函数静态栈没有增加。

## 3. 位置无关门禁

构建器已逐字节验证：

1. `0xa00b6dd4` 的原调用字节与已校验官方固件一致；
2. 该调用只改为跳向独立近跳板，近跳板只跳向 `ap01_agents_location_stub`；
3. 占位入口反汇编只对传入缓冲区的偏移 `0` 至 `9` 执行十次单字节写入，
   没有调用其他固件入口；
4. 空指针返回 `0`；正常缓冲区得到九个数字字符和一个终止字节，并返回 `1`；
5. 位置取得以外的原厂首次调度、网络上下文、网络执行和失败重试继续复用；
6. 两个请求格式均等于 `%s`，占位文本不会发送到服务。

## 4. 测试与服务

- 全项目 `95` 项自动测试全部通过；
- 独立交互模拟覆盖 `18` 个定向场景、`110` 个轨迹步骤和 `4,356` 条连续事件序列，
  失败为 `0`；
- 四个数据源均健康；
- `/a` 当次返回 `115,752` 字节，四页长度分别为 `25,703`、`28,706`、`26,184`
  和 `35,095` 字节，完整包解码通过。

这些结果允许进入云端全量回读和一次自动安装，不代替设备真实请求和屏幕验收。

