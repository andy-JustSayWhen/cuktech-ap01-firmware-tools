# 2026-08-01 FW-TRANSPORT-PROBE-001 安装后未请求

## 1. 安装对象

| 项目 | 结果 |
| --- | --- |
| 成品 | `ap01-1.0.2_0031-reference-transport-probe.bin` |
| SHA-256 | `5d08284aa733546e0eae8c0ee97b8796bcfb0b96ddef42c17142ca7d2c4e5bd6` |
| 上传后全量回读 | 字节数和两种文件指纹一致 |
| 安装 | 下载、安装、重启和重新上线均完成 |

安装过程中，下发后约 `117` 秒完成下载，约 `122` 秒完成安装，约 `125`
秒观测到运行时间重新计数。

## 2. 固定请求窗口

参考项目原版服务持续监听 `192.168.31.174:18766`，本机可正常读取
`/screen.gif`。设备重启后超过一个原厂后备条件可能触发的等待窗口，服务仍未收到
来自 AP01 的请求。本机自读请求没有计为设备成功。

## 3. 结论

该探针不包含本项目的四页包解析、AGENTS 页面、旋钮局部分支或功率保护，仍然没有
发起请求。因此“本项目四页处理阻止请求”的假设被否定。直接原因和下一步复用边界
只见
[`2026-08-01 原厂天气任务首次取数前置条件静态定位`](2026-08-01-原厂天气任务首次取数前置条件静态定位.md)。

