# 2026-08-01 原厂固件功率页对照安装

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

## 1. 对照原因

`FW-AGENTS-009` 和已验收设置阶段成品 `opt-setting.bin` 的真机一级导航都找不到
功率页。用户要求刷入当前小米云官方原厂固件进行对照。

## 2. 官方来源与文件身份

执行前实时查询用户远端参考项目 `main`，本地与远端均为提交
`56b64082b1aed9404856fcb940ccde6fb7fd3d34`。复用该项目的小米账号授权和
当前 `.env` 登录态，直接查询小米云并下载官方文件；不使用本地改版替代。

| 项目 | 结果 |
| --- | --- |
| 官方型号 | `njcuk.enstor.ap01` |
| 官方版本 | `1.0.2_0031` |
| 小米云 MD5 | `469a7329d496f81ae8625c4d76ccf56d` |
| 文件长度 | `6,804,520` 字节 |
| 文件头 | `BFNP` |
| SHA-256 | `8a721fc8ef25458d415b2460e4a251e0503a82f7743fdff85b12612190e5c1cb` |
| MD5 | `469a7329d496f81ae8625c4d76ccf56d` |

本项目的原厂基线结构检查通过：型号、版本、长度、文件头、完整文件指纹和
文件尾恢复校验全部匹配。

## 3. 上传与完整回读

参考项目的授权与安装 `8` 项测试通过。不切换用户当前网络配置，只复用
正在监听的 `7897` 端口上传官方文件。云端完整回读 `6,804,520` 字节，
每秒 `2,445,280` 字节；回读对象与下载的官方文件逐字节一致，两个完整
文件指纹也一致。

## 4. 安装、重启与重新上线

安装前账号内恰好一台目标设备在线，设备自报版本为 `1.0.2_0031`，更新空闲。
只下发一次官方固件安装：

1. 设备接受安装指令，下载进度从 `0` 连续增长；
2. `114` 秒时下载完成，进度为 `100`；
3. `119` 秒时进入已安装，进度为 `100`；
4. `122` 秒时运行时间从 `602` 秒重置为 `8` 秒，设备重启并重新上线；
5. 紧接着的一次只读核对确认设备在线、更新空闲、运行时间为 `24`
   秒，自报版本仍为 `1.0.2_0031`。

官方原厂固件已完成安装、重启和重新上线。当前只等待用户确认功率页是否在
原厂导航中可达；本记录不用安装成功替代该物理对照。
