# 2026-08-03 FW-PAGE-003-A 安装

## 1. 安装对象与刷前事实

| 项目 | 结果 |
| --- | --- |
| 成品 | `ap01-1.0.2_0031-page-settings-read-only-entry.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `456008470df3cad464a60bb7432013686ad33c0f1f4abb9697cdb1badb3f697e` |
| MD5 | `7f75f58e649c822e765bfa18f36c1a72` |
| 账号内目标 | 恰好一台 `njcuk.enstor.ap01` |
| 刷前状态 | 在线，更新空闲，运行时间 `159,557` 秒 |
| 小米云最新版本 | `1.0.2_0031` |
| 原厂 MD5 | `469a7329d496f81ae8625c4d76ccf56d` |

参考项目在执行前实时同步到用户远端 `main` 提交
`56b64082b1aed9404856fcb940ccde6fb7fd3d34`。本地没有项目虚拟环境，文档中的隔离环境测试命令
无法启动；改用本机现有 Python 执行相同的 `test_mi_login.py` 和
`test_ap01_install_firmware.py`，共 `8` 项测试全部通过。授权继续复用该项目权限为 `0600` 的
既有登录态，没有生成新二维码，也没有改用其他授权项目。

## 2. 上传与完整回读

同一只读冻结成品上传到小米文件存储后，通过 AP01 官方兼容分发地址完整回读：

| 项目 | 结果 |
| --- | --- |
| 回读字节数 | `6,804,520` |
| 回读耗时 | `3` 秒 |
| 回读 SHA-256 | `456008470df3cad464a60bb7432013686ad33c0f1f4abb9697cdb1badb3f697e` |
| 回读 MD5 | `7f75f58e649c822e765bfa18f36c1a72` |
| 逐字节比较 | 一致 |

上传和回读期间没有调用已停用的“仅下载”入口，设备运行内容没有被单独的下载测试切换。

## 3. 单次安装结果

设备只接受一次正式安装命令，状态顺序为：

1. `0` 秒进入下载；
2. `125` 秒进入已下载，进度为 `50`；
3. `128` 秒进入已安装，进度为 `100`；
4. `131` 秒设备暂时离线，运行时间不可读；
5. `140` 秒设备重新上线，运行时间从刷前约 `159,562` 秒重置为 `14` 秒。

安装后即时复查时间为 2026-08-03 04:34:18（北京时间）：账号内仍恰好一台目标设备、设备
在线、更新空闲、运行时间为 `28` 秒，最近五分钟没有更新错误。自动证据证明同一冻结成品已
完成上传、完整回读、单次安装、重启和重新上线。

## 4. 当前边界与物理验收

本成品是页面开关四段方案的第一段，只包含设置第八项只读入口，不包含 AGENTS 看板、页面
开关对象、保存或一级导航过滤。安装后还必须由屏幕与物理旋钮确认：

1. 已离开开机动画并进入正常一级页面；
2. 系统设置共有八项，“开关一级页面”位于原厂返回行之前；
3. 左右旋转都能在新增行和返回行之间移动；
4. 按下新增行时仍停留在设置页，不打开页面、不重启；
5. 按下原厂返回行仍退出设置；
6. 原厂一级页面和七个原设置项没有回归。

上述物理结果尚未由自动证据替代。通过前不得进入 `FW-PAGE-003-B`。
