# 2026-08-03 FW-PAGE-003-A 离线构建

## 1. 代码与输入

| 项目 | 结果 |
| --- | --- |
| 固定提交 | `ea169dab06c405cfe151a4c52ed84b7368f4bd71` |
| 远端分支 | `origin/opt-bin` 已包含该提交 |
| 阶段输入 | 只读 `opt-setting.bin` |
| 输入 SHA-256 | `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` |
| 输出文件 | `ap01-1.0.2_0031-page-settings-read-only-entry.bin` |
| 清单类型 | `page-settings-read-only-entry-firmware` |
| 交互合同 | `FW-PAGE-003-A` |

## 2. 固结构建结果

| 项目 | 结果 |
| --- | --- |
| 字节数 | `6,804,520` |
| SHA-256 | `456008470df3cad464a60bb7432013686ad33c0f1f4abb9697cdb1badb3f697e` |
| MD5 | `7f75f58e649c822e765bfa18f36c1a72` |
| 恢复校验 | `0xf3f2e66e` |
| 设备端载荷 | `435` 字节 |
| 固定空间剩余 | `60,499` 字节 |
| 最大单函数静态栈 | `48` 字节 |
| 两次完整固件构建 | 逐字节一致 |

两次完整构建分别在项目成品目录和新建临时目录执行，输出字节、SHA-256 与 MD5 全部相同。
载荷内部还独立链接两次并逐字节比较，重定位数量为 `0`。

## 3. 修改边界与原厂复用

构建器已逐字节验证：

1. 输入身份、只读权限以及四个挂接位置的原字节完全匹配；
2. 原厂首张动图只按 DESIGN 第 10.1 节执行无损整理，逐帧像素、时序和独立解码结果一致；
3. 设置项上限、普通行分发、两个设置回调指针、固定载荷区和文件尾恢复校验之外没有越界修改；
4. 序号 0 至 5、序号 6 新增普通行和序号 7 原厂返回行分别回到 DESIGN 固定的原厂入口；
5. 设置回调只补状态 `54`、`55` 的相邻移动和确认边界，未消费事件继续进入实际注册的原厂回调；
6. 反汇编未出现对象创建与删除、内存申请与释放、持久化、一级导航、AGENTS 或网络入口。

## 4. 自动测试与模拟

- 页面开关模块 `7` 项自动测试全部通过；
- 全项目 `98` 项自动测试全部通过；
- 刷机安装策略 `6` 项自动测试全部通过；
- 严格设置事件模拟覆盖 `8` 个定向场景和全部长度五的 `1,944` 条连续事件序列，失败为 `0`；
- 模拟明确验证新增行确认不会创建页面、保存数据或改变一级导航，原厂返回行仍按原路径退出。

本记录允许按 DESIGN 第 14.5.1 节进入云端全量回读和唯一测试设备的一次自动安装。自动模拟
不替代设备启动、设置八项、真实旋钮移动、确认行为以及原厂页面回归的物理验收。
