# 2026-08-03 FW-PAGE-004-A 安装

## 1. 安装对象与刷前状态

| 项目 | 结果 |
| --- | --- |
| 成品 | `ap01-1.0.2_0031-page-settings-startup-passthrough.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b` |
| MD5 | `a066fab43085e8ab4c8b8aa33ff8af6f` |
| 账号内目标 | 恰好一台 `njcuk.enstor.ap01` |
| 刷前状态 | 在线，更新空闲，进度 `101`，运行时间 `2,872` 秒 |
| 最近十五分钟更新错误 | 0 |
| 刷前等价模拟 | 六个原厂可达序号全部通过，失败 0 |

参考刷机项目在本次操作前实时同步用户远端 `main`；本地、远端均为
`56b64082b1aed9404856fcb940ccde6fb7fd3d34`。授权与安装 8 项测试通过，继续复用权限为
`0600` 的既有登录态。

## 2. 上传与完整回读

冻结成品上传后，通过 AP01 官方兼容分发地址全量回读：

| 项目 | 结果 |
| --- | --- |
| 回读字节数 | `6,804,520` |
| 回读 SHA-256 | `8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b` |
| 回读 MD5 | `a066fab43085e8ab4c8b8aa33ff8af6f` |
| 逐字节比较 | 一致 |

没有调用已停用的仅下载入口。完整回读一致后，使用同一上传对象只下发一次安装命令。临时授权
地址、上传日志与回读副本在安装后已经删除。

## 3. 单次安装状态序列

1. `0` 秒进入下载，进度为 `0`，运行时间为 `2,917` 秒；
2. `122` 秒进入已下载，进度为 `100`；
3. `126` 秒进入已安装，进度为 `100`；
4. `129` 秒更新状态恢复空闲标记，进度为 `101`，运行时间从 `3,043` 秒重置为 `8` 秒；
5. 安装程序确认设备已经完成安装、重启并重新上线；
6. 即时复查时账号内仍恰好一台目标设备，设备在线、更新空闲、运行时间为 `40` 秒，最近十五
   分钟更新错误为 0。

上述证据证明唯一冻结成品完成了上传、完整回读、单次安装、重启和重新上线。

## 4. 当前验收边界

本阶段没有用户可见新增功能，自动状态不能替代屏幕启动完成。当前只等待用户确认屏幕已经进入
正常一级页面。确认前不得制作 `FW-PAGE-004-B`；若仍卡在开机动画，立即否定本阶段并回刷
已验收 `opt-setting.bin`。
