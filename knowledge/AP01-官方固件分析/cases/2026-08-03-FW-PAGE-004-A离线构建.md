# 2026-08-03 FW-PAGE-004-A 离线构建

## 1. 目标与边界

本次只实现 DESIGN 第 14.5.2 节固定的启动基础透传，不增加第八项，不替换设置回调，不改变
一级导航、物理旋钮、AGENTS、网络或持久化行为。失败方案的分层复查只见
[`FW-PAGE-003-A 启动修改分层复查`](2026-08-03-FW-PAGE-003-A启动修改分层复查.md)。

## 2. 输入与当前官方版本

- 制作提交：`f4c5b6016525f9107dfc8b4910afcc0d7d234e49`，本地与远端 `opt-bin` 一致；
- 当前官方版本：制作前从小米云实时查询仍为 `1.0.2_0031`；
- 当前官方文件 MD5：`469a7329d496f81ae8625c4d76ccf56d`；
- 阶段输入：只读 `opt-setting.bin`，长度 `6,804,520` 字节；
- 阶段输入 SHA-256：`348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1`；
- 阶段输入 MD5：`13a7286f4824b1ad87d9bc32f1d3d39c`。

参考刷机项目在本次查询前实时同步用户远端 `main`，本地、远端均为
`56b64082b1aed9404856fcb940ccde6fb7fd3d34`；授权与安装 8 项测试通过，本机登录态文件权限为
`0600`。

## 3. 实现结果

实现文件为 `features/primary_page_settings/startup_passthrough.S`、
`startup_passthrough.ld`、`startup_passthrough.py` 与 `test_startup_passthrough.py`，应用入口为
`page-settings-startup-passthrough-build`。

载荷只有 28 字节，运行地址为 `0xa02465d0`，不使用栈，不调用函数，只包含两个原厂继续地址：

- 原厂普通行路径 `0xa01989e0`；
- 原厂返回行路径 `0xa0198ad0`。

六个原厂可达序号逐项比较全部相同：序号 1 至 5 返回普通行路径，序号 6 返回原厂返回行路径。
文件偏移 `0x1999b4` 的七项循环上限、`0x0be8cc` 与 `0x0be8d4` 的原厂设置回调原字节均保持
不变。

## 4. 自动门禁

- 页面启动透传专用测试 4 项通过；
- 全项目 102 项测试通过；
- 安装策略 6 项测试通过；
- 原厂动图逐帧像素、时序与双解码门禁通过；
- 六个原厂可达序号等价模拟失败为 0；
- 两次载荷链接逐字节一致；
- 两次独立完整固件构建逐字节一致；
- 修改区间外一致，文件尾恢复校验通过；
- 设置循环上限和两个设置回调地址均逐字节保持原值。

## 5. 冻结成品

- 文件名：`ap01-1.0.2_0031-page-settings-startup-passthrough.bin`；
- 长度：`6,804,520` 字节；
- SHA-256：`8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b`；
- MD5：`a066fab43085e8ab4c8b8aa33ff8af6f`；
- 载荷长度：28 字节；
- 固定载荷余量：60,906 字节；
- 文件权限：`0444`。

允许变化只有原厂动图长度、无损整理后的动图数据、28 字节透传载荷、设置创建分支跳转和文件
尾恢复校验五类精确区间。

## 6. 能力边界与下一步

离线门禁不能执行真机界面初始化，不能证明首次启动已经通过。下一步只能把上述唯一指纹成品
上传并完整回读，逐字节一致后下发一次安装。真机只验能否正常进入一级页面、设备重新在线且
更新空闲；用户确认正常启动前不得制作 `FW-PAGE-004-B`。
