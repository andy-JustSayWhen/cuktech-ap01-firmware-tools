# 2026-08-03 FW-PAGE-004-B 安装

## 1. 安装对象与刷前门禁

| 项目 | 结果 |
| --- | --- |
| 成品 | `ap01-1.0.2_0031-page-settings-row-creation.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `256eaebe72d218788498b54f3e1442f6ef263c9f81293601087c454f7deebbbd` |
| MD5 | `caf89edd3f32807efb2d42e8367071fb` |
| 当前官方版本 | `1.0.2_0031` |
| 当前官方 MD5 | `469a7329d496f81ae8625c4d76ccf56d` |
| 账号内目标 | 恰好一台 `njcuk.enstor.ap01` |
| 刷前设备状态 | 在线、更新空闲、进度 `101`、运行时间 `60,545` 秒 |
| 最近十五分钟更新错误 | 0 |
| 刷前创建模拟 | 八个序号全部通过，失败 0，原厂设置回调不变 |

参考刷机项目执行前实时同步用户远端 `main`；本地、远端均为
`56b64082b1aed9404856fcb940ccde6fb7fd3d34`。授权与安装 8 项测试通过，继续复用权限为
`0600` 的既有登录态。

## 2. 上传与完整回读

| 项目 | 结果 |
| --- | --- |
| 回读字节数 | `6,804,520` |
| 回读 SHA-256 | `256eaebe72d218788498b54f3e1442f6ef263c9f81293601087c454f7deebbbd` |
| 回读 MD5 | `caf89edd3f32807efb2d42e8367071fb` |
| 逐字节比较 | 一致 |

没有调用已停用的仅下载入口。完整回读一致后，只下发一次安装命令。临时授权地址、上传日志和
回读副本已经删除。

## 3. 单次安装状态序列

1. `0` 秒进入下载，进度为 `0`，运行时间为 `60,591` 秒；
2. `125` 秒进入已下载，进度为 `100`；
3. `130` 秒进入已安装，进度为 `100`；
4. `133` 秒设备暂时离线；
5. `154` 秒设备重新上线，更新空闲、进度 `101`，运行时间为 `23` 秒；
6. 即时复查时账号内仍恰好一台目标设备，设备在线、更新空闲、运行时间为 `41` 秒，最近十五
   分钟更新错误为 0。

上述证据证明 B 阶段唯一冻结成品完成上传、完整回读、单次安装、重启和重新上线。

## 4. 当前物理验收

本次有真实可见变化：设置列表应有八项，“开关一级页面”位于原厂返回行之前。当前只需要确认：

1. 已正常进入一级页面；
2. 设置列表出现“开关一级页面”；
3. 旋转可选中该行。

本阶段保留原厂设置回调，按下新增行会按原厂状态 54 的行为退出设置；不要求按下。上述三项
确认前不得制作 C 阶段。
