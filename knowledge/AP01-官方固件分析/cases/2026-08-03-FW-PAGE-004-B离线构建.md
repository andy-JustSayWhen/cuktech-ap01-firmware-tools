# 2026-08-03 FW-PAGE-004-B 离线构建

## 1. 目标与输入

本阶段只在已经通过首次启动验收的 A 阶段上增加一个原厂普通行，不替换设置回调。实现边界只见
DESIGN 第 14.5.3 节，原厂容量与所有权证据只见
[`设置列表第八项容量与生命周期静态复查`](2026-07-30-设置列表第八项容量与生命周期静态复查.md)。

| 项目 | 结果 |
| --- | --- |
| 制作提交 | `d9ab9238d282308227fd51feeb052338e6abf0d2` |
| 输入 | `ap01-1.0.2_0031-page-settings-startup-passthrough.bin` |
| 输入字节数 | `6,804,520` |
| 输入 SHA-256 | `8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b` |
| 输入 MD5 | `a066fab43085e8ab4c8b8aa33ff8af6f` |
| 输入权限 | `0444` |

制作提交已经与远端 `opt-bin` 一致。

## 2. 实现证据

实现文件为 `features/primary_page_settings/row_creation.S`、`row_creation.ld`、
`row_creation.py` 与 `test_row_creation.py`，应用入口为 `page-settings-row-creation-build`。

反汇编确认：

1. 序号 1 至 5 回到原厂普通行路径 `0xa01989e0`；
2. 序号 6 以原父列表 `x9` 调用一次原厂普通行入口 `0xa0192fb4`，标签为“开关一级页面”，
   完成后回到 `0xa01989b2`；
3. 序号 7 回到 `0xa0198ad0`，继续创建原厂返回行；
4. 载荷只有上述一次原厂函数调用；
5. A 阶段在 `0x1999dc` 的分发挂接不变；
6. `0x0be8cc`、`0x0be8d4` 的原厂设置回调地址逐字节不变。

## 3. 自动门禁

- 新增行专用测试 4 项通过；
- 全项目 106 项测试通过；
- 安装策略 6 项测试通过；
- 八个创建序号逐项模拟通过，失败 0；
- 两次载荷链接逐字节一致；
- 两次独立完整固件构建逐字节一致；
- 修改区间外一致，文件尾恢复校验通过；
- 原厂设置回调、一级分发挂接与 A 阶段输入身份保持门禁通过。

## 4. 冻结成品

| 项目 | 结果 |
| --- | --- |
| 文件名 | `ap01-1.0.2_0031-page-settings-row-creation.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `256eaebe72d218788498b54f3e1442f6ef263c9f81293601087c454f7deebbbd` |
| MD5 | `caf89edd3f32807efb2d42e8367071fb` |
| 载荷长度 | 91 字节 |
| 固定空间余量 | 60,843 字节 |
| 文件权限 | `0444` |

相对 A 阶段只允许变化：`0x2475d0` 起的 91 字节载荷范围、`0x1999b4` 起的两字节循环上限和
文件尾四字节恢复校验。实际变化 90 字节，全部落在上述范围内。

## 5. 真机边界

本阶段有可见新增行，但仍使用原厂设置回调。原厂回调把状态 54 作为返回项，因此选中新增行后
按下会退出设置，原厂返回行暂时不能被选中。当前只验正常启动、八项列表、新增行位置与可选中；
不把新增行按下行为当作完整功能。三项通过前不得制作 C 阶段。
