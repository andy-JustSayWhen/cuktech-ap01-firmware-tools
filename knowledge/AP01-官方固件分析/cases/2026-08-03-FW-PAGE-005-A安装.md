# 2026-08-03 FW-PAGE-005-A 安装

## 1. 安装对象

安装对象只见 [`FW-PAGE-005-A 离线构建`](2026-08-03-FW-PAGE-005-A离线构建.md)：

| 项目 | 结果 |
| --- | --- |
| 文件名 | `ap01-1.0.2_0031-page-settings-single-iteration-append.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `2714e4b195840ab3d8a6bacdb5be07867028f874f4bd3c9b988982c955618264` |
| MD5 | `539d928290ff681073ae9f273400a884` |

## 2. 上传与完整回读

通过用户远端参考项目当前 `main` 提交
`56b64082b1aed9404856fcb940ccde6fb7fd3d34` 上传。云端对象完整回读结果为：

- 字节数 `6,804,520`；
- SHA-256 `2714e4b195840ab3d8a6bacdb5be07867028f874f4bd3c9b988982c955618264`；
- MD5 `539d928290ff681073ae9f273400a884`；
- 与本地冻结成品逐字节一致；
- 回读速度约每秒 `3,049,487` 字节。

完成比对后只下发一次正式安装，没有下发仅下载任务，也没有重复下发。

## 3. 安装与重启证据

| 安装后经过时间 | 设备更新状态 | 进度 | 运行时间 |
| ---: | --- | ---: | ---: |
| 0 秒 | 下载中 | 0 | 888 |
| 58 秒 | 下载中 | 25 | 946 |
| 119 秒 | 已下载 | 50 | 1010 |
| 125 秒 | 状态切换 | 101 | 9 |

运行时间从 `1010` 重置到 `9`，随后设备在线、更新空闲、运行时间增长到 `24`，且本次下发后没有
更新错误，证明同一次任务已经安装、重启并重新上线。

## 4. 工具纠偏

参考安装工具当前只把“安装中”“已安装”或进度 100 记为安装阶段；本次设备在进度 50 时已经
上报“已下载”，随后直接重启，因此工具把重启后的进度 101 误报为失败。设备重启、重新上线与
无更新错误证据均已取得，不得因该误报重复下发。下一次刷机前，先在参考项目文档写明把“已下载”
纳入安装阶段，再修改工具与测试。

## 5. 物理验收边界

当前自动证据没有证明屏幕已经离开开机动画。用户必须立即确认：

1. 屏幕正常进入一级页面；
2. 设置列表存在八个项目；
3. “开关一级页面”位于原厂返回行之前且可选中。

本阶段仍保持原厂设置回调，不按下新增行。若仍卡开机动画，立即否定本方案并回刷 A 阶段，后续
停止使用启动期原厂列表扩展结构。
