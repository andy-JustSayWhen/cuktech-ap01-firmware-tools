# 2026-08-03 FW-PAGE-005-A 离线构建

## 1. 目标与输入

本阶段落实 DESIGN 第 14.5.4 节：保持原厂七次循环，只在原厂序号 6 的同一次迭代内依次创建
新增普通行与原厂返回行。原厂循环边界和两个创建入口的事实只见
[`FW-PAGE-004-B 开机动画回归`](2026-08-03-FW-PAGE-004-B开机动画回归.md)。

| 项目 | 结果 |
| --- | --- |
| 制作提交 | `91af1b91f24a15ce72b626c53d75d3b0014cacad` |
| 输入 | `ap01-1.0.2_0031-page-settings-startup-passthrough.bin` |
| 输入字节数 | `6,804,520` |
| 输入 SHA-256 | `8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b` |
| 输入 MD5 | `a066fab43085e8ab4c8b8aa33ff8af6f` |
| 输入权限 | `0444` |

制作提交已经与远端 `opt-bin` 一致。

## 2. 实现证据

实现文件为 `features/primary_page_settings/single_iteration_append.S`、
`single_iteration_append.ld`、`single_iteration_append.py` 与
`test_single_iteration_append.py`，应用入口为
`page-settings-single-iteration-append-build`。

反汇编与逐字节门禁确认：

1. 文件偏移 `0x1999b4` 保持原厂七次循环字节 `9d47`；
2. 序号 1 至 5 回到原厂普通行路径 `0xa01989e0`；
3. 序号 6 以原父列表 `x9` 调用一次原厂普通行入口 `0xa0192fb4`；
4. 同一次分支继续调用一次原厂返回行入口 `0xa0193058`，再回到 `0xa01989b2`；
5. 载荷不存在序号 7 分支；
6. A 阶段在 `0x1999dc` 的分发挂接不变；
7. `0x0be8cc`、`0x0be8d4` 的原厂设置回调地址逐字节不变。

## 3. 自动门禁

- 本阶段专用测试 4 项通过；
- 全项目 110 项测试通过；
- 参考刷机项目当前远端提交 `56b64082b1aed9404856fcb940ccde6fb7fd3d34` 与本地一致，授权与
  安装定向测试 8 项通过；
- 七次循环创建八个项目的逐项模拟通过，失败 0，不存在第八次迭代；
- 两次独立完整固件构建逐字节一致；
- 修改区间外一致，文件尾恢复校验通过；
- 原厂循环上限、设置回调、一级分发挂接与 A 阶段输入身份保持门禁通过。

## 4. 冻结成品

| 项目 | 结果 |
| --- | --- |
| 文件名 | `ap01-1.0.2_0031-page-settings-single-iteration-append.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `2714e4b195840ab3d8a6bacdb5be07867028f874f4bd3c9b988982c955618264` |
| MD5 | `539d928290ff681073ae9f273400a884` |
| 载荷长度 | 91 字节 |
| 固定空间余量 | 60,843 字节 |
| 实际变化 | 79 字节 |
| 文件权限 | `0444` |

相对 A 阶段只允许变化：`0x2475d0` 起的 91 字节载荷范围与文件尾四字节恢复校验。实际变化
区间为 `0x2475e0..0x24762a` 和 `0x67d424..0x67d427`，全部落在允许范围内。

## 5. 刷前现场

2026-08-03 刷前重新查询小米云：官方最新版本仍为 `1.0.2_0031`，MD5 仍为
`469a7329d496f81ae8625c4d76ccf56d`。目标设备在线，更新状态为空闲，最近十五分钟没有更新错误；
A 阶段回刷已经完成并重新上线。

## 6. 真机边界

自动证据只批准一次正式安装，不代替屏幕验收。成功条件只有：正常进入一级页面；设置列表存在
八个项目；“开关一级页面”位于原厂返回行之前且可选中。本阶段仍保持原厂设置回调，不按下新增
行。任一启动条件失败时回刷 A 阶段，并停止继续使用启动期原厂列表扩展结构。
