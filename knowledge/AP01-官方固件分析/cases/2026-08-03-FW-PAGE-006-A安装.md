# 2026-08-03 FW-PAGE-006-A 安装

## 1. 安装对象与完整回读

安装对象只见 [`FW-PAGE-006-A 离线构建`](2026-08-03-FW-PAGE-006-A离线构建.md)：

| 项目 | 结果 |
| --- | --- |
| 文件名 | `ap01-1.0.2_0031-page-settings-delayed-row-creation.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `d9b67a6d1fb94b387051e1fb979f8509faf6e53b0d08655b998f32f22d1c04fd` |
| MD5 | `a158126685acdf7c8e3bc184358006c4` |

刷前重新查询小米云，官方最新版本仍为 `1.0.2_0031`，MD5 仍为
`469a7329d496f81ae8625c4d76ccf56d`。目标设备在线、更新空闲且最近十五分钟没有更新错误。

云端对象完整回读 `6,804,520` 字节，两个文件指纹与本地冻结成品相同并逐字节一致。回读速度约
每秒 `1,398,424` 字节。完成比对后只下发一次正式安装。

## 2. 安装状态序列

| 下发后经过时间 | 更新状态 | 进度 | 运行时间 |
| ---: | --- | ---: | ---: |
| 0 秒 | 下载中 | 0 | 491 |
| 56 秒 | 下载中 | 24 | 548 |
| 117 秒 | 已下载 | 50 | 609 |
| 121 秒 | 已安装 | 100 | 612 |
| 124 秒 | 状态切换 | 101 | 10 |

运行时间从 `612` 重置到 `10`，修正后的刷机工具正确判定安装、重启并重新上线。即时复查显示
设备在线、更新空闲、运行时间增长到 `26`，且没有本次更新错误。临时上传地址、上传日志和完整
回读副本均已删除。

## 3. 物理验收边界

当前自动证据只证明冻结成品已经安装、重启并重新上线，不证明屏幕结果。用户必须按顺序确认：

1. 屏幕正常进入一级页面；
2. 再按原厂路径进入设置，确认列表出现八项；
3. “开关一级页面”位于原厂返回行之前。

本阶段设置回调仍保持原厂，不按下新增行。若第一步失败，否定返回地址门并回刷 A 阶段；若第一
步通过而第二步失败，停止继续调用原厂普通行创建入口，不再制作设置列表追加候选。

## 4. 启动物理验收

2026-08-04 用户确认屏幕已经进入一级页面，并实际看到时间、设置、萌宠、天气和日历五个页面。
因此第一步“正常启动并进入一级页面”通过，返回地址门成功让开机调用保持原厂路径。

本候选只验证设置列表延迟创建，不包含 AGENTS 看板，所以本次一级页面中没有 AGENTS 属于成品
范围。用户本次没有同时说明基座连接状态，故“未看到功率页”只记录为现场现象，不据此判断功率
页是否异常。第二步仍待进入设置确认八项和新增行位置。

## 5. 进入设置失败

用户随后在一级设置页按下旋钮，设备立即死机。第二步验收失败，本成品已经否定并禁止再次安装。
失败边界与回退处置只见
[`FW-PAGE-006-A 进入设置死机`](2026-08-04-FW-PAGE-006-A进入设置死机.md)。
