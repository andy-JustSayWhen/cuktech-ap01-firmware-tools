# 2026-08-03 FW-PAGE-006-A 离线构建

## 1. 输入与目标

本阶段落实 DESIGN 第 14.5.5 节，只解决
[`FW-PAGE-005-A 双调用者定位`](2026-08-03-FW-PAGE-005-A开机动画回归与双调用者定位.md)
确认的开机调用误入新增行路径问题。

| 项目 | 结果 |
| --- | --- |
| 制作提交 | `a7e57d7fa3765a743ec68604a6a595e8e684030e` |
| 输入 | `ap01-1.0.2_0031-page-settings-startup-passthrough.bin` |
| 输入字节数 | `6,804,520` |
| 输入 SHA-256 | `8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b` |
| 输入 MD5 | `a066fab43085e8ab4c8b8aa33ff8af6f` |
| 输入权限 | `0444` |

制作提交已经与远端 `opt-bin` 一致。

## 2. 实现与反汇编证据

实现文件为 `features/primary_page_settings/delayed_row_creation.S`、
`delayed_row_creation.ld`、`delayed_row_creation.py` 与
`test_delayed_row_creation.py`，应用入口为
`page-settings-delayed-row-creation-build`。

反汇编确认：

1. 序号 6 在 `0xa02465e0` 从当前栈偏移 `+124` 读取原厂保存的调用者返回地址；
2. 返回地址 `0xa00b4002` 直接进入 `0xa0198ad0` 原厂返回行路径，不调用新增行入口；
3. 返回地址 `0xa00bd9ea` 才调用一次 `0xa0192fb4` 和一次 `0xa0193058`；
4. 未识别调用者同样进入原厂返回行路径；
5. 文件偏移 `0x1999b4` 的原厂七次循环、`0x1999dc` 的 A 阶段挂接和两个设置回调地址不变；
6. 不存在序号 7 分支，也不包含设置回调、一级导航、网络或持久化入口。

## 3. 自动门禁

- 本阶段专用测试 4 项通过；
- 全项目 114 项测试通过；
- 参考刷机项目已经在提交 `0d50863fd6989ec09f380d653a40be3252f2f7ba` 修正“已下载 50 后
  直接重启”的状态识别；授权与安装定向测试 9 项、全项目 35 项通过；
- 开机调用模拟创建七项，新增普通行调用数为零；
- 设置进入调用模拟创建八项，新增普通行和返回行各调用一次；
- 未识别调用者模拟失败关闭到原厂返回行；
- 两次载荷链接和两次独立完整固件构建逐字节一致；
- 修改区间外一致，文件尾恢复校验通过。

## 4. 冻结成品

| 项目 | 结果 |
| --- | --- |
| 文件名 | `ap01-1.0.2_0031-page-settings-delayed-row-creation.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `d9b67a6d1fb94b387051e1fb979f8509faf6e53b0d08655b998f32f22d1c04fd` |
| MD5 | `a158126685acdf7c8e3bc184358006c4` |
| 载荷长度 | 123 字节 |
| 固定空间余量 | 60,811 字节 |
| 实际变化 | 111 字节 |
| 文件权限 | `0444` |

相对 A 阶段只允许变化：`0x2475d0` 起的 123 字节载荷范围与文件尾四字节恢复校验。实际变化
区间为 `0x2475e0..0x24764a` 和 `0x67d424..0x67d427`，全部落在允许范围内。

## 5. 刷前现场与真机边界

`FW-PAGE-005-A` 失败后，A 阶段回刷任务用同一次下发完成下载、重启和重新上线。即时查询显示
设备在线、更新空闲且没有本次更新错误。

本阶段必须先确认正常进入一级页面，再由用户进入设置确认八项。本阶段设置回调保持原厂，不按下
新增行。开机失败时否定调用者门；开机通过但进入设置失败时停止继续调用原厂普通行创建入口。
