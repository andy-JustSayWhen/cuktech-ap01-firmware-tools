# 2026-08-04 FW-AGENTS-015 离线构建

返回主文档：[`AP01 官方固件分析`](../AP01-官方固件分析.md)

实现依据只见 [`AP01 1.0.2_0031 优化固件 DESIGN`](../../../reference/DESIGN/AP01-1.0.2_0031-opt.bin.md)
第 10.26、10.27 节。本次只完成代码、测试、模拟和离线构建，没有上传或安装固件。

## 1. 固定身份

| 项目 | 结果 |
| --- | --- |
| 制作提交 | `9d808a1cb00eee641894f1cb482673549a0e2938` |
| 阶段输入 | 只读 `opt-setting.bin` |
| 输入长度 | `6,804,520` 字节 |
| 输入 SHA-256 | `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` |
| 输出文件 | `ap01-1.0.2_0031-agents-live-data-validated-package.bin` |
| 输出长度 | `6,804,520` 字节 |
| 输出 SHA-256 | `cba16a9f86674ae191eb0d76e8aaa37ab523f4b5a8431f271d6f41cd39f487b6` |
| 输出 MD5 | `8b22bb0d99d9272399c6b6e16292b7c1` |
| 清单类型 | `agents-live-data-validated-package-firmware` |
| 安装许可 | 否 |

第一次执行在写出成品前被只读门禁拦下。复核确认阶段输入内容和指纹未变，但所有者写权限仍
存在；移除写权限并再次核对指纹后才开始两次正式构建。该次停止没有生成候选成品。

## 2. 自动验证

- 固件构建测试 23 项通过；
- 交互模拟测试 12 项通过；
- 全项目 126 项测试通过；
- 独立连续操作模拟覆盖 18 个场景、110 个步骤和 4,356 条序列，失败为 0；
- 两次完整固件构建逐字节一致；
- 载荷为 `31,440` 字节，剩余 `29,494` 字节；
- 单函数最大静态栈为 `64` 字节；
- 下载状态保持 `136` 字节。

两份构建清单均写明：动图结构检查、结束标记检查、单帧拒绝和失败时清空未发布槽四页已经
启用；安装许可为否。

## 3. 当前边界

离线证据只证明冻结成品可重复生成，并证明已有自动检查覆盖的行为。它不能替代物理设备在旧
结果已显示时收到可控损坏包的刷新验收。必须先把该真机步骤和服务端损坏包方案写入 DESIGN；
在此之前不得上传或安装本成品。
