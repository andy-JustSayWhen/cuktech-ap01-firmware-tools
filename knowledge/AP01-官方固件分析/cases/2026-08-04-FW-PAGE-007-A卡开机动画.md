# 2026-08-04 FW-PAGE-007-A 卡开机动画

## 1. 已确认现象

`FW-PAGE-007-A` 的云端完整回读、单次安装、重启和重新上线自动证据均通过，
但用户随后确认设备卡在开机动画，未进入一级页面。因此启动物理验收失败，
自动证据不能把本成品判定为可用。

失败后立即查询：目标设备唯一、云端在线、更新状态空闲、运行时间为 1,001 秒，
近 10 分钟更新错误为 0。这些只证明在线回退通道当前可用，不证明界面启动正常。

## 2. 结论与边界

`FW-PAGE-007-A` 已否定并禁止再次安装。现有证据只能确认“修改原厂返回行固定文字指针”
与本次启动失败同时存在；没有崩溃栈或开机日志，不指定更细的单一根因。

按优化固件 DESIGN 第 14.5.6 节的既定停止条件，立即使用已通过启动与原厂设置物理验收的
A 阶段冻结成品执行一次在线回刷。本成品不授权继续按下或其他物理验收。

## 3. A 阶段回刷首次尝试

A 阶段成品上传前身份复核通过。直连小米账号登录请求曾在上传前遇到加密连接中断，
复用当前 `7897` 代理后上传成功。云端完整回读 `6,804,520` 字节，两种指纹与逐字节
比较全部一致。

回刷任务只下发一次。任务在 0 秒显示下载中、进度 0；随后状态和运行时间不可读，
211 秒仍只能读到进度 0。360 秒轮询结束时未观察到重启前阶段或运行时间重置。
结束后单次查询显示设备云端在线、状态仍为下载中，进度和运行时间不可读，近 15 分钟
新增 1 条更新错误。

本次回刷无成功证据，已停止且不重复下发。临时签名地址、回读副本与上传日志已删除。
下一步只允许用户断电重启一次，然后重新查询现场；不在当前未知状态下再发任务。

## 4. 断电重启后的恢复现场

用户完成一次断电重启。重启后只读查询仍只找到一台目标设备：设备在线、更新空闲、
进度为 101、运行时间为 49 秒。近 15 分钟的 1 条更新错误属于断电前唯一一次失败任务；
断电后尚未下发新任务。

未知下载状态已经清除。下一步重新生成临时地址，完整回读同一 A 阶段冻结成品；只有对象
身份、逐字节比较和下发前现场全部通过，才再下发一次回刷。任一条件不符立即停止。

同一 A 阶段成品随后重新上传成功。云端完整回读 `6,804,520` 字节，速度为每秒
`2,534,899` 字节；SHA-256、MD5 和逐字节比较均与本机只读成品一致。临时签名地址、
回读副本和上传日志权限均为 `600`，不进入提交。下发前仍须再查询一次现场。

第一次下发前查询显示设备唯一、在线、更新空闲、运行时间 306 秒，近 15 分钟更新错误为 0。
临时检查器仍要求错误数等于已经超出时间窗口的旧值 1，因此在设备任务之前主动停止；本次
没有下发。门禁改为近期错误必须为 0 后，再重新检查并执行单次回刷。

## 5. A 阶段恢复完成

纠正后的下发前检查显示设备唯一、在线、更新空闲、运行时间 387 秒，近 15 分钟更新错误
为 0。回刷任务只下发一次：124 秒达到已下载、进度 100；128 秒达到已安装；131 秒运行
时间从 519 秒重置为 7 秒并重新上线。

即时收尾查询显示设备唯一、在线、更新空闲、运行时间 27 秒，近 10 分钟本次更新错误为 0。
自动恢复证据通过；临时签名地址、回读副本、上传日志和安装日志均已删除。只保留用户肉眼
确认是否进入正常一级页面，不做长时间观察。
