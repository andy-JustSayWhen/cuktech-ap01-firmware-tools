# 2026-08-04 FW-PAGE-007-A 离线构建

## 1. 输入与范围

本阶段落实优化固件 DESIGN 第 14.5.6 节，不增加设置列表对象，只复用原厂返回行
的文字指针。原厂证据只见
[`原厂返回行标签与文字更新入口静态定位`](2026-08-04-原厂返回行标签与文字更新入口静态定位.md)。

| 项目 | 结果 |
| --- | --- |
| 制作提交 | `e6e6e7e867882e5c960732d21f9817d0ddfeeb8b` |
| 输入 | `ap01-1.0.2_0031-page-settings-startup-passthrough.bin` |
| 输入字节数 | `6,804,520` |
| 输入 SHA-256 | `8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b` |
| 输入 MD5 | `a066fab43085e8ab4c8b8aa33ff8af6f` |
| 输入权限 | `0444` |

制作提交已与远端 `opt-bin` 一致。

## 2. 实现与静态证据

实现文件为 `features/primary_page_settings/return_row_label.S`、
`return_row_label.ld`、`return_row_label.py` 与 `test_return_row_label.py`，应用入口为
`page-settings-return-label-build`。

构建结果确认：

1. 载荷前 28 字节与 A 阶段透传代码指纹一致；
2. 固定文字位于运行地址 `0xa02465ec`，载荷中只出现一次并包含结尾零字节；
3. 文件偏移 `0x19408c` 的指令为 `b76524a0`，`0x1940a4` 的指令为
   `9385c55e`，解析后共同指向 `0xa02465ec`；
4. 原厂七次循环、A 阶段分发挂接和两个设置回调字节不变；
5. 载荷没有函数调用，不含普通行创建、返回行创建或文字更新入口地址。

## 3. 自动门禁

- 本阶段定向测试 4 项通过；
- 全项目 118 项测试通过；
- 模拟结果为 7 个对象、状态 `48..54`、新增对象数为 0；
- 最后一行显示文字为“开关一级页面”，按下路径仍为原厂返回；
- 两次载荷构建和两次彼此隔离的完整固件构建均逐字节一致；
- 修改区间外一致，文件尾恢复校验通过。

## 4. 冻结成品

| 项目 | 结果 |
| --- | --- |
| 文件名 | `ap01-1.0.2_0031-page-settings-return-label.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `00e5fab6bab3fcc5b3aa4b9716c858e26955759c3a89c06b04feb6184c7d2df3` |
| MD5 | `b562917d96dcdde627153e9d6d03a5aa` |
| 载荷长度 | 47 字节 |
| 固定空间余量 | 60,887 字节 |
| 文件权限 | `0444` |

实际变化区间为 `0x19408e..0x19408e`、`0x1940a7..0x1940a7`、
`0x2475ec..0x2475fe` 和 `0x67d424..0x67d427`，全部落在 DESIGN 允许范围内。

## 5. 真机验收边界

模拟门禁通过后按刷机策略直接安装一次。安装后只验收：正常进入一级页面；设置页按下
后不死机；列表仍为七个对象；最后一行显示“开关一级页面”。本阶段不按下最后一行。
