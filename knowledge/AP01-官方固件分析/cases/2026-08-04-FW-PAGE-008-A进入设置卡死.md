# 2026-08-04 FW-PAGE-008-A 进入设置卡死

## 1. 失败现象与边界

`FW-PAGE-008-A` 完成单次安装、重启和重新上线后，用户在一级页面选中设置并按下旋钮，
设备立即卡死。失败发生在进入设置路径，尚无设置列表显示结果，也没有设备崩溃栈。

能够确认的边界只有：

1. 冻结构建、四种离线对象链模拟和云端逐字节回读不能替代真实设置进入环境；
2. 当前对原创建函数返回对象、子对象层级或文字更新时点至少有一项假设不成立；
3. 没有崩溃栈，不能把原因缩小到其中某一项；
4. `FW-PAGE-008-A` 立即否定并禁止再次安装。

## 2. 卡死后的现场

卡死后，小米云设备列表仍显示唯一目标设备在线，版本为 `1.0.2_0031`，更新状态为空闲，
近 10 分钟没有新增更新错误；但设备命令随后出现连续不可读，说明仅凭设备列表在线不能证明
界面任务和设备命令处理正常。

## 3. A 阶段单次恢复

恢复对象为已经物理验收通过的 A 阶段固件：

| 项目 | 结果 |
| --- | --- |
| 文件名 | `ap01-1.0.2_0031-page-settings-startup-passthrough.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b` |
| MD5 | `a066fab43085e8ab4c8b8aa33ff8af6f` |
| 权限 | `0444` |

恢复固件重新上传后完整回读，文件头、长度、两种指纹和逐字节比较均一致，回读速度约为
每秒 2.57 兆字节。最后门禁确认设备唯一、在线、版本匹配、更新空闲且近 10 分钟无新增错误，
随后只下发一次恢复任务。

卡死状态下，恢复任务一度停在 0%，设备列表显示在线但设备命令不可读。没有重复下发。原任务
随后恢复推进，约 532 秒到达已下载、进度 50；约 566 秒设备重新在线、更新空闲、进度 101，
运行时间重置为 12 秒。刷后即时查询设备在线、更新空闲、运行时间 25 秒；卡死和慢速下载期间
共有 2 条旧错误记录，重启后的 30、60、120 秒窗口新增错误均为 0。

临时签名地址、完整回读副本和上传日志已经删除。

## 4. 当前门禁

自动恢复完成后，用户确认设备已经回到正常一级页面，A 阶段恢复闭环完成。

下一方案必须先取得真实设置进入路径的运行期对象证据，再更新 DESIGN；不得继续复用本候选的
返回对象和子对象层级假设。
