# 2026-08-04 FW-PAGE-009-A 离线构建

## 1. 成品

| 项目 | 结果 |
| --- | --- |
| 文件名 | `ap01-1.0.2_0031-page-settings-constructor-label.bin` |
| 字节数 | `6,804,520` |
| SHA-256 | `0452b954f48b0162f6dc4c0533dd482eca0c917c6a926dba4e837c900a397b9e` |
| MD5 | `6adde9b9ea755adeea7ed663873bd05a` |
| 文件头 | `BFNP` |
| 权限 | `0444` |
| 载荷 | 83 字节，剩余 60,851 字节 |
| 制作提交 | `020ffc2f5b17f71559b470f4bf4ffb2c213efec0` |

## 2. 原厂调用保持

- 开机列表创建调用 `0xa00b3ffe` 和返回地址 `0xa00b4002` 保持原字节；
- 用户列表创建调用 `0xa00bd9e6` 和返回地址 `0xa00bd9ea` 保持原字节；
- 列表返回值写入、首行处理、七次循环、两条原厂固定文字指针和两个设置回调保持原字节；
- 唯一挂接点是返回行内部 `0xa01930aa` 的共享文字对象调用；挂接返回地址仍为
  `0xa01930ae`。

## 3. 载荷门禁

条件入口只读取 `124(x8)` 保存的列表创建调用者返回地址。值为 `0xa00bd9ea` 时把文字参数换成
“开关一级页面”，其他值保持原文字参数；两条路径都尾跳原厂 `0xa00b1bb4`。

反汇编确认载荷：

- 静态栈为 0；
- 没有可变状态和内存写入；
- 不遍历或解释任何对象；
- 不调用 `0xa00c5d84`；
- 不调用 `0xa0086bcc`；
- 不改变原厂返回行后续图标、样式和事件构造。

双调用者与重复调用模拟覆盖开机、连续两次用户进入和未知调用者，均只调用一次原厂文字对象
构造入口且不保留状态。全项目 124 项测试通过；两次完整构建逐字节一致。批准按刷机门禁执行
一次测试安装。

## 4. 真机边界

只验正常启动、按下设置能进入和最后一行显示“开关一级页面”。本阶段不得按下最后一行。
