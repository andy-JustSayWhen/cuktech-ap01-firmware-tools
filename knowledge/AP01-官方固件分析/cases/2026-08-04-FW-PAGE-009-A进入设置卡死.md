# 2026-08-04 FW-PAGE-009-A 进入设置卡死

## 1. 真机结果

`FW-PAGE-009-A` 完成单次安装、重启和重新上线后，用户在一级页面选中设置并按下旋钮，设备
再次卡死。失败发生在用户设置列表创建阶段；当前没有设备崩溃栈、异常地址或复位原因记录，
因此不能把根因缩写成某一条载荷指令。

## 2. 已确认边界

本候选只在原厂返回行文字对象构造调用处挂接条件入口。入口读取返回行帧指针 `x8` 偏移
`+124` 的值，并把它解释为上层列表创建调用者保存的返回地址。离线反汇编、双调用者模拟、
零栈检查和 124 项自动测试只能证明静态布局与模拟输入一致，不能证明真机执行到该位置时这项
解释仍成立。

本次直接证据只确认：

1. A 阶段保持原厂设置创建路径时设备可恢复；
2. `FW-PAGE-008-A` 的创建后对象遍历和 `FW-PAGE-009-A` 的构造点条件入口都在按下设置后
   卡死；
3. 云端没有记录本次更新错误，但这不能代替设备端运行异常记录；
4. 在取得真机异常地址、栈或等价运行期证据前，不能继续用静态栈位置推断制作设置页候选。

## 3. 处置

`FW-PAGE-009-A` 已否定并禁止再次安装。当前所有通过改写设置列表创建过程来显示新增入口的
方案整体封停；不得继续制作同路线变体。

恢复对象固定为只读 A 阶段固件
`ap01-1.0.2_0031-page-settings-startup-passthrough.bin`，长度 6,804,520 字节，SHA-256 为
`8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b`。

恢复对象上传后完整回读，文件头、长度、指纹和逐字节比较全部一致，回读速度约为每秒
2.02 兆字节。刷前查询确认唯一设备在线、版本匹配、更新空闲且近五分钟更新错误为 0，随后
只下发一次恢复安装：121 秒进入安装，124 秒安装完成，127 秒检测到运行时间重置并重新上线。
刷后查询设备在线、更新空闲、运行时间 20 秒且近五分钟更新错误为 0。临时签名地址、回读副本
和日志已经删除。

自动恢复已经完成；正常一级页面仍需用户肉眼确认。本次不得要求用户再次进入设置。
