# `0x00582c..0x00584b` 七日天气七列布局描述表

## 1. 覆盖范围

- 原厂镜像：`ap01-1.0.2_0031.bin`
- 文件偏移：`0x00582c..0x00584b`
- 复制后运行地址：`0x62fc4d18..0x62fc4d37`
- 长度：32 字节
- 本段原始字节散列：`8663105bbad28bf4fef568286f5ae93506e8c3683a60f4b3630faf9a5138042e`
- 运行地址换算：文件偏移加 `0x62fbf4ec`

本段是七日天气详情页的七列布局描述表：七个列宽都为 44，后跟一个
布局描述结束标记。表的整体地址被原厂页面构造函数传给列布局样式属性，
不是七个独立全局整数。

## 2. 逐项内容

| 文件偏移 | 运行地址 | 小端数值 | 归属 |
| --- | --- | --- | --- |
| `0x00582c..0x00582f` | `0x62fc4d18..0x62fc4d1b` | 44 | 第 1 列宽度 |
| `0x005830..0x005833` | `0x62fc4d1c..0x62fc4d1f` | 44 | 第 2 列宽度 |
| `0x005834..0x005837` | `0x62fc4d20..0x62fc4d23` | 44 | 第 3 列宽度 |
| `0x005838..0x00583b` | `0x62fc4d24..0x62fc4d27` | 44 | 第 4 列宽度 |
| `0x00583c..0x00583f` | `0x62fc4d28..0x62fc4d2b` | 44 | 第 5 列宽度 |
| `0x005840..0x005843` | `0x62fc4d2c..0x62fc4d2f` | 44 | 第 6 列宽度 |
| `0x005844..0x005847` | `0x62fc4d30..0x62fc4d33` | 44 | 第 7 列宽度 |
| `0x005848..0x00584b` | `0x62fc4d34..0x62fc4d37` | `0x1fffffff` | 布局描述结束标记 |

## 3. 页面构造路径

原厂文件字符串 `lv_power_ui/lv_ui_wx1_dtl.c` 标识的七日天气详情构造路径
在 `0xa00bd530..0xa00bd5d2` 执行以下步骤：

1. 将父容器设为宽 320、高 18；
2. 把 `0x62fc4d18` 作为一个整体指针，传给数值为 130 的布局样式属性；
3. 把后方 `0x62fc4da0` 的两项表传给数值为 129 的布局样式属性；
4. 设置布局模式后，连续创建 7 个子对象，为每个子对象设置独立的布局位置。

后方 `0x62fc4da0` 的表内容为“18、`0x1fffffff`”，与父容器高度 18 和单行
结束标记一致。因此数值 129 对应行描述表，数值 130 对应本段的列描述表；
七个 44 与后续七个子对象数量一一对应。

## 4. 尺寸关系

七列的内容宽度总和为 308。父容器宽度为 320，剩余 12 像素由原厂布局对齐
和间隔规则处理。已读指令没有把这 12 像素作为第八列，也没有改写本表中的
任意一项。

## 5. 能力边界

- 已闭合：全部 32 个源字节；七个 44 的顺序、结束标记、整表传入的样式
  属性数值、单行描述表的反向印证，以及七个后续子对象的数量对应。
- 保留未定：原厂构建环境中为数值 129 和 130 保留的宏名未出现在镜像字符串中；
  本文只根据两张表的实际内容、容器尺寸和子对象数量确定行列方向。
- 本段不授权删除结束标记，不授权把七个 44 合并成一个总宽度，也不授权
  忽略父容器宽度与七列宽度总和之间的布局余量。
