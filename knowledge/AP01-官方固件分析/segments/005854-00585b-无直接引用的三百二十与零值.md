# `0x005854..0x00585b` 无直接引用的三百二十与零值

## 1. 覆盖范围

- 原厂镜像：`ap01-1.0.2_0031.bin`
- 文件偏移：`0x005854..0x00585b`
- 复制后运行地址：`0x62fc4d40..0x62fc4d47`
- 长度：8 字节
- 本段原始字节散列：`b90c058de56b45d6cb964bb732a18ab25f204171094ca433a731cd9333af0a25`
- 运行地址换算：文件偏移加 `0x62fbf4ec`

本段介于七日天气子对象双槽零初值表与后方四张坐标表之间。
它含一个数值 320 和一个零值；在已覆盖的整份指令结果中，未找到
指向两者起始运行地址的直接引用。

## 2. 逐字内容

| 文件偏移 | 运行地址 | 原始字节 | 小端数值 | 证据状态 |
| --- | --- | --- | --- | --- |
| `0x005854..0x005857` | `0x62fc4d40..0x62fc4d43` | `40 01 00 00` | 320 | 无直接地址引用 |
| `0x005858..0x00585b` | `0x62fc4d44..0x62fc4d47` | `00 00 00 00` | 0 | 无直接地址引用 |

## 3. 前后边界

- 前一段 `0x00584c..0x005853` 的整体地址 `0x62fc4d38` 被以槽数 2
  传入原厂入口；该范围仅覆盖两个四字节槽，不延伸到本段。
- 后一段从 `0x00585c` 开始，运行地址 `0x62fc4d48` 被两个直接
  调用点作为坐标表起点传入，因此本段不属于后方第一张坐标表。

## 4. 能力边界

- 已闭合：全部 8 个源字节、两个四字节数值，以及与前后
  已引用对象的地址边界。
- 保留未定：原厂符号名、两个数值是否属于同一对象，以及数值
  320 的业务语义。
- 本段不授权把 320 命名为屏幕宽度或布局宽度，也不授权把
  后方坐标表的直接引用倒推为本段引用。
