# `0x00585c..0x0058b3` 四张界面对象坐标表

## 1. 覆盖范围

- 原厂镜像：`ap01-1.0.2_0031.bin`
- 文件偏移：`0x00585c..0x0058b3`
- 复制后运行地址：`0x62fc4d48..0x62fc4d9f`
- 长度：88 字节
- 本段原始字节散列：`618be0e9c644e4122da5606618d5ca1acb16992e2656835ee56392da109b37f6`
- 运行地址换算：文件偏移加 `0x62fbf4ec`

本段由四张界面对象坐标表紧邻组成：前三张每张包含 3 个
二维坐标点，第四张只包含 2 个点。点数来自各自直接调用点，
不是根据数值外形猜测。

## 2. 逐表与逐点内容

| 表 | 文件偏移 | 运行地址 | 调用点传入点数 | 按两个有符号四字节数一组解释 |
| --- | --- | --- | --- | --- |
| 第 1 张 | `0x00585c..0x005873` | `0x62fc4d48` | 3 | `(0, 0)`、`(8, 7)`、`(0, 14)` |
| 第 2 张 | `0x005874..0x00588b` | `0x62fc4d60` | 3 | `(0, 0)`、`(8, 7)`、`(0, 14)` |
| 第 3 张 | `0x00588c..0x0058a3` | `0x62fc4d78` | 3 | `(8, 0)`、`(0, 7)`、`(7, 15)` |
| 第 4 张 | `0x0058a4..0x0058b3` | `0x62fc4d90` | 2 | `(0, 0)`、`(0, 8)` |

逐字小端值如下：

| 文件偏移 | 数值 | 归属 |
| --- | --- | --- |
| `0x00585c..0x005873` | `0, 0, 8, 7, 0, 14` | 第 1 张的 6 个坐标分量 |
| `0x005874..0x00588b` | `0, 0, 8, 7, 0, 14` | 第 2 张的 6 个坐标分量 |
| `0x00588c..0x0058a3` | `8, 0, 0, 7, 7, 15` | 第 3 张的 6 个坐标分量 |
| `0x0058a4..0x0058b3` | `0, 0, 0, 8` | 第 4 张的 4 个坐标分量 |

## 3. 原厂接收入口

原厂界面对象构造入口 `0xa00b1f78..0xa00b201a` 的关键路径是：

1. 先创建一个新界面对象；
2. 把调用者传入的坐标表地址和点数原样传给
   `0xa00854b6..0xa0085528`；
3. 接收入口把表地址保存到对象内偏移 44，把点数保存到
   对象内偏移 48，然后更新状态并触发重绘；
4. 构造入口继续设置线宽、颜色、不透明度和几何位置。

这条路径证明本段是由界面对象持有的坐标数据，不是四字节
标量参数的偶然连续排列。

## 4. 直接调用关系

| 坐标表 | 直接调用点 | 传入点数 | 同次调用的可确定参数 |
| --- | --- | --- | --- |
| `0x62fc4d48` | `0xa00bccf6`、`0xa00bcea2` | 3 | 线宽 2，几何大小 7 × 270，颜色分别为 `0x89d8f3`、`0x808080` |
| `0x62fc4d60` | `0xa00b90be` | 3 | 线宽 2，几何大小 7 × 257，颜色 0 |
| `0x62fc4d78` | `0xa00bd046` | 3 | 线宽 2，几何大小 7 × 20，颜色 `0xffffff` |
| `0x62fc4d90` | `0xa00b911a` | 2 | 线宽 1，几何大小 20 × 10，颜色 `0x808080` |

第 4 张表的调用点明确传入点数 2，所以只读取
`0x0058a4..0x0058b3` 的四个分量。紧接着的 `0x0058b4` 已被另一个直接
调用点作为独立布局表起点使用，不属于第 4 张坐标表。

## 5. 能力边界

- 已闭合：全部 88 个源字节，四张表的地址、点数、逐点值，
  以及原厂接收入口保存表指针与点数的完整交接路径。
- 保留未定：四张表的原厂符号名，以及它们在各页面上对应的
  可视图形名称。本文只依据实际接口称为坐标表，不根据外形命名箭头方向。
- 本段不授权改写点数、合并两张内容相同的表，或把后方的
  布局结束标记当成坐标分量。
