# `0x0058b4..0x0058bb` 七日天气单行布局描述表

## 1. 覆盖范围

- 原厂镜像：`ap01-1.0.2_0031.bin`
- 文件偏移：`0x0058b4..0x0058bb`
- 复制后运行地址：`0x62fc4da0..0x62fc4da7`
- 长度：8 字节
- 本段原始字节散列：`918c260fad1554c6760c01756ed621d406afde43fbbed29be6ee9b8c75959eff`
- 运行地址换算：文件偏移加 `0x62fbf4ec`

本段是七日天气详情页主容器的单行布局描述表。它与
`0x00582c..0x00584b` 的七列描述表在同一构造路径中分别传给
属性 129 和属性 130。

## 2. 逐项内容

| 文件偏移 | 运行地址 | 原始字节 | 小端数值 | 作用 |
| --- | --- | --- | --- | --- |
| `0x0058b4..0x0058b7` | `0x62fc4da0..0x62fc4da3` | `12 00 00 00` | 18 | 唯一一行的尺寸描述 |
| `0x0058b8..0x0058bb` | `0x62fc4da4..0x62fc4da7` | `ff ff ff 1f` | `0x1fffffff` | 描述表结束标记 |

## 3. 构造调用链

七日天气详情页构造路径在 `0xa00bd530..0xa00bd592` 明确将主容器
设为 320 × 18，完成边距、对齐和位置设置后，于：

- `0xa00bd596..0xa00bd5ae` 把七列描述表 `0x62fc4d18` 传给属性 130；
- `0xa00bd5b2..0xa00bd5ca` 把本段地址 `0x62fc4da0` 传给属性 129；
- 随后按七列循环创建子对象。

主容器的高度 18 与本表唯一尺寸项 18 相同，而七列表由
7 个 44 和结束标记组成；这一成对关系闭合了“七列一行”的布局结构。

## 4. 能力边界

- 已闭合：全部 8 个源字节、单行尺寸 18、结束标记、属性 129
  传入点，以及与七列表和主容器尺寸的成对关系。
- 保留未定：属性 129 的原厂宏名；本段不为它补造名称。
- 本段不授权修改尺寸 18，也不授权删除结束标记。
