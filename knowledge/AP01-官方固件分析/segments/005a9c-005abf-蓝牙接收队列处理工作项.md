# `0x005a9c..0x005abf` 蓝牙接收队列处理工作项

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

## 1. 覆盖范围

- 原厂镜像：`ap01-1.0.2_0031.bin`
- 文件偏移：`0x005a9c..0x005abf`
- 复制后运行地址：`0x62fc4f88..0x62fc4fab`
- 长度：36 字节
- 本段原始字节散列：`a12e61afbbc23236685a0b58b27b4c9d72ef0a21e844407a1b9ab7c19d6262ad`
- 运行地址换算：文件偏移加 `0x62fbf4ec`

## 2. 全部原始字段

| 对象偏移 | 原始值 | 直接证据 |
| --- | --- | --- |
| `+0..+19` | 五个全零字 | 静态工作项的链路、等待和私有状态初值均为空；本段没有证据支持继续拆出原厂字段名 |
| `+20..+23` | `0xa00dbf06` | 蓝牙接收队列处理函数地址 |
| `+24..+27` | `0` | 排队链接初值为空；后方工作队列函数会在排队时改写 |
| `+28..+31` | `0` | 排队状态初值为零；后方工作队列函数直接读取和改写 |
| `+32..+35` | `8` | 工作项状态字原厂初值；各位的原厂枚举名保留未定 |

以上九个 32 位字依次为：
`0, 0, 0, 0, 0, 0xa00dbf06, 0, 0, 8`，完整覆盖 36 字节。

## 3. 排队入口和触发条件

原厂有两条直接提交路径，均把工作队列 `0x62fced74` 和本对象
`0x62fc4f88` 交给 `0xa0105f8a`：

- `0xa00da93a..0xa00da96c` 在接收队列从空变为非空后保存新的队首、队尾，
  随即提交本工作项；队列已经非空时只追加，不重复走首次提交分支。
- `0xa00dc0f0..0xa00dc10a` 在处理完一条接收数据后检查蓝牙接收队列入口
  `0x62fc4110`；队列仍有数据时再次提交本工作项。

提交函数 `0xa0105f8a` 在关中断保护下调用内部排队函数 `0xa0105eb8`，
后者读取本对象 `+24`、`+28`、`+32`，将工作项接入目标队列，并在需要时唤醒工作线程。
因此本对象不是普通函数指针表，而是带排队状态的实际工作项。

## 4. 回调直接行为

回调 `0xa00dbf06` 的控制流为：

1. 在关中断保护下从蓝牙主机接收队列 `0x62fc4110` 取出一条缓冲；
2. 队列暂时为空时结束本轮，并恢复中断状态；
3. 根据缓冲类型分别进入连接数据接收或控制器事件处理；
4. 对长度不足、类型不支持或拼包状态不一致的记录输出错误并归还缓冲；
5. 一条处理结束后检查队列，仍有记录就通过前述路径重新提交本工作项。

本镜像采用“工作项排队处理接收队列”的实现。官方当前同源代码在对应编译配置下展示的
是独立接收线程循环，因此这里只用原厂镜像指令确定对象身份和行为，不拿当前线程版本覆盖
本镜像的工作项结构。

## 5. 能力边界

- 已闭合：全部 36 个源字节、唯一回调地址、两条提交路径、排队状态访问和接收队列
  逐条处理关系。
- `+0..+19` 的五个零字和状态字 8 已按原值保存；在没有同采用时期结构定义前，
  不猜分字段名称和位枚举。
- 本段不授权把工作项替换为当前官方代码中的独立接收线程，也不授权改变回调、队列地址
  或状态初值；两种实现的调度边界不同。
