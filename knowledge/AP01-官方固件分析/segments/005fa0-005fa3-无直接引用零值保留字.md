# `0x005fa0..0x005fa3` 无直接引用零值保留字

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

## 1. 覆盖范围

- 原厂镜像：`ap01-1.0.2_0031.bin`
- 文件偏移：`0x005fa0..0x005fa3`
- 复制后运行地址：`0x62fc548c..0x62fc548f`
- 长度：4 字节
- 本段原始字节散列：`df3f619804a92fdb4057192dc43dd748ea778adc52bc498ce80524c014b81119`
- 运行地址换算：文件偏移加 `0x62fbf4ec`

## 2. 原始内容与边界

本段原始内容是一个四字节零值。当前镜像中没有找到对 `0x62fc548c` 的直接读写、整体地址
引用或数据指针引用，因此不根据相邻内容给它虚构字段名。

前方 `0x005f9c..0x005f9f` 已由国家设置函数固定三字节复制和独立首地址闭合；后方
`0x005fa4` 已由界面构造调用直接作为三点坐标表首地址使用。因此本零字既不属于前方国家
代码，也不属于后方坐标表。

## 3. 能力边界

- 已闭合：全部 4 个源字节，以及与前后两个已直接寻址对象的边界。
- 保留未定：原厂符号名、所属模块和用途。没有直接引用不能证明运行时绝对不会通过更大对象
  的间接地址访问，所以本文只记录当前可验证边界，不把“未发现”写成“永远不用”。
