# `0x0060c8..0x0060f7` 三张双点界面线段坐标表

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

坐标点结构和接收入口的唯一事实原文见
[`0x00585c..0x0058b3` 四张界面对象坐标表](00585c-0058b3-四张界面对象坐标表.md)；本文只记录
本段三张表的值和调用。

## 1. 覆盖范围

- 文件偏移：`0x0060c8..0x0060f7`
- 复制后运行地址：`0x62fc55b4..0x62fc55e3`
- 长度：48 字节
- 本段原始字节散列：`1e43ed6251fbe9160ef472fa83a8c3feae324692b364dff66fac4d2269617e05`

## 2. 全部点值

| 文件范围 | 运行地址 | 点序列 | 几何结果 |
| --- | --- | --- | --- |
| `0x0060c8..0x0060d7` | `0x62fc55b4` | `(0, 198)`、`(0, 0)` | 198 像素高的竖线 |
| `0x0060d8..0x0060e7` | `0x62fc55c4` | `(0, 0)`、`(82, 0)` | 82 像素宽的水平线 |
| `0x0060e8..0x0060f7` | `0x62fc55d4` | `(0, 0)`、`(36, 0)` | 36 像素宽的水平线 |

三张表均为两个点，各占 16 字节，没有共用尾项。

## 3. 直接调用

- `0xa01acccc..0xa01accd6` 把第一张表和点数 2 交给坐标接收入口；同一父对象此前被设为
  198 × 82。
- `0xa01acd42..0xa01acdda` 把第二张表和点数 2 交给构造入口共六次。首次定位参数包含
  `4, 17, 5`，其余五次共用 `9, 30, 0`。
- `0xa01ad590..0xa01ad5ac` 把第三张表和点数 2 交给构造入口一次，定位参数包含
  `1, 130, 17`。

## 4. 能力边界

- 已闭合：48 个源字节、三张表边界、全部点值、点数和八条实际构造调用。
- 几何名称只描述点形成的线段；原厂按钮、分隔线或页面控件名称没有直接证据，保留未定。
