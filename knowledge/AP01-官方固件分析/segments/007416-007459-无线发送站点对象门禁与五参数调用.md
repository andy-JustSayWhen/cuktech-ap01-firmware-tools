# `0x007416..0x007459` 无线发送站点对象门禁与五参数调用

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段重新取得当前站点项 `+0xac` 所指对象的首地址，要求对象 `+0x60` 字节等于 1；通过后
保存两个描述对象地址，并把五个可确定参数传给后方函数 `0xa0007e38`。

## 1. 覆盖范围

- 文件偏移：`0x007416..0x007459`
- 执行地址：`0xa0006416..0xa0006459`
- 长度：68 字节
- 本段原始字节散列：`a754b5697badaf884fcfb62d58dbd611ec32af6f7934a97b451a1f946ab7b7fa`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006416` | `022d4703` | 读取发送描述 `+0x22` 的站点编号。 |
| `0xa000641a` | `23000793` | 把站点项跨度设为 560 字节。 |
| `0xa000641e` | `20f7148b` | 以站点表基址 `0x62fd4a10` 定位当前站点项。 |
| `0xa0006422` | `0ac4a783` | 读取当前站点项 `+0xac` 的对象地址。 |
| `0xa0006426` | `438c` | 读取该对象开头记录的另一对象地址。 |
| `0xa0006428` | `120581e3` | 内层对象地址为空时跳往后方 `0xa0006d4a` 路径。 |
| `0xa000642c` | `054d2b03` | 读取并保存发送描述 `+0x54` 的对象地址。 |
| `0xa0006430` | `0605c703` | 读取站点内层对象 `+0x60` 的门禁字节。 |
| `0xa0006434` | `4785` | 把允许值设为 1。 |
| `0xa0006436` | `058d2483` | 读取并保存发送描述 `+0x58` 的对象地址。 |
| `0xa000643a` | `080b0913` | 计算描述 `+0x54` 对象 `+0x80` 的地址并保留给后方。 |
| `0xa000643e` | `12f71de3` | 门禁字节不等于 1 时跳往后方 `0xa0006d78` 路径。 |
| `0xa0006442` | `020d4703` | 把描述 `+0x20` 的单字节值设为第五参数。 |
| `0xa0006446` | `014d0693` | 把描述 `+0x14` 的地址设为第四参数。 |
| `0xa000644a` | `00ed0613` | 把描述 `+0x0e` 的地址设为第三参数。 |
| `0xa000644e` | `05058593` | 把站点内层对象 `+0x50` 的地址设为第二参数。 |
| `0xa0006452` | `fb040513` | 把当前函数栈中回填槽地址设为第一参数。 |
| `0xa0006456` | `1e3010ef` | 调用后方函数 `0xa0007e38`。 |

## 3. 门禁与调用参数

只有站点项 `+0xac` 所指对象开头记录的内层对象非空，且内层对象 `+0x60 == 1` 时才执行
当前调用。五个参数依次是：

1. 当前函数栈中的回填槽地址；
2. 站点内层对象 `+0x50` 的地址；
3. 发送描述 `+0x0e` 的地址；
4. 发送描述 `+0x14` 的地址；
5. 发送描述 `+0x20` 的单字节值。

调用前还保存了描述 `+0x54`、`+0x58` 两个对象地址，并形成 `描述[+0x54] + 0x80`，这些
值从后续链表处理继续使用。`0xa0007e38` 尚未按文件顺序读到，本文不提前定义其回填内容。

## 4. 能力边界

- 已闭合：68 个源字节、18 条指令、两层站点对象门禁、三个保留地址和五项调用参数。
- `0xa0006d4a`、`0xa0006d78` 两条失败路径待顺序读取。
- `0xa0007e38` 的内部算法与栈回填格式尚待后续通读。
- 大型函数仍未闭合。
