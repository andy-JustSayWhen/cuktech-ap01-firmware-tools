# `0x007528..0x00754d` 无线发送新编号状态项预填与调用

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段以新编号在 `0x62fd858c` 表中定位 32 字节状态项，预填描述站点编号和描述索引值，
保存新编号后调用 `0xa012f664`。

## 1. 覆盖范围

- 文件偏移：`0x007528..0x00754d`
- 执行地址：`0xa0006528..0xa000654d`
- 长度：38 字节
- 本段原始字节散列：`a9395cd3ba119d0fcd4a85d300b8e611c480b4d4cb1f2fa67da56fb7939d5aab`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006528` | `fa842603` | 从栈中恢复发送描述 `+0x20` 的索引值。 |
| `0xa000652c` | `62fd85b7` | 装入 32 字节状态项表基址的高位。 |
| `0xa0006530` | `00531993` | 计算新编号乘 32 的状态项偏移。 |
| `0xa0006534` | `58c58593` | 得到状态项表基址 `0x62fd858c`。 |
| `0xa0006538` | `99ae` | 得到新编号对应的 32 字节状态项地址。 |
| `0xa000653a` | `01798823` | 把发送描述 `+0x22` 的站点编号写入状态项 `+0x10`。 |
| `0xa000653e` | `00c98a23` | 把描述 `+0x20` 的索引值写入状态项 `+0x14`。 |
| `0xa0006542` | `fa642223` | 把新编号暂存到当前函数栈。 |
| `0xa0006546` | `00129097` | 形成后方函数地址的高位并保存返回点。 |
| `0xa000654a` | `11e080e7` | 调用 `0xa012f664`。 |

## 3. 状态项与调用现场

状态项地址公式是 `0x62fd858c + 新编号 * 32`。调用前只明确预填两字节：状态项 `+0x10`
接收描述站点编号，状态项 `+0x14` 接收描述 `+0x20` 的索引值。

按调用约定中原厂此处实际保留的前三个参数寄存器，调用 `0xa012f664` 时分别是：前一函数
`0xa01642f4` 的返回对象、状态表基址 `0x62fd858c`、描述 `+0x20` 的索引值。新编号另存栈中，
因为调用返回后还会继续使用。

## 4. 能力边界

- 已闭合：38 个源字节、10 条指令、32 字节状态项定位、两项预填、栈保存和调用现场。
- `0xa012f664` 实际读取哪些参数及内部修改哪些对象，待顺序读到函数本体后闭合。
- 当前状态项仍有更多字段在后续写入。
- 大型函数仍未闭合。
