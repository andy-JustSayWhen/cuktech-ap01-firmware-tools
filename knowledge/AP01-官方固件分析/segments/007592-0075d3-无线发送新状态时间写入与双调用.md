# `0x007592..0x0075d3` 无线发送新状态时间写入与双调用

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段把固定地址当前读数写入新状态项，把新编号回写到十二字节索引区并暂存，随后以两组
不同的七参数连续调用同一个后方函数 `0xa0165966`。

## 1. 覆盖范围

- 文件偏移：`0x007592..0x0075d3`
- 执行地址：`0xa0006592..0xa00065d3`
- 长度：66 字节
- 本段原始字节散列：`273f3f6f5308d9d82e64c66fbc7afb1f8b0a2a5f193656c7dd0cddc130f82bd2`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006592` | `120c2783` | 读取固定地址 `0x24b00120` 的当前四字节值。 |
| `0xa0006596` | `fa442303` | 从栈中恢复新编号。 |
| `0xa000659a` | `85ce` | 把新编号对应的 32 字节状态项地址准备为第二参数。 |
| `0xa000659c` | `00f9a423` | 把固定地址当前读数写入状态项 `+0x08`。 |
| `0xa00065a0` | `1a6b00a3` | 把新编号写入当前十二字节索引地址 `+0x1a1`。 |
| `0xa00065a4` | `855e` | 把发送描述 `+0x22` 的站点编号设为第一参数。 |
| `0xa00065a6` | `4801` | 把第七参数设为 0。 |
| `0xa00065a8` | `02700793` | 把第六参数设为 39。 |
| `0xa00065ac` | `4701` | 把第五参数设为 0。 |
| `0xa00065ae` | `4681` | 把第四参数设为 0。 |
| `0xa00065b0` | `4609` | 把第三参数设为 2。 |
| `0xa00065b2` | `fa642623` | 把新编号另存到当前函数栈的后续槽位。 |
| `0xa00065b6` | `0015f097` | 形成后方函数地址的高位并保存返回点。 |
| `0xa00065ba` | `3b0080e7` | 以第一组七参数调用 `0xa0165966`。 |
| `0xa00065be` | `4801` | 把第二次调用第七参数设为 0。 |
| `0xa00065c0` | `4701` | 把第五参数设为 0。 |
| `0xa00065c2` | `4681` | 把第四参数设为 0。 |
| `0xa00065c4` | `85ce` | 把同一状态项地址设为第二参数。 |
| `0xa00065c6` | `855e` | 把同一站点编号设为第一参数。 |
| `0xa00065c8` | `4781` | 把第六参数设为 0。 |
| `0xa00065ca` | `4601` | 把第三参数设为 0。 |
| `0xa00065cc` | `0015f097` | 再次形成同一后方函数地址并保存返回点。 |
| `0xa00065d0` | `39a080e7` | 以第二组七参数调用 `0xa0165966`。 |

## 3. 两次调用参数

两次调用的第一、第二参数相同，分别是描述站点编号和新编号对应的 32 字节状态项地址。
完整参数依次为：

| 调用 | 参数 1..7 |
| --- | --- |
| 第一次 | `站点编号, 状态项地址, 2, 0, 0, 39, 0` |
| 第二次 | `站点编号, 状态项地址, 0, 0, 0, 0, 0` |

两次返回值在当前函数中都没有被读取。第一次调用前，新编号还覆盖写入十二字节索引地址
`+0x1a1`，并保存到栈中供后续更新旧时间字段。

## 4. 能力边界

- 已闭合：66 个源字节、23 条指令、时间读数写入、新编号回填和两组完整七参数。
- `0xa0165966` 的内部动作以及两种参数组合的业务差异待顺序读到函数本体后闭合。
- 当前状态项初始化尚有后续时间与标志更新。
- 大型函数仍未闭合。
