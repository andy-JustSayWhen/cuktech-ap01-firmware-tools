# `0x0075d4..0x007605` 无线发送新编号组合与两次收尾调用

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段恢复新编号并组合成 `新编号 * 256 + 8`，把固定地址当前读数更新到十二字节索引区，
随后用两组确定参数调用两个后方函数，最后跳回共用路径 `0xa0006184`。

## 1. 覆盖范围

- 文件偏移：`0x0075d4..0x007605`
- 执行地址：`0xa00065d4..0xa0006605`
- 长度：50 字节
- 本段原始字节散列：`c6aefe86f98eb8e4ee7fc0c942d053bbdd97f5d7af95cc253a07eb33c61147af`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa00065d4` | `fac42303` | 从栈中恢复新编号。 |
| `0xa00065d8` | `120c2783` | 读取固定地址 `0x24b00120` 的当前四字节值。 |
| `0xa00065dc` | `0007d637` | 把第一次调用第三参数设为 `0x0007d000`。 |
| `0xa00065e0` | `00831993` | 把新编号左移 8 位。 |
| `0xa00065e4` | `0089e993` | 得到组合值 `新编号 * 256 + 8`。 |
| `0xa00065e8` | `85ce` | 把组合值设为第一次调用第二参数。 |
| `0xa00065ea` | `18fb2c23` | 把固定地址当前读数写入十二字节索引地址 `+0x198`。 |
| `0xa00065ee` | `6509` | 把第一次调用第一参数设为 `0x2000`。 |
| `0xa00065f0` | `0015e097` | 形成第一个后方函数地址的高位并保存返回点。 |
| `0xa00065f4` | `34e080e7` | 调用 `0xa016493e(0x2000, 组合值, 0x7d000)`。 |
| `0xa00065f8` | `4589` | 把第二次调用第二参数设为 2。 |
| `0xa00065fa` | `854e` | 把组合值设为第二次调用第一参数。 |
| `0xa00065fc` | `0015f097` | 形成第二个后方函数地址的高位并保存返回点。 |
| `0xa0006600` | `e28080e7` | 调用 `0xa0165424(组合值, 2)`。 |
| `0xa0006604` | `b641` | 跳回共用路径 `0xa0006184`。 |

## 3. 状态更新与调用次序

本段先更新 `0x62fd4a10 + 描述[+0x20] * 12 + 0x198` 的四字节值为当前
`读取四字节[0x24b00120]`，再执行两次调用。两次调用的返回值均未在当前函数中使用，且第二次
调用完成后无条件回到 `0xa0006184`，不会顺序进入下一段。

组合值在两个调用间保持不变，精确公式是 `新编号 << 8 | 8`。

## 4. 能力边界

- 已闭合：50 个源字节、15 条指令、新编号组合公式、旧时间更新、两组调用参数和最终去向。
- `0xa016493e` 与 `0xa0165424` 的内部行为待顺序读到函数本体后闭合。
- 十二字节索引区 `+0x198` 的正式字段名仍无原厂结构定义证据。
- 大型函数仍未闭合。
