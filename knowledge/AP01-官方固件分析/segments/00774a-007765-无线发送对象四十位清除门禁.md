# `0x00774a..0x007765` 无线发送对象四十位清除门禁

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段检查扩展对象 `+0x106` 的 `0x40` 位；该位未设置时直接进入后方计算，设置时则先按
对象 `+0x118` 的半字值决定清位或转入另一条处理路径。

## 1. 覆盖范围

- 文件偏移：`0x00774a..0x007765`
- 执行地址：`0xa000674a..0xa0006765`
- 长度：28 字节
- 本段原始字节散列：`677ddf3207ce3b90fdd2b5f6f5e7516c8915e8909543a5b7298e50af74b00cf1`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa000674a` | `04077713` | 从前段已经读取的扩展对象 `+0x106` 标志中提取 `0x40` 位。 |
| `0xa000674e` | `cf01` | `0x40` 位为零时直接进入 `0xa0006766` 的后方计算。 |
| `0xa0006750` | `118cd683` | `0x40` 位置位时读取扩展对象 `+0x118` 的 16 位值。 |
| `0xa0006754` | `4725` | 准备比较上界常数 `9`。 |
| `0xa0006756` | `10d76063` | `+0x118` 大于 `9` 时跳到 `0xa0006856` 的另一条处理路径。 |
| `0xa000675a` | `106cc703` | `+0x118` 不大于 `9` 时重新读取扩展对象 `+0x106` 标志。 |
| `0xa000675e` | `fbf77713` | 清除该标志的 `0x40` 位。 |
| `0xa0006762` | `10ec8323` | 把清位结果写回扩展对象 `+0x106`，随后顺序进入 `0xa0006766`。 |

## 3. 分支关系

本段形成三条去向：

1. 对象 `+0x106` 的 `0x40` 位为零：直接去 `0xa0006766`；
2. `0x40` 位置位且对象 `+0x118` 不大于 `9`：清除 `0x40` 位，再去 `0xa0006766`；
3. `0x40` 位置位且对象 `+0x118` 大于 `9`：保留该位并跳到 `0xa0006856`。

因此 `+0x118` 的数值边界只在 `0x40` 位置位时参与控制流。

## 4. 能力边界

- 已闭合：28 个源字节、8 条指令、对象 `0x40` 位的检查、半字上界比较、清位写回和三条去向。
- `0xa0006856` 目标路径位于后续尚未顺序覆盖的范围，本段只记录直接分支关系，不提前解释其作用。
- 对象 `+0x106`、`+0x118` 的正式字段名称尚无原厂结构定义证据。
- 大型函数仍未闭合。
