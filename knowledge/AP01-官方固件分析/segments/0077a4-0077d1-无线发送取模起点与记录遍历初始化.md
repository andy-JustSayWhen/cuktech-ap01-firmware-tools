# `0x0077a4..0x0077d1` 无线发送取模起点与记录遍历初始化

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段再次调用前文同一后方函数，以返回值低 16 位对对象 `+0x118` 取余，随后准备记录比较
常数、循环编号、二十字节跨度和记录区基址，并以对象 `+0x118` 作为遍历上界。

## 1. 覆盖范围

- 文件偏移：`0x0077a4..0x0077d1`
- 执行地址：`0xa00067a4..0xa00067d1`
- 长度：46 字节
- 本段原始字节散列：`063595ebf78760307fed953c89d03bd29c3bf8c4fc3403e1635bbc44fafec6a4`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa00067a4` | `00129097` | 形成后方函数地址的高位并保存返回点。 |
| `0xa00067a8` | `ec0080e7` | 调用 `0xa012f664`。 |
| `0xa00067ac` | `3c05370b` | 提取函数返回值的低 16 位。 |
| `0xa00067b0` | `118cd503` | 读取扩展对象 `+0x118` 的 16 位值，作为取余除数。 |
| `0xa00067b4` | `6bbd` | 开始构造比较常数 `0xf333`。 |
| `0xa00067b6` | `333b8793` | 完成比较常数 `0xf333`，十进制为 `62,259`。 |
| `0xa00067ba` | `4301` | 把遍历编号初始化为 `0`。 |
| `0xa00067bc` | `4ed1` | 准备记录跨度常数 `20`。 |
| `0xa00067be` | `faf42423` | 暂存比较常数 `0xf333`，供循环体使用。 |
| `0xa00067c2` | `4b85` | 准备循环体使用的常数 `1`。 |
| `0xa00067c4` | `9c66` | 把前段保存的 `20 × 对象[+0xe8]` 加到对象基址，得到当前记录基址。 |
| `0xa00067c6` | `02a77733` | 计算函数返回低 16 位对对象 `+0x118` 的无符号余数。 |
| `0xa00067ca` | `118cdd83` | 再次读取对象 `+0x118`，作为遍历上界。 |
| `0xa00067ce` | `9db359e3` | 初始编号 `0` 不小于上界时回到 `0xa00061a0`；否则进入循环体。 |

## 3. 初始化结果

进入后续循环体前，本段建立：

- 起始余数：`低16位(0xa012f664 返回值) % 对象[+0x118]`；
- 遍历编号：`0`；
- 遍历上界：对象 `+0x118`；
- 当前记录基址：`对象基址 + 20 × 对象[+0xe8]`；
- 记录跨度：`20`；
- 比较常数：`0xf333`；
- 辅助常数：`1`。

若遍历上界不大于初始编号 `0`，控制流不进入循环体。

## 4. 能力边界

- 已闭合：46 个源字节、14 条指令、调用、低半字提取、取余公式和循环初始化。
- `0xa012f664` 的业务功能仍待顺序读到函数本体后确定；本段没有用地址扫描替代函数解读。
- 比较常数 `0xf333` 的业务含义以及后续循环筛选条件尚待连续覆盖。
- 大型函数仍未闭合。
