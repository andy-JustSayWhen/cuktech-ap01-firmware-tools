# `0x007810..0x007855` 无线发送候选选定与描述状态更新

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段处理前段初筛通过的候选编号：把编号和活动值写入扩展对象，设置描述 `+0x1c` 的
`0x2000` 位并清描述 `+0x42` 的低两位。随后检查描述 `+0x30` 的 `0x00200000` 位；该位
为零时还会设置一个按索引定位对象的两位标志，然后回到前方主路径。

## 1. 覆盖范围

- 文件偏移：`0x007810..0x007855`
- 执行地址：`0xa0006810..0xa0006855`
- 长度：70 字节
- 本段原始字节散列：`8364241853dda546885ddd0009b0569efc58c8c6f1a1332eb417d026b9760687`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006810` | `4705` | 准备活动值 `1`。 |
| `0xa0006812` | `11bc8223` | 把候选编号低字节写入扩展对象 `+0x104`。 |
| `0xa0006816` | `0eec8d23` | 把活动值 `1` 写入扩展对象 `+0xfa`。 |
| `0xa000681a` | `01cd5703` | 读取描述对象 `+0x1c` 的 16 位标志。 |
| `0xa000681e` | `6689` | 准备半字标志位 `0x2000`。 |
| `0xa0006820` | `8f55` | 设置描述半字的 `0x2000` 位。 |
| `0xa0006822` | `00ed1e23` | 把设置结果写回描述对象 `+0x1c`。 |
| `0xa0006826` | `042d4703` | 读取描述对象 `+0x42` 的单字节标志。 |
| `0xa000682a` | `002006b7` | 准备字标志位 `0x00200000`。 |
| `0xa000682e` | `9b71` | 清除描述字节的最低两位。 |
| `0xa0006830` | `04ed0123` | 把清位结果写回描述对象 `+0x42`。 |
| `0xa0006834` | `030d2703` | 读取描述对象 `+0x30` 的 32 位标志。 |
| `0xa0006838` | `8ef9` | 提取该字的 `0x00200000` 位。 |
| `0xa000683a` | `36069d63` | `0x00200000` 位置位时跳到 `0xa0006bb4`。 |
| `0xa000683e` | `23000713` | 准备索引对象跨度 `560`。 |
| `0xa0006842` | `86a6` | 以先前保存的基址初始化目标地址。 |
| `0xa0006844` | `20eb168b` | 在基址上加 `560 × 索引`，定位对应对象。 |
| `0xa0006848` | `14e6c703` | 读取该对象 `+0x14e` 的单字节标志。 |
| `0xa000684c` | `03076713` | 设置该字节的 `0x30` 两位。 |
| `0xa0006850` | `14e68723` | 把设置结果写回索引对象 `+0x14e`。 |
| `0xa0006854` | `b2b1` | 回到大型函数前方的 `0xa00061a0`。 |

## 3. 选定后的写入

前段候选通过当前顺序路径后，本段产生以下直接结果：

- `扩展对象[+0x104] = 候选编号低8位`；
- `扩展对象[+0xfa] = 1`；
- `描述对象[+0x1c] |= 0x2000`；
- `描述对象[+0x42] &= 0xfc`；
- 若 `描述对象[+0x30] & 0x00200000 == 0`，则
  `索引对象[基址 + 560 × 索引 + 0x14e] |= 0x30`。

描述字位非零时转到 `0xa0006bb4`，不会在本段执行最后一项索引对象写入。

## 4. 能力边界

- 已闭合：70 个源字节、21 条指令、候选编号落位、四处状态修改、描述字位分支和主路径回跳。
- `0xa0006bb4` 目标路径尚未顺序覆盖，本段不提前赋予业务含义。
- 两类对象及其字段的正式结构名称尚无原厂定义证据。
- 大型函数仍未闭合。
