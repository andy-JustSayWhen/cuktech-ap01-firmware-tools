# `0x007882..0x0078b1` 无线发送六项暂存分派与默认项写入

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段以此前保存的返回值作为项目编号，对编号 `1`、`2`、`3` 以及大于 `3` 的值分别转入
后方路径；其余值在当前路径调用 `0xa01638f0`，把返回低 16 位写入十二字节暂存区对应项。
各项处理后编号加一，未达到 `6` 时回到分派入口。

## 1. 覆盖范围

- 文件偏移：`0x007882..0x0078b1`
- 执行地址：`0xa0006882..0xa00068b1`
- 长度：48 字节
- 本段原始字节散列：`6a2f8761706a935525bdd61c872011a145afd62f05c660148aaf6155bd18bf7f`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006882` | `478d` | 准备分派比较值 `3`。 |
| `0xa0006884` | `10f98263` | 项目编号等于 `3` 时跳到 `0xa0006988`。 |
| `0xa0006888` | `0937e963` | 项目编号大于 `3` 时跳到 `0xa000691a`。 |
| `0xa000688c` | `4785` | 准备分派比较值 `1`。 |
| `0xa000688e` | `0af98f63` | 项目编号等于 `1` 时跳到 `0xa000694c`。 |
| `0xa0006892` | `4789` | 准备分派比较值 `2`。 |
| `0xa0006894` | `0cf98463` | 项目编号等于 `2` 时跳到 `0xa000695c`。 |
| `0xa0006898` | `8566` | 当前直行项目把扩展对象基址作为第一参数。 |
| `0xa000689a` | `0015d097` | 形成后方函数地址的高位并保存返回点。 |
| `0xa000689e` | `056080e7` | 调用 `0xa01638f0`。 |
| `0xa00068a2` | `faa41823` | 把函数返回值低 16 位写入十二字节暂存区首项。 |
| `0xa00068a6` | `0985` | 项目编号加一。 |
| `0xa00068a8` | `0ff9f993` | 把递增结果限制为低 8 位。 |
| `0xa00068ac` | `4799` | 准备项目结束值 `6`。 |
| `0xa00068ae` | `fcf99ae3` | 项目编号不等于 `6` 时回到 `0xa0006882` 再次分派。 |

## 3. 分派关系

本段直接确定：

| 项目编号条件 | 去向 |
| --- | --- |
| 等于 `1` | `0xa000694c` |
| 等于 `2` | `0xa000695c` |
| 等于 `3` | `0xa0006988` |
| 大于 `3` | `0xa000691a` |
| 以上均不满足 | 调用 `0xa01638f0`，返回低半字写到暂存区首项 |

每条项目处理路径后方都会合到 `0xa00068a6`。本段已确认会合处把编号按低八位加一，并以
`6` 作为循环结束值；各后方分支具体写入哪一项，需继续顺序覆盖后闭合。

## 4. 能力边界

- 已闭合：48 个源字节、15 条指令、五路分派、当前直行调用、首项半字写入和循环控制。
- `0xa01638f0` 的函数本体尚未顺序覆盖，本段只保存直接参数和返回值用途。
- 四个后方分支目标将在后续地址顺序读到时解释，不以地址扫描提前替代。
- 大型函数仍未闭合。
