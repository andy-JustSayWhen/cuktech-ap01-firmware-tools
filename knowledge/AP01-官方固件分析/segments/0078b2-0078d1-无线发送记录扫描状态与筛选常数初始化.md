# `0x0078b2..0x0078d1` 无线发送记录扫描状态与筛选常数初始化

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段在六项暂存值生成结束后初始化另一轮记录扫描：读取对象 `+0x118` 作为上界，清零对象
`+0x08`，把扫描编号设为 `1`、辅助编号设为 `0`，并准备记录跨度、地址和四个比较常数。

## 1. 覆盖范围

- 文件偏移：`0x0078b2..0x0078d1`
- 执行地址：`0xa00068b2..0xa00068d1`
- 长度：32 字节
- 本段原始字节散列：`426264607fa88d4c7426f3e247db0686e8b99e9cc0a14730a5cdba2586d793d9`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa00068b2` | `118cde83` | 读取扩展对象 `+0x118` 的 16 位扫描上界。 |
| `0xa00068b6` | `6329` | 开始构造比较常数 `0xa665`。 |
| `0xa00068b8` | `6e41` | 开始构造比较常数 `0xffff`。 |
| `0xa00068ba` | `000ca423` | 把扩展对象 `+0x08` 的 32 位字段清零。 |
| `0xa00068be` | `4605` | 把扫描编号初始化为 `1`。 |
| `0xa00068c0` | `4801` | 把辅助编号初始化为 `0`。 |
| `0xa00068c2` | `4551` | 准备记录跨度常数 `20`。 |
| `0xa00068c4` | `66530313` | 完成比较常数 `0xa665`，十进制为 `42,597`。 |
| `0xa00068c8` | `4f15` | 准备比较常数 `5`。 |
| `0xa00068ca` | `1e7d` | 完成比较常数 `0xffff`。 |
| `0xa00068cc` | `016c8f93` | 准备扩展对象 `+0x16` 的地址。 |
| `0xa00068d0` | `42a9` | 准备比较常数 `10`。 |

## 3. 初始化结果

后续扫描入口接收到以下状态：

- 扫描上界：对象 `+0x118`；
- 扫描编号：`1`；
- 辅助编号：`0`；
- 记录跨度：`20`；
- 辅助地址：对象基址 `+0x16`；
- 比较常数：`0xa665`、`0xffff`、`5`、`10`；
- 对象 `+0x08` 已清零。

## 4. 能力边界

- 已闭合：32 个源字节、12 条指令、扫描上界、两项编号、记录跨度、辅助地址、四个常数和清零写入。
- 各比较常数参与哪些字段判定，要从 `0xa00068d2` 起继续顺序闭合。
- 对象 `+0x08` 与 `+0x16` 的正式字段名称尚无原厂结构定义证据。
- 大型函数仍未闭合。
