# `0x00794c..0x007987` 无线发送第一二编号与第二三暂存项写入

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合六项暂存分派的编号 `1` 和编号 `2`。编号 `1` 调用 `0xa01638f0` 后直接写入
暂存区第二项；编号 `2` 以前文保存的第一项记录半字调用 `0xa016339a`，仅在值发生变化且
`0xa0163f1a` 返回非零时写入暂存区第三项。

## 1. 覆盖范围

- 文件偏移：`0x00794c..0x007987`
- 执行地址：`0xa000694c..0xa0006987`
- 长度：60 字节
- 本段原始字节散列：`dab4c49bef2d57d3eb138e8fc7f48ceb2a553cac695f1506861c95f1ec5ce5c1`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa000694c` | `8566` | 编号 `1` 路径把扩展对象基址作为第一参数。 |
| `0xa000694e` | `0015d097` | 形成后方函数地址的高位并保存返回点。 |
| `0xa0006952` | `fa2080e7` | 调用 `0xa01638f0`。 |
| `0xa0006956` | `faa41923` | 把返回值低 16 位写入十二字节暂存区 `+2`，即第二项。 |
| `0xa000695a` | `b7b1` | 跳到项目编号递增处 `0xa00068a6`。 |
| `0xa000695c` | `85de` | 编号 `2` 路径把前文保存的第一项记录半字作为第二参数。 |
| `0xa000695e` | `8566` | 把扩展对象基址作为第一参数。 |
| `0xa0006960` | `0015d097` | 形成第一转换函数地址的高位并保存返回点。 |
| `0xa0006964` | `a3a080e7` | 调用 `0xa016339a`。 |
| `0xa0006968` | `85aa` | 把转换结果同时准备为检查函数第二参数。 |
| `0xa000696a` | `f2ab8ee3` | 转换结果等于原第一项记录半字时直接去项目编号递增处。 |
| `0xa000696e` | `faa42623` | 暂存发生变化的转换结果。 |
| `0xa0006972` | `8566` | 把扩展对象基址作为检查函数第一参数。 |
| `0xa0006974` | `0015d097` | 形成检查函数地址的高位并保存返回点。 |
| `0xa0006978` | `5a6080e7` | 调用 `0xa0163f1a`。 |
| `0xa000697c` | `d50d` | 检查返回值为零时直接去项目编号递增处。 |
| `0xa000697e` | `fac42583` | 检查非零时取回暂存的转换结果。 |
| `0xa0006982` | `fab41a23` | 把转换结果低 16 位写入暂存区 `+4`，即第三项。 |
| `0xa0006986` | `b705` | 跳到项目编号递增处 `0xa00068a6`。 |

## 3. 两条写入路径

编号 `1` 无额外门禁：

`暂存区第二项 = 低16位(0xa01638f0(扩展对象))`

编号 `2` 的第三项仅在以下条件全部满足时更新：

1. `新值 = 0xa016339a(扩展对象, 第一项记录半字)`；
2. 新值不等于原第一项记录半字；
3. `0xa0163f1a(扩展对象, 新值)` 返回非零；
4. `暂存区第三项 = 新值低16位`。

## 4. 能力边界

- 已闭合：60 个源字节、19 条指令、编号 `1` 和 `2` 的调用参数、门禁、写入位置和共同会合点。
- 三个被调用函数的本体尚未顺序覆盖，本段只记录直接输入、返回判断和写回效果。
- 暂存半字的正式业务名称仍无原厂结构证据。
- 大型函数仍未闭合。
