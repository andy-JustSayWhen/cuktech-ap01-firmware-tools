# `0x007c36..0x007ca7` 无线发送二百模式与零模式半字布局

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段按前方构造模式值处理两种半字布局。模式 `0x0200` 时，从描述对象 `+0x0e..+0x18`
选取六个半字写入构造区；模式 `0` 时，前三个半字仍来自描述对象 `+0x0e..+0x12`，后三个
改从另一对象区“既有偏移加 `364`”处连续读取。

## 1. 覆盖范围

- 文件偏移：`0x007c36..0x007ca7`
- 执行地址：`0xa0006c36..0xa0006ca7`
- 长度：114 字节
- 本段原始字节散列：`62405988c598a25f36e18caa7bb7d456737cee78bbd8bb8d2ba050415f64083f`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006c36` | `20000813` | 准备构造模式比较值 `0x0200`。 |
| `0xa0006c3a` | `03079a63` | 当前模式不等于 `0x0200` 时转到模式零或其他非零模式分流。 |
| `0xa0006c3e` | `00ed5783` | 模式 `0x0200`：读取描述对象 `+0x0e` 半字。 |
| `0xa0006c42` | `00f99223` | 写入构造区 `+0x04`。 |
| `0xa0006c46` | `010d5783` | 读取描述对象 `+0x10` 半字。 |
| `0xa0006c4a` | `00f99323` | 写入构造区 `+0x06`。 |
| `0xa0006c4e` | `012d5783` | 读取描述对象 `+0x12` 半字。 |
| `0xa0006c52` | `00f99423` | 写入构造区 `+0x08`。 |
| `0xa0006c56` | `014d5783` | 读取描述对象 `+0x14` 半字。 |
| `0xa0006c5a` | `00f99823` | 写入构造区 `+0x10`。 |
| `0xa0006c5e` | `016d5783` | 读取描述对象 `+0x16` 半字。 |
| `0xa0006c62` | `00f99923` | 写入构造区 `+0x12`。 |
| `0xa0006c66` | `018d5783` | 读取描述对象 `+0x18` 半字。 |
| `0xa0006c6a` | `f66ff06f` | 回到 `0xa00063d0`，由前方公共代码写入构造区 `+0x14`。 |
| `0xa0006c6e` | `ef8d` | 当前模式为其他非零值时跳到 `0xa0006ca8`；为零时顺序继续。 |
| `0xa0006c70` | `00ed5783` | 模式 `0`：读取描述对象 `+0x0e` 半字。 |
| `0xa0006c74` | `00f99223` | 写入构造区 `+0x04`。 |
| `0xa0006c78` | `010d5783` | 读取描述对象 `+0x10` 半字。 |
| `0xa0006c7c` | `00f99323` | 写入构造区 `+0x06`。 |
| `0xa0006c80` | `012d5783` | 读取描述对象 `+0x12` 半字。 |
| `0xa0006c84` | `00f99423` | 写入构造区 `+0x08`。 |
| `0xa0006c88` | `16c68793` | 在前方既有偏移上增加 `364`，得到另一对象区的源偏移。 |
| `0xa0006c8c` | `00f90733` | 把源偏移加到另一对象区基址，保存连续读取地址。 |
| `0xa0006c90` | `a0f9478b` | 从另一对象区该偏移读取首个无符号半字。 |
| `0xa0006c94` | `00f99823` | 把首个半字写入构造区 `+0x10`。 |
| `0xa0006c98` | `00275783` | 读取另一对象区下一半字。 |
| `0xa0006c9c` | `00f99923` | 把第二个半字写入构造区 `+0x12`。 |
| `0xa0006ca0` | `00475783` | 读取另一对象区第三个半字。 |
| `0xa0006ca4` | `f2cff06f` | 回到 `0xa00063d0`，由前方公共代码写入构造区 `+0x14`。 |

## 3. 两种布局

模式 `0x0200`：

| 构造区 | 来源 |
| --- | --- |
| `+0x04/+0x06/+0x08` | 描述对象 `+0x0e/+0x10/+0x12` |
| `+0x10/+0x12/+0x14` | 描述对象 `+0x14/+0x16/+0x18` |

模式 `0`：

| 构造区 | 来源 |
| --- | --- |
| `+0x04/+0x06/+0x08` | 描述对象 `+0x0e/+0x10/+0x12` |
| `+0x10/+0x12/+0x14` | 另一对象区 `既有偏移 + 364/+366/+368` |

两条路径最后都回到 `0xa00063d0`，由已覆盖的公共指令写入最后一个半字并继续构造。

## 4. 能力边界

- 已闭合：114 个源字节、29 条指令、模式 `0x0200` 与模式 `0` 的六半字来源、目标和共同回接。
- 另一对象区基址与前方既有偏移的正式结构名称尚无原厂定义证据。
- 其他非零模式从 `0xa0006ca8` 起继续顺序判定。
- 大型函数仍未闭合。
