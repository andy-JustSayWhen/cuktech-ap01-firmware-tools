# `0x007ca8..0x007d03` 无线发送其余非零模式九半字布局

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段处理既不为 `0x0200`、也不为 `0` 的其余非零构造模式。它从索引对象区
`560 × 索引 + 36` 起复制三个半字到构造区前部，再从描述对象 `+0x0e..+0x18` 连续复制
六个半字到构造区后部，共写入九个半字。

## 1. 覆盖范围

- 文件偏移：`0x007ca8..0x007d03`
- 执行地址：`0xa0006ca8..0xa0006d03`
- 长度：92 字节
- 本段原始字节散列：`c01f49e7c6cbc0839389141beb8872e4279b77d389365dd87a279b6df0cb0059`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006ca8` | `23000793` | 准备索引对象跨度 `560`。 |
| `0xa0006cac` | `02f70733` | 计算 `560 × 既有索引`。 |
| `0xa0006cb0` | `02470713` | 在索引偏移上增加字段偏移 `36`。 |
| `0xa0006cb4` | `00e487b3` | 把偏移加到索引对象区基址，保存连续读取地址。 |
| `0xa0006cb8` | `a0e4c70b` | 读取索引对象字段区首个无符号半字。 |
| `0xa0006cbc` | `00e99223` | 把首个半字写入构造区 `+0x04`。 |
| `0xa0006cc0` | `0027d703` | 读取索引对象字段区第二个半字。 |
| `0xa0006cc4` | `00e99323` | 把第二个半字写入构造区 `+0x06`。 |
| `0xa0006cc8` | `0047d783` | 读取索引对象字段区第三个半字。 |
| `0xa0006ccc` | `00f99423` | 把第三个半字写入构造区 `+0x08`。 |
| `0xa0006cd0` | `00ed5783` | 读取描述对象 `+0x0e` 半字。 |
| `0xa0006cd4` | `00f99823` | 写入构造区 `+0x10`。 |
| `0xa0006cd8` | `010d5783` | 读取描述对象 `+0x10` 半字。 |
| `0xa0006cdc` | `00f99923` | 写入构造区 `+0x12`。 |
| `0xa0006ce0` | `012d5783` | 读取描述对象 `+0x12` 半字。 |
| `0xa0006ce4` | `00f99a23` | 写入构造区 `+0x14`。 |
| `0xa0006ce8` | `014d5783` | 读取描述对象 `+0x14` 半字。 |
| `0xa0006cec` | `00f99c23` | 写入构造区 `+0x18`。 |
| `0xa0006cf0` | `016d5783` | 读取描述对象 `+0x16` 半字。 |
| `0xa0006cf4` | `00f99d23` | 写入构造区 `+0x1a`。 |
| `0xa0006cf8` | `018d5783` | 读取描述对象 `+0x18` 半字。 |
| `0xa0006cfc` | `00f99e23` | 写入构造区 `+0x1c`。 |
| `0xa0006d00` | `ed4ff06f` | 回到 `0xa00063d4`，继续前方公共构造流程。 |

## 3. 九半字映射

| 构造区 | 来源 |
| --- | --- |
| `+0x04/+0x06/+0x08` | 索引对象区 `560 × 索引 + 36/+38/+40` |
| `+0x10/+0x12/+0x14` | 描述对象 `+0x0e/+0x10/+0x12` |
| `+0x18/+0x1a/+0x1c` | 描述对象 `+0x14/+0x16/+0x18` |

本段写完最后一个半字后直接回到 `0xa00063d4`，区别于前两种模式回到 `0xa00063d0`
再补写最后一项。

## 4. 能力边界

- 已闭合：92 个源字节、23 条指令、其余非零模式的九项来源、目标和公共回接。
- 既有索引由前方控制流提供，本段只确认其参与 `560 × 索引 + 36` 的地址公式。
- 索引对象字段和模式值的正式业务名称尚无原厂结构定义证据。
- 大型函数仍未闭合。
