# `0x007d04..0x007d77` 无线发送八字节内容与分段描述提交

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段先把栈上八字节区域交给 `0xa016f85e`，再通过已顺序确认的字节复制函数把八字节内容
复制到对象 `+0x94`。随后在同一对象 `+0x80..+0x90` 写入固定值 `0xcafefade`、内容起止
地址和清零字段，并更新描述关联对象中的构造区起止指针、计数以及描述对象计数，最后回到
前方公共发送路径。

## 1. 覆盖范围

- 文件偏移：`0x007d04..0x007d77`
- 执行地址：`0xa0006d04..0xa0006d77`
- 长度：116 字节
- 本段原始字节散列：`ba97516901cf02528229633deef70959d5314ce8d4aa1f52c4eb8db094e1b51a`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006d04` | `fb040513` | 把栈位置 `-80` 的八字节区域地址作为第一参数。 |
| `0xa0006d08` | `0124a223` | 把既有对象指针写入另一对象 `+0x04`。 |
| `0xa0006d0c` | `00169097` | 形成内容生成函数地址的高位并保存返回点。 |
| `0xa0006d10` | `b52080e7` | 调用 `0xa016f85e`。 |
| `0xa0006d14` | `4621` | 把复制长度设置为 `8`。 |
| `0xa0006d16` | `fb040593` | 把栈上八字节区域作为复制源地址。 |
| `0xa0006d1a` | `094b0513` | 把目标对象 `+0x94` 作为复制目标地址。 |
| `0xa0006d1e` | `a7ffe0ef` | 调用已确认的字节复制函数 `0xa000579c`。 |
| `0xa0006d22` | `caff07b7` | 开始构造固定值 `0xcafefade`。 |
| `0xa0006d26` | `ade78793` | 完成固定值 `0xcafefade`。 |
| `0xa0006d2a` | `094b0713` | 取得目标对象 `+0x94` 的内容起始地址。 |
| `0xa0006d2e` | `08fb2023` | 把固定值写入目标对象 `+0x80`。 |
| `0xa0006d32` | `08eb2423` | 把内容起始地址写入目标对象 `+0x88`。 |
| `0xa0006d36` | `041d4783` | 读取描述对象 `+0x41` 的内容长度字节。 |
| `0xa0006d3a` | `080b2223` | 把目标对象 `+0x84` 清零。 |
| `0xa0006d3e` | `080b2823` | 把目标对象 `+0x90` 清零。 |
| `0xa0006d42` | `17fd` | 内容长度减一。 |
| `0xa0006d44` | `97ba` | 把减一后的长度加到内容起始地址。 |
| `0xa0006d46` | `08fb2623` | 把所得内容结束地址写入目标对象 `+0x8c`。 |
| `0xa0006d4a` | `058d2783` | 读取描述对象 `+0x58` 的关联对象指针。 |
| `0xa0006d4e` | `0137a823` | 把构造区起始地址写入关联对象 `+0x10`。 |
| `0xa0006d52` | `03ed4783` | 读取描述对象 `+0x3e` 的构造区长度字节。 |
| `0xa0006d56` | `058d2703` | 再次读取描述对象 `+0x58` 的关联对象指针。 |
| `0xa0006d5a` | `17fd` | 构造区长度减一。 |
| `0xa0006d5c` | `99be` | 把减一后的长度加到构造区起始地址。 |
| `0xa0006d5e` | `01372a23` | 把构造区结束地址写入关联对象 `+0x14`。 |
| `0xa0006d62` | `058d2783` | 再次读取描述对象 `+0x58` 的关联对象指针。 |
| `0xa0006d66` | `054d2703` | 读取描述对象 `+0x54` 的计数对象指针。 |
| `0xa0006d6a` | `4bdc` | 读取关联对象 `+0x14` 的构造区结束地址。 |
| `0xa0006d6c` | `0785` | 结束地址加一，得到构造区末端后一地址。 |
| `0xa0006d6e` | `c77c` | 把末端后一地址写入计数对象 `+0x4c`。 |
| `0xa0006d70` | `040d1723` | 把描述对象 `+0x4e` 的 16 位计数清零。 |
| `0xa0006d74` | `800ff06f` | 回到大型函数前方公共路径 `0xa0005d74`。 |

## 3. 分段描述布局

目标对象本段写入：

| 偏移 | 内容 |
| --- | --- |
| `+0x80` | 固定值 `0xcafefade` |
| `+0x84` | `0` |
| `+0x88` | 目标对象 `+0x94`，即八字节内容起始地址 |
| `+0x8c` | 内容起始地址 `+ 描述[+0x41] - 1` |
| `+0x90` | `0` |
| `+0x94` | 从栈区域复制的八字节内容起点 |

关联对象同时得到构造区起始地址和结束地址；计数对象 `+0x4c` 保存结束地址加一。
`0xa000579c` 的函数事实唯一原文见
[`底层字节复制入口与零至七字节路径`](00679c-00682b-底层字节复制入口与零至七字节路径.md)。

## 4. 能力边界

- 已闭合：116 个源字节、33 条指令、八字节复制参数、分段描述六项布局、构造区边界、计数更新和回接。
- `0xa016f85e` 的函数本体尚未顺序覆盖，本段只确认其接收栈上八字节区域地址。
- 三类对象的正式结构名称尚无原厂定义证据。
- 大型函数仍未闭合。
