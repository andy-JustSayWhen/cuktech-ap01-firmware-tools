# `0x007dc4..0x007e29` 无线发送索引对象取得与主字段初始化

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段以 `0x62fd73a8 + 8 × 索引` 为参数调用 `0xa01642f4` 取得对象。返回空指针时清描述
对象 `+0x30` 并递增工作对象 `+0x44` 字节；成功时把返回对象写入工作对象 `+0x20`，
初始化返回对象的编号、状态、描述指针和模式等字段，再用三个直接参数调用
`0xa0168560`。

## 1. 覆盖范围

- 文件偏移：`0x007dc4..0x007e29`
- 执行地址：`0xa0006dc4..0xa0006e29`
- 长度：102 字节
- 本段原始字节散列：`35336857a3295fa2b1f582298a6b4836171dcaf82635f2a539e5d7fe30868b24`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006dc4` | `62fd7537` | 开始构造索引表基址 `0x62fd73a8`。 |
| `0xa0006dc8` | `3a850513` | 完成索引表基址 `0x62fd73a8`。 |
| `0xa0006dcc` | `0745150b` | 计算 `0x62fd73a8 + 8 × 索引`，作为第一参数。 |
| `0xa0006dd0` | `0015d097` | 形成对象取得函数地址的高位并保存返回点。 |
| `0xa0006dd4` | `524080e7` | 调用 `0xa01642f4`。 |
| `0xa0006dd8` | `02ada023` | 把返回对象指针写入工作对象 `+0x20`。 |
| `0xa0006ddc` | `84aa` | 保存返回对象指针供后续初始化。 |
| `0xa0006dde` | `e911` | 返回指针非零时跳到成功初始化路径。 |
| `0xa0006de0` | `020d2823` | 返回空指针时把描述对象 `+0x30` 清零。 |
| `0xa0006de4` | `044dc783` | 读取工作对象 `+0x44` 的字节计数。 |
| `0xa0006de8` | `0785` | 计数加一。 |
| `0xa0006dea` | `04fd8223` | 把递增结果写回工作对象 `+0x44`。 |
| `0xa0006dee` | `85cff06f` | 跳到大型函数前方 `0xa0005e4a`。 |
| `0xa0006df2` | `016506a3` | 成功路径把既有编号字节写入返回对象 `+0x0d`。 |
| `0xa0006df6` | `4b05` | 准备状态常数 `1`。 |
| `0xa0006df8` | `00052423` | 把返回对象 `+0x08` 清零。 |
| `0xa0006dfc` | `01750623` | 把另一既有编号字节写入返回对象 `+0x0c`。 |
| `0xa0006e00` | `01650723` | 把状态常数 `1` 写入返回对象 `+0x0e`。 |
| `0xa0006e04` | `11a52623` | 把描述对象指针写入返回对象 `+0x10c`。 |
| `0xa0006e08` | `10052423` | 把返回对象 `+0x108` 清零。 |
| `0xa0006e0c` | `034d2783` | 读取描述对象 `+0x34` 的 32 位字段。 |
| `0xa0006e10` | `02098613` | 在既有构造偏移上增加 `32`。 |
| `0xa0006e14` | `964a` | 再加另一既有对象基址，得到第三参数地址。 |
| `0xa0006e16` | `c15c` | 把描述对象 `+0x34` 字写入返回对象 `+0x04`。 |
| `0xa0006e18` | `042d4783` | 读取描述对象 `+0x42` 的模式字节。 |
| `0xa0006e1c` | `85d2` | 把当前索引作为第二参数。 |
| `0xa0006e1e` | `14f50c23` | 把模式字节写入返回对象 `+0x158`。 |
| `0xa0006e22` | `00161097` | 形成后方初始化函数地址的高位并保存返回点。 |
| `0xa0006e26` | `73e080e7` | 调用 `0xa0168560(返回对象, 当前索引, 第三参数地址)`。 |

## 3. 成功与失败路径

失败路径：

- `工作对象[+0x20] = 0`；
- `描述对象[+0x30] = 0`；
- `工作对象[+0x44]` 字节加一；
- 回到 `0xa0005e4a`。

成功路径写入返回对象：

| 偏移 | 内容 |
| --- | --- |
| `+0x04` | 描述对象 `+0x34` 字 |
| `+0x08` | `0` |
| `+0x0c/+0x0d` | 两个既有编号字节 |
| `+0x0e` | `1` |
| `+0x108` | `0` |
| `+0x10c` | 描述对象指针 |
| `+0x158` | 描述对象 `+0x42` 模式字节 |

## 4. 能力边界

- 已闭合：102 个源字节、29 条指令、索引地址公式、对象取得、空指针处理、成功字段布局和三项调用参数。
- `0xa01642f4` 与 `0xa0168560` 的函数本体尚未顺序覆盖，本段不替它们命名具体业务功能。
- 工作对象和返回对象的正式结构名称尚无原厂定义证据。
- 大型函数仍未闭合。
