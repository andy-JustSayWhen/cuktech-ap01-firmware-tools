# `0x007fa2..0x007fab` 无线发送既有候选门禁失败回退

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段是前方既有候选状态、掩码或双编号门禁失败的统一落点。它以当前索引调用
`0xa0007eba`，然后回到大型函数前方 `0xa0005e14`。

## 1. 覆盖范围

- 文件偏移：`0x007fa2..0x007fab`
- 执行地址：`0xa0006fa2..0xa0006fab`
- 长度：10 字节
- 本段原始字节散列：`bc0d0c73f9a0d340bab71eb54e7c091a88c3c83edb2edc517edc6b8eb86c18a5`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0006fa2` | `8552` | 把当前索引作为第一参数。 |
| `0xa0006fa4` | `717000ef` | 调用 `0xa0007eba`。 |
| `0xa0006fa8` | `e6dfe06f` | 调用后回到大型函数前方 `0xa0005e14`。 |

## 3. 控制流闭合

前段四项门禁中的任一失败都到达本段：空状态、固定掩码值命中、第一编号不匹配或第二编号
不匹配。统一动作只有 `0xa0007eba(当前索引)`，随后从 `0xa0005e14` 继续，不执行容量足够
路径中的长度、标志和计数提交。

## 4. 能力边界

- 已闭合：10 个源字节、3 条指令、调用参数和回接地址。
- `0xa0007eba` 的函数本体尚未顺序覆盖，本段不提前命名其作用。
- 本段不包含容量不足路径；该路径从下一地址 `0xa0006fac` 开始。
- 大型函数仍未闭合。
