# `0x0081f4..0x00822d` 无线发送半字比值更新分支尾与饱和计数

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合前一函数的三条分支尾：中间比值采用“原半字乘 `80`、比值乘 `48`、总和右移
`7`”；对象 `+0x11` 状态为零时，把对象 `+0x08` 写成比值减一或零并把 `+0x11`
置一；对象 `+0x04` 基准为零时，只把对象 `+0x10` 字节递增，值为 `255` 时保持不变。

## 1. 覆盖范围

- 文件偏移：`0x0081f4..0x00822d`
- 执行地址：`0xa00071f4..0xa000722d`
- 长度：58 字节
- 本段原始字节散列：`669a5164889c0e4b8568a626e71ce084f974b24ee8bee55705f095ba6beedec6`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa00071f4` | `05000693` | 中间比值路径准备原半字乘数 `80`。 |
| `0xa00071f8` | `02d787b3` | 计算原半字乘以 `80`。 |
| `0xa00071fc` | `03000693` | 把比值乘数设为 `48`。 |
| `0xa0007200` | `b7e1` | 回到 `0xa00071c8`，加“比值乘 `48`”并右移 `7` 位写回。 |
| `0xa0007202` | `4601` | 对象 `+0x11` 状态为零路径把待写半字初始化为 `0`。 |
| `0xa0007204` | `00d7e563` | 左移前的对象 `+0x06` 数值小于基准半字时保留零。 |
| `0xa0007208` | `177d` | 左移值不小于基准时把前段比值减一。 |
| `0xa000720a` | `3c07360b` | 提取比值减一结果低 16 位作为待写半字。 |
| `0xa000720e` | `4785` | 准备状态值 `1`。 |
| `0xa0007210` | `00c51423` | 把零或比值减一写入对象 `+0x08` 半字。 |
| `0xa0007214` | `00f508a3` | 把状态值 `1` 写入对象 `+0x11`。 |
| `0xa0007218` | `bfd9` | 回到 `0xa00071ee` 恢复现场并返回。 |
| `0xa000721a` | `01054783` | 基准半字为零路径读取对象 `+0x10` 的字节计数。 |
| `0xa000721e` | `0ff00713` | 准备饱和值 `255`。 |
| `0xa0007222` | `fce786e3` | 当前计数等于 `255` 时直接返回。 |
| `0xa0007226` | `0785` | 当前计数小于 `255` 时加一。 |
| `0xa0007228` | `00f50823` | 把递增结果写回对象 `+0x10`。 |
| `0xa000722c` | `b7c9` | 回到 `0xa00071ee` 恢复现场并返回。 |

## 3. 三条分支结果

1. `13,106 < 比值 <= 57,670` 且对象 `+0x11 != 0`：
   `对象[+0x08] = 低16位(((原值 × 80) + (比值 × 48)) >> 7)`。
2. 对象 `+0x04 != 0` 且对象 `+0x11 == 0`：
   - 若 `(对象[+0x06] << 16) < 对象[+0x04]`，`对象[+0x08] = 0`；
   - 否则 `对象[+0x08] = 低16位(比值 - 1)`；
   - `对象[+0x11] = 1`。
3. 对象 `+0x04 == 0`：对象 `+0x10` 按字节递增，但最大保持 `255`。

## 4. 能力边界

- 已闭合：58 个源字节、18 条指令、三条分支公式、状态置一、饱和计数和共同返回。
- 执行地址 `0xa0007194..0xa000722d`、文件偏移 `0x008194..0x00822d` 的整个函数已完成逐指令覆盖。
- 对象各字段的正式业务名称尚无原厂结构定义证据。
- 下一函数从执行地址 `0xa000722e` 开始。
