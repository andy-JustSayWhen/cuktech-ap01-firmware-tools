# `0x00847a..0x0084b9` 候选记录写入与八参数提交

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段接收前方确定的候选编号与候选半字，按二十字节跨度定位记录，把候选半字写入记录
偏移 `+22`，清记录偏移 `+20` 半字和 `+29` 字节，并把候选编号同时写入对象
`+232`、`+234`、`+236` 三个半字。随后它以常数 `7`、固定表地址、候选半字、四份
候选编号和对象 `+238` 半字组成八个参数，调用后方 `0xa01351ce`；下一条路径不使用其
返回值。

## 1. 覆盖范围

- 文件偏移：`0x00847a..0x0084b9`
- 执行地址：`0xa000747a..0xa00074b9`
- 长度：64 字节
- 本段原始字节散列：`856edc9bc23ae65a1ad32cb48d7fce1c257517a268620f759e8d9ed4bbeb5f51`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa000747a` | `4751` | 准备记录跨度 `20`。 |
| `0xa000747c` | `87a6` | 复制对象基址。 |
| `0xa000747e` | `20e8178b` | 形成 `对象 + 20 × 候选编号`。 |
| `0xa0007482` | `a053d5b7` | 准备固定表地址高位。 |
| `0xa0007486` | `8742` | 把候选编号复制到第四参数。 |
| `0xa0007488` | `86c2` | 把候选编号复制到第三参数。 |
| `0xa000748a` | `2b458593` | 完成固定表地址 `0xa053d2b4`，作为第二参数。 |
| `0xa000748e` | `451d` | 把第一参数设为常数 `7`。 |
| `0xa0007490` | `00c79b23` | 把候选半字写入候选记录偏移 `+22`。 |
| `0xa0007494` | `00078ea3` | 清候选记录偏移 `+29` 字节。 |
| `0xa0007498` | `00079a23` | 清候选记录偏移 `+20` 半字。 |
| `0xa000749c` | `0ee4d883` | 读取对象 `+238` 无符号半字，作为第八参数。 |
| `0xa00074a0` | `3c08378b` | 把候选编号限制到低 16 位。 |
| `0xa00074a4` | `0ef49423` | 把低 16 位候选编号写入对象 `+232`。 |
| `0xa00074a8` | `0ef49523` | 把低 16 位候选编号写入对象 `+234`。 |
| `0xa00074ac` | `0ef49623` | 把低 16 位候选编号写入对象 `+236`。 |
| `0xa00074b0` | `87c2` | 把候选编号复制到第五参数；第六、七参数仍保留同一编号。 |
| `0xa00074b2` | `0012e097` | 形成后方函数地址的高位。 |
| `0xa00074b6` | `d1c080e7` | 调用 `0xa01351ce`。 |

## 3. 写入关系与调用参数

令 `i = 低8位候选编号`、`C = 候选半字`：

- `对象半字[20i + 22] = C`；
- `对象半字[20i + 20] = 0`；
- `对象字节[20i + 29] = 0`；
- `对象半字[+232] = 对象半字[+234] = 对象半字[+236] = i`。

调用 `0xa01351ce` 时八个参数依次为：

`7, 0xa053d2b4, C, i, i, i, i, 对象半字[+238]`。

调用后的下一条指令直接检查此前保存的第二函数参数，不读取返回寄存器，因此本调用返回值
在当前函数中不参与后续控制流或数据写回。

## 4. 能力边界

- 已闭合：64 个源字节、19 条指令、候选记录地址、三项字段更新、三个对象索引同步、
  八项调用参数和返回值未使用事实。
- 固定表 `0xa053d2b4` 与后方函数 `0xa01351ce` 的内容尚未顺序到达，本文不定义其业务语义。
- 候选编号的有效范围依赖前段搜索或取余路径，本段没有再次做边界检查。
- 本大型函数继续于执行地址 `0xa00074ba`、文件偏移 `0x0084ba`。
