# `0x0088aa..0x0088c3` 输入三位模式与低七位拆分转调

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本独立函数把输入值第 11 至 13 位提取为第一参数，把输入值低 7 位提取为第二参数，调用
后方 `0xa0007c10`，随后不改写返回寄存器，直接把该函数的返回值交还调用者。后方函数体
尚未到达当前顺序端点，因此本段只闭合参数映射和返回透传。

## 1. 覆盖范围

- 文件偏移：`0x0088aa..0x0088c3`
- 执行地址：`0xa00078aa..0xa00078c3`
- 长度：26 字节
- 本段原始字节散列：`359a4830e4e57c687a77d9f0c82673fc42b4651a0f92da4c16da69919a5a8482`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa00078aa` | `1141` | 栈顶下移 16 字节。 |
| `0xa00078ac` | `c422` | 保存原 `s0`。 |
| `0xa00078ae` | `c606` | 保存返回地址。 |
| `0xa00078b0` | `0800` | 建立当前栈框基址。 |
| `0xa00078b2` | `07f57593` | 提取输入值低 7 位作为第二参数。 |
| `0xa00078b6` | `34b5350b` | 提取输入值第 11 至 13 位作为第一参数。 |
| `0xa00078ba` | `2e99` | 调用 `0xa0007c10(三位模式, 低7位值)`。 |
| `0xa00078bc` | `40b2` | 恢复返回地址。 |
| `0xa00078be` | `4422` | 恢复 `s0`。 |
| `0xa00078c0` | `0141` | 回收 16 字节栈框。 |
| `0xa00078c2` | `8082` | 原样返回 `0xa0007c10` 的返回值。 |

## 3. 参数与返回关系

令入口输入值为 `X`：

`返回值 = 0xa0007c10(X[13:11], X[6:0])`。

函数没有读取输入值其他位，也没有在被调函数返回后对结果做截断、比较或改写。

## 4. 能力边界

- 已闭合：26 个源字节、11 条指令、两项位域参数映射、直接调用和返回透传。
- `0xa0007c10` 的函数体尚未顺序到达，本文不定义其业务语义或返回公式。
- 下一独立函数从执行地址 `0xa00078c4`、文件偏移 `0x0088c4` 开始。
