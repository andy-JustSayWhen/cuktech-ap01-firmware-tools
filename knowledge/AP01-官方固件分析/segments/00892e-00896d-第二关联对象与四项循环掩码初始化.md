# `0x00892e..0x00896d` 第二关联对象与四项循环掩码初始化

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段承接输入对象 `+334` 标志最低位为一的路径。它读取输入对象 `+296` 字节和
`+324` 的第二关联对象指针，保存第二关联对象 `+232` 地址，并为后续四项循环建立
第二栈快照指针、多个状态寄存器以及 `0x3fff`、`0x00000400`、`0xfffcffff`、
`0xfffbffff`、`0x00040000` 五个精确常数。末条指令重新载入第二关联对象 `+232`
地址，准备下一段按索引读取半字。

## 1. 覆盖范围

- 文件偏移：`0x00892e..0x00896d`
- 执行地址：`0xa000792e..0xa000796d`
- 长度：64 字节
- 本段原始字节散列：`906e060307932129969afd5ba26ec134c4469446faa6a49d1933695cf8c047ad`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa000792e` | `12854783` | 读取输入对象 `+296` 字节。 |
| `0xa0007932` | `14452383` | 读取输入对象 `+324` 的第二关联对象指针。 |
| `0xa0007936` | `fffd0fb7` | 准备第一个高位清除掩码的高位。 |
| `0xa000793a` | `f8f42e23` | 把输入对象 `+296` 字节保存到栈内。 |
| `0xa000793e` | `0e838793` | 形成第二关联对象 `+232` 地址。 |
| `0xa0007942` | `f8f42c23` | 把该地址保存到栈内。 |
| `0xa0007946` | `6791` | 准备低十四位掩码高位。 |
| `0xa0007948` | `17fd` | 完成低十四位掩码 `0x00003fff`。 |
| `0xa000794a` | `fffc02b7` | 准备第二个高位清除掩码的高位。 |
| `0xa000794e` | `88d6` | 把第二栈快照区起点保存为循环写指针。 |
| `0xa0007950` | `4805` | 把累计状态初始化为一。 |
| `0xa0007952` | `4301` | 把候选一致状态初始化为零。 |
| `0xa0007954` | `4701` | 把候选比较值初始化为零。 |
| `0xa0007956` | `4681` | 把四项循环编号初始化为零。 |
| `0xa0007958` | `f8f42a23` | 把低十四位掩码保存到栈内。 |
| `0xa000795c` | `4f11` | 准备循环项数 `4`。 |
| `0xa000795e` | `40000d13` | 准备常数 `0x00000400`。 |
| `0xa0007962` | `1ffd` | 完成掩码 `0xfffcffff`。 |
| `0xa0007964` | `12fd` | 完成掩码 `0xfffbffff`。 |
| `0xa0007966` | `00040db7` | 准备常数 `0x00040000`。 |
| `0xa000796a` | `f9842783` | 在循环入口重新载入第二关联对象 `+232` 地址。 |

## 3. 入口状态

进入下一段前已经确定：

- 第二关联对象指针来自 `输入对象 + 324`；
- 半字索引数组起点为 `第二关联对象 + 232`；
- 循环编号为 `0`，上界为 `4`；
- 第二栈快照区写指针指向入口时复制的关联对象 `+36..+51` 四个字；
- 两个状态值分别初始化为 `1` 与 `0`，候选比较值初始化为 `0`；
- 五个常数按本段开头所列值原样保存在寄存器或栈中。

## 4. 能力边界

- 已闭合：64 个源字节、21 条指令、第二关联对象取得、两个栈保存值、四项循环初态和
  五个精确常数。
- 候选比较值和一致状态只描述后续指令用途，不是原厂结构字段名称。
- 第二关联对象指针在后续会被直接解引用；本段没有空指针检查。
- 本大型函数继续于执行地址 `0xa000796e`、文件偏移 `0x00896e`。
