# `0x008ac2..0x008ae9` 工作字双调用返回值八位合成

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段读取第二关联对象 `+252` 工作字，先调用已经闭合的 `0xa00078aa` 位域拆分转调函数，
再以同一工作字调用后方 `0xa0007dce`。第一个返回值直接保留，第二个返回值左移八位后
与之按位合并，最终写入第二关联对象 `+256`。`0xa0007dce` 尚未到达当前顺序端点，
本文只记录它的参数与返回值位置关系。

## 1. 覆盖范围

- 文件偏移：`0x008ac2..0x008ae9`
- 执行地址：`0xa0007ac2..0xa0007ae9`
- 长度：40 字节
- 本段原始字节散列：`e292cc7b8773c36d0923684671a7256e1c8519a9b04e3404a69370a9041b2a97`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0007ac2` | `0fcc2503` | 读取第二关联对象 `+252` 工作字。 |
| `0xa0007ac6` | `100c2023` | 清零第二关联对象 `+256`。 |
| `0xa0007aca` | `33c5` | 调用 `0xa00078aa(工作字)`。 |
| `0xa0007acc` | `100c2783` | 读取当前为零的第二关联对象 `+256`。 |
| `0xa0007ad0` | `8fc9` | 合入第一次调用返回值。 |
| `0xa0007ad2` | `0fcc2503` | 再次读取第二关联对象 `+252` 工作字作为参数。 |
| `0xa0007ad6` | `10fc2023` | 把第一次调用返回值写入第二关联对象 `+256`。 |
| `0xa0007ada` | `2cd5` | 调用 `0xa0007dce(工作字)`。 |
| `0xa0007adc` | `00851793` | 把第二次调用返回值左移八位，保留低 32 位。 |
| `0xa0007ae0` | `100c2503` | 读取已经保存的第一次调用返回值。 |
| `0xa0007ae4` | `8d5d` | 合入第二次调用返回值的左移结果。 |
| `0xa0007ae6` | `10ac2023` | 把合成结果写回第二关联对象 `+256`。 |

## 3. 合成关系

令 `W = 字(第二关联对象 + 252)`：

- `R1 = 0xa00078aa(W)`；
- `R2 = 0xa0007dce(W)`；
- `字(第二关联对象 + 256) = R1 | 低32位(R2 << 8)`。

`0xa00078aa` 的已知参数拆分见
[`0x0088aa..0x0088c3`](0088aa-0088c3-输入三位模式与低七位拆分转调.md)。

## 4. 能力边界

- 已闭合：40 个源字节、12 条指令、同一工作字两次调用、两项返回值位置、左移截断与
  最终写回。
- `R1` 与 `R2 << 8` 是否存在重叠置位，本段不做掩码限制；实际合成就是按位或。
- `0xa0007dce` 的函数体和返回公式尚未顺序到达。
- 本大型函数继续于执行地址 `0xa0007aea`、文件偏移 `0x008aea`。
