# `0x008bdc..0x008c0f` 第六标志表字段合并与大型函数闭合

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段是输入对象 `+334` 标志字节第六位置一的专用路径。它按输入对象 `+32` 字节索引
一张每项跨度为 `1128` 字节的表，从表项 `+464` 字读取第 24 至 29 位，覆盖第一关联
对象保存字 `+8` 的第 20 至 25 位，然后跳回已闭合的中断保护提交路径。至此，文件偏移
`0x0088c4..0x008c0f` 的整个大型函数已经顺序读完。

## 1. 覆盖范围

- 文件偏移：`0x008bdc..0x008c0f`
- 执行地址：`0xa0007bdc..0xa0007c0f`
- 长度：52 字节
- 本段原始字节散列：`e3e52444e5987f848bd20653d079ec769917f3fe9b70475d15479baff379d216`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0007bdc` | `fc1007b7` | 准备清除第 20 至 25 位所需掩码的高位。 |
| `0xa0007be0` | `17fd` | 完成掩码 `0xfc0fffff`。 |
| `0xa0007be2` | `020cc683` | 读取输入对象 `+32` 的无符号字节索引。 |
| `0xa0007be6` | `00f97933` | 清除第一关联对象保存字 `+8` 的第 20 至 25 位。 |
| `0xa0007bea` | `62fd67b7` | 准备表基址的高位。 |
| `0xa0007bee` | `46800713` | 准备单项跨度 `1128` 字节。 |
| `0xa0007bf2` | `1a078793` | 完成表基址 `0x62fd61a0`。 |
| `0xa0007bf6` | `20e6978b` | 计算 `表基址 + 索引 × 1128`。 |
| `0xa0007bfa` | `1d07a703` | 读取该表项 `+464` 的 32 位字。 |
| `0xa0007bfe` | `4781` | 先把待合并字段设为零。 |
| `0xa0007c00` | `00074463` | 若表字段按有符号数小于零，则保留零值并跳过位提取。 |
| `0xa0007c04` | `7587378b` | 否则提取表字段的第 24 至 29 位。 |
| `0xa0007c08` | `07d2` | 把六位结果左移 20 位。 |
| `0xa0007c0a` | `0127e933` | 合入已经清除对应位域的第一关联对象保存字。 |
| `0xa0007c0e` | `bdd5` | 跳回 `0xa0007b02` 的中断保护提交路径。 |

## 3. 字段合并关系

令输入对象 `+32` 字节为 `j`，第一关联对象入口保存的 `+8` 字为 `S`：

- `表项地址 = 0x62fd61a0 + 1128 × j`；
- `T = 读取32位字(表项地址 + 464)`；
- 若 `T` 按有符号数小于零，则 `F = 0`；否则 `F = T` 的第 24 至 29 位；
- `新S = (原S & 0xfc0fffff) | (F << 20)`。

随后复用 `0xa0007b02` 的提交路径，把新 `S` 写入第一关联对象 `+8`，写回两组四字
快照，清除输入对象 `+334` 标志字节并返回第一关联对象地址。

## 4. 能力边界

- 已闭合：52 个源字节、15 条指令、索引来源、表项跨度、字段地址、有符号负值分支、
  六位提取、目标位域和提交回接。
- 文件偏移 `0x0088c4..0x008c0f` 的大型函数已经全部顺序读完；其入口快照、四项合并、
  动态记录、双调用重组、三类标志分流和最终提交均已有连续证据。
- `0x62fd61a0` 是运行时地址；当前顺序进度尚未证明该表的初始化来源和业务名称，因此
  本文只记录地址、跨度和被读取字段，不为它命名。
- 下一独立函数始于执行地址 `0xa0007c10`、文件偏移 `0x008c10`。
