# `0x008c10..0x008c85` 双输入编号分组规范化

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段是一个独立函数的入口分流。它接收两个无符号输入值，先按第一输入选择允许的第二
输入范围，再把两者规范为后续查表使用的组号与组内编号。范围不匹配时统一改成组号 `1`、
组内编号 `7`。本段只完成输入规范化；从 `0xa0007c86` 开始才读取状态字节和表数据。

## 1. 覆盖范围

- 文件偏移：`0x008c10..0x008c85`
- 执行地址：`0xa0007c10..0xa0007c85`
- 长度：118 字节
- 本段原始字节散列：`3aaac0bb4caaa2f19040de526f9f070ca12cc45ccf0bc5d8b256a308bf2792af`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0007c10` | `4785` | 准备边界值 `1`。 |
| `0xa0007c12` | `02a7f963` | 第一输入不大于 `1` 时转入第二输入分组路径。 |
| `0xa0007c16` | `ffe50713` | 计算第一输入减 `2`。 |
| `0xa0007c1a` | `0ff77713` | 把差值截成无符号字节。 |
| `0xa0007c1e` | `00e7fe63` | 差值不大于 `1`，即常规字节范围内第一输入为 `2` 或 `3` 时转入对应路径。 |
| `0xa0007c22` | `4791` | 准备第一输入值 `4`。 |
| `0xa0007c24` | `04f50c63` | 第一输入等于 `4` 时转入组号 `3` 路径。 |
| `0xa0007c28` | `156d` | 计算第一输入减 `5`。 |
| `0xa0007c2a` | `0ff57513` | 把差值截成无符号字节并暂存在第一参数位置。 |
| `0xa0007c2e` | `4789` | 准备差值上限 `2`。 |
| `0xa0007c30` | `04a7f163` | 差值不大于 `2`，即常规字节范围内原第一输入为 `5` 至 `7` 时转入对应路径。 |
| `0xa0007c34` | `459d` | 不支持的组合把组内编号改为 `7`。 |
| `0xa0007c36` | `4505` | 不支持的组合把组号改为 `1`。 |
| `0xa0007c38` | `a0b9` | 跳到后续共同查表入口。 |
| `0xa0007c3a` | `479d` | 为原第一输入 `2` 或 `3` 准备第二输入上限 `7`。 |
| `0xa0007c3c` | `4509` | 把规范组号设为 `2`。 |
| `0xa0007c3e` | `feb7ebe3` | 第二输入大于 `7` 时转入不支持组合路径。 |
| `0xa0007c42` | `a091` | 合法时保持第二输入并跳到共同查表入口。 |
| `0xa0007c44` | `478d` | 为第一输入不大于 `1` 的路径准备第二输入边界 `3`。 |
| `0xa0007c46` | `00b7ff63` | 第二输入不大于 `3` 时进入组号 `0` 路径。 |
| `0xa0007c4a` | `ffc58793` | 第二输入大于 `3` 时计算组内编号 `第二输入 - 4`。 |
| `0xa0007c4e` | `472d` | 准备第二输入最大值 `11`。 |
| `0xa0007c50` | `0ff7f793` | 把组内编号截成无符号字节。 |
| `0xa0007c54` | `feb760e3` | 第二输入大于 `11` 时转入不支持组合路径。 |
| `0xa0007c58` | `0045b593` | 判断第二输入是否小于 `4`；当前合法分支结果为零。 |
| `0xa0007c5c` | `0015c513` | 反转判断结果，得到规范组号 `1`。 |
| `0xa0007c60` | `85be` | 把 `第二输入 - 4` 作为规范组内编号。 |
| `0xa0007c62` | `a015` | 跳到共同查表入口。 |
| `0xa0007c64` | `87ae` | 暂存原第二输入，供组内编号使用。 |
| `0xa0007c66` | `0045b593` | 判断第二输入是否小于 `4`；当前路径结果为一。 |
| `0xa0007c6a` | `0015c513` | 反转判断结果，得到规范组号 `0`。 |
| `0xa0007c6e` | `85be` | 把原第二输入作为规范组内编号。 |
| `0xa0007c70` | `bfcd` | 回接共同跳转。 |
| `0xa0007c72` | `47a5` | 为原第一输入 `5` 至 `7` 准备第二输入上限 `9`。 |
| `0xa0007c74` | `4511` | 把规范组号设为 `4`。 |
| `0xa0007c76` | `fab7efe3` | 第二输入大于 `9` 时转入不支持组合路径。 |
| `0xa0007c7a` | `a031` | 合法时保持第二输入并跳到共同查表入口。 |
| `0xa0007c7c` | `47a5` | 为原第一输入 `4` 准备第二输入上限 `9`。 |
| `0xa0007c7e` | `450d` | 把规范组号设为 `3`。 |
| `0xa0007c80` | `fcb7f1e3` | 第二输入不大于 `9` 时保持它并进入共同查表入口。 |
| `0xa0007c84` | `bf45` | 第二输入大于 `9` 时转入不支持组合路径。 |

## 3. 常规字节输入的规范结果

令入口第一输入为 `X`、第二输入为 `Y`，出口组号为 `G`、组内编号为 `N`：

| 输入条件 | `G` | `N` |
| --- | ---: | ---: |
| `X <= 1` 且 `Y <= 3` | `0` | `Y` |
| `X <= 1` 且 `4 <= Y <= 11` | `1` | `Y - 4` |
| `X` 为 `2` 或 `3` 且 `Y <= 7` | `2` | `Y` |
| `X = 4` 且 `Y <= 9` | `3` | `Y` |
| `X` 为 `5` 至 `7` 且 `Y <= 9` | `4` | `Y` |
| 以上范围以外 | `1` | `7` |

## 4. 能力边界

- 已闭合：118 个源字节、41 条指令、五个规范组、各组第二输入范围和共同不支持组合值。
- 表中只归纳常规无符号字节范围；机器指令对部分减法结果再次截成字节，不能在尚未找到
  所有调用者前，把表格外的更宽输入行为简化成同一规则。
- 当前证据只证明编号变换，尚不能为组号和组内编号赋予业务名称。
- 后续共同处理从执行地址 `0xa0007c86`、文件偏移 `0x008c86` 开始。
