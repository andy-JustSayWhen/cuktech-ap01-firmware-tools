# `0x008f90..0x008fc9` 第二关联对象调用与状态项共同收尾

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段是大型函数的共同收尾。它以第二关联对象 `+340` 的 32 位值和第二关联对象自身为
两个参数调用远端函数，随后按入口编号重新定位 72 字节状态项，清零状态项 `+32` 的
32 位字段，把状态项 `+68` 字节加一，恢复全部现场并返回。

## 1. 覆盖范围

- 文件偏移：`0x008f90..0x008fc9`
- 执行地址：`0xa0007f90..0xa0007fc9`
- 长度：58 字节
- 本段原始字节散列：`64d047f4bb3043c96645bb024bfebdabdf26037a0a1ec63fab4ca24e6bec00ad`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0007f90` | `15492503` | 读取第二关联对象 `+340` 的 32 位值，作为第一参数。 |
| `0xa0007f94` | `85ca` | 把第二关联对象自身作为第二参数。 |
| `0xa0007f96` | `0015d097` | 准备远端调用地址。 |
| `0xa0007f9a` | `038080e7` | 调用 `0xa0164fce(第二关联对象[+340], 第二关联对象)`。 |
| `0xa0007f9e` | `04800713` | 准备状态项跨度 `72` 字节。 |
| `0xa0007fa2` | `87da` | 恢复状态表基址作为地址计算起点。 |
| `0xa0007fa4` | `20ea978b` | 按入口编号重新计算当前状态项地址。 |
| `0xa0007fa8` | `0447c703` | 读取状态项 `+68` 的无符号字节。 |
| `0xa0007fac` | `0207a023` | 清零状态项 `+32` 的 32 位字段。 |
| `0xa0007fb0` | `0705` | 把状态项 `+68` 原值加一。 |
| `0xa0007fb2` | `04e78223` | 以字节宽度写回加一结果。 |
| `0xa0007fb6` | `40f2` | 恢复返回地址。 |
| `0xa0007fb8` | `4462` | 恢复帧指针寄存器。 |
| `0xa0007fba` | `44d2` | 恢复第一关联对象暂存寄存器。 |
| `0xa0007fbc` | `4942` | 恢复第二关联对象暂存寄存器。 |
| `0xa0007fbe` | `49b2` | 恢复第三关联对象暂存寄存器。 |
| `0xa0007fc0` | `4a22` | 恢复状态项地址暂存寄存器。 |
| `0xa0007fc2` | `4a92` | 恢复入口编号暂存寄存器。 |
| `0xa0007fc4` | `4b02` | 恢复状态表基址暂存寄存器。 |
| `0xa0007fc6` | `6105` | 释放 32 字节栈帧。 |
| `0xa0007fc8` | `8082` | 返回调用者。 |

## 3. 共同收尾关系

令第二关联对象为 `B`，入口编号为 `I`，当前状态项为
`E = 0x62fd8248 + 72 × I`：

- 调用 `0xa0164fce(读取32位字(B + 340), B)`；
- `E[+32] = 0`；
- `E[+68] = (E[+68] + 1) & 0xff`；
- 恢复现场并返回。

## 4. 能力边界

- 已闭合：58 个源字节、21 条指令、远端调用的两个参数、状态项回查、指针清零、
  字节计数递增和完整返回现场。
- 远端函数 `0xa0164fce` 的函数体尚未顺序到达，本文不为其命名。
- 后续文件偏移 `0x008fca` 开始的是前方分支尾和状态项 `+65` 不等于 `1` 的另一主路径；
  大型函数尚未闭合。
