# `0x009014..0x009057` 状态非一路对象联接与大型函数闭合

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合状态项 `+65` 不等于 `1` 的主路径。它把第二关联对象 `+84` 的地址写入第三
关联对象 `+8`，把第一关联对象 `+48` 字置 `0x100` 位后写入第三关联对象 `+56`，并
置第二关联对象 `+8` 半字最低位。该半字原 `0x0002` 位非零时，还会更新一个下游对象
字段并调用后方函数。最后以当前状态项自身 `+16` 的地址和第二关联对象回到共同收尾，
至此文件偏移 `0x008eba..0x009057` 的整个大型函数完成。

## 1. 覆盖范围

- 文件偏移：`0x009014..0x009057`
- 执行地址：`0xa0008014..0xa0008057`
- 长度：68 字节
- 本段原始字节散列：`afe29778343750b1b6097e902d32ccc78340cb1a6dde33cf10a3bfa3d93aed14`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008014` | `05490593` | 计算第二关联对象自身 `+84` 的地址。 |
| `0xa0008018` | `00b9a423` | 把该地址写入第三关联对象 `+8`。 |
| `0xa000801c` | `589c` | 读取第一关联对象 `+48` 的 32 位字。 |
| `0xa000801e` | `1007e793` | 把该字的 `0x100` 位置一。 |
| `0xa0008022` | `02f9ac23` | 把结果写入第三关联对象 `+56`。 |
| `0xa0008026` | `00895783` | 读取第二关联对象 `+8` 的原无符号半字。 |
| `0xa000802a` | `0017e713` | 把该半字最低位置一。 |
| `0xa000802e` | `00e91423` | 写回第二关联对象 `+8`。 |
| `0xa0008032` | `8b89` | 从原半字只保留 `0x0002` 位。 |
| `0xa0008034` | `cb91` | 原 `0x0002` 位为零时跳过下游对象写入和后方调用。 |
| `0xa0008036` | `00492703` | 读取第二关联对象 `+4` 的 32 位字。 |
| `0xa000803a` | `03492783` | 读取第二关联对象 `+52` 保存的下游对象地址。 |
| `0xa000803e` | `8656` | 把入口状态项编号作为第三调用参数。 |
| `0xa0008040` | `01090513` | 把第二关联对象自身 `+16` 的地址作为第一调用参数。 |
| `0xa0008044` | `cbd8` | 把第二关联对象 `+4` 的值写入下游对象 `+20`。 |
| `0xa0008046` | `2c1d` | 调用 `0xa000827c(第二关联对象+16, 第二关联对象+84, 状态项编号)`。 |
| `0xa0008048` | `04800793` | 准备状态项跨度 `72` 字节。 |
| `0xa000804c` | `855a` | 以状态表基址作为地址计算起点。 |
| `0xa000804e` | `20fa950b` | 按入口编号计算当前状态项地址。 |
| `0xa0008052` | `85ca` | 把第二关联对象作为共同调用第二参数。 |
| `0xa0008054` | `0541` | 把状态项自身 `+16` 的地址作为共同调用第一参数。 |
| `0xa0008056` | `b781` | 跳到 `0xa0007f96`，执行远端调用、状态项清理、计数加一和返回。 |

## 3. 状态非一路尾部关系

沿用第一关联对象 `A`、第二关联对象 `B`、第三关联对象 `C`、状态项 `E` 和入口编号 `I`：

- `C[+8] = B自身地址 + 84`；
- `C[+56] = A[+48] | 0x100`；
- `B[+8] = 原半字 | 1`；
- 若原 `B[+8] & 0x0002` 非零，令 `D = B[+52]`，执行
  `D[+20] = B[+4]`，再调用 `0xa000827c(B自身地址+16, B自身地址+84, I)`；
- 最后调用共同尾部的 `0xa0164fce(E自身地址+16, B)`，然后清 `E[+32]`、递增 `E[+68]`
  并返回。

## 4. 整个大型函数能力边界

- 已闭合：68 个源字节、22 条指令、三项对象联接、半字置位、条件下游写入、后方调用
  参数和共同尾部回接。
- 文件偏移 `0x008eba..0x009057` 已全部顺序读完；两条主路径、前方分支尾和共同返回
  均已闭合。
- `0xa000827c` 与两个远端函数的内部能力仍需在顺序到达其函数体时判定，当前只掌握直接
  调用参数和本函数内可见的写入后果。
- 下一独立函数始于执行地址 `0xa0008058`、文件偏移 `0x009058`。
