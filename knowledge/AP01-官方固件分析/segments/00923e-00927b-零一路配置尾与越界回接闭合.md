# `0x00923e..0x00927b` 零一路配置尾与越界回接闭合

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合选择值 `1` 的完整配置、选择值 `0` 的写入尾和其他选择值的越界路径。选择值
`1` 检查 `0x24b08188` 第 8 至 9 位，未命中字段值 `2` 时把第一参数写入
`0x24b081a0` 并准备置位值 `0x400`；选择值 `0` 写 `0x24b0819c` 并准备 `0x200`。
其他选择值先执行固定参数调用，再回到前段共同寄存器更新。至此文件偏移
`0x009148..0x00927b` 的整个函数完成。

完整四路对应表只在入口文档
[`0x009148..0x0091df` 四路寄存器选择入口与二三路配置](009148-0091df-四路寄存器选择入口与二三路配置.md)
维护，本文不另建重复定义。

## 1. 覆盖范围

- 文件偏移：`0x00923e..0x00927b`
- 执行地址：`0xa000823e..0xa000827b`
- 长度：62 字节
- 本段原始字节散列：`288ba94b61d65cd4e2d055b8fffaf9a9862a8bb2423a99fda6b88615fa001117`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa000823e` | `24b08737` | 选择值 `1` 路径准备寄存器区域基址。 |
| `0xa0008242` | `18872683` | 读取寄存器 `0x24b08188`。 |
| `0xa0008246` | `59800593` | 准备该路固定数值 `1432`。 |
| `0xa000824a` | `2486b68b` | 提取寄存器第 8 至 9 位。 |
| `0xa000824e` | `f2f686e3` | 字段等于 `2` 时跳回共同固定地址调用并返回。 |
| `0xa0008252` | `1aa72023` | 字段不等于 `2` 时把第一参数写入寄存器 `0x24b081a0`。 |
| `0xa0008256` | `40000793` | 准备该路置位值 `0x400`。 |
| `0xa000825a` | `bfb9` | 回到 `0xa00081b8`，写 `0x24b08180` 并进入共同更新。 |
| `0xa000825c` | `18a72e23` | 选择值 `0` 写入尾把第一参数写入寄存器 `0x24b0819c`。 |
| `0xa0008260` | `20000793` | 准备该路置位值 `0x200`。 |
| `0xa0008264` | `bf91` | 回到 `0xa00081b8`，写 `0x24b08180` 并进入共同更新。 |
| `0xa0008266` | `a053c537` | 其他选择值路径准备固定地址的高位。 |
| `0xa000826a` | `5a200593` | 准备固定数值 `1442`。 |
| `0xa000826e` | `46050513` | 完成固定地址 `0xa053c460`。 |
| `0xa0008272` | `0015c097` | 准备后方调用地址。 |
| `0xa0008276` | `27e080e7` | 调用 `0xa01644f0(0xa053c460, 1442)`。 |
| `0xa000827a` | `b79d` | 跳回共同偏移表和三寄存器更新路径。 |

## 3. 后置分支关系

- 选择值 `1` 且寄存器第 8 至 9 位等于 `2`：调用
  `0xa0164394(0xa053c460, 1432)` 后返回；否则写第一参数到 `0x24b081a0`，并把
  `0x400` 写到 `0x24b08180` 后进入共同更新。
- 选择值 `0` 且前段检查字段不等于 `2`：写第一参数到 `0x24b0819c`，把 `0x200`
  写到 `0x24b08180` 后进入共同更新。
- 选择值不在 `0..3`：调用 `0xa01644f0(0xa053c460, 1442)`，不执行四路专用槽和
  `0x24b08180` 写入，直接进入共同更新。

## 4. 能力边界

- 已闭合：62 个源字节、17 条指令、选择值零与一的写入、选择值一提前返回、越界调用
  和共同路径回接。
- 文件偏移 `0x009148..0x00927b` 已全部顺序读完，四路门禁、四个输入槽、四个置位值、
  越界路径和共同寄存器更新均已闭合。
- 两个后方函数和两个固定地址内容仍未顺序到达，当前只掌握实参和调用后的控制流。
- 下一独立函数始于执行地址 `0xa000827c`、文件偏移 `0x00927c`。
