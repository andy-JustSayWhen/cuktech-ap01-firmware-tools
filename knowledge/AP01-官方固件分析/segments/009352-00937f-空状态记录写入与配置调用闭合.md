# `0x009352..0x00937f` 空状态记录写入与配置调用闭合

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合状态项原首字为零的路径。它读取 `0x62fd8454 + 28 × 编号` 的 `+46` 字节；
该字节非零，或者全局 `+180` 控制字最低位非零且当前编号等于全局 `+162` 字节时，
把第一参数写入当前控制记录 `+36`。其余情况调用已经闭合的四路寄存器配置函数。两条
路径随后都把第二参数写为当前 72 字节状态项的新首字。至此文件偏移
`0x00927c..0x00937f` 的整个函数完成。

## 1. 覆盖范围

- 文件偏移：`0x009352..0x00937f`
- 执行地址：`0xa0008352..0xa000837f`
- 长度：46 字节
- 本段原始字节散列：`ec78cc647b13951382d1b21f8b6774debf9e1d3308e0fb184a749485e4793614`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008352` | `46f1` | 准备控制记录跨度 `28` 字节。 |
| `0xa0008354` | `873e` | 以全局控制区 `0x62fd8454` 作为地址起点。 |
| `0xa0008356` | `20d6170b` | 计算当前编号的控制记录地址。 |
| `0xa000835a` | `02e74703` | 读取控制记录 `+46` 的无符号字节。 |
| `0xa000835e` | `eb09` | 该字节非零时跳到控制记录 `+36` 写入。 |
| `0xa0008360` | `0b47a703` | 读取全局控制区 `+180` 的 32 位控制字。 |
| `0xa0008364` | `0a27c683` | 读取全局控制区 `+162` 的无符号编号字节。 |
| `0xa0008368` | `8b05` | 只保留控制字最低位。 |
| `0xa000836a` | `cb01` | 最低位为零时转入四路寄存器配置调用。 |
| `0xa000836c` | `00d61763` | 最低位非零但当前编号不等于全局编号时也转入配置调用。 |
| `0xa0008370` | `4771` | 准备控制记录跨度 `28` 字节。 |
| `0xa0008372` | `20e4978b` | 按保存的编号重新计算当前控制记录地址。 |
| `0xa0008376` | `d3c8` | 把第一参数写入当前控制记录 `+36`。 |
| `0xa0008378` | `b75d` | 回到状态项新首字写回。 |
| `0xa000837a` | `85a6` | 把保存的编号作为四路配置函数第二参数。 |
| `0xa000837c` | `33f1` | 调用 `0xa0008148(第一参数, 编号)`。 |
| `0xa000837e` | `b745` | 回到状态项新首字写回。 |

## 3. 空状态路径关系

令编号为 `I`，全局控制区为 `G = 0x62fd8454`，当前控制记录基址为 `R = G + 28 × I`：

- 若 `无符号字节(R + 46) != 0`，执行 `R[+36] = 第一参数`；
- 否则，若 `G[+180]` 最低位非零且 `I == 无符号字节(G + 162)`，同样执行该写入；
- 其余情况调用 `0xa0008148(第一参数, I)`；
- 两条路径最后都复用前段共同写入：
  `0x62fd8248 + 72 × I` 的首字等于第二参数。

## 4. 能力边界

- 已闭合：46 个源字节、17 条指令、空状态下的记录门禁、控制记录写入、四路配置调用
  和状态项首字共同写回。
- 文件偏移 `0x00927c..0x00937f` 已全部顺序读完；相等提前返回、非空交接、空状态处理
  和新首字写回均已闭合。
- 下一独立函数始于执行地址 `0xa0008380`、文件偏移 `0x009380`。
