# `0x009594..0x0095f1` 三分支尾回接与组合事件确认

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合组合事件路径的三组前向分支。状态对象 `+48` 的 `0x00200000` 位非零时，
以状态对象调用后方函数，重读状态项 `+4` 对象并回到状态项 `+14` 门禁。状态项 `+14`
非零时，给状态项 `+24` 所指对象 `+8` 半字置 `0x10` 位，执行两个调用，清状态项
`+14` 与 `+24` 后回接共同清理。全局控制字次低位非零时则清除该位。上述路径及前方
多个跳转最终都向 `0x24b0807c` 写 `0x20000800`。

## 1. 覆盖范围

- 文件偏移：`0x009594..0x0095f1`
- 执行地址：`0xa0008594..0xa00085f1`
- 长度：94 字节
- 本段原始字节散列：`44e254cbeea0f0b4b9b6760ca1d7ac60a46905278d22fd84f8a7584eb6ae610e`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008594` | `00151097` | 状态对象特定位非零路径准备后方调用地址。 |
| `0xa0008598` | `590080e7` | 调用 `0xa0159b24(状态对象)`；第一参数由前方保持。 |
| `0xa000859c` | `00492983` | 重新读取当前状态项 `+4` 保存的状态对象地址。 |
| `0xa00085a0` | `b79d` | 回到 `0xa0008506`，重新检查状态项 `+14` 字节。 |
| `0xa00085a2` | `01892703` | 状态项 `+14` 非零路径读取状态项 `+24` 保存的对象地址。 |
| `0xa00085a6` | `010b8513` | 以编号乘 72 的偏移加 `16`。 |
| `0xa00085aa` | `9552` | 再加状态表基址，得到当前状态项自身 `+16` 的地址。 |
| `0xa00085ac` | `00875783` | 读取 `+24` 对象 `+8` 的无符号半字。 |
| `0xa00085b0` | `0107e793` | 给该半字置 `0x10` 位。 |
| `0xa00085b4` | `00f71423` | 写回 `+24` 对象 `+8`。 |
| `0xa00085b8` | `0015c097` | 准备第一个后方调用地址。 |
| `0xa00085bc` | `d3c080e7` | 调用 `0xa01642f4(当前状态项自身+16)`。 |
| `0xa00085c0` | `a053c7b7` | 准备固定表地址的高位。 |
| `0xa00085c4` | `4a878793` | 完成固定表地址 `0xa053c4a8`。 |
| `0xa00085c8` | `4567c50b` | 按当前编号乘四读取一个 32 位表项作为第一参数。 |
| `0xa00085cc` | `0015c097` | 准备第二个后方调用地址。 |
| `0xa00085d0` | `cb2080e7` | 调用 `0xa016427e(固定表[当前编号])`。 |
| `0xa00085d4` | `00090723` | 清零当前状态项 `+14` 字节。 |
| `0xa00085d8` | `00092c23` | 清零当前状态项 `+24` 的 32 位字段。 |
| `0xa00085dc` | `b795` | 跳回 `0xa0008540`，执行编号门禁、硬件与全局控制收尾。 |
| `0xa00085de` | `9bf5` | 全局控制字次低位非零路径清除该位。 |
| `0xa00085e0` | `0af72a23` | 写回全局控制区 `+180`。 |
| `0xa00085e4` | `200017b7` | 准备组合事件确认值的高位。 |
| `0xa00085e8` | `24b08737` | 准备硬件寄存器区域基址。 |
| `0xa00085ec` | `80078793` | 完成确认值 `0x20000800`。 |
| `0xa00085f0` | `df7c` | 把确认值写入寄存器 `0x24b0807c`。 |

## 3. 三处分支结果

- 状态对象 `+48` 的 `0x00200000` 位非零：调用 `0xa0159b24(状态对象)`，重读对象，
  回到状态项 `+14` 门禁，形成显式循环。
- 状态项 `+14` 非零：置 `状态项[+24]所指对象[+8] |= 0x10`，调用
  `0xa01642f4(状态项自身+16)` 和 `0xa016427e(0xa053c4a8[当前编号])`，清状态项
  `+14` 与 `+24`，再回接前段共同清理。
- 全局控制字 `+180` 次低位非零：清该位后写回。
- 共同结果：`0x24b0807c = 0x20000800`。

## 4. 能力边界

- 已闭合：94 个源字节、26 条指令、对象循环回接、状态项附属对象处理、两个调用实参、
  双字段清零、全局次低位清除和组合事件确认写入。
- 固定表内容和三个后方函数体尚未顺序到达，当前不追加业务名称。
- 大型函数随后检查入口硬件状态的另一组位，尚未闭合。
- 本函数继续于执行地址 `0xa00085f2`、文件偏移 `0x0095f2`。
