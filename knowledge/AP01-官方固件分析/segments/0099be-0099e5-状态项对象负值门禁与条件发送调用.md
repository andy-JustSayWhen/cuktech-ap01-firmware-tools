# `0x0099be..0x0099e5` 状态项对象负值门禁与条件发送调用

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段读取当前状态项 `+24` 保存的对象地址，再读取该对象 `+144` 的有符号字。该字非负时
直接进入此前已经闭合的现场恢复返回；该字为负时，继续按对象 `+88`、状态字节和状态项
`+32` 三项条件决定是否以事件编号调用发送处理函数。

## 1. 覆盖范围

- 文件偏移：`0x0099be..0x0099e5`
- 执行地址：`0xa00089be..0xa00089e5`
- 长度：40 字节
- 本段原始字节散列：`0dde156210bd0313e381f1fb5df310f1a76ea07633838f7c697c559cdab21909`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa00089be` | `018bac03` | 读取当前状态项 `+24` 保存的对象地址。 |
| `0xa00089c2` | `090c2a83` | 读取该对象 `+144` 的 32 位字。 |
| `0xa00089c6` | `d00ad6e3` | 该字按有符号数非负时跳到现场恢复返回 `0xa00086d2`。 |
| `0xa00089ca` | `058c2683` | 该字为负时读取该对象 `+88` 的 32 位字。 |
| `0xa00089ce` | `ee81` | 对象 `+88` 非零时跳过条件发送调用。 |
| `0xa00089d0` | `044bc603` | 对象 `+88` 为零时读取状态项 `+68` 的无符号字节。 |
| `0xa00089d4` | `4685` | 准备比较值 `1`。 |
| `0xa00089d6` | `00d61863` | 状态字节不等于 `1` 时跳过条件发送调用。 |
| `0xa00089da` | `020ba683` | 状态字节等于 `1` 时读取状态项 `+32` 的 32 位字。 |
| `0xa00089de` | `c681` | 该字为零时跳过条件发送调用。 |
| `0xa00089e0` | `8526` | 把当前事件编号作为第一参数。 |
| `0xa00089e2` | `cd8ff0ef` | 调用 `0xa0007eba(事件编号)`。 |

## 3. 精确门禁

令当前状态项为 `E`、`X = E[+24]`、事件编号为 `I`：

- 先读取 `V = X[+144]`；`V >= 0` 时直接进入现场恢复返回，不执行本段后续动作；
- `V < 0` 时，只有 `X[+88] == 0 && E[+68] == 1 && E[+32] != 0` 三项同时成立，
  才调用 `0xa0007eba(I)`；
- `0xa0007eba` 的完整函数体已经在
  [`008eba-009057 无线发送处理函数`](008eba-008ef9-无线发送状态项入口与三对象关系.md)
  至其后连续分段中闭合，本段只记录当前调用条件与实参。

## 4. 能力边界

- 已闭合：40 个源字节、12 条指令、两级对象字段读取、有符号门禁、直接返回地址、
  三项条件和发送调用实参。
- `X[+144]` 和 `X[+88]` 的业务名称没有由当前证据确定，不作命名推断。
- 负值路径继续于执行地址 `0xa00089e6`、文件偏移 `0x0099e6`。
