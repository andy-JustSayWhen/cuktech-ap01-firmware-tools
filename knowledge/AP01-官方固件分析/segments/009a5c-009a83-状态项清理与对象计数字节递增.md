# `0x009a5c..0x009a83` 状态项清理与对象计数字节递增

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段承接对象半字目标位已经出现的成功路径。它清状态项 `+14` 字节和 `+24` 字，递减
状态项 `+68` 字节，再检查当前对象 `+88`。该字段非空时，把当前对象地址写入状态项
`+28`，递增对象自身 `+14` 字节，并回到当前状态项检查入口。

## 1. 覆盖范围

- 文件偏移：`0x009a5c..0x009a83`
- 执行地址：`0xa0008a5c..0xa0008a83`
- 长度：40 字节
- 本段原始字节散列：`e3e871461b618c59fa7b5f7efd486aa3c0f2aac7f61db9ca3a7afc6a2305d888`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008a5c` | `044bc683` | 读取当前状态项 `+68` 的无符号字节。 |
| `0xa0008a60` | `000b8723` | 把状态项 `+14` 字节清零。 |
| `0xa0008a64` | `000bac23` | 把状态项 `+24` 的 32 位字清零。 |
| `0xa0008a68` | `16fd` | 把先前读取的状态字节减一。 |
| `0xa0008a6a` | `04db8223` | 把递减结果的低八位写回状态项 `+68`。 |
| `0xa0008a6e` | `058c2683` | 读取当前对象 `+88` 的 32 位字。 |
| `0xa0008a72` | `ce85` | 该字为零时跳到后方状态项与硬件事件清理。 |
| `0xa0008a74` | `018bae23` | 该字非零时把当前对象地址写入状态项 `+28`。 |
| `0xa0008a78` | `00ec4683` | 读取当前对象 `+14` 的无符号字节。 |
| `0xa0008a7c` | `0685` | 把该字节加一。 |
| `0xa0008a7e` | `00dc0723` | 把递增结果的低八位写回当前对象 `+14`。 |
| `0xa0008a82` | `b125` | 跳回当前状态项检查入口 `0xa00086aa`。 |

## 3. 精确写入关系

令当前状态项为 `E`、当前对象为 `X`：

- 执行 `E[+14] = 0`、`E[+24] = 0`；
- 执行 `E[+68] = 低八位(E[+68] - 1)`；
- 若 `X[+88] != 0`，执行 `E[+28] = X` 和 `X[+14] = 低八位(X[+14] + 1)`，
  然后跳回 `0xa00086aa`；
- 若 `X[+88] == 0`，跳到执行地址 `0xa0008aaa`，其动作尚待顺序闭合。

## 4. 能力边界

- 已闭合：40 个源字节、12 条指令、两个状态字段清零、状态字节递减、对象地址转存、
  对象字节递增和循环回接。
- 当前证据只能确认对象 `+88` 是分支门禁；当前路径并未把其字段内容写入状态项。
- 本函数继续于执行地址 `0xa0008a84`、文件偏移 `0x009a84`。
