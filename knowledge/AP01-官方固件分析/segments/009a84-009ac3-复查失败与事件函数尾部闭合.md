# `0x009a84..0x009ac3` 复查失败与事件函数尾部闭合

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合大型事件处理函数的三条后方尾路：五次复查后的最终位判断与失败调用、对象负值
特定位为零时对状态项 `+16` 地址的调用回接，以及对象 `+88` 为空时清状态首字和硬件
事件位。其后 `0xa0008ac4` 出现新的栈帧入口，因此始于 `0xa0008380` 的函数至此完整闭合。

## 1. 覆盖范围

- 文件偏移：`0x009a84..0x009ac3`
- 执行地址：`0xa0008a84..0xa0008ac3`
- 长度：64 字节
- 本段原始字节散列：`141bc799fc5c91950c00fde61239a30acd22570fdfcba4e6b40d2eb819fb9d3f`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008a84` | `f2e1` | 第五次调用后目标位已经出现时回接成功路径 `0xa0008a44`。 |
| `0xa0008a86` | `a053c537` | 目标位仍未出现时准备固定地址的高位。 |
| `0xa0008a8a` | `36b00593` | 准备固定数值 `875`。 |
| `0xa0008a8e` | `7ac50513` | 完成固定地址 `0xa053c7ac`。 |
| `0xa0008a92` | `0015c097` | 准备失败调用地址。 |
| `0xa0008a96` | `902080e7` | 调用 `0xa0164394(0xa053c7ac, 875)`。 |
| `0xa0008a9a` | `b925` | 调用返回后跳到现场恢复返回 `0xa00086d2`。 |
| `0xa0008a9c` | `fb842503` | 对象负值特定位为零时，从栈内取状态项 `+16` 的地址作为第一参数。 |
| `0xa0008aa0` | `0015c097` | 准备后方调用地址。 |
| `0xa0008aa4` | `854080e7` | 调用 `0xa01642f4(状态项加十六的地址)`。 |
| `0xa0008aa8` | `bf71` | 调用后回接对象半字置位与编号表调用路径 `0xa0008a44`。 |
| `0xa0008aaa` | `000ba023` | 对象 `+88` 为空时把当前状态项首字清零。 |
| `0xa0008aae` | `24b08737` | 准备硬件事件寄存器地址的高位。 |
| `0xa0008ab2` | `08c72683` | 读取寄存器 `0x24b0808c`。 |
| `0xa0008ab6` | `0166f6b3` | 用当前事件位的反码清除该位。 |
| `0xa0008aba` | `08d72623` | 把清位结果写回寄存器 `0x24b0808c`。 |
| `0xa0008abe` | `09372423` | 把当前事件位写入寄存器 `0x24b08088`。 |
| `0xa0008ac2` | `b6e5` | 跳回当前状态项检查入口 `0xa00086aa`。 |

## 3. 三条尾路

令当前状态项为 `E`、事件位为 `M`：

- 五次复查后若对象半字 `0x20` 位出现，回接成功路径；仍未出现则调用
  `0xa0164394(0xa053c7ac, 875)`，然后直接恢复现场返回；
- 前方对象 `+144` 负值的 `0x00800000` 位为零时，调用 `0xa01642f4(E + 16)`，实参是
  字段地址，再回接成功路径；
- 前方当前对象 `+88` 为空时，执行 `E[+0] = 0`、
  `读取32位字(0x24b0808c) &= ~M`、`写入32位字(0x24b08088, M)`，然后重新检查状态项。

## 4. 函数闭合结论

- 已闭合：64 个源字节、18 条指令、最终复查、失败调用、状态项地址实参、两处回接和硬件清位。
- `0xa0008380..0xa0008ac3` 对应文件偏移 `0x009380..0x009ac3` 的全部连续指令、前向分支尾、
  循环回接和现场恢复均已顺序覆盖；该大型事件处理函数完整闭合。
- `0xa0008ac4` 建立新栈帧，是下一独立函数入口；下一文件偏移为 `0x009ac4`。
