# `0x009bc0..0x009c25` 对象索引定长记录双字节门禁与清零

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

这是一个完整独立函数。它先调用 `0xa0164fce(0x62fd844c, 输入对象)`，随后检查输入对象
`+40` 的 `0x00800000` 位。该位非零时，按对象 `+33` 字节索引一张每项 1128 字节的
运行时表；只有记录 `+94` 为零且 `+96` 非零时才清记录 `+112` 字节。最后无条件以
`0x00080000` 为参数调用另一函数并返回。

## 1. 覆盖范围

- 文件偏移：`0x009bc0..0x009c25`
- 执行地址：`0xa0008bc0..0xa0008c25`
- 长度：102 字节
- 本段原始字节散列：`c3c5e8625264c5dd54d7c9befb3bff5c579c9ae7730ae3723494cd6fdae9ed26`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008bc0` | `1141` | 分配 16 字节栈空间。 |
| `0xa0008bc2` | `c422` | 保存帧指针寄存器。 |
| `0xa0008bc4` | `c226` | 保存第一个跨调用暂存寄存器。 |
| `0xa0008bc6` | `c606` | 保存返回地址。 |
| `0xa0008bc8` | `c04a` | 保存第二个跨调用暂存寄存器。 |
| `0xa0008bca` | `0800` | 建立当前栈帧基准。 |
| `0xa0008bcc` | `02154903` | 读取输入对象 `+33` 的无符号字节并保存为索引。 |
| `0xa0008bd0` | `84aa` | 保存输入对象地址。 |
| `0xa0008bd2` | `85aa` | 把输入对象地址复制为第二参数。 |
| `0xa0008bd4` | `62fd8537` | 准备固定第一参数地址的高位。 |
| `0xa0008bd8` | `44c50513` | 完成固定第一参数 `0x62fd844c`。 |
| `0xa0008bdc` | `0015c097` | 准备后方调用地址。 |
| `0xa0008be0` | `3f2080e7` | 调用 `0xa0164fce(0x62fd844c, 输入对象)`。 |
| `0xa0008be4` | `5498` | 读取输入对象 `+40` 的 32 位字。 |
| `0xa0008be6` | `008007b7` | 准备位值 `0x00800000`。 |
| `0xa0008bea` | `8ff9` | 提取对象字的该位。 |
| `0xa0008bec` | `c38d` | 该位为零时跳过运行时记录处理。 |
| `0xa0008bee` | `62fd67b7` | 准备运行时记录表基址的高位。 |
| `0xa0008bf2` | `46800713` | 准备单项记录长度 1128 字节。 |
| `0xa0008bf6` | `1a078793` | 完成运行时记录表基址 `0x62fd61a0`。 |
| `0xa0008bfa` | `20e9178b` | 把对象索引乘 1128 后累加到表基址。 |
| `0xa0008bfe` | `05e7c703` | 读取选中记录 `+94` 的无符号字节。 |
| `0xa0008c02` | `e711` | 该字节非零时跳过记录清零。 |
| `0xa0008c04` | `0607c703` | 记录 `+94` 为零时读取记录 `+96` 的无符号字节。 |
| `0xa0008c08` | `c319` | 记录 `+96` 为零时跳过记录清零。 |
| `0xa0008c0a` | `06078823` | 两项门禁命中时把选中记录 `+112` 字节清零。 |
| `0xa0008c0e` | `00080537` | 准备固定第一参数 `0x00080000`。 |
| `0xa0008c12` | `0015b097` | 准备后方调用地址。 |
| `0xa0008c16` | `66c080e7` | 调用 `0xa016427e(0x00080000)`。 |
| `0xa0008c1a` | `40b2` | 恢复返回地址。 |
| `0xa0008c1c` | `4422` | 恢复帧指针寄存器。 |
| `0xa0008c1e` | `4492` | 恢复第一个跨调用暂存寄存器。 |
| `0xa0008c20` | `4902` | 恢复第二个跨调用暂存寄存器。 |
| `0xa0008c22` | `0141` | 释放 16 字节栈空间。 |
| `0xa0008c24` | `8082` | 返回调用者。 |

## 3. 完整状态关系

令输入对象为 `X`、`I = X[+33]`、`R = 0x62fd61a0 + 1128 × I`：

- 先无条件调用 `0xa0164fce(0x62fd844c, X)`；
- 仅当 `X[+40] & 0x00800000 != 0` 时检查记录；
- 仅当 `R[+94] == 0 && R[+96] != 0` 时执行 `R[+112] = 0`；三处记录字段均按字节读写；
- 最后无条件调用 `0xa016427e(0x00080000)`，再恢复现场返回。

## 4. 能力边界

- 已闭合：102 个源字节、35 条指令、两个固定调用、对象索引、1128 字节记录公式、
  双字节门禁、清零宽度和返回路径；整个函数完整闭合。
- 当前范围不确定记录表及三个字节字段的业务身份，不作命名推断。
- 下一独立函数入口为执行地址 `0xa0008c26`、文件偏移 `0x009c26`。
