# `0x009d68..0x009db5` 全局或对象双位编号选择与状态项调用

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段为前方第二类型路径选择一个编号。默认读取全局对象 `0x62fd8454` 的 `+162` 字节；
若全局对象 `+180` 字的最低位为零，则改取当前对象 `+84` 字的第 11 至 12 位。随后按编号
定位 72 字节状态项的 `+16` 地址并调用后方函数；返回地址为空时执行固定参数调用，再进入
公共收尾，非空时保存返回地址并继续。

## 1. 覆盖范围

- 文件偏移：`0x009d68..0x009db5`
- 执行地址：`0xa0008d68..0xa0008db5`
- 长度：78 字节
- 本段原始字节散列：`696900525a94c021a809566fc81ae7bb502330fb7d8a66cb0dff2ab0ac5af6c8`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008d68` | `62fd8737` | 准备全局对象地址的高位。 |
| `0xa0008d6c` | `45470713` | 完成全局对象地址 `0x62fd8454`。 |
| `0xa0008d70` | `0a274503` | 默认读取全局对象 `+162` 的无符号字节作为编号。 |
| `0xa0008d74` | `0b472703` | 读取全局对象 `+180` 的 32 位字。 |
| `0xa0008d78` | `8b05` | 只保留该字最低位。 |
| `0xa0008d7a` | `e319` | 最低位非零时保留全局字节编号。 |
| `0xa0008d7c` | `30b7b50b` | 最低位为零时改取当前对象 `+84` 字的第 11 至 12 位作为编号。 |
| `0xa0008d80` | `04800793` | 准备状态项长度 72 字节。 |
| `0xa0008d84` | `02f50533` | 把编号乘 72。 |
| `0xa0008d88` | `62fd87b7` | 准备状态表基址的高位。 |
| `0xa0008d8c` | `24878793` | 完成状态表基址 `0x62fd8248`。 |
| `0xa0008d90` | `0541` | 给编号偏移增加状态项内偏移 `16`。 |
| `0xa0008d92` | `953e` | 加状态表基址，形成选中状态项 `+16` 的地址。 |
| `0xa0008d94` | `0015b097` | 准备后方调用地址。 |
| `0xa0008d98` | `560080e7` | 调用 `0xa01642f4(选中状态项加十六的地址)`。 |
| `0xa0008d9c` | `84aa` | 保存调用返回的地址。 |
| `0xa0008d9e` | `ed01` | 返回地址非空时进入后续对象处理。 |
| `0xa0008da0` | `a053d537` | 返回地址为空时准备固定地址的高位。 |
| `0xa0008da4` | `13f00593` | 准备固定数值 `319`。 |
| `0xa0008da8` | `9cc50513` | 完成固定地址 `0xa053c9cc`。 |
| `0xa0008dac` | `0015b097` | 准备固定参数调用地址。 |
| `0xa0008db0` | `5e8080e7` | 调用 `0xa0164394(0xa053c9cc, 319)`。 |
| `0xa0008db4` | `a0e1` | 固定调用后跳到公共收尾 `0xa0008e7c`。 |

## 3. 编号与状态项公式

令当前对象 `+84` 字为 `V`、全局对象为 `G = 0x62fd8454`：

- 若 `G[+180] & 1 != 0`，编号 `I = G[+162]`；
- 否则 `I = V` 的第 11 至 12 位；
- 调用 `R = 0xa01642f4(0x62fd8248 + 72 × I + 16)`；实参是状态项内地址；
- `R == 0` 时调用 `0xa0164394(0xa053c9cc, 319)` 后进入公共收尾；
- `R != 0` 时保存为后续对象地址，从 `0xa0008db6` 继续。

## 4. 能力边界

- 已闭合：78 个源字节、23 条指令、编号双来源、位域、状态项公式、地址实参、返回空值分流
  和固定调用。
- 调用返回对象的结构尚待后续顺序范围闭合；当前段不为编号或状态项指定业务名称。
- 本函数继续于执行地址 `0xa0008db6`、文件偏移 `0x009db6`。
