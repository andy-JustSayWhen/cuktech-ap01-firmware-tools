# `0x009e20..0x009e3b` 五百六十字节记录与缓冲扫描初始化

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段为前方分类 `22` 的成功路径建立扫描循环上下文。它用返回对象 `+12` 字节乘 560，
定位运行时表 `0x62fd4a10` 中的一项；把前方缓冲区地址加 18 作为扫描起点，把当前对象
`+48` 半字减 18 作为剩余长度，并准备后续循环使用的四个固定比较值。

## 1. 覆盖范围

- 文件偏移：`0x009e20..0x009e3b`
- 执行地址：`0xa0008e20..0xa0008e3b`
- 长度：28 字节
- 本段原始字节散列：`e2db5664973e81082e08e4d153c342378fccc2a0e075730ffffd0ae65a3e7e98`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008e20` | `23000793` | 准备单项记录长度 560 字节。 |
| `0xa0008e24` | `8ed6` | 复制运行时记录表基址 `0x62fd4a10`。 |
| `0xa0008e26` | `20f81e8b` | 把返回对象 `+12` 字节乘 560 后累加到表基址，得到选中记录。 |
| `0xa0008e2a` | `6505` | 准备固定掩码的高位。 |
| `0xa0008e2c` | `05c9` | 把前方缓冲区地址增加 18，形成扫描起点。 |
| `0xa0008e2e` | `1739` | 把当前对象 `+48` 半字减 18，形成剩余长度。 |
| `0xa0008e30` | `4885` | 准备长度下限比较值 `1`。 |
| `0xa0008e32` | `7fd00313` | 准备低十一位比较值 `2045`。 |
| `0xa0008e36` | `80050513` | 完成固定掩码 `0x0800`。 |
| `0xa0008e3a` | `4e0d` | 准备另一长度比较值 `3`。 |

## 3. 建立的循环上下文

令返回对象为 `R`、缓冲区地址为 `B`、当前对象为 `X`：

- 选中记录 `D = 0x62fd4a10 + 560 × R[+12]`；
- 扫描地址 `P = B + 18`；
- 剩余长度 `N = X[+48] - 18`；
- 固定值为 `1`、`3`、`2045` 和 `0x0800`；
- 当前段只建立上下文，循环条件和每项处理从 `0xa0008e3c` 开始。

## 4. 能力边界

- 已闭合：28 个源字节、10 条指令、记录表公式、扫描起点、剩余长度和四个常数。
- 当前段尚未证明记录字段或缓冲项结构，需由后续循环指令闭合。
- 本函数继续于执行地址 `0xa0008e3c`、文件偏移 `0x009e3c`。
