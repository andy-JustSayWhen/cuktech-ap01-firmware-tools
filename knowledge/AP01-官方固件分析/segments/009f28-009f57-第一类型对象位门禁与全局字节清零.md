# `0x009f28..0x009f57` 第一类型对象位门禁与全局字节清零

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段处理前方关联半字掩码结果等于 `36` 的第一类型分支。它要求当前对象 `+84` 字的
`0x00002000` 位非零，命中后把全局对象 `0x62fd8454` 的 `+172` 字节清零，并要求当前
对象 `+48` 半字按无符号数大于 `28`；长度门禁失败时执行固定参数调用后进入公共收尾。

## 1. 覆盖范围

- 文件偏移：`0x009f28..0x009f57`
- 执行地址：`0xa0008f28..0xa0008f57`
- 长度：48 字节
- 本段原始字节散列：`1367ba3f576d0aa8a78cbe650c60e134178a8d5b473f6912b5c388637e01b5c2`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008f28` | `6709` | 准备位值 `0x00002000`。 |
| `0xa0008f2a` | `8f7d` | 从当前对象 `+84` 字中提取该位。 |
| `0xa0008f2c` | `db21` | 该位为零时跳到公共收尾 `0xa0008e7c`。 |
| `0xa0008f2e` | `62fd84b7` | 该位非零时准备全局对象地址的高位。 |
| `0xa0008f32` | `45448713` | 完成全局对象地址 `0x62fd8454`。 |
| `0xa0008f36` | `0a070623` | 把全局对象 `+172` 字节清零。 |
| `0xa0008f3a` | `0309d603` | 读取当前对象 `+48` 的无符号半字。 |
| `0xa0008f3e` | `46f1` | 准备无符号长度门限 `28`。 |
| `0xa0008f40` | `45448493` | 保存全局对象地址 `0x62fd8454`。 |
| `0xa0008f44` | `00c6ea63` | 当前对象半字按无符号数大于 `28` 时进入后续处理。 |
| `0xa0008f48` | `6585` | 长度门禁失败时准备固定数值的高位。 |
| `0xa0008f4a` | `93f58593` | 完成固定数值 `2367`。 |
| `0xa0008f4e` | `a053d537` | 准备固定地址的高位。 |
| `0xa0008f52` | `88850513` | 完成固定地址 `0xa053c888`。 |
| `0xa0008f56` | `bd99` | 跳到共同调用点，执行 `0xa0164394(0xa053c888, 2367)` 后收尾。 |

## 3. 精确门禁关系

令当前对象为 `X`、全局对象为 `G = 0x62fd8454`：

- `X[+84] & 0x00002000 == 0` 时直接进入公共收尾；
- 该位非零时先执行 `G[+172] = 0`；
- 随后仅当无符号半字 `X[+48] > 28` 时从 `0xa0008f58` 继续；
- `X[+48] <= 28` 时执行 `0xa0164394(0xa053c888, 2367)` 后进入公共收尾。

## 4. 能力边界

- 已闭合：48 个源字节、15 条指令、对象位门禁、全局字节清零、无符号半字门限和固定调用。
- 长度门禁通过后的处理尚待顺序闭合；本文不为全局字节或半字长度指定业务名称。
- 本函数继续于执行地址 `0xa0008f58`、文件偏移 `0x009f58`。
