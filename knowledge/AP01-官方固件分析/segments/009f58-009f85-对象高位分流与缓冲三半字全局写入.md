# `0x009f58..0x009f85` 对象高位分流与缓冲三半字全局写入

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段承接第一类型的长度门禁成功路径。它检查当前对象 `+84` 字的 `0x02000000` 位；
该位为零时，从当前对象 `+28` 所指对象的 `+8` 缓冲区读取偏移 `10`、`12`、`14`
三个无符号半字，分别写入全局对象 `0x62fd8454` 的 `+152`、`+154`、`+156`，然后进入
共同运行时更新。该位非零时跳到后续记录路径。

## 1. 覆盖范围

- 文件偏移：`0x009f58..0x009f85`
- 执行地址：`0xa0008f58..0xa0008f85`
- 长度：46 字节
- 本段原始字节散列：`aee6a1cdaf7484da7e345ea81317348b2176860b11de75b23ad27af2d7b43a76`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0008f58` | `020006b7` | 准备位值 `0x02000000`。 |
| `0xa0008f5c` | `8efd` | 从当前对象 `+84` 字中提取该位。 |
| `0xa0008f5e` | `e685` | 该位非零时跳到后续记录路径 `0xa0008f86`。 |
| `0xa0008f60` | `008c2783` | 该位为零时读取关联对象 `+8` 保存的缓冲区地址。 |
| `0xa0008f64` | `00a7d783` | 读取缓冲区 `+10` 的无符号半字。 |
| `0xa0008f68` | `08f71c23` | 把该半字写入全局对象 `+152`。 |
| `0xa0008f6c` | `008c2783` | 再次读取关联对象 `+8` 的缓冲区地址。 |
| `0xa0008f70` | `00c7d783` | 读取缓冲区 `+12` 的无符号半字。 |
| `0xa0008f74` | `08f71d23` | 把该半字写入全局对象 `+154`。 |
| `0xa0008f78` | `008c2783` | 再次读取关联对象 `+8` 的缓冲区地址。 |
| `0xa0008f7c` | `00e7d783` | 读取缓冲区 `+14` 的无符号半字。 |
| `0xa0008f80` | `08f71e23` | 把该半字写入全局对象 `+156`。 |
| `0xa0008f84` | `bde5` | 跳到共同运行时更新 `0xa0008e7c`。 |

## 3. 精确对象与写入关系

令当前对象为 `X`、关联对象为 `Y = X[+28]`、缓冲区为 `B = Y[+8]`、
全局对象为 `G = 0x62fd8454`：

- `X[+84] & 0x02000000 != 0` 时跳到 `0xa0008f86`；
- 该位为零时执行 `G[+152] = B[+10]`、`G[+154] = B[+12]`、`G[+156] = B[+14]`；
- 六处访问均为半字宽度；三项写完后进入共同运行时更新。

## 4. 能力边界

- 已闭合：46 个源字节、13 条指令、对象位门禁、缓冲区地址来源、三个半字读写偏移及回接。
- 三个半字的业务身份和特定位非零路径尚未闭合，不在当前段命名。
- 本函数继续于执行地址 `0xa0008f86`、文件偏移 `0x009f86`。
