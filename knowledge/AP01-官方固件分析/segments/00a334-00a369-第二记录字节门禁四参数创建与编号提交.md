# `0x00a334..0x00a369` 第二记录字节门禁四参数创建与编号提交

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段承接 560 字节记录双字段门禁成功路径。它用当前记录 `+32` 字节索引每项 1128 字节
的第二记录表；第二记录 `+94` 字节为零时把创建调用第二参数设为 `6`，非零时设为 `5`，
再以固定四参数调用后方函数。返回地址的首字节写入当前对象编号，随后以该返回地址调用
本镜像后方函数 `0xa00094ce`。

## 1. 覆盖范围

- 文件偏移：`0x00a334..0x00a369`
- 执行地址：`0xa0009334..0xa0009369`
- 长度：54 字节
- 本段原始字节散列：`670cd6b901bc489e61c74ba38aebe7798d19712e2219ea3b2b842e7f8032d917`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0009334` | `0207c683` | 读取当前 560 字节记录 `+32` 的无符号字节。 |
| `0xa0009338` | `62fd6737` | 准备第二记录表地址的高位。 |
| `0xa000933c` | `1a070793` | 完成第二记录表基址 `0x62fd61a0`。 |
| `0xa0009340` | `46800713` | 准备第二记录单项长度 1128 字节。 |
| `0xa0009344` | `20e6978b` | 把第一记录 `+32` 字节乘 1128 后累加到第二记录表基址。 |
| `0xa0009348` | `6505` | 准备创建调用第一参数的高位。 |
| `0xa000934a` | `46a1` | 把创建调用第四参数设为 `8`。 |
| `0xa000934c` | `4615` | 把创建调用第三参数设为 `5`。 |
| `0xa000934e` | `41950513` | 完成创建调用第一参数 `5145`。 |
| `0xa0009352` | `05e7c583` | 读取选中第二记录 `+94` 的无符号字节。 |
| `0xa0009356` | `0015b593` | 把该字节是否为零转换成零或一。 |
| `0xa000935a` | `0595` | 给转换结果加五，形成第二参数 `5` 或 `6`。 |
| `0xa000935c` | `0015b097` | 准备四参数创建调用地址。 |
| `0xa0009360` | `56e080e7` | 调用 `0xa01648ca(5145, 第二参数, 5, 8)`。 |
| `0xa0009364` | `01250023` | 把前段保存的当前对象编号写入返回地址首字节。 |
| `0xa0009368` | `229d` | 以返回地址为第一参数调用 `0xa00094ce`。 |

## 3. 精确调用关系

令当前 560 字节记录为 `D`、当前对象编号为 `I`，第二记录
`E = 0x62fd61a0 + 1128 × D[+32]`：

- 第二参数 `P = (E[+94] == 0 ? 6 : 5)`；
- 调用 `R = 0xa01648ca(5145, P, 5, 8)`；
- 随后执行 `R[0] = I`，写入宽度为字节；
- 再调用 `0xa00094ce(R)`；该目标位于尚未顺序读到的文件偏移 `0x00a4ce`，当前段不提前
  推断其内部能力。

## 4. 能力边界

- 已闭合：54 个源字节、16 条指令、第二记录地址公式、字节零值映射、四个调用参数、
  返回地址首字节写入和后方调用关系。
- 两个调用目标的内部能力及返回地址容量不由当前现场推断。
- 本函数继续于执行地址 `0xa000936a`、文件偏移 `0x00a36a`。
