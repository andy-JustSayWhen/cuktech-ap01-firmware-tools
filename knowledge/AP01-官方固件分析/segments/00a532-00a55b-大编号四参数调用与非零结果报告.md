# `0x00a532..0x00a55b` 大编号四参数调用与非零结果报告

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合执行地址 `0xa00094ce` 开始的独立函数。它处理前置半字低编号大于 13 的路径，
从输入指针前两字节取得长度并加 16，以 `0x1000`、输入指针减 12、调整后长度和 20
调用固定函数。调用返回零时，本函数返回零；返回非零时，原厂以固定字符串和行号调用
另一个函数，再从共同现场恢复路径返回。

## 1. 覆盖范围

- 文件偏移：`0x00a532..0x00a55b`
- 执行地址：`0xa0009532..0xa000955b`
- 长度：42 字节
- 本段原始字节散列：`ee143563f4b3b850b7e8ae14e7387cbbcf4f970d5d58d53b213cf17706d7af06`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0009532` | `ffe55603` | 从输入指针前两字节读取无符号半字。 |
| `0xa0009536` | `46d1` | 准备第四调用参数 20。 |
| `0xa0009538` | `6505` | 准备第一调用参数 `0x1000`。 |
| `0xa000953a` | `0641` | 把前置半字加 16，作为第三调用参数。 |
| `0xa000953c` | `00181097` | 形成目标函数地址的高位并保存返回点。 |
| `0xa0009540` | `778080e7` | 调用执行地址 `0xa018acb4`；第二参数已由前段准备为输入指针减 12。 |
| `0xa0009544` | `d175` | 调用返回零时跳到共同现场恢复路径。 |
| `0xa0009546` | `a053d537` | 返回非零时开始准备固定字符串地址 `0xa053d708`。 |
| `0xa000954a` | `0b600593` | 准备固定行号 182。 |
| `0xa000954e` | `70850513` | 完成固定字符串地址 `0xa053d708`。 |
| `0xa0009552` | `0015c097` | 形成第二目标函数地址的高位并保存返回点。 |
| `0xa0009556` | `3f2080e7` | 调用执行地址 `0xa0165944(0xa053d708, 182)`。 |
| `0xa000955a` | `b7f9` | 跳到执行地址 `0xa0009528`，恢复现场并返回。 |

## 3. 精确调用公式

令输入指针为 `U`，前段已经准备 `U - 12`：

`R = 0xa018acb4(0x1000, U - 12, 无符号半字(U - 2) + 16, 20)`

- 若 `R == 0`，本函数经共同恢复路径返回零；
- 若 `R != 0`，调用 `0xa0165944(0xa053d708, 182)`，随后经共同恢复路径返回；
- 非零路径最终返回寄存器没有被本函数重新赋值，因此若第二目标函数会返回，本函数返回其返回寄存器现值；其稳定含义须由该目标本体后续顺序证据确定。

## 4. 独立函数闭合结论

- 已闭合本段 42 个源字节、13 条指令、前置长度读取、四参数调用、零值返回和非零报告调用。
- 至此，执行地址 `0xa00094ce..0xa000955b`、文件偏移 `0x00a4ce..0x00a55b` 的独立函数全部顺序字节和分支路径均已覆盖。
- `0xa018acb4` 与 `0xa0165944` 的函数本体尚未顺序覆盖；当前不能先行命名操作类型，也不能把非零结果解释为特定错误。
- 下一独立函数从文件偏移 `0x00a55c`、执行地址 `0xa000955c` 开始。
