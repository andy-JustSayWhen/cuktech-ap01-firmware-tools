# `0x00a96c..0x00a9bb` 第二全局半字门禁与对齐值触发返回

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段闭合执行地址 `0xa00098d6` 开始的独立函数。原厂先调用
`0xa00272cc(0x62fc3c74)`，把同一地址半字按有符号 16 位重读，要求值不大于 76；
越界时以行号 151 进入前方固定报告路径。正常时再调用 `0xa0009a76(0)`：其返回值低
16 位按有符号数解释后必须大于零，且原返回值低两位必须为零，才调用
`0xa0052262(1, 0)`。最后恢复进入受保护区前的完整处理器状态，并返回本函数入口时保存的
原下一节点指针。

## 1. 覆盖范围

- 文件偏移：`0x00a96c..0x00a9bb`
- 执行地址：`0xa000996c..0xa00099bb`
- 长度：80 字节
- 本段原始字节散列：`3803926420cda36b4722c7cf54eddfad552b687ba9bc0cb54c757612d863f786`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa000996c` | `7b418513` | 计算第二全局半字地址 `0x62fc3c74`，作为调用第一参数。 |
| `0xa0009970` | `15d1d0ef` | 调用执行地址 `0xa00272cc(0x62fc3c74)`。 |
| `0xa0009974` | `7b418913` | 保存第二全局半字地址。 |
| `0xa0009978` | `00095783` | 调用后读取第二全局半字。 |
| `0xa000997c` | `04c00713` | 准备上限 76。 |
| `0xa0009980` | `3c07a78b` | 把第二全局半字按有符号 16 位扩展。 |
| `0xa0009984` | `00f75663` | 更新后值不大于 76 时继续正常路径。 |
| `0xa0009988` | `4601` | 更新后值大于 76 时准备第三报告参数零。 |
| `0xa000998a` | `09700593` | 准备固定报告行号 151。 |
| `0xa000998e` | `bf9d` | 跳到执行地址 `0xa0009904`，以固定字符串 `0xa01be59c` 调用报告函数。 |
| `0xa0009990` | `4501` | 正常路径准备第一调用参数零。 |
| `0xa0009992` | `20d5` | 调用执行地址 `0xa0009a76(0)`。 |
| `0xa0009994` | `3c05278b` | 取返回值低 16 位并按有符号数扩展。 |
| `0xa0009998` | `00f05863` | 有符号低 16 位不大于零时跳过触发调用。 |
| `0xa000999c` | `890d` | 有符号低 16 位为正时，提取原返回值低两位。 |
| `0xa000999e` | `e509` | 低两位非零时跳过触发调用。 |
| `0xa00099a0` | `4581` | 两项门禁通过时准备第二参数零。 |
| `0xa00099a2` | `4505` | 准备第一参数一。 |
| `0xa00099a4` | `0bf480ef` | 调用执行地址 `0xa0052262(1, 0)`。 |
| `0xa00099a8` | `30099073` | 恢复进入受保护区前保存的完整处理器状态字。 |
| `0xa00099ac` | `40f2` | 恢复返回地址。 |
| `0xa00099ae` | `4462` | 恢复帧指针。 |
| `0xa00099b0` | `4942` | 恢复第二全局半字地址暂存寄存器。 |
| `0xa00099b2` | `49b2` | 恢复处理器状态暂存寄存器。 |
| `0xa00099b4` | `8526` | 把入口保存的原下一节点指针作为函数返回值。 |
| `0xa00099b6` | `44d2` | 恢复原下一节点暂存寄存器。 |
| `0xa00099b8` | `6105` | 释放 32 字节栈帧。 |
| `0xa00099ba` | `8082` | 返回调用方。 |

## 3. 精确门禁与返回

- 先调用 `0xa00272cc(0x62fc3c74)`；
- 令 `C = 有符号16位(半字(0x62fc3c74))`；`C <= 76` 时继续，`C > 76` 时以行号 151 进入前方报告路径；
- 正常时令 `R = 0xa0009a76(0)`；
- 只有 `有符号16位(低16位(R)) > 0` 且 `R & 3 == 0`，才调用 `0xa0052262(1, 0)`；
- 无论是否触发最后一次调用，正常路径都恢复原处理器状态，并返回入口输入节点原 `+0` 指针。

## 4. 独立函数闭合结论

- 已闭合本段 80 个源字节、28 条指令、第二全局半字调用与重读、七十六上限、返回值符号和对齐门禁、固定触发调用、状态恢复和返回。
- 至此，执行地址 `0xa00098d6..0xa00099bb`、文件偏移 `0x00a8d6..0x00a9bb` 的独立函数全部顺序字节和分支目标均已覆盖。
- `0xa0009a76` 位于后续即将顺序覆盖区域；当前段只记录其直接参数和返回值用法，不以向前查看替代后续通读。
- 完整函数会把输入节点挂入全局链，但把挂接前保存的原下一节点作为返回值，便于调用者继续处理原链。
- 下一独立函数从文件偏移 `0x00a9bc`、执行地址 `0xa00099bc` 开始。
