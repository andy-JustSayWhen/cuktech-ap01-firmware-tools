# `0x00ae78..0x00ae8d` 连续源写分段链默认取得方式包装

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段是执行地址 `0xa0009e78` 开始的完整包装函数。它不改写前五个入口参数，
只把第六参数强制设为零，再调用已通读的 `0xa0009742` 连续源写分段链函数，
并原样返回该函数的结果。这使后续扩链时固定选择 `0xa0009dd4` 直接节点取得
路径。

## 1. 覆盖范围

- 文件偏移：`0x00ae78..0x00ae8d`
- 执行地址：`0xa0009e78..0xa0009e8d`
- 长度：22 字节
- 本段原始字节散列：`b8dea7aefa173c283c065c3b05ae4c79bdaa094dd33a59ae512680ba40f2dc61`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0009e78` | `1141` | 建立 16 字节栈帧。 |
| `0xa0009e7a` | `c422` | 保存帧指针。 |
| `0xa0009e7c` | `c606` | 保存返回地址。 |
| `0xa0009e7e` | `0800` | 建立本函数帧指针。 |
| `0xa0009e80` | `4781` | 把第六参数固定为零。 |
| `0xa0009e82` | `8c1ff0ef` | 调用已通读的执行地址 `0xa0009742`，传入原前五参数与第六参数零。 |
| `0xa0009e86` | `40b2` | 恢复返回地址，保留被调函数的返回值。 |
| `0xa0009e88` | `4422` | 恢复帧指针。 |
| `0xa0009e8a` | `0141` | 释放 16 字节栈帧。 |
| `0xa0009e8c` | `8082` | 原样返回 `0xa0009742` 的结果。 |

## 3. 精确包装关系

原函数为：

`0xa0009742(目标分段链, 连续源地址, 写入长度, 目标逻辑偏移, 新节点取得参数, 取得方式选择值)`。

本包装函数的等价关系为：

`返回 0xa0009742(原参数1, 原参数2, 原参数3, 原参数4, 原参数5, 0)`。

## 4. 能力边界

- 已闭合：22 个源字节、10 条指令、第六参数固定、已知直接调用、返回值透传和整个函数。
- 本函数自身不执行范围检查、字节复制或节点取得；这些行为由已通读的
  `0xa0009742..0xa000984d` 函数实现。
- 下一独立函数从文件偏移 `0x00ae8e`、执行地址 `0xa0009e8e` 开始。
