# `0x00afae..0x00afd3` 关联字比较与全局半字递增早退

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段继续执行地址 `0xa0009f5e` 开始的大型三参数函数。前段已读得第二
对象 `+68` 所指对象地址。当前段比较该关联对象 `+8` 字段与第二对象 `+48`
字段；后者无符号不小于前者时继续后方构造路径。后者较小时，原厂以
第一对象调用 `0xa000c498`，将全局半字 `0x62fc8924` 加一，把后方比较候选
设为零，并跳到尚待顺序覆盖的共同清理尾。

## 1. 覆盖范围

- 文件偏移：`0x00afae..0x00afd3`
- 执行地址：`0xa0009fae..0xa0009fd3`
- 长度：38 字节
- 本段原始字节散列：`a3d237b47161b9ca2987bb0a5c1d6cc025cb971d8b93eda0034d1dec5698fd60`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa0009fae` | `4798` | 读取第二对象 `+68` 所指关联对象 `+8` 的 32 位字段。 |
| `0xa0009fb0` | `030a2783` | 读取第二对象 `+48` 的 32 位字段。 |
| `0xa0009fb4` | `02e7f063` | 第二对象 `+48` 无符号不小于关联对象 `+8` 时跳到后方构造路径。 |
| `0xa0009fb8` | `854a` | 比较不满足时，把第一对象作为后方调用参数。 |
| `0xa0009fba` | `4de020ef` | 调用执行地址 `0xa000c498(第一对象)`。 |
| `0xa0009fbe` | `62fc97b7` | 准备全局区基址 `0x62fc88fc` 的高位。 |
| `0xa0009fc2` | `8fc78793` | 完成全局区基址 `0x62fc88fc`。 |
| `0xa0009fc6` | `0287d703` | 读取全局无符号半字 `0x62fc8924`。 |
| `0xa0009fca` | `0705` | 把该半字读值加一。 |
| `0xa0009fcc` | `02e79423` | 把加一结果的低 16 位写回 `0x62fc8924`。 |
| `0xa0009fd0` | `4781` | 把后方共同比较候选设为零。 |
| `0xa0009fd2` | `a8cd` | 跳到执行地址 `0xa000a0c4` 开始的共同比较与清理尾。 |

## 3. 精确比较与早退

令第一对象为 `A`、第二对象为 `B`、`Q = 字(B+68)`：

1. 前段已确保当前路径上 `Q != 0`；
2. 读取 `X = 字(Q+8)` 与 `Y = 字(B+48)`；
3. `Y >= X` 按无符号比较成立时，进入下一字节开始的共同构造路径；
4. `Y < X` 时，调用 `0xa000c498(A)`，再写
   `半字(0x62fc8924) = 低16位(半字(0x62fc8924) + 1)`；
5. 将后方比较候选设为零，跳到 `0xa000a0c4`。

## 4. 能力边界

- 已闭合：38 个源字节、12 条指令、两个关联字读取、无符号比较、第一对象调用、
  全局半字低 16 位递增、零候选和共同尾跳转。
- `0xa000c498` 本体尚未按顺序覆盖，因此只能记录它以第一对象为实参被调用，
  不命名其业务效果。
- `0xa000a0c4` 的共同尾尚未按当前顺序读到，当前只记录实际跳转和传入零候选，
  不提前声称其完整效果。
- 下一字节 `0x00afd4` 开始，空关联对象和比较通过两条路径在同一构造入口汇合。
