# `0x00b67c..0x00b6a1` 对象五十六有符号半字负值调用

返回总账：[`AP01 原厂固件逐字节与逐指令通读总账`](../原厂固件逐字节与逐指令通读总账.md)

本段是一个完整的单参数函数。原厂读取对象 `+56` 半字，按有符号
16 位数扩展。结果非负时直接返回；结果为负时，以对象 `+56` 地址
调用 `0xa00272cc`，再返回。

## 1. 覆盖范围

- 文件偏移：`0x00b67c..0x00b6a1`
- 执行地址：`0xa000a67c..0xa000a6a1`
- 长度：38 字节
- 本段原始字节散列：`9d8e0751151d40ea8becc89cc6fed662352fba9d94dab26aa933ba3ab6458f07`

## 2. 全部指令

| 地址 | 原始指令 | 作用 |
| --- | --- | --- |
| `0xa000a67c` | `03855783` | 读取对象 `+56` 的无符号半字。 |
| `0xa000a680` | `3c07a78b` | 把该半字按有符号 16 位数扩展到 32 位。 |
| `0xa000a684` | `0007de63` | 扩展结果大于或等于零时跳到直接返回。 |
| `0xa000a688` | `1141` | 负值路径建立 16 字节栈帧。 |
| `0xa000a68a` | `c422` | 保存帧指针。 |
| `0xa000a68c` | `c606` | 保存返回地址。 |
| `0xa000a68e` | `0800` | 建立本函数帧指针。 |
| `0xa000a690` | `03850513` | 计算对象 `+56` 地址作为调用参数。 |
| `0xa000a694` | `4391c0ef` | 调用执行地址 `0xa00272cc(对象+56)`。 |
| `0xa000a698` | `40b2` | 恢复返回地址。 |
| `0xa000a69a` | `4422` | 恢复帧指针。 |
| `0xa000a69c` | `0141` | 释放 16 字节栈帧。 |
| `0xa000a69e` | `8082` | 负值路径返回。 |
| `0xa000a6a0` | `8082` | 非负路径直接返回。 |

## 3. 精确分流

令对象为 `A`、`V = 有符号16位(半字(A+56))`：

- `V >= 0` 时直接返回；
- `V < 0` 时调用 `0xa00272cc(A+56)` 后返回。

## 4. 能力边界

- 已闭合：38 个源字节、14 条指令、对象字段、有符号扩展、正负分流、
  调用实参和整个函数。
- `0xa00272cc` 本体尚未按顺序覆盖，因此不命名其效果。
- 两条路径返回前没有统一设定返回寄存器：非负路径保留入口对象地址，
  负值路径保留被调函数返回值；本文不将它们合并为单一返回值语义。
- 下一字节 `0x00b6a2` 开始新的链表累加函数。
