# AP01 `1.0.2_0031` 优化固件 DESIGN

本文对应 [`SPEC.md`](../SPEC.md)。
SPEC 与 DESIGN 的文档职责以项目根目录 [`AGENTS.md`](../../AGENTS.md) 中的唯一声明为准。

| 项目 | 当前状态 |
| --- | --- |
| 文档状态 | `FW-PAGE-006-A` 已安装、重启并重新上线，待启动与设置进入两步物理验收 |
| 固件实现 | 开机调用保持原厂七项；只在用户进入设置的第二次调用追加新增普通行与返回行；设置回调不变 |
| 离线构建 | 专用 4 项与全项目 114 项测试通过；两个调用者分别模拟；两次完整构建逐字节一致 |
| 当前冻结成品 | `FW-PAGE-006-A`，SHA-256 `d9b67a6d1fb94b387051e1fb979f8509faf6e53b0d08655b998f32f22d1c04fd`；A 阶段为回退对象 |
| 真机安装 | 云端完整回读一致后只安装一次；121 秒上报已安装，124 秒运行时间重置并重新上线；更新空闲且没有本次更新错误 |
| 原厂回刷 | 远程普通更新路径已通过；功率页可见性与基座连接状态的原厂调用链已静态确认 |
| 离线恢复 | AP01 专属接口未找到，恢复工具当前不可用，恢复演练未执行 |
| 专用测试设备 | `FW-PAGE-006-A` 已安装并重新上线；自动证据不替代启动和进入设置两步验收 |

## 0. 当前有效方案与纠偏记录

设计纠偏状态和“一次性循环”只见 [`design.md`](../design.md) 第 2 节。

### 0.1 当前有效方案

| 方案编号 | 承接范围 | 当前方案 | 状态 | 下一验证门槛 |
| --- | --- | --- | --- | --- |
| `FW-MATERIAL-001` | `SPEC-FIRMWARE-001` 至 `SPEC-FIRMWARE-008` | 把已跟踪固件作为版本化来源；构建前核对指纹并生成本机私有只读工作副本，构建器只接收该副本 | 拟定 | macOS 与 Windows 都能生成只读副本；复制前后指纹一致；直接传入仓库可写文件仍被拒绝 |
| `FW-ASSET-002` | `SPEC-DASHBOARD-010` 至 `SPEC-DASHBOARD-014`、`SPEC-FIRMWARE-007` 至 `SPEC-FIRMWARE-008` | 四张已经批准且指纹固定的等待页作为功能模块专属冻结资源；构建只复制、重读并校验，不在冻结构建中重新渲染 | 已验证的离线资源方案 | 进入完整集成候选的两次确定性构建；原工作区六个看板文件保持原样 |
| `FW-SETTING-003` | `SPEC-FIRMWARE-004` 至 `SPEC-FIRMWARE-005`、`SPEC-ROTARY-001` 至 `SPEC-ROTARY-008` | 保持已经验收的四个等长修改区间和同格方向过滤，不再改动 | 已验证的历史阶段成品 | 用当前提交和跟踪材料重新确定性构建、运行自动测试并逐字节复核 |
| `FW-AGENTS-005` | `SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 不先改左右分支；先在原厂键值回调局部路径补充可导出的旋钮与页面状态观测 | 拟定 | 证明观测不改变原厂分支、写入有固定上限、能区分原始键值、过滤方向、活动页面、AGENTS 状态尾部和实际目标分支 |
| `FW-PAGE-003` | `SPEC-PAGE-001` 至 `SPEC-PAGE-007` | 第一段曾同时组合载荷、八项创建和设置回调；实现只见第 14.5.1 节 | 启动失败，已否定 | 用户确认卡在开机动画；禁止再次安装，失败与回退证据已经固定 |
| `FW-PAGE-004` | `SPEC-PAGE-001` 至 `SPEC-PAGE-007` | A 阶段启动基础通过；B 阶段把循环扩成八次并在新迭代创建返回行 | B 阶段启动失败，已否定 | 禁止再次安装；失败与回退证据已经固定 |
| `FW-PAGE-005` | `SPEC-PAGE-001` 至 `SPEC-PAGE-004` | 保持原厂七次循环；在原厂序号 6 的同一次分支内依次创建新增普通行与原厂返回行 | 启动失败，已否定 | 两次失败共同边界证明开机调用不能创建新增普通行；禁止再次安装 |
| `FW-PAGE-006` | `SPEC-PAGE-001` 至 `SPEC-PAGE-004` | 从创建函数保存的调用者返回地址区分开机构建与用户进入设置；开机调用保持七项，第二次调用才追加新增普通行与返回行 | 已安装并重新上线，待两步物理验收 | 先验证启动；再进入设置验证八项与新增行位置；通过后才设计设置回调包装 |
| `FW-DASHBOARD-002` | `SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-028` | 保留现有采集、渲染和完整结果实现；局域网服务直接提供不含账号原文的聚合四页包，不依赖设备与主机共享配置 | 验证中 | 当前 macOS 服务重启后直接取包通过；其他主机、多地址切换和局域网边界待后续验证 |
| `FW-INTEGRATION-001` | `SPEC-FIRMWARE-006` 至 `SPEC-FIRMWARE-008` | 从只读 `opt-setting.bin` 组合页面开关对象、当前原厂四个局部分支 AGENTS 实现和后台刷新，固定生成 `ap01-1.0.2_0031-opt-integration-candidate.bin` | 已否定 | 现有组合入口依赖替换一级总回调，与第 6.1 节真机否定结论冲突；构建器正确拒绝生成 |
| `FW-INTEGRATION-002` | `SPEC-PAGE-005` 至 `SPEC-PAGE-007`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033`、`SPEC-FIRMWARE-006` 至 `SPEC-FIRMWARE-008` | 保留一级总回调注册地址，只把原厂回调内汇合后的实际切页调用挂到页面过滤入口；与四个 AGENTS 局部分支、设置对象和后台刷新一起生成固定名称的完整离线组合候选 | 已冻结但审计失效，不可安装 | 新的唯一旋钮事实原文证明键值方向必须按实际回调分别解释；重新审计一级窗口、系统设置、未消费返回路径及链接进成品的旧导航实现前，既有 73 项自动测试不能恢复安装资格 |
| `FW-INTEGRATION-003` | `SPEC-PAGE-001` 至 `SPEC-PAGE-007`、`SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-033`、`SPEC-FIRMWARE-006` 至 `SPEC-FIRMWARE-008` | 从已验收设置阶段输入重建优化载荷；只保留设置回调包装、原厂实际切页过滤、萌宠左/右/确认三个局部分支和后台同步；删除优化成品中的旧总键值包装、旧通用窗口导航和重复的功率左键局部分支 | 真机启动失败，已否定 | 仅下载动作实际触发候选运行并卡在开机动画；立即回刷已验收 `opt-setting.bin`，该候选禁止再次安装 |
| `FW-TRANSPORT-002` | `SPEC-DASHBOARD-002` 至 `SPEC-DASHBOARD-005` | 继续复用参考项目已经真机验证的原厂天气网络任务、流式写入和三组临时槽；四页包头固定为 64 字节，保留四页长度与逐页损坏检查；请求只使用固定局域网地址 | 已有历史实现，正按新边界复用 | 保持下载状态不超过 160 字节、单函数静态栈不超过 320 字节；构建中不得出现设备配置或身份材料 |
| `FW-AGENTS-006` | `SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-005`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 从已验收 `opt-setting.bin` 组合 `FW-TRANSPORT-002` 与原厂萌宠三个局部分支 | 功率确认重启，已否定 | 真机证据只见 [`2026-07-31 FW-AGENTS-006 功率页确认重启`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-006功率页确认重启.md)；禁止再次安装 |
| `FW-AGENTS-007` | `SPEC-DASHBOARD-001`、`SPEC-DASHBOARD-006` 至 `SPEC-DASHBOARD-014`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 从已验收 `opt-setting.bin` 只组合四张内置页面与原厂萌宠左、右、确认三个局部分支；不改原厂后台网络回调、调用、定时和地址 | 功率确认卡住，已否定 | AGENTS 概览右旋直接切到功率，绕过原厂萌宠右旋继续地址；禁止再次安装 |
| `FW-AGENTS-008` | `SPEC-DASHBOARD-001`、`SPEC-DASHBOARD-006` 至 `SPEC-DASHBOARD-014`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 保持 `FW-AGENTS-007` 的后台隔离与内置页面；AGENTS 概览右旋时先恢复萌宠，再恢复原参数并返回 `0xa00bc716`，完全交还原厂右旋分支，不再直接调用切页函数 | 功率页不可达，已否定 | 用户无法在一级导航中找到原厂功率页；禁止再次安装。当前只能排除页面创建字节被改，尚不能区分同一格多事件与交还原厂分支时状态错位 |
| `FW-AGENTS-009` | `SPEC-DASHBOARD-001`、`SPEC-DASHBOARD-006` 至 `SPEC-DASHBOARD-014`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 不改键值和功率专用分支；AGENTS 离开序号 `7` 前只关闭独立状态，由 `0xa00bc716` 完成原厂萌宠收尾；只在 `0xa00bc092` 的原厂专用收尾之后处理共享序号 `7` 的萌宠或 AGENTS 画面 | 功率页仍不可达，已否定 | 离线门禁、安装、重启和重新上线通过；用户物理导航仍找不到功率页；禁止再次安装 |
| `FW-AGENTS-010` | `SPEC-DASHBOARD-001`、`SPEC-DASHBOARD-006` 至 `SPEC-DASHBOARD-014`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 保留 `FW-AGENTS-009` 的三个萌宠局部分支和统一切页点；在功率确认局部入口先核对原厂设备端口数与功率汇总数据指针，有效时回到原厂确认，无效时复用原厂离线切页入口回到时钟 | 已安装，待物理验收 | 冻结构建与云端完整回读逐字节一致；一次安装后运行时间重置并重新上线；证据只见 [`FW-AGENTS-010 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-010安装.md) |
| `FW-AGENTS-011` | `SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-005`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 在 `FW-AGENTS-010` 上恢复四页包与三槽提交，但没有完整复用成功参考实现的天气格式与界面定时包装 | 已否定 | 安装和重启成功；设备运行 369 秒后服务请求计数仍未增加，证据只见 [`FW-AGENTS-011 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-011安装后未取数.md) |
| `FW-AGENTS-012` | `SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-005`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 保留 `FW-AGENTS-010` 页面与功率保护；两个天气格式区严格写 `%s`；界面定时包装先调用原厂入口，再在 AGENTS 状态有效时应用提交结果 | 已否定 | 安装重启成功，但设备运行 328 秒仍未请求；证据只见 [`FW-AGENTS-012 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-012安装后未取数.md) |
| `FW-AGENTS-013` | `SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-005`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 完整继承 `FW-AGENTS-012`；只用原厂已链接的内存申请与释放入口承载 136 字节四页下载状态，后台包装入口不再把该状态放在原厂天气任务栈上 | 已否定 | 安装重启成功，但设备运行 307 秒仍未请求；证据只见 [`FW-AGENTS-013 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-013安装后未取数.md) |
| `FW-TRANSPORT-PROBE-001` | `SPEC-DASHBOARD-002` 至 `SPEC-DASHBOARD-005` 的传输触发隔离 | 直接使用参考项目当前远端提交的兼容图制作和单页实时加载器，不链接本项目页面、旋钮、功率保护或四页解析；独立服务提供一张有效动图 | 已否定 | 上传回读、安装、重启均成功，但没有 AP01 请求；证据只见 [`安装后未请求`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-TRANSPORT-PROBE-001安装后未请求.md) |
| `FW-AGENTS-014` | `SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-005`、`SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033` | 继承 `FW-AGENTS-013`；只把原厂位置取得调用挂到有界占位入口，保留原厂首次调度、网络上下文、执行入口和重试 | 已安装，真实取包与用户总体实机确认已通过 | 95 项测试、反汇编挂接、缓冲区语义测试、独立模拟、两次构建、云端全量回读和 AP01 真实 `/a` 请求通过；2026-08-03 用户确认刷入后使用正常，未逐项点名的基座专项状态不由该句替代 |
| `FW-SIM-001` | `SPEC-DASHBOARD-029` 至 `SPEC-DASHBOARD-033`、`SPEC-ACCEPT-003` | 在 AGENTS 固件模块内建立刷前连续事件模拟门禁；逐步记录可见页面、原厂分派序号、AGENTS 状态、基座与功率数据状态、输入事件、实际分支和最终目标，任何无法证明最终页面的原厂继续路径都判失败 | 基座生命周期覆盖已实现 | `FW-AGENTS-010` 的 4 种端口组合、18 个定向场景、110 个轨迹步骤和 4,356 条穷举序列失败为 0；仍不替代真机 |
| `FW-INTEGRATION-004` | `SPEC-PAGE-001` 至 `SPEC-PAGE-007`、`SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-033`、`SPEC-FIRMWARE-006` 至 `SPEC-FIRMWARE-008` | 只在新 AGENTS 局部成品、后台同步隔离版与 `FW-PAGE-003` 四段成品分别通过后，从同一 `opt-setting.bin` 组合最终功能 | 阻塞 | `FW-AGENTS-009` 已否定；不允许直接修改或复用旧成品 |
| `FW-FINAL-001` | `SPEC-FIRMWARE-006` 至 `SPEC-FIRMWARE-008` | 只在四个功能候选分别通过后，由批准清单从同一原厂基线确定性组合完整成品 | 阻塞 | 页面开关、AGENTS 全交互、恢复门禁和原厂回刷全部通过 |

`FW-AGENTS-005` 的观测字段只用于专用测试成品，不进入发布固件、看板结果或普通用户日志。
观测载荷必须在异常、空间不足或导出失败时直接退出原厂局部分支，不得修改导航结果。

### 0.2 纠偏记录

| 日期 | 方案 | 结果与证据 | 后续 |
| --- | --- | --- | --- |
| 2026-07-26 | `FW-SETTING-001`：只修改一级设置列表首尾候选 | 首尾能到达，但视觉位置与选中状态不同步 | 先在 DESIGN 中改为直接同步位置、状态和样式，再形成 `FW-SETTING-002` |
| 2026-07-26 | `FW-SETTING-002`：直接首尾切换 | 真机出现同一物理格内反相抖动 | 先在 DESIGN 中增加同格方向过滤，再形成 `FW-SETTING-003` |
| 2026-07-26 | `FW-SETTING-003`：四个等长修改区间与同格方向过滤 | 阶段成品自动测试和真机首尾、连续同向、快速反转验收通过 | 保持不变，只做当前提交重建复核 |
| 2026-07-27 | `FW-PAGE-001`：一次组合入口、设置对象、保存、导航和看板 | 真机停留在启动动画，历史组合成品已否定 | 拆分最小候选 |
| 2026-07-27 | `FW-PAGE-002`：设置列表空挂接观察 | 自动安装与基础在线检查通过，只证明空挂接未立即破坏启动，不能证明完整页面开关 | 形成 `FW-PAGE-003` 四段验证顺序 |
| 2026-07-27 至 2026-07-29 | `FW-AGENTS-001`：新增独立对象或覆盖对象 | 先后出现确认键、重启、日历或功率路径回归失败，相关候选已否定 | 改为复用原厂对象和精确原厂调用链 |
| 2026-07-29 | `FW-AGENTS-002`：复用原厂萌宠对象并替换总分派或总回调 | 原厂其他一级页面交互出现回归，相关候选已否定 | 只修改四个原厂局部分支 |
| 2026-07-29 | `FW-AGENTS-003`：原厂四个局部分支 | 自动安装和主动取数通过；物理左旋出现与预期不符的页面序列，物理验收失败；现场证据不足以确定根因 | 停止继续操作，不直接交换左右分支 |
| 2026-07-30 | `FW-AGENTS-004`：按现象直接修正左右分支 | 设计审查认定会把猜测写入固件，未进入代码 | 改为 `FW-AGENTS-005` 先补观测证据 |
| 2026-07-30 | `FW-MATERIAL-000`：直接使用仓库跟踪的固件文件执行构建与真实材料测试 | 三个固件文件指纹与历史记录一致，但文件权限均为可写；67 项测试中 1 项失败、6 项报错均先被只读门禁截停，方案已否定 | 改为 `FW-MATERIAL-001`，先准备指纹一致的本机私有只读工作副本 |
| 2026-07-30 | `FW-MATERIAL-001`：统一生成只读工作副本 | 10 项定向测试通过；全套测试扩展到 72 项，原可写输入错误全部消失 | 保留该方案，继续验证 Windows |
| 2026-07-30 | `FW-ASSET-000`：直接在含用户未提交看板动图的原工作区构建 | 72 项中 1 项失败、2 项报错均为 `fallback-overview.gif` 固定指纹不匹配；门禁正确停止，方案已否定 | 改为 `FW-ASSET-001`，在当前提交的干净构建工作区测试和冻结 |
| 2026-07-30 | `FW-ASSET-001`：在当前提交的干净工作区重新渲染等待页 | 干净工作区仍得到同样三项失败；当前 Pillow 12.2.0 与现有三份字体生成的四张文件均不等于已批准指纹，已证明问题不是用户未提交文件 | 改为 `FW-ASSET-002`，冻结构建复用已批准资源，重新渲染转为独立审计 |
| 2026-07-30 | `FW-INTEGRATION-001`：用组合入口替换一级总回调 | 构建器拒绝组合；该入口会让功率、时间、日期、天气、设置和原厂详情事件再次经过自定义代码，与既有真机失败边界冲突 | 改为 `FW-INTEGRATION-002`，只挂接原厂回调内的实际切页调用 |
| 2026-07-30 | `FW-INTEGRATION-002`：实际切页调用局部过滤 | 新增完整组合集成测试后，首次被隔离门禁拒绝，原因为设置页自己的精确回调区间未加入允许范围；改为只接收页面设置修改器返回的四个精确区间后，单项集成测试和全项目 73 项测试通过；两次冻结逐字节一致，区间外变化为 0 | 保留为不可安装的完整离线组合候选，等待第 14.3 至 14.6 节真机证据 |
| 2026-07-30 | `FW-ASSET-002`：模块内冻结已批准等待页 | 9 项 AGENTS 固件定向测试和全套 72 项自动测试全部通过；四张资源长度、指纹、尺寸、帧数、循环和时长均由构建重读核对 | 保持为当前离线构建方案，继续完整集成候选 |
| 2026-07-30 | 原厂各页面旋钮调用链统一复查 | 一级窗口已定位分支与系统设置回调对数值 `19`、`20` 的方向解释不同；数字键值不能跨回调作为全局方向合同，链接进成品的旧导航实现也必须排除误用 | 先以《原厂各页面物理旋钮交互实现》为唯一依据修订 DESIGN；完成逐回调代码审计和纠偏前，`FW-INTEGRATION-002` 保持失效且禁止安装 |
| 2026-07-30 | `FW-INTEGRATION-003`：按最新文档重写优化固件 | 静态复查证明第八项只有“序号 6 单独创建一行、序号 7 复用原厂返回”的限定结构可用；随后已删除旧总键值包装、旧通用窗口导航和重复功率左键分支，三个萌宠局部分支与原厂实际切页过滤通过反汇编检查，全项目 73 项自动测试通过；提交 `01aed05` 的两次冻结构建逐字节一致，载荷 `41108` 字节、余量 `19826` 字节 | 固定候选指纹并保持不可安装；等待逐次授权后再进行真机验证，不从离线结果推断物理旋钮已经通过 |
| 2026-07-31 | `FW-INTEGRATION-003` 安装状态语义纠偏 | 原文同时写“不可安装”和“逐次授权后进行真机验证”，无法形成可执行顺序；构建清单的关闭状态应阻止普通安装、自动安装和发布，专用测试设备的一次人工实验验证仍由《固件制作规范》第 7.1 节单独授权 | 保持冻结成品和构建清单不变；仅下载成功后展示完整即时材料，取得一次安装确认后才下发 |
| 2026-07-31 | 当前重写候选仅下载校验 | 云端完整回读逐字节一致，设备下载到 100% 后程序报告尚未安装；随后设备运行计时从 `162527` 变为 `30`，证明发生了预期外重启；重启后超过候选 300 秒刷新周期仍没有访问 home02 服务，支持仍运行原内容，但不是运行身份的直接证据 | 停止正式安装；等待用户核对设备可见页面，证据只见对应真机案例 |
| 2026-07-31 | 当前重写候选开机动画回归 | 用户物理观察确认设备卡在开机动画；云端仍在线且更新状态为空闲，说明当前仍存在在线回刷窗口；仅下载没有保持“只下载、不切换运行内容”的边界 | `FW-INTEGRATION-003` 标为已否定并禁止再次安装；先回刷已验收 `opt-setting.bin`，恢复后再分解启动路径变更 |
| 2026-07-31 | 启动失败在线回退 | 已验收 `opt-setting.bin` 的只读副本和云端完整回读逐字节一致；设备接受回退、重新上线，运行计时重新开始并连续增长，更新状态为空闲且没有新错误 | 等待用户确认已经离开开机动画；确认前不把自动证据写成回退完成 |
| 2026-07-31 | `FW-TRANSPORT-001` 与完整组合复查 | 失败候选使用旧机器地址；下载状态在未测原厂栈余量时达到 540 字节；页面设置创建也与历史开机动画失败共同出现。设备没有导出崩溃栈，不能指定单一根因 | 改为 `FW-TRANSPORT-002` 小包头与单校验状态；先生成排除全部页面设置改写的 `FW-AGENTS-006` |
| 2026-07-31 | `FW-TRANSPORT-002` 与 `FW-AGENTS-006` 离线实现 | 第二版结果包头 64 字节；下载状态 136 字节；单函数最大静态栈 208 字节；完整组合入口已经在代码层拒绝；73 项全项目测试通过；两次独立构建逐字节一致 | 冻结最小候选，只允许完成上传全量回读后申请一次正式安装；不再执行仅下载 |
| 2026-07-31 | `FW-AGENTS-006` 首次安装 | 首个临时地址在下发设备命令前过期；重新上传同一冻结成品并完成全量回读后只下发一次安装。设备下载、安装、重启和重新上线通过，运行计时重新开始 | 自动刷机检查结束；立即提示用户肉眼确认画面，再分别测试看板通信和旋钮 |
| 2026-07-31 | `FW-AGENTS-006` 功率确认回归 | 用户在功率一级页按下旋钮后界面卡住；运行计时倒推证明现象前后包含一次整机重启；该成品没有访问 home02 数据服务，也没有取得复位原因或异常地址 | 否定 `FW-AGENTS-006`；先形成不改后台网络的 `FW-AGENTS-007`，不在无崩溃记录时指定唯一根因 |
| 2026-07-31 | `FW-AGENTS-007` 首次安装 | 云端对象完整回读与冻结成品逐字节一致；设备接受第一次安装任务，下载期间一次连接错误后进度继续增长；重复命令被忙碌状态拒绝，没有形成第二个任务；最终运行计时从 `3205` 秒重置为 `15` 秒并重新上线 | 自动刷机检查结束；立即由用户先验功率页确认，再验 AGENTS 一级与二级交互；证据只见 [`首次安装案例`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-007首次安装.md) |
| 2026-07-31 | `FW-AGENTS-007` 功率确认回归 | 用户在功率一级页按下旋钮后界面卡住；设备仍在线，运行计时为 `1108` 秒且没有重置。静态复查发现 AGENTS 概览到功率的自定义路径直接调用切页函数，绕过原厂萌宠右旋继续地址 `0xa00bc716` 及其内部状态处理 | 否定 `FW-AGENTS-007`；形成 `FW-AGENTS-008`，先恢复萌宠，再把事件交还原厂右旋继续地址；在真机通过前只把该偏差列为待验证原因 |
| 2026-07-31 | `FW-AGENTS-008` 离线实现 | 已删除 AGENTS 概览右旋的直接切页、结果复读和自定义退出；旧 `FW-AGENTS-007` 构建入口强制拒绝；74 项自动测试通过；提交 `fac7ccf` 的两次独立构建逐字节一致 | 冻结新文件名与指纹；云端全量回读一致后刷入，仍先验功率确认 |
| 2026-07-31 | `FW-AGENTS-008` 首次安装动作 | 云端对象全量回读逐字节一致；设备接受一次安装命令，但进度停留在 0 或空值，后续云端调用连续 10 次返回空值；局域网发现仍响应且时间值连续增长，证明没有重启 | 不宣布刷入成功、不重复下发；人工断电重启后先恢复只读状态核对，再重新上传和安装；证据只见 [`首次安装未完成`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-008首次安装未完成.md) |
| 2026-07-31 | `FW-AGENTS-008` 人工重启后恢复 | 用户确认已经人工重启；账号内恰好一台目标设备在线，云端返回更新空闲、进度 `101`、运行时间 `128` 秒，设备自报型号与版本均匹配 | 保持冻结成品不变，从上传对象全量回读重新执行一次安装；未取得安装、重启和重新上线证据前仍按 `FW-AGENTS-007` 处理 |
| 2026-07-31 | `FW-AGENTS-008` 重新安装 | 直连上传超过三分钟未返回后已终止，未向设备下发；复用当前 7897 代理后重新上传成功，云端全量回读 6,804,520 字节并与冻结成品逐字节一致；设备只接受一次安装，状态依次进入下载、安装中和已安装，进度达到 100 后运行时间从 777 秒重置为 8 秒并重新上线 | 安装证据成立；不做长时间等待，立即由用户肉眼确认已进入一级页面，再只测试功率确认；证据只见 [`重新安装案例`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-008重新安装.md) |
| 2026-08-01 | `FW-AGENTS-008` 启动画面验收 | 用户肉眼确认设备已经进入一级页面，默认显示时钟页，没有停留在开机动画 | 启动验收通过；下一步只进入功率一级页按下一次旋钮，确认是否进入功率详情；结果返回前不测试其他页面 |
| 2026-08-01 | `FW-AGENTS-008` 功率页查找失败 | 用户从一级时钟页旋转后无法找到原厂功率页。静态复核证明原厂显示初始化、功率对象创建和功率确认区间与阶段输入一致，且没有接入一级页面过滤；但当前成品继承作用于所有页面的底层方向过滤，并挂接萌宠左右分支，把 AGENTS 概览插入萌宠到功率的导航路径 | 否定当前真机方案并停止其他页面操作；“误隐藏”已被静态证据排除，跳页原因仍缺少真机事件记录，不能在同一格多事件和状态错位之间二选一；证据只见 [`功率页未找到`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-008功率页未找到.md) |
| 2026-08-01 | `FW-SIM-001` 刷前交互模拟门禁 | 既有 15 组局部分支状态检查只能证明返回动作名称，不能证明连续事件后的可见页面，也没有阻止 `restore-then-stock-resume` 这类最终目标未知的路径进入成品；新门禁已完成 16 种页面组合、38 个定向场景和深度 5 的 3,630 条穷举序列，80 项全项目测试通过 | 当前 `FW-AGENTS-008` 报告明确失败，构建入口在写出成品与清单前停止；完整结果只见 [`FW-AGENTS-008 刷前交互模拟报告`](../../artifacts/build/agents-interaction-simulation/FW-AGENTS-008.json)，取得完整模型或改成可证明路径后才能生成下一成品 |
| 2026-08-01 | `FW-AGENTS-009` 原厂收尾顺序纠偏 | 只读反汇编证明早期功率左旋抢占会跳过 `0xa00bc33a` 后的功率内部状态处理；`FW-AGENTS-008` 又在返回 `0xa00bc716` 前先重置动图，使原厂萌宠收尾面对的不再是事件开始时的画面状态 | 不改键值映射；不挂功率专用分支；概览离开时只关闭状态尾并交还 `0xa00bc716`；返回萌宠画面改到 `0xa00bc092` 的原厂收尾之后执行。直接证据只见 [`原厂功率与萌宠退出收尾静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-原厂功率与萌宠退出收尾静态定位.md) |
| 2026-08-01 | `FW-AGENTS-009` 离线实现与安装 | 87 项全项目测试、4 种端口组合、14 个定向场景、94 个轨迹步骤、3,630 条穷举序列和两次确定性构建全部通过；云端完整回读逐字节一致；一次安装后运行时间重置并重新上线 | 不做长时间等待；立即由用户肉眼验收屏幕、功率页确认和 AGENTS 双向导航；证据只见 [`FW-AGENTS-009 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-009安装.md) |
| 2026-08-01 | `FW-AGENTS-009` 物理功率页可达性 | 用户在一级导航中仍找不到功率页；随后只读核对显示设备仍在线、更新空闲、运行时间为 619 秒，未观测到重启；当前没有旋钮事件或分派序号导出 | 否定 `FW-AGENTS-009`，禁止再次安装；不在缺少事件证据时指定跳步、状态错位或方向过滤为根因；证据只见 [`FW-AGENTS-009 功率页仍未找到`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-009功率页仍未找到.md) |
| 2026-08-01 | `FW-AGENTS-009` 回退对照 | 已验收的 `opt-setting.bin` 通过 87 项自动测试和云端完整回读；回读对象与本机文件逐字节一致；一次安装后运行时间从 1,251 秒重置到 10 秒并重新上线 | 待用户只确认功率页是否恢复；取得对照结果前不构建下一 AGENTS 成品；证据只见 [`FW-AGENTS-009 回退到 opt-setting`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-009回退到opt-setting.md) |
| 2026-08-01 | 原厂固件功率页对照 | `opt-setting.bin` 上仍找不到功率页；重新查询小米云并下载官方 `1.0.2_0031`，官方 MD5、长度、文件头和恢复校验匹配；云端完整回读逐字节一致；安装后运行时间从 602 秒重置到 8 秒并重新上线 | 待用户只确认功率页是否在官方原厂固件中可达；证据只见 [`原厂固件功率页对照安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-原厂固件功率页对照安装.md) |
| 2026-08-01 | 原厂基座离线与功率确认生命周期复查 | 原厂无条件创建功率页对象，但端口数决定初始页和导航资格；离线累计超过五次失败后释放并清零功率数据，界面回调才离开功率页；功率确认创建详情时直接读取该指针而不判空 | 形成 `FW-AGENTS-010`：只在已筛选的功率确认局部入口增加端口数与数据指针保护；证据只见 [`基座离线功率页残留与确认路径静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-基座离线功率页残留与确认路径静态定位.md) |
| 2026-08-01 | `FW-AGENTS-010` 提交前实现验证 | 功率确认局部保护、基座与功率数据模拟、构建器拒绝门禁和新命令已实现；全项目 90 项测试通过；临时审计构建的 18 个定向场景、110 个轨迹步骤、4,356 条穷举序列失败为 0，反汇编中的两个原厂状态地址与四个出口一一对应 | 先提交并同步实现，再从该提交进行两次正式冻结构建；不使用提交前临时审计文件刷机 |
| 2026-08-01 | `FW-AGENTS-010` 正式冻结构建 | 提交 `9cef7f3` 已与远端一致；官方实时版本和唯一在线设备匹配；90 项测试通过；两次独立构建逐字节一致；冻结成品为 6,804,520 字节，SHA-256 为 `d3a97fddfa6c5eb7dd23972b991a6a004ee21a6b0033e014a5e4ae836fadf289`；独立刷前模拟再次通过 | 使用同一只读成品自动上传、全量回读并安装一次；证据只见 [`FW-AGENTS-010 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-010离线构建.md) |
| 2026-08-01 | `FW-AGENTS-010` 自动安装 | 云端对象完整回读 6,804,520 字节并与冻结成品逐字节一致；只下发一次安装；118 秒进入安装，122 秒显示已安装，125 秒运行时间从 3,829 秒重置为 8 秒并重新上线；即时核对在线、更新空闲 | 不做长时间观察；由用户按基座连接状态验收功率页与旋钮，再验 AGENTS 双向导航；证据只见 [`FW-AGENTS-010 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-010安装.md) |
| 2026-08-01 | `FW-AGENTS-010` 看板真实数据缺失 | 当前构建清单和 `local_ui_loader.c` 明确禁用文件与网络取包；home02 实时服务与四个数据源全部健康，但设备没有持续请求 | 形成 `FW-AGENTS-011`：保留现有页面与功率保护，只恢复低内存后台取包；复查证据只见 [`AGENTS 真实数据同步复用复查`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-AGENTS真实数据同步复用复查.md) |
| 2026-08-01 | `FW-AGENTS-011` 独立模拟清单识别失败 | 构建器内部的同一连续事件模拟通过，但独立刷前命令因清单类型允许列表没有 `agents-live-data-base-safe-firmware` 而停止，没有生成独立报告 | 先把新清单类型映射到继承自 `FW-AGENTS-010` 的交互合同并增加自动测试，再重新提交、重新构建并运行独立模拟；通过前不部署或刷机 |
| 2026-08-01 | `FW-AGENTS-011` 正式冻结构建与服务部署 | 修复提交 `330eb24` 已与远端一致；88 项测试通过；独立模拟的 18 个定向场景、110 个轨迹步骤、4,356 条穷举序列失败为 0；两次构建逐字节一致；冻结成品为 6,804,520 字节，SHA-256 为 `660842121da4464667a5253dedad50db92b2dfc9c761d053ba3a35c8f84aa7dd`；home02 服务删除共享配置后四个数据源和无查询字段取包通过 | 重新核对小米云与唯一设备，完成云端全量回读后自动安装一次；证据只见 [`FW-AGENTS-011 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-011离线构建.md) |
| 2026-08-01 | `FW-AGENTS-011` 安装后未取数 | 云端全量回读一致；一次安装于 130 秒完成重启上线；设备运行 369 秒后服务请求计数仍未增加，四个数据源始终健康 | 否定并禁止再次安装；形成 `FW-AGENTS-012`，把参考项目已成功的 `%s` 天气格式和先原厂后 AGENTS 的界面定时包装作为整体完整复用；证据只见 [`FW-AGENTS-011 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-011安装后未取数.md) |
| 2026-08-01 | `FW-AGENTS-012` 正式冻结构建 | 提交 `d8d463c` 已与远端一致；90 项测试通过；独立模拟的 18 个定向场景、110 个轨迹步骤、4,356 条穷举序列失败为 0；两个格式区均为 `%s`；界面定时包装先调用原厂入口；两次构建逐字节一致；成品 SHA-256 为 `e9ab51d573b17299a35c7817f0c91ece0d1fc5242c72890db531117c8c4a2e29` | 重新核对小米云和唯一设备，完成云端全量回读后自动安装一次；证据只见 [`FW-AGENTS-012 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-012离线构建.md) |
| 2026-08-01 | `FW-AGENTS-012` 安装后未取数 | 云端全量回读逐字节一致；一次安装在 128 秒完成重启上线；设备运行 328 秒仍没有请求。随后本地单播、地址解析和 AP01 到电脑的受控出站数据包均通过，排除当次局域网出站阻断；参考实现后台包装入口静态栈为 96 字节，当前实现为 208 字节 | 否定并禁止再次安装；形成 `FW-AGENTS-013`，只把 136 字节下载状态移出原厂天气任务栈；栈差异是待验证原因，不写成已证实根因。证据只见 [`FW-AGENTS-012 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-012安装后未取数.md) |
| 2026-08-01 | `FW-AGENTS-013` 正式冻结构建 | 提交 `71137f5` 已与远端一致；93 项测试通过；独立模拟 18 个场景、110 个步骤、4,356 条序列失败为 0；后台包装入口静态栈降至 64 字节；两次构建逐字节一致；成品 SHA-256 为 `e17f491f909ee4a77436b7cd83cc233f45056316d4cca546a7286995509e82e9` | 重新核对小米云和唯一设备，完成云端全量回读后自动安装一次；证据只见 [`FW-AGENTS-013 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-013离线构建.md) |
| 2026-08-01 | `FW-AGENTS-013` 安装后未取数 | 云端回读逐字节一致；一次安装在 126 秒完成重启上线；设备运行 307 秒仍没有请求，服务数据源健康；后台包装入口静态栈已由 208 字节降至 64 字节 | 否定并禁止再次安装；不再继续改四页解析器，执行第 10.24 节参考项目原版单页传输探针。证据只见 [`FW-AGENTS-013 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-013安装后未取数.md) |
| 2026-08-01 | `FW-TRANSPORT-PROBE-001` 正式冻结构建 | 参考项目本地与远端均为 `56b6408`；34 项测试通过；直接使用其原版制作与加载器；两次构建逐字节一致；成品 SHA-256 为 `5d08284aa733546e0eae8c0ee97b8796bcfb0b96ddef42c17142ca7d2c4e5bd6`；原版服务的单页动图读取通过 | 完成云端全量回读后自动安装一次，只观察来自 AP01 地址的真实请求；证据只见 [`参考传输探针离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-TRANSPORT-PROBE-001离线构建.md) |
| 2026-08-01 | `FW-TRANSPORT-PROBE-001` 安装后未请求 | 参考项目原版单页载荷完成云端全量回读、安装、重启和重新上线，但独立服务没有收到 AP01 请求；原厂反汇编证明位置编号失败后还要求运行超过 100 秒且城市或行政区字段非空，当前实时属性为空 | 否定四页解析和界面代码是请求未创建的原因；形成 `FW-AGENTS-014`，只解除局域网取包对天气位置的依赖 |
| 2026-08-01 | `FW-AGENTS-014` 正式冻结构建 | 提交 `b3a8968` 已与远端一致；95 项测试通过；占位入口空指针与十字节有界写入通过；独立模拟的 18 个场景、110 个步骤和 4,356 条序列失败为 0；两次构建逐字节一致；成品 SHA-256 为 `8827881cf094b846a4966f45985115d810676eefd34bb1659fd4fd9c399d6203` | 核对小米云唯一设备和更新空闲，完成云端全量回读后自动安装一次；证据只见 [`FW-AGENTS-014 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-014离线构建.md) |
| 2026-08-01 | `FW-AGENTS-014` 安装与真实取包 | 云端全量回读与本地成品逐字节一致；单次安装在 124 秒完成重启上线；服务请求数从 3 增加到 4，日志明确记录 `192.168.31.231 GET /a`，四个数据源全部正常 | 后台传输验收通过；保留当前成品，只进行屏幕显示、旋钮进出、三张详情和功率页物理验收；证据只见 [`FW-AGENTS-014 安装与真实取包`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-014安装与真实取包.md) |
| 2026-08-03 | `FW-PAGE-003-A` 只读挂接启动 | 用户确认 `FW-AGENTS-014` 刷入后使用正常，并要求继续制作一级页面开关 | 从已验收 `opt-setting.bin` 单独制作第八项入口候选；只实现本 DESIGN 第 14.5 节写明的普通行、返回行和设置回调边界，不组合 AGENTS、设置对象、保存或一级导航 |
| 2026-08-03 | `FW-PAGE-003-A` 代码与自动检查 | 新增普通行分发、最小设置回调、严格事件模拟和独立构建入口；页面开关模块 7 项测试与全项目 98 项测试全部通过 | 先提交并同步当前实现；只从干净提交执行两次完整固件构建并逐字节比较，结果一致后才进入自动安装 |
| 2026-08-03 | `FW-PAGE-003-A` 正式冻结构建 | 固定提交 `ea169dab06c405cfe151a4c52ed84b7368f4bd71` 已与远端一致；两次完整固件构建逐字节一致；成品 SHA-256 为 `456008470df3cad464a60bb7432013686ad33c0f1f4abb9697cdb1badb3f697e`；435 字节载荷剩余 60,499 字节；8 个定向场景和 1,944 条穷举序列失败为 0 | 核对小米云唯一设备和更新空闲，完成云端全量回读后自动安装一次；构建证据只见 [`FW-PAGE-003-A 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-003-A离线构建.md) |
| 2026-08-03 | `FW-PAGE-003-A` 自动安装 | 云端对象完整回读 6,804,520 字节并与冻结成品逐字节一致；只下发一次安装；125 秒进入已下载，128 秒进入已安装，140 秒运行时间从约 159,562 秒重置为 14 秒并重新上线；即时核对在线、更新空闲且没有更新错误 | 不做长时间观察；由用户立即确认正常一级页面、设置八项、新增行确认、返回行和原厂页面；证据只见 [`FW-PAGE-003-A 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-003-A安装.md) |
| 2026-08-03 | `FW-PAGE-003-A` 开机动画回归 | 用户肉眼确认设备仍卡在开机动画；云端重新上线不能替代屏幕启动验收，离线模拟也没有覆盖启动期对象创建 | 立即否定并禁止再次安装；先回刷已验收 `opt-setting.bin`，再按第 14.5.1 节的新边界拆分；证据只见 [`FW-PAGE-003-A 开机动画回归`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-003-A开机动画回归.md) |
| 2026-08-03 | 启动失败后的回刷窗口核对 | 账号内仍只有同一台 AP01，但连续四次云端查询均离线；本地设备协议单播和广播均无响应，在线回刷无法送达；尚未上传或下发回刷 | 停止盲目操作；用户只手动重启一次，确认后重新查询；只有在线且更新空闲时才从完整回读开始自动回刷 `opt-setting.bin` |
| 2026-08-03 | 用户重启后的 `opt-setting.bin` 回刷 | 用户重启后设备在线、更新空闲；回退对象全量回读逐字节一致；设备接受唯一一次回刷，但进度始终为 0，轮询期间一度离线，随后应用恢复响应且运行时间为 398 秒，仍无下载或安装进展 | 不宣布回刷成功，不重复下发或重启；先取得用户眼前屏幕现象，并等待同一任务变为空闲、推进或返回明确错误 |
| 2026-08-03 | `opt-setting.bin` 延迟回退恢复 | 用户确认已经进入正常一级页面；更新最终空闲且无错误；两次运行时间样本从 1,251 增至 1,262 秒，反推启动时间均为 05:02:59，证明回刷任务接受后又发生一次新启动 | 判定回退恢复完成；`FW-PAGE-003-A` 继续禁止安装；不得直接进入第二段，先为下一最小启动实验更新 DESIGN |
| 2026-08-03 | `FW-PAGE-003-A` 启动修改分层复查 | 重新反汇编确认分支处 `x15` 确为循环序号低八位、`x9` 确为普通行父列表，排除两类简单寄存器错误；没有崩溃栈，四类启动修改仍不能指定单一根因 | 形成 `FW-PAGE-004-A → B → C`；先只实现无用户行为变化的启动基础透传，前一阶段真机启动通过前不进入后一阶段 |
| 2026-08-03 | `FW-PAGE-004-A` 正式冻结构建 | 提交 `f4c5b6016525f9107dfc8b4910afcc0d7d234e49` 已与远端一致；专用 4 项、全项目 102 项、安装策略 6 项测试通过；28 字节无调用载荷只含两个原厂继续地址；六个序号等价；两次完整构建逐字节一致；成品 SHA-256 为 `8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b` | 完成云端全量回读后自动安装一次；只验首次启动，不进入八项创建；证据只见 [`FW-PAGE-004-A 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-A离线构建.md) |
| 2026-08-03 | `FW-PAGE-004-A` 自动安装 | 云端全量回读 6,804,520 字节并与冻结成品逐字节一致；只下发一次安装；122 秒进入已下载，126 秒进入已安装，129 秒运行时间从 3,043 秒重置为 8 秒并重新上线；即时在线、更新空闲且没有更新错误 | 不做长时间观察；只由用户立即确认是否进入正常一级页面；证据只见 [`FW-PAGE-004-A 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-A安装.md) |
| 2026-08-03 | `FW-PAGE-004-A` 首次启动物理验收 | 用户确认已经进入一级页面；启动基础透传没有再次造成开机动画回归 | A 阶段改为已验证；进入第 14.5.3 节 B 阶段，只增加第八项普通行，不替换设置回调 |
| 2026-08-03 | `FW-PAGE-004-B` 正式冻结构建 | 提交 `d9ab9238d282308227fd51feeb052338e6abf0d2` 已与远端一致；专用 4 项、全项目 106 项、安装策略 6 项测试通过；91 字节载荷只调用一次原厂普通行入口；八项创建序号失败为 0；两次完整构建逐字节一致；成品 SHA-256 为 `256eaebe72d218788498b54f3e1442f6ef263c9f81293601087c454f7deebbbd` | 完成云端全量回读后自动安装一次；只验启动、八项列表和新增行可选中；证据只见 [`FW-PAGE-004-B 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-B离线构建.md) |
| 2026-08-03 | `FW-PAGE-004-B` 自动安装 | 云端全量回读 6,804,520 字节并与冻结成品逐字节一致；只下发一次安装；125 秒进入已下载，130 秒进入已安装，154 秒运行时间重置并重新上线；即时在线、更新空闲且没有更新错误 | 不做长时间观察；由用户立即确认启动、八项列表和新增行可选中；证据只见 [`FW-PAGE-004-B 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-B安装.md) |
| 2026-08-03 | `FW-PAGE-004-B` 开机动画回归 | 用户确认卡在开机动画并已断电重启；A 阶段已物理通过，B 相对 A 只新增八次循环、普通行载荷和恢复校验；没有崩溃栈，不能指定单一根因 | B 阶段立即否定并禁止再次安装；回刷 A 阶段；形成第 14.5.4 节单迭代追加方案，删除第八次循环 |
| 2026-08-03 | `FW-PAGE-005-A` 单迭代追加离线构建 | 保持原厂七次循环；91 字节载荷在序号 6 依次调用普通行和返回行入口；专用 4 项、全项目 110 项、刷机定向 8 项测试及两次独立完整构建通过；成品 SHA-256 为 `2714e4b195840ab3d8a6bacdb5be07867028f874f4bd3c9b988982c955618264` | 按刷机门禁自动单次安装；只验启动、八项列表和新增行可选中；证据只见 [`FW-PAGE-005-A 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-005-A离线构建.md) |
| 2026-08-03 | `FW-PAGE-005-A` 自动安装 | 云端完整回读与冻结成品逐字节一致；只下发一次；119 秒上报已下载，125 秒运行时间从 1010 重置到 9，随后在线、更新空闲且没有本次更新错误 | 不重复下发；立即由用户确认启动、八项列表和新增行可选中；安装工具对本次进度 50 后直接重启产生误报，证据与纠偏只见 [`FW-PAGE-005-A 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-005-A安装.md) |
| 2026-08-03 | `FW-PAGE-005-A` 开机动画回归 | 用户确认仍卡开机动画；该版已经保持原厂七次循环，和 B 阶段失败版唯一共同的新运行行为是开机调用 `0xa0192fb4` 创建新增普通行 | 否定并禁止再次安装；回刷 A 阶段；改为第 14.5.5 节双调用者延迟创建方案，证据只见 [`双调用者定位`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-005-A开机动画回归与双调用者定位.md) |
| 2026-08-03 | `FW-PAGE-006-A` 双调用者延迟创建离线构建 | 从栈偏移 `+124` 区分两个原厂调用者；开机调用七项、设置进入调用八项与未知调用者原厂回退模拟通过；专用 4 项、全项目 114 项、两次完整构建通过；刷机工具状态门修正后定向 9 项与全项目 35 项通过；成品 SHA-256 为 `d9b67a6d1fb94b387051e1fb979f8509faf6e53b0d08655b998f32f22d1c04fd` | 自动单次安装；先验启动，再进设置验八项；证据只见 [`FW-PAGE-006-A 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-006-A离线构建.md) |
| 2026-08-03 | `FW-PAGE-006-A` 自动安装 | 云端完整回读逐字节一致；只下发一次；117 秒已下载，121 秒已安装，124 秒运行时间重置并重新上线；更新空闲且没有本次更新错误 | 立即由用户先确认启动，再进入设置确认八项；不按下新增行；证据只见 [`FW-PAGE-006-A 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-006-A安装.md) |

## 1. 设计目标与硬边界

本设计实现三项设备端能力：

1. 六个可开关一级页面及其持久化；
2. AGENTS 看板概览、三个详情页和后台刷新；
3. 系统设置菜单旋钮首尾循环。

本设计全部功能必须遵守 [`design.md`](../design.md) 第 2.2 节的原厂复用先行门禁。每项
当前有效方案必须先完成原厂逻辑调查，并在对应章节精确引用
`knowledge/AP01-官方固件分析/` 下的事实文档；引用和复用边界未写入本设计前，不得修改
代码。原厂逻辑不适用或实测失败时，先更新第 0.2 节纠偏记录和下一方案，再进入代码纠偏。

原厂页面、米家通信、官方在线更新、恢复记录、启动槽选择和设备基础能力必须保留。

固件制作全过程必须遵守
[`固件制作规范`](../固件制作规范.md)。当前没有已验证的离线救砖通道，且尚未完成同一设备
原厂回刷，因此通用安装与发布门禁仍未通过。2026-07-28 用户对精确指纹绑定的四页同步实验
成品作出一次性安装授权；该次安装结果不替代恢复证据，也不授权其他成品或后续安装。

恢复层级、产物类型和现有保护的能力边界只见
[`AP01 离线恢复与救砖证据`](../../knowledge/AP01-官方固件分析/离线恢复与救砖证据.md)，
恢复操作顺序只见
[`AP01 分级恢复与救砖操作规程`](../SOP/AP01-分级恢复与救砖.md)。本设计不把芯片家族或
开发板资料转写成 AP01 接线、分区地址或恢复命令。

## 2. 已确认事实

下列事实的定义、证据位置和复查方法以
[`AP01 官方固件分析`](../../knowledge/AP01-官方固件分析/AP01-官方固件分析.md) 为唯一原文：

- 目标设备型号为 `njcuk.enstor.ap01`，原厂版本为 `1.0.2_0031`；
- 原厂文件为 6,804,520 字节，文件头为 `BFNP`；
- 主程序为 32 位 RISC-V 指令；
- 设备使用 NuttX、LVGL、libuv 和 mbedTLS；
- 原厂程序存在六个一级页面创建函数；
- `/data/sysconfig.bin` 是原厂设置文件，但没有一级页面显隐字段；
- `/data` 可用于原厂持久化设置，设备还存在临时文件系统；
- 原厂固件区分当前槽和更新槽，并包含在线更新与恢复代码；
- 原厂 `BFNP` 文件属于应用更新包，不是已经证明可整片写回的全闪存备份；
- 原厂文件内未检出博流分区表标记，文件头安全字段也不能证明芯片的一次性配置状态；
- 博流芯片家族具有串口和调试器读写能力，但没有找到 AP01 主板的对应焊盘和接线证据；
- 参考项目只真机验证了一个复用原厂虚拟形象页的实时画面加载器；
- 参考项目没有实现本 SPEC 所需的七页导航、页面开关、四页原子结果和原厂回刷；
- 当前没有 AP01 专属离线恢复成功案例。

当前基线文件指纹和文件尾恢复校验值以
[`固件制作规范`](../固件制作规范.md) 第 4 节为唯一记录，本文不重复维护。

## 3. 总体实现结构

本项目应用架构的完整规则只见 [`Architecture.md`](../Architecture.md)。设备端优化固件由
构建工具把经过验证的最小修改和新增载荷写入原厂基线，服务端生成看板完整结果，设备按
优先顺序主动取得。

```mermaid
flowchart LR
    A["已校验原厂固件"] --> B["版本专用分析结果"]
    B --> C["设备端载荷与资源"]
    C --> D["带旧字节断言的修改器"]
    D --> E["优化固件与构建清单"]
    E --> F["离线验证"]
    F --> G["同机 R0 回刷与独立恢复演练门禁"]
    G --> H["严格模拟通过与上传对象完整回读"]
    H --> I["自动单次真机安装与验收"]
    I --> J["同一设备原厂回刷验收"]
```

设备端职责拆分为：

| 模块 | 单一职责 |
| --- | --- |
| 一级导航候选与过滤 | 在原厂既有页面与 AGENTS 虚拟状态之间计算下一目标，不创建页面 |
| 页面开关设置 | 展示六个复选项，保存成功后更新实际状态 |
| 旋钮事件路由 | 区分唤醒、一级导航、详情导航和设置操作 |
| 设置菜单循环 | 根据当次有效设置项计算前后项 |
| 看板页面 | 显示概览和三个详情，处理详情循环与返回 |
| 结果刷新 | 按服务端优先顺序取得一份完整结果 |
| 结果提交 | 完整校验后一次切换四页结果 |
| 持久化 | 保存页面开关和最后一次成功结果 |

服务端职责拆分为：

| 模块 | 单一职责 |
| --- | --- |
| 数据读取 | 从当前用户已授权环境读取可确认数据 |
| 统计计算 | 生成北京时间范围内的固定字段 |
| 页面渲染 | 生成四个 320×240 成品页面 |
| 完整结果封装 | 把同一轮四页结果组成不可拆分的数据包 |
| 局域网取包 | 在固定地址直接提供不含账号原文的聚合四页包 |
| 多位置部署 | 在主电脑、NAS、云服务器提供相同协议 |

### 3.1 制作工具首批模块

首批实现只建立离线校验与制作门禁，不声明尚未确认的修改地址：

| 模块路径 | 单一职责 |
| --- | --- |
| `core/firmware_image/` | 校验原厂身份、文件尾恢复记录、完整文件指纹和修改区间 |
| `features/offline_firmware_build/` | 执行带旧字节断言的修改计划，生成成品与构建清单，并按阶段门禁拒绝越权产物 |
| `features/settings_menu_wrap/` | 复现并校验当前版本系统设置菜单首尾循环的 4 个等长修改区间，生成待批准清单和批准范围绑定清单 |
| `features/optimized_firmware_build/` | 校验已验收设置菜单阶段成品的精确身份及其相对原厂基线的批准差异，为完整优化固件生成制作就绪报告 |
| `features/primary_page_navigation/` | 自动复核当前版本一级窗口创建、实际注册回调、交互分派、左右切换调用链和禁止调用 |
| `core/firmware_payload_space/` | 无损优化固定原厂动图，逐帧验证画面与时序等价，并生成确定性的载荷空间 |
| `app/ap01_firmware.py` | 接收命令参数并组合离线制作功能，不承载固件处理规则 |

本阶段没有经过证据文档批准的修改区间时，只允许输出原厂基线检查报告；不得把未修改副本
命名为优化固件或标记为离线构建完成。

用户批准后，版本控制内的批准记录必须同时绑定目标型号、原厂版本、原厂完整文件指纹、证据
路径、修改区间数量、全部修改前后字节、代码间隙和日志保持范围。制作入口重新从真实原厂
基线生成修改内容并计算批准范围指纹；任一字段不一致时继续保持待批准状态。命令行不得提供
直接把任意清单改成已批准状态的参数。

## 4. 固件修改策略

### 4.1 基线与输出

设置菜单阶段构建器只接受第 2 节精确原厂版本。完整优化固件构建器同时接受该原厂基线和
下列已验收的设置菜单阶段成品；两者任一身份或相对差异不匹配都必须停止：

| 项目 | 固定值 |
| --- | --- |
| 输入文件名 | `opt-setting.bin` |
| 文件字节数 | 6,804,520 |
| MD5 | `13a7286f4824b1ad87d9bc32f1d3d39c` |
| SHA-256 | `348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` |
| 文件尾恢复校验值 | `0xe57875a7` |

完整优化固件制作必须先证明该输入相对原厂基线只包含已批准的设置菜单四个修改区间和文件尾
恢复校验值变化，再以它作为后续新增功能的字节输入。完整功能全部实现并通过分阶段验证后，
最终组合成品固定命名为 `ap01-1.0.2_0031-opt.bin`。当前该名称被第 10.4 节历史失败成品
占用；最终构建前必须按 [`固件制作规范`](../固件制作规范.md) 第 6 节先完成失败成品改名和
引用核对，构建器不得覆盖现有文件。两种成品都不得覆盖原厂文件。

当前设置菜单阶段制作入口只接受第 6.1 节已经批准的四个精确修改区间。它不包含参考项目
实时画面加载器，也不代表第 5、7、8、9 节已经进入固件实现。

完整优化固件在新增修改区间尚未完成直接证据、DESIGN 记录和批准前，只允许生成制作就绪
报告，不得生成 `ap01-1.0.2_0031-opt.bin`。

构建器必须：

1. 从仓库外只读路径打开原厂文件，禁止输入路径与输出路径相同；
2. 校验型号、版本、总字节数和两个完整文件指纹；
3. 校验文件头、主程序起点、文件尾恢复记录和安全字段均与当前证据基线一致；
4. 校验每个修改位置的原字节；
5. 编译并检查新增载荷边界；
6. 只写入知识证据和本 DESIGN 已批准的事件、刷新挂接点，并证明页面注册点保持原字节；
7. 写入已由对应 DESIGN 固定的局域网取包地址；
8. 保持容器总长度、文件头安全字段和恢复记录格式；
9. 重算并复核文件尾恢复校验；
10. 生成全部修改区间、修改前后字节和修改区间外逐字节一致报告；
11. 对冻结成品重新计算两个完整文件指纹，并以只读方式保存。

当前尚未形成的地址、调用约定和空闲空间不得写入本文。它们必须先形成带反汇编位置、
原字节和调用关系的分析记录，随后再进入本节和构建器。

### 4.2 禁止触碰

本固件不修改：

- 启动引导和分区表；
- 当前槽、更新槽与恢复选择；
- 无线网络初始化；
- 米家通信；
- 官方在线更新、下载和安装；
- 原厂恢复记录格式；
- 看门狗启动逻辑；
- 第二阶段启动程序、两份分区表、校准、设备身份和密钥数据；
- 一次性配置、防降级版本、安全启动设置和调试密码。

如果后续实现发现必须修改上述任一范围，本设计立即停止，先更新
[`固件制作规范`](../固件制作规范.md) 的恢复门禁并取得新的明确授权。

构建器不得输出整片擦除、地址零写入、分区表写入或一次性配置写入任务。原厂 `BFNP` 文件
只能进入已经验证的在线更新路径，不得被转换成全闪存写入任务。

### 4.3 安装器拒绝条件

安装器默认只允许离线检查。下列记录全部存在且匹配后，才显示正式安装入口：

1. 唯一目标设备、型号、当前版本和在线状态；
2. 原厂输入和优化成品的完整字节数与两个文件指纹；
3. 构建清单、修改范围、离线测试和文件尾恢复校验；
4. 上传对象被完整下载回本机后，与冻结成品逐字节一致；
5. 同一测试设备的 R0 原厂回刷案例；
6. 同一测试设备至少一条独立恢复演练案例；
7. 当前槽、更新槽和本次写入目标不会覆盖当前运行槽的 AP01 直接证据；
8. 匹配原厂应用更新包、同机全闪存备份和冻结恢复记录均可用；
9. 与本次唯一设备和唯一成品指纹绑定的严格刷前交互模拟报告通过。

用户发起“制作并刷入”流程时已经完成本次执行授权。正式安装要求第 1～9 项再次成立；全部
成立后自动上传、完整回读并下发一次正式安装，不再询问用户。第 5～8 项客观上
无法取得时，专用测试设备只允许使用
[`固件制作规范`](../固件制作规范.md) 第 7.1 节定义的持续实验授权；第 1～4、9 项及该节
保留的完整性检查不得跳过。不能只检查文件开头四字节；本机成品、上传对象和正式安装必须
使用同一份逐字节相同的文件。`--download-only` 已被真机证明会切换运行内容，安装器必须拒绝。

如果现有设备接口不能取得当前槽、更新槽和目标槽的直接证据，第 8 项判定失败。芯片家族
通用更新源码只能作为调查线索，不能替代 AP01 真机结果。

### 4.4 新增逻辑的失败隔离

新增入口必须挂在原厂页面、联网和官方更新任务已经完成初始化之后。准确挂接点必须先由
当前版本调用关系证明，不能按函数名猜测。

每个新增模块必须满足：

1. 未取得合法持久化记录时使用固件内安全默认值；
2. 网络、文件、图片或授权校验失败时继续显示最后成功结果或原厂页面；
3. 新增任务退出、超时或分配失败时不阻塞原厂界面、米家通信和更新任务；
4. 所有缓冲区有固定上限，写入前检查长度，释放路径覆盖全部失败分支；
5. 新增状态只能写入已经验证的独立文件，不扩展原厂设置结构；
6. 新功能连续失败时停止自身刷新，不触发设备复位或修改启动状态；
7. 原厂更新开始时停止新增网络请求和持久化写入，释放相关资源。

这些隔离只能降低新增逻辑间接影响原厂路径的概率，不能替代独立恢复演练。

## 5. 一级页面与开关实现

### 5.1 固定页面表

一级导航候选表按原厂真实对象顺序固定如下：

| 导航位置 | 页面 | 原厂交互分派序号 | 是否允许关闭 |
| ---: | --- | ---: | --- |
| 0 | 功率 | 0 | 是 |
| 1 | 时钟 | 3 | 是 |
| 2 | 日历 | 4 | 是 |
| 3 | 天气 | 5 | 是 |
| 4 | 设置 | 6 | 否 |
| 5 | 萌宠 | 7 | 是 |
| 6 | AGENTS 看板 | 虚拟状态 | 是 |

序号 1、2 的 C1、C2 条件页和序号 8 的米家详情不进入自定义一级导航候选表。每项保存页面
标识、原厂交互分派序号或 AGENTS 虚拟标识，以及对应开关位；设置页的启用值在运行时固定为
真，不从持久化记录读取。

设备只创建原厂既有对象。AGENTS 不创建页面对象，而是按第 6.1、7.1.1 节复用原厂萌宠
动图对象。关闭状态只从一级导航候选表中排除对应位置，不改变原厂初始化、对象生命周期、
直接子对象数量或米家详情序号。向任一方向寻找下一页时最多检查七个导航位置；达到七项仍
未命中即选择设置页，防止异常数据造成死循环。

第 10.11 节的原厂精确调用链观察成品尚不实现页面开关，七个导航位置全部视为启用，以便
逐一复测六个原厂页面的确认、详情和返回。只有第 5.4 节设置实现门禁解除后，最终组合成品
才读取第 5.2、5.3 节状态并排除关闭位置。

### 5.2 页面开关状态

六个可开关页面使用一个 6 位状态值。位序与
[`SPEC-PAGE-002`](../SPEC.md#spec-primary-page-switches) 的复选项顺序完全一致。

首次没有有效记录时使用：

| 页面 | 值 |
| --- | --- |
| 时钟 | 开 |
| 功率 | 开 |
| 天气 | 关 |
| 日历 | 关 |
| 萌宠 | 关 |
| AGENTS 看板 | 开 |

恢复出厂设置时删除优化固件自己的有效记录，再按上表重新建立。不得扩大或改写原厂
`/data/sysconfig.bin` 的现有结构。

### 5.3 保存事务

页面开关使用 `/data/.ap01-page0` 与 `/data/.ap01-page1` 两份 16 字节独立小记录轮换
保存。每份记录依次包含固定标记、递增序号、6 位状态值和异或校验值。启动或导航前读取两份
记录，选择校验有效且序号较新的记录；两份都无效时使用第 5.2 节默认值。

切换复选项时：

1. 在内存中生成候选状态；
2. 选择非当前有效记录对应的文件并以固定长度覆盖；
3. 关闭后重新打开、完整回读并校验；
4. 校验成功后更新当前界面；
5. 任一步失败即保留旧状态并恢复界面。

写入时始终保留另一份有效记录，单次写入中断不会同时破坏两份记录。设备端文件系统剩余空间
和反复断电结果仍需真机验收；验收前构建清单必须把持久化标为待验证。

### 5.4 设置菜单与开关页实现门禁

原厂设置列表创建入口、循环边界和分支位置的现有静态证据只见
[`一级页面开关挂接静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-28-一级页面开关挂接静态定位.md)；
空挂接真机边界只见
[`设置列表空挂接首次安装`](../../knowledge/AP01-官方固件分析/cases/2026-07-28-设置列表空挂接首次安装.md)。
空挂接只证明原分支可以安全进入并返回，没有证明原厂创建循环、局部数组、状态编号、事件
回调或对象生命周期可以容纳第八项。曾经同时增加循环数量、设置行和覆盖页的组合成品卡在
开机动画，因此当前阶段必须保持原厂项目数 7、原厂“返回”分支、设置键值回调和对象创建
路径不变。

第八项的局部表、行对象所有权、选择状态、原厂返回和临时开关页生命周期证据只见
[`设置列表第八项容量与生命周期静态复查`](../../knowledge/AP01-官方固件分析/cases/2026-07-30-设置列表第八项容量与生命周期静态复查.md)。
实现只允许该记录第 6 节限定的四处挂接与一个新增普通行；序号 0～5 必须走原厂路径，序号
6、7 不得读取原厂六项标签表，返回继续调用原厂返回入口。

自动测试必须反汇编证明上述限定结构、设置回调未处理事件原参数尾跳原厂、一级窗口九个对象
不变。该静态证据不替代真机，八项列表滚动、左右边界、确认、开关页返回、反复进入退出、
设置页删除和全部原厂设置项目仍必须逐项验收。

### 5.5 当前版本导航挂接与页面注册保护

当前版本的页面、导航和萌宠控件直接证据只见
[`一级页面注册与导航静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-28-一级页面注册与导航静态定位.md)
与
[`AGENTS 原厂萌宠控件复用静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-AGENTS原厂萌宠控件复用静态定位.md)。

原厂页面注册调用、运行地址 `0xa00b2732`、文件偏移 `0x0b3732` 以及页面跳板必须保持
`opt-setting.bin` 原字节，不再挂接页面注册载荷。AGENTS 不创建一级对象、萌宠子对象、
图片对象或事件对象；原厂对象树、九个一级直接子对象和米家详情序号全部保持不变。

完整离线组合候选只挂接设置键值包装、原厂键值回调内的实际切页调用、AGENTS 三个萌宠局部分支
和后台同步回调，并按第 6.1 节扩大原厂萌宠状态的单次申请长度。实际切页调用位于运行地址
`0xa00bc092`、文件偏移 `0x0bd092`，原字节为 `ef30d01b`；挂接入口收到原厂已经计算好的
容器、目标序号和方向，只允许按已保存的 6 位状态值继续寻找下一个启用位置，最终仍调用
原厂实际切页入口 `0xa00bfa4e`。方向不是原厂左、右值，或者目标是 C1、C2 条件页时，必须
原参数调用原厂入口。

原厂一级顺序在页面过滤中固定映射为功率、时间、日历、天气、设置、萌宠所在位置；设置始终
保留。萌宠与 AGENTS 共用原厂序号 7：向右进入该序号时先选萌宠再选 AGENTS，向左进入时
先选 AGENTS 再选萌宠；两项都关闭时继续寻找。过滤最多检查六个原厂位置，失败时原参数调用
原厂入口，不形成无限循环。

构建器必须证明页面注册调用和旧跳板没有修改。AGENTS 局部分支及其调用路径中不得出现
`lv_obj_create`、`lv_gif_create`、`malloc` 或 `free` 调用；这些入口只允许由第 5.4 节
页面开关设置页的独立创建与销毁路径使用。AGENTS 键值包装不得写入原厂萌宠状态偏移
`0`、`4`、`8`、`12`。

## 6. 旋钮事件与导航

本 DESIGN 必须遵守
[`AP01 原厂各页面物理旋钮交互实现`](../../knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md)。
该文档是旋钮输入、键值边界、实际注册回调、页面分派、原厂状态字段和调用入口的唯一事实
原文。后续任何旋钮方案都不得绕过、弱化或用本 DESIGN 重新定义其中的事实。

实现时必须从当前对象的实际事件注册位置进入实际回调，分别解释该回调内的键值分支。一级
窗口、系统设置和原厂详情不得共用一套全局左右键值常量。一级窗口只能复用其实际调用的
`0xa00be388`、`0xa00be3ca`、`0xa00bfa4e` 及原方向参数；不得替换为固件中的相似窗口
入口。实测否定方案时，先更新本 DESIGN 的纠偏记录和下一方案，再修改代码。

各实际回调内的新增局部处理分别按以下优先级判断；本顺序不授权建立跨页面总回调：

1. 屏幕处于熄屏或屏幕保护状态：消费本次有效事件，只执行原厂唤醒和计时重置；
2. 页面开关设置：左右移动复选项，完整按下与松开后切换；
3. 系统设置菜单：左右按当次有效项目首尾循环；
4. AGENTS 看板详情：左右在三个详情间循环，完整按下与松开后返回概览；
5. AGENTS 看板概览：完整按下与松开后进入周剩余额度详情；
6. 一级页面：左右在当前已启用页面间循环；
7. 其他原厂页面：交还原厂处理。

每个物理有效格只产生一次页面移动。确认操作只在同一轮按下、松开配对完整后执行，避免
按下阶段和松开阶段各执行一次。

### 6.1 AGENTS 看板局部分支挂接

一级窗口在运行地址 `0xa00b2838` 和 `0xa00b2842` 组合装入原厂键值回调
`0xa00bbfee`。真机已经证明，替换这两条地址装入指令后，即使功率页确认键在调整栈、调用
函数或写内存前直接尾跳原厂回调，功率页仍会出现界面线程卡死。因此完整优化固件必须保持
这两处字节与 `opt-setting.bin` 一致；不得替换一级窗口总键值回调，也不得用任何跳板让
时间、日期、天气、设置或原厂内部详情的确认事件经过 AGENTS 代码。功率确认只允许使用
第 10.20 节的局部连接保护；该保护位于原厂已经完成确认键和功率序号筛选之后，不得扩展为
总回调包装。

原厂萌宠没有旋转或确认专用回调。`FW-INTEGRATION-003` 只在原厂总回调已经按键值和交互
分派序号完成筛选后，挂接以下三个萌宠局部分支目标：

| 组合 | 原厂目标 | 文件偏移 | 原厂继续地址 | 载荷行为 |
| --- | --- | --- | --- | --- |
| 左键、萌宠序号 7 | `0xa00bc460` | `0x0bd460` | `0xa00bc464` | AGENTS 返回萌宠或切换详情；关闭时恢复原厂 |
| 右键、萌宠序号 7 | `0xa00bc712` | `0x0bd712` | `0xa00bc716` | 萌宠进入 AGENTS、AGENTS 进入功率或切换详情 |
| 确认键、萌宠序号 7 | `0xa00bda64` | `0x0bea64` | `0xa00bda68` | 概览与详情互相切换；关闭时恢复原厂 |

三个原厂目标只把 `a0`、`a1` 准备好后继续执行。载荷未消费事件时必须复现被替换的参数
准备指令并跳到对应原厂继续地址；消费事件时直接进入原厂回调公共退出地址
`0xa00bc1d2`。原厂入口、条件位置、目标字节和跳板空间只见
[`原厂键值回调局部分支静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-原厂键值回调局部分支静态定位.md)。

功率左键保持原厂字节，不再设置重复局部分支。原厂算出目标并汇合到实际切页调用后，由
`0xa00bc092` 的页面过滤入口按方向 `2` 选择萌宠或 AGENTS，再调用 `0xa00bfa4e`。这样同一
物理事件只存在一个自定义决策点。

优化载荷不得再链接未挂接的一级总键值包装，也不得链接调用 `0xa00b0290`、
`0xa00b0570`、`0xa00b06f4` 的旧通用窗口导航。构建器必须在最终反汇编中证明这些符号和
调用地址都不存在。

原厂对象序号、交互状态和切换调用链只见本节开头引用的唯一事实原文。产品交互仍按第 5.1 节
候选表执行；局部入口必须沿一级窗口回调已经判定的左键树、右键树和确认键树实现：

| 当前状态 | 一级窗口左键树 | 一级窗口右键树 | 确认键树 |
| --- | --- | --- | --- |
| 概览 | 前一个启用的一级位置 | 后一个启用的一级位置 | 显示周剩余额度详情 |
| 周剩余额度 | 切到近 30 天详情 | 切到今日消耗详情 | 返回概览 |
| 今日消耗 | 切到周剩余额度详情 | 切到近 30 天详情 | 返回概览 |
| 近 30 天消耗 | 切到今日消耗详情 | 切到周剩余额度详情 | 返回概览 |

该表不把数值 `19`、`20` 定义为全局物理方向。载荷只能从已证明的四个一级窗口局部分支进入，
不得用系统设置回调的数字方向解释重新分派。

AGENTS 局部入口从一级窗口序号 7 取得萌宠根对象，读取其偏移 `16` 的原厂包装数据，再读取包装数据
偏移 `4` 的原厂萌宠状态。原厂包装类型必须为 `10`，状态偏移 `0` 的原厂动图对象必须有效；
任一条件不符时立即交回原厂。

原厂萌宠状态申请长度在 `0xa00b402c` 从 16 字节改为 20 字节，原字节 `41 45` 改为
`51 45`。原厂创建和删除调用、原厂四个字段以及所有界面对象保持不变；新增且四字节对齐的
状态偏移 `16` 专用于 AGENTS。该扩展的直接证据和能力边界只见
[`AGENTS 原厂萌宠控件复用静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-AGENTS原厂萌宠控件复用静态定位.md)
第 5 节。

偏移 `16` 使用一次对齐 32 位读写的状态字：高 16 位固定为 `0xa501`，第 15～8 位保存界面
状态，低 8 位保存界面状态与 `0xff` 的异或结果。固定标记、互补值或状态范围任一不符时按
关闭处理。界面状态只允许以下值：

| 值 | 含义 |
| ---: | --- |
| 0 | AGENTS 关闭 |
| 1 | 概览 |
| 2 | 周剩余额度 |
| 3 | 今日消耗 |
| 4 | 近 30 天消耗 |

页面状态首次读取无效时，先把既有动图对象恢复为原厂描述，再写入关闭状态。状态只存在于
萌宠页面状态块的新增尾部，随原厂页面状态释放；设备重启、页面销毁后重建或异常重新分配时
都从关闭状态开始。不得把该状态复制到原厂四个字段、一级窗口字段或持久化文件。

进入或切换 AGENTS 时，先对既有动图对象调用 `0xa00cf8d8` 并确认对象偏移 `0x5c` 的解码
描述有效，再以一次对齐写入提交新界面状态；失败时立即恢复原画面并写入关闭状态。离开
AGENTS 时先写入关闭状态，再把既有动图对象恢复为原厂描述地址 `0xa01f7090`。调用
`0xa00bfa4e` 后必须再次用 `0xa00be388` 核对结果；结果不是目标序号时恢复原厂萌宠画面并
写入关闭状态。

后台界面定时回调先执行原厂定时回调，再读取 `0xa00be388`。当前交互分派不是 `7` 时不读取
萌宠状态、不替换动图数据源；当前为 `7` 时才沿已验证对象关系取得动图对象。状态无效时，
恢复原厂描述并初始化为关闭；状态为关闭且解码描述无效时重试恢复原厂描述；状态有效且开启
时才应用对应页面。实现不得新增内存申请或释放调用，不得替换萌宠根对象的用户数据，不得
写入原厂萌宠状态偏移 `0`、`4`、`8`、`12`。

原厂旋钮输入解码把一次旋转步骤中的中途方向变化直接按新方向强制完成。机械触点抖动形成
一次有效的相反相位时，该路径会立即更新旋钮位置并向界面上报反方向。方向过滤必须在原厂
判定“已有步骤且新方向与当前方向不同”之后、强制完成之前返回，不更新旋钮位置、不发出
按键事件，也不改变原厂同方向处理、完整步骤处理和按钮处理。

系统设置菜单不得使用写死的首尾序号。每次事件从实际显示项目表取得数量；数量为零时不
移动，数量为一时保持当前项。
选中状态、选中样式和列表可视位置的同步验收只见
[`SPEC-ROTARY-005`](../SPEC.md#spec-settings-rotary)。

### 6.2 当前设置页实现

当前版本的设置列表创建、控件数量、对象查找、左右处理、原字节、首次候选和真机根因只见
[`2026-07-27 系统设置菜单首尾循环静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-27-系统设置菜单首尾循环静态定位.md)。

实现放入 `features/settings_menu_wrap/`。原厂左右分支先遍历页面根控件并把系统设置列表数据
保存在 `s3`。当前安装版从 `s3 + 8` 取得实际设置列表控件，再调用原厂子控件计数入口
`0xa00c5fe4`，把返回值作为当次设置项目数：小于 2 或状态越界时返回；位于当次首尾时调用
原厂选择切换完成循环；其余有效位置交回原厂相邻切换。不得使用进入挂接前保存在 `s5` 的
页面根控件子控件数作为项目数。

原厂内部相邻切换按需要先调用列表位置调整入口 `0xa00c6dfe`，再写入选中状态并调用选择切换。
当前安装版的两个首尾分支只执行后两步，造成选中状态已经循环、列表仍停在原位置。视觉同步
修正版必须复用原厂顺序：

1. 最后一项向右循环到第一项时，调用列表位置调整入口，位置参数为 `0`，动画参数为 `0`；
2. 第一项向左循环到最后一项时，调用列表位置调整入口，位置参数为 `400`，动画参数为 `0`；
3. 列表位置调整返回后，才写入新状态并调用原厂选择切换；
4. 内部相邻切换继续直接回到原厂分支。

动画参数 `0` 只关闭跨越中间条目的滚动过渡，不取消列表位置调整。两个首尾分支仍在同一次
旋钮事件中分别把列表直接移动到顶部和底部；原厂内部相邻切换继续使用原厂动画参数。

菜单循环修正范围只包含系统设置一级列表。二级菜单不能首尾循环的原厂行为保持不变。方向
过滤属于 `core/rotary_encoder/`，作用于所有页面的旋钮旋转，不处理按钮；系统设置功能通过
该系统能力取得单方向事件。原厂日志字符串和日志调用代码保留；被过滤的中途反向边沿不再
进入原厂“强制完成”日志。图形资源、持久化和运行时内存分配均不修改。版本限定处理程序
继续使用已经验证的原厂零填充代码间隙。

首次候选的批准和离线构建只见上述证据记录第 10、11 节，真机结果与根因只见第 13、14 节。
修正版全部精确修改前后字节、控制流、验证结果和批准范围指纹只见第 15 节，重新批准、离线
构建与单次真机安装只见第 16～18 节。

当前安装版已经完成上传对象完整回读、设备接受、下载、安装、重启和连续在线检查。真机观察、
视觉误判说明和静态根因只见上述证据记录第 19 节。首尾立即定位修正版的完整字节、控制流、
验证结果和新批准范围只见第 21 节；此前批准范围不适用于该修正版。

顶部第一项左旋偶发落到第二项的真机失败、能够写入第二项的原厂路径和旋钮解码证据只见上述
证据记录第 22 节。方向过滤必须新增一个精确修改区间，并与第 21 节三个区间合成新的完整
批准范围；第 21 节批准不得继续使用。固定实现、静态验证和新范围指纹只见第 23 节。

方向过滤成品的构建、安装和自动检查只见上述证据记录第 24、25 节；用户真机确认与保留成品
身份只见第 26 节。该成品的用户功能范围和首尾循环唯一功能定义只见 [`SPEC`](../SPEC.md)
第 1.1 节。

## 7. AGENTS 看板页面

### 7.1 页面对象

AGENTS 看板在产品导航上仍是独立一级页面；实现上复用原厂萌宠页已经存在的动图对象，不新增
或替换任何界面对象。页面内容包括：

- 一个概览页面；
- 周剩余额度详情；
- 今日消耗详情；
- 近 30 天消耗详情。

进入详情时固定把详情索引设为周剩余额度。离开详情、切换一级页面、重启或恢复出厂设置都
不保存当前详情索引。

#### 7.1.1 当前版本对象复用

原厂萌宠创建代码在运行地址 `0xa00b40c8` 把原厂动图描述地址 `0xa01f7090` 传给
`0xa01930fe`，返回的动图对象保存在萌宠状态偏移 `0`。当前版本沿原厂和参考项目已经验证的
对象关系取得同一对象，只调用 `0xa00cf8d8` 切换数据源。萌宠状态字段的可读写边界只见
[`AGENTS 原厂萌宠控件复用静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-AGENTS原厂萌宠控件复用静态定位.md)。

载荷不挂接页面注册点，不创建或删除对象，不改变对象大小、坐标、样式、标记、父子关系和
用户数据。进入 AGENTS 前若四页包无有效记录，则在同一个原厂动图对象上显示对应内置等待页；
离开时无条件恢复 `0xa01f7090`。页面活动状态和详情索引只使用第 6.1 节新增的独立状态尾部，
不占用原厂萌宠状态偏移 `0`、`4`、`8`、`12`。

载荷链接起点为运行地址 `0xa02465d0`。挂接点先跳到运行地址 `0xa001b0ac` 的 8 字节绝对
跳板，再进入载荷，避免直接跳转距离超出当前指令范围。跳板位于已验收设置菜单载荷之后仍为
全零的原厂函数间隙；载荷位于第 10.1 节固定候选区。

零新增对象版本必须重新计算载荷体积、静态栈和剩余空间；未通过第 10.11 节门禁前不命名为
完整优化固件。

### 7.2 成品画面

服务端生成四个 320×240 成品画面，设备只负责校验、保存和显示，不在设备中读取 Codex
任务原文或重新计算统计。

当前效果图、唯一视觉方向和生成提示只见
[`AP01 AGENTS 看板效果图评审`](../image/DESIGN/AP01-AGENTS看板/README.md)。
四张页面已经按原厂时间页、功率页风格以及
[`SPEC-DASHBOARD-015` 至 `SPEC-DASHBOARD-018`](../SPEC.md#spec-dashboard-values)
完成修订。用户已经确认四张页面的内容
与视觉方向，允许实现服务端渲染器并生成真实数据对照图；对照图通过评审前不转为固件内置
资源。

周剩余额度详情需要多批重置卡时，由服务端生成一份动图；自动换批不改变设备端详情索引。
其他静态页面优先使用经过真机验证后体积更小的格式。采用 PNG 或不同 GIF 约束前，必须先
补充设备端加载与真机结果。

从未有成功结果时，固件内置四张完整的“等待首次同步”页面。内置资源必须计入修改区间和
资源容量，不得覆盖未经确认的原厂资源。

#### 7.2.1 内置等待页面

四张等待页面分别使用“AGENTS 看板”“周剩余额度”“今日消耗”“近30天消耗”作为页标题，
中央统一显示“等待首次同步”，下方显示“连接服务后自动更新”。黑色背景、浅灰页标题、白色
主提示、灰色说明和黄色状态点沿用第 7.2 节唯一视觉方向。

每张等待页面固定为 `320×240`、GIF89a、双帧；第二帧只改变右上角状态点亮度，单帧持续
800 毫秒并循环。四张资源逐张固定长度和完整指纹。设备端为每张
资源生成 28 字节原厂格式图片描述。首次进入 AGENTS 时只调用原厂动图数据源切换入口，让
既有萌宠动图对象显示概览等待页；详情事件继续切换同一对象的数据源，不调用动图创建入口。

| 页面 | 字节数 | SHA-256 |
| --- | ---: | --- |
| AGENTS 看板 | 6,757 | `282cb48522a920c4cea84c7517d7d948f90a6e1de122b4155f0181c5fd45904d` |
| 周剩余额度 | 6,762 | `43d8b196db000746678bfa06ec34c6eee2fbd137893c6ef8f546225088267dad` |
| 今日消耗 | 6,586 | `312b91c230598a95bc06a1a7957fe74010b2fd5ab94ea7d8e30e802fb7395237` |
| 近30天消耗 | 6,732 | `5501b430d78c434757887887c808c909e2cbac53031fabd5a78c203c10b1bb2b` |

四张文件存放在 `features/agents_dashboard_firmware/assets/`，属于 AGENTS 固件功能专属冻结
资源。冻结构建复制到构建目录后，逐张复核本表长度、完整指纹、画面尺寸、双帧、循环和时长；
任一不一致时停止。

重新渲染只用于独立审计：审计结果先与冻结资源逐帧比较并查明图片程序库、字体或编码器差异，
不得在构建过程中用新结果覆盖冻结资源，也不得直接更新本表指纹。只有视觉重新评审通过并先
更新本 DESIGN 后，才能替换模块资源。

### 7.3 数值格式化与占位

页面渲染器使用一个公共 Token 格式化入口，严格实现
[`SPEC-DASHBOARD-015` 至 `SPEC-DASHBOARD-018`](../SPEC.md#spec-dashboard-values)，
返回互相独立的“整数数值”和“单位”
两段文字。所有页面只能使用该入口，禁止各页自行拼接单位。

格式化器使用整数运算完成四舍五入，不把 Token 数量转换为浮点数。它先按 SPEC 表格选择
除数，再用“原始值加半个除数后执行整数除法”得到显示整数；结果达到下一单位门槛时，重新
使用下一单位计算。输入必须是非负 64 位整数，超出 SPEC 上限或发生加法溢出时拒绝生成
本轮画面并继续保留最后成功结果。

数值文字位置按四位数字和一个可选千位分隔符预留，使用固定右边界；单位文字使用独立固定
位置。字体变化、单位变化和四舍五入都不能移动相邻指标。渲染完成后必须检查四张页面没有
小数点、文字越界、重叠或裁切。

今日消耗页的主数值按 `56 px`、`48 px`、`45 px`、`42 px`、`36 px`、`32 px` 依次测量，
输入、输出和缓存命中数值按 `30 px`、`24 px` 两档字号依次测量，选择能完整放入固定数值
位置的最大一档。各档共用同一右边界和基线；最小字号仍放不下时拒绝生成本轮画面。
`9,000 Token`、`9,999 万 Token`、`20 亿 Token`、`10 亿 Token` 和 `5 亿 Token` 是必须
通过的版面边界用例。

概览页今日消耗和近 30 天消耗的数值按 `38 px`、`31 px`、`27 px`、`23 px`、`20 px`、
`19 px` 依次测量；最小字号必须完整容纳 `9,999` 和千位分隔符。

百分比和金额使用公共整数显示入口。请求数等原始整数不做缩放；百分比与金额完成四舍五入
后再拼接各自符号。概览页只把本周剩余额度传给圆环控件，今日消耗和近 30 天消耗只进入
普通数值与单位位置。

圆环和进度条的颜色方向只实现
[`AP01 AGENTS 看板效果图评审` 第 2 节](../image/DESIGN/AP01-AGENTS看板/README.md#2-视觉方向)；
表示剩余额度和命中率时不得使用相反色序。近 30 天页只在“活动洞察”和“常用插件”两个
组标题旁分别绘制一个组图标，十条数据行不创建图标、圆弧或进度控件。每条数据的字段名、
数值和单位共用同一个文字颜色，不按数值或单位设置点缀色。

### 7.4 本机真实数据采集

`features/agents_dashboard` 负责一次性采集、整理和渲染四张页面。外部只能通过该模块入口
调用，不直接读取模块内部文件。

Cockpit Tools、CC Switch 和 Codex 桌面端只作为已经验证过的取数实现参考，不是安装依赖，
也不是运行时数据源。即使三者均未安装，采集器仍必须能直接读取当前 Codex 登录态、官方接口
和本机会话记录。

#### 7.4.1 数据清单、原始字段与直接来源

本节是 AGENTS 看板“需要哪些数据、直接从哪里取得、怎样换算”的唯一原文。其他文档只引用
本节。

| 数据组 | 最终取得的数据 | 直接来源 | 原始字段 | 整理方法 |
| --- | --- | --- | --- | --- |
| 一周额度 | 剩余百分比、重置时间 | `https://chatgpt.com/backend-api/wham/usage` | `rate_limit.primary_window`、`rate_limit.secondary_window` 内的 `limit_window_seconds`、`used_percent`、`reset_at`、`reset_after_seconds` | 选择 `limit_window_seconds = 604800` 的窗口；剩余值为 `100 - used_percent` |
| 重置卡 | 可用数量，以及每张卡的状态、获得时间、失效时间和已使用时间 | `https://chatgpt.com/backend-api/wham/rate-limit-reset-credits` | `available_count`、`credits[].status`、`granted_at`、`expires_at`、`redeemed_at` | 排除已使用和已过期项目，按失效时间排序 |
| 今日消耗 | 按模型汇总的请求数、原始输入、新增输入、缓存命中、缓存写入、输出、推理输出、长上下文请求数和 API 成本状态；当天总 Token、总请求数、命中率和 API 成本 | 当前 Codex 目录下的 `sessions/**/*.jsonl` 与 `archived_sessions/*.jsonl` | `session_meta.payload.id` 或 `session_id`；`turn_context.payload.model`；`event_msg.payload.thread_settings.model`；`event_msg.payload.info.total_token_usage` 或 `last_token_usage` 中的 `input_tokens`、`cached_input_tokens`、`cache_write_input_tokens`、`output_tokens`、`reasoning_output_tokens`；记录 `timestamp` | 按第 7.4.2 节逐条归集，不读取 CC Switch 数据库 |
| 近 30 天与近 7 天 | 北京时间 30 个自然日的每日 Token、30 天累计和最后 7 天曲线 | `https://chatgpt.com/backend-api/wham/profiles/me` | `stats.daily_usage_buckets[].start_date`、`tokens` | 固定取今天及此前 29 天；缺日补零；最后 7 项用于曲线 |
| 活动洞察 | 快速模式比例、最常用推理强度及比例、不同技能数、技能使用总数、任务总数、最长任务 | 同上 | `stats.fast_mode_usage_percentage`、`most_used_reasoning_effort`、`most_used_reasoning_effort_percentage`、`unique_skills_used`、`total_skills_used`、`total_threads`、`longest_running_turn_sec` | 只做类型检查、中文显示映射、百分比取整和秒转分钟 |
| 常用插件 | 前五个插件名称和调用次数 | 同上 | `stats.top_invocations[].type`、`plugin_name`、`usage_count` | 只保留 `type = plugin`，按官方返回顺序最多取五项 |

一周额度与重置卡的请求和字段归一参考 Cockpit Tools；今日 Token 增量参考 CC Switch 3.16.1；
个人统计字段映射参考 Codex 桌面端。三者只用于确认实现方式，运行时不读取其页面、缓存或
数据库。

#### 7.4.2 按模型归集今日原始 Token

采集器流式扫描每个会话记录文件，并为每个文件独立维护会话编号、当前模型、上一组累计
Token 和正增量事件序号：

1. `thread_settings_applied`（设置已经应用的记录）中的 `thread_settings.model` 更新当前
   模型；没有该记录时使用最近的 `turn_context.payload.model`；
2. `total_token_usage`（会话累计 Token）使用当前累计值减去上一累计值得到非负增量；只有
   `last_token_usage` 时直接使用本次值；
3. `last_token_usage`（本次请求 Token）和累计差值都读取原始输入、缓存命中、缓存写入、
   输出与推理输出；缓存命中与缓存写入总和不得超过原始输入，推理输出不得超过输出；
4. 原始输入、缓存命中、缓存写入和输出均为零的记录不计为请求；
5. 使用“会话编号 + 正增量事件序号”去除活动目录与归档目录中的重复记录；
6. 只保留记录时间落在北京时间当天的正增量，再按模型汇总；
7. 新增输入等于原始输入减去缓存命中；当天总 Token、命中率和请求数继续执行
   [`SPEC-DASHBOARD-021` 至 `SPEC-DASHBOARD-028`](../SPEC.md#spec-dashboard-fields)。

安全聚合结果保存每个模型标识、请求数、原始输入、新增输入、缓存命中、缓存写入、输出、
推理输出、长上下文请求数、单价是否匹配和 API 成本，不保存原始会话行、任务标题、提示词、
回复正文或会话编号。

#### 7.4.3 API 成本计算

模型标识映射、固定单价、长上下文规则、唯一公式、取整顺序和无法计算条件只见
[`Codex 模型 API 计费表`](../Codex-模型API计费表.md)。实现放在
`features/agents_dashboard/pricing.py`，使用十进制定点计算，不使用二进制浮点数。

采集器逐次请求计算未取整美元费用，以便正确应用长上下文规则；随后按模型汇总，最后对当天
总和只取整一次。当天全部正 Token 用量均能匹配模型、必要 Token 字段和单价时，今日页显示
带美元符号的整数“API 成本”；任何一组无法匹配时显示“无法获取”。

自动测试必须固定当前表内每个本地模型标识、四类单价和长上下文规则，检查实现表与唯一计费
文档逐行一致，并覆盖多模型求和后只取整一次、缓存写入、长上下文、未知模型和未公布价格
模型。

#### 7.4.4 官方请求、缓存与安全边界

三个官方请求都使用当前 Codex 登录文件中的访问凭据和账号标识，设置连接与总响应超时，
绝不打印、保存或写入快照。额度和重置卡每 5 分钟最多联网一次；个人统计沿用 Codex 桌面端
6 小时有效期。规范化后的安全聚合结果保存到被版本控制忽略的 `env/agents-dashboard-cache/`。
联网失败、登录过期、字段缺失或返回类型变化时使用对应来源的最后一次成功缓存；没有成功缓存
时才把该来源标记为无法获取。

服务运行时允许显式指定一个 Codex 数据根目录和一个安全聚合缓存目录。未指定数据根目录时
使用当前用户的 `CODEX_HOME`（告诉程序去哪里找 Codex 数据的环境参数），该环境参数不存在
时使用当前用户目录下的 `.codex`；未指定
缓存目录时使用项目的 `env/agents-dashboard-cache/`。运行状态和构建清单只记录各来源是否
可用，不记录这两个目录的绝对路径。

采集结果只保存第 7.4.1 节列出的安全聚合数值、中文展示名称、数据截止时间和来源可用状态。
测试必须扫描结果键值，确认不含提示词、回复正文、令牌、账号、设备标识和本机绝对路径；
还必须证明删除三个参考应用后不影响采集路径。

#### 7.4.5 换机时的运行参数连续性

当前服务电脑的运行参数只包含数据根目录、缓存目录、字体目录、结果目录、绑定地址、端口和
刷新周期。固件只固定第 8.2 节的局域网取包地址；换机后地址变化时，必须先修改本 DESIGN，
再重新制作和安装与新地址匹配的固件。服务和固件均不创建、不读取、不复制设备共享配置。

服务健康结果只公开四类数据来源是否可用、最后成功刷新、最后设备请求、请求次数、是否正在
刷新以及安全错误摘要。不得公开登录信息、账号原文、本机绝对路径或请求内容。

额度、重置卡和个人统计使用目标服务电脑的数据根目录中的当前登录状态；今日本地会话只扫描
该目录中的 `sessions/` 和 `archived_sessions/`。当前实现不合并多台电脑的本地会话，也不把
任一电脑的登录文件或会话原文复制到另一台电脑。完整换机操作与验收步骤只见
[`AGENTS 看板跨电脑数据获取方法`](../SOP/AGENTS看板跨电脑数据获取/AGENTS看板跨电脑数据获取方法.md)。

### 7.5 字体、图标与光栅化

服务端渲染器以 4 倍画布绘制后缩小到 320×240，生成不带透明通道的 PNG。字重层级只见
[`AP01 AGENTS 看板效果图评审` 第 2 节](../image/DESIGN/AP01-AGENTS看板/README.md#2-视觉方向)。
渲染器使用 Michroma Regular 绘制超大主数字和主要 Token 数字，使用 MiSans Medium、
Semibold 和 Bold 绘制次级数字、中文、单位、字段名和状态标题，并以语义角色调用，不允许
页面代码任意填写字体文件名。字体文件只保存在被版本控制忽略的 `env/fonts/`；渲染器找不到
任一字体时拒绝生成，不能静默换成系统字体。

两套字体的取得地址与许可记录在 [`requirements.md`](../requirements.md)。成品图片和使用
说明注明所用字体，不把字体文件复制进仓库、安装包或固件。

自动测试必须验证主数字使用 Michroma，另外三个文字角色映射到 MiSans 的不同字重，并在本机
字体齐全时验证小字号文字的墨迹量依次增加，防止文件名不同但实际仍呈现为同一粗细。

“活动洞察”和“常用插件”组标题，以及今日页的输入、输出和缓存符号，使用仓库内随模块
保存的 Material Design Icons 矢量源文件。模块同时保存上游许可文件和来源清单；渲染前
将图标着色并缩放，不用圆环替代语义图标。

颜色函数只接受 0 至 100 的整数，按红、橙、黄、绿顺序插值得到当前颜色；未完成部分使用
暗灰色。圆弧和进度条只能接收百分比字段，Token 字段类型检查失败时拒绝绘制。

### 7.6 真实数据对照产物

真实数据对照图写入效果图目录下的 `真实数据/`，包含四张 320×240 PNG、一个只含聚合字段
的数据快照、一份生成说明，以及一张把四组效果图与真实图并排排版的审阅总图。每次刷新
真实数据时必须同时刷新总图，不能保留上一轮数据。它们用于比较服务端实际渲染与效果图的
差距，不是设备端资源，也不代表已经通过 PNG 真机加载测试。

## 8. 完整结果协议

### 8.1 单次完整包

服务端每次只发布一个完整结果包。第二版固定使用小端序，包头为 64 字节，随后按
“概览、周剩余额度、今日消耗、近 30 天消耗”的顺序连续存放四个页面。包头字段固定如下：

| 偏移 | 长度 | 内容 |
| ---: | ---: | --- |
| 0 | 4 | 固定标记 `APAG` |
| 4 | 2 | 协议版本，第二版为 2 |
| 6 | 2 | 包头长度，固定为 64 |
| 8 | 4 | 非零结果代号 |
| 12 | 4 | 整包字节数 |
| 16 | 8 | 北京时间生成时刻对应的 Unix 秒数 |
| 24 | 4 | 页面数，固定为 4 |
| 28 | 4 | 保留位，固定为 0 |
| 32 | 16 | 四个页面各自的字节数 |
| 48 | 16 | 四个页面各自的 CRC-32（边接收边计算的四字节损坏检查值） |

每页必须是 320×240、GIF89a、至少双帧，单页不超过 96 KiB；完整包不超过 384 KiB。
结果代号在同一服务端配置内必须单调增加。

设备必须在临时位置接收完整数据包，完成长度、四页、文件格式、尺寸和逐页损坏检查后，才把
“当前结果代号”切换到新包。任何一项失败都删除临时结果并继续使用旧包。

首次后台刷新实验只写入 `/tmp` 的三组轮换槽，每组四个页面；接收方不会覆盖已提交槽和
界面已确认使用的槽。该阶段用于验证联网、整包校验、四页显示和连续刷新，不宣称重启后保留。
最终持久化位置仍需先完成 `/data` 可用空间与读写寿命测试。

### 8.2 局域网取包边界

`FW-AGENTS-014` 的两个原厂天气地址固定为
`http://192.168.31.174:18765/a`，两个原厂格式区只输出该地址，不附加查询字段。服务收到
`/a` 请求后直接返回第 8.1 节完整包；服务和固件均不持有设备代号、访问标识、秘密值或共享
配置。该入口只允许在当前受控局域网内运行，路由器不得把端口映射到公网。

云服务器只允许使用 HTTPS（给网络传输加密并核对服务器身份的连接方式），并启用服务器
证书校验。虽然原厂固件包含加密通信程序库，但本项目尚未证明新增载荷可以正确复用证书
校验路径；证明完成前，云服务器位置不得启用。

## 9. 刷新、故障切换与状态保持

每次刷新从主电脑开始，依次尝试 NAS 和云服务器。每个位置必须设置连接、读取和整包总超时，
一个位置失败后才进入下一个。具体秒数需要在真机网络测试后写入，不在当前无证据状态下猜测。

AGENTS 看板关闭时取消刷新定时器，不发起网络请求，保留最后成功包。重新开启时立即触发
一次后台刷新，完成后再按 5 分钟周期运行。

最后成功包必须位于重启后仍存在的存储中；新包只能替换完整旧包。恢复出厂设置删除优化
固件的页面开关记录和最后成功包，但不改变三个服务端的固定优先顺序。

服务进程启动时先尝试生成新包。生成失败但磁盘上存在一份能通过第 8.1 节全部检查的旧包时，
服务继续提供旧包并进入降级状态，后台按固定周期重试；旧包不存在或损坏时停止启动。健康
结果中的 `ok`（表示当前完整包能安全提供的状态字段）只表示当前
存在可安全提供的完整包，刷新错误单独标记，不能因为一次刷新失败把仍可提供的旧包报告为
完全不可用。

## 10. 线程、内存与失败隔离

### 10.1 固定载荷空间

载荷空间的资源、工具、文件偏移、输入输出指纹和无损等价检查只见
[`2026-07-28 原厂动图无损优化与载荷空间`](../../knowledge/AP01-官方固件分析/cases/2026-07-28-原厂动图无损优化与载荷空间.md)。

构建器使用 Gifsicle 1.96 和固定参数 `-O3` 优化数据起点 `0x1f80ac` 的原厂动图。只有优化
结果指纹完全匹配，且尺寸、41 帧、循环、每帧时长以及 Pillow、FFmpeg 两套解码器得到的
每帧合成像素均与原动图一致时，才允许把 `0x2475d0` 至 `0x2563d6` 作为载荷候选区。
候选容量固定为 60,934 字节。

动图数据指针、原资源槽边界、固件总长度和其他原厂资源不得变化。历史页面载荷曾在该区间
完成符号、重定位和调用检查，但该结果不适用于第 6.1 节的新实现。新载荷必须从零重新计算
体积、符号、重定位、禁止调用和剩余空间；后续每次增加刷新或持久化代码都必须重做检查。

网络读取、文件写入、图片校验和结果提交不得在界面事件处理过程中同步执行。界面线程只读取
已经提交的当前结果。

`FW-TRANSPORT-002` 的下载状态结构不得超过 160 字节，任何链接进入该候选的单函数静态栈
不得超过 320 字节；构建清单必须记录这两个实测值。超过任一限制时构建失败，不得进入上传。
`FW-AGENTS-006` 不创建设置对象、不修改页面开关记录、不挂接一级切页过滤，也不加入最终
页面组合代码。它只验证低栈四页传输与原厂萌宠三个局部分支能否在已验收设置阶段上安全共存。
当前冻结实现的下载状态为 136 字节，单函数最大静态栈为 208 字节，均低于本节门禁。

实现前必须测量并固定：

- 新增载荷的代码和只读数据大小；
- 每个任务的栈上限；
- 接收缓冲、解码缓冲和页面对象峰值；
- `/data` 可用空间；
- 四页最大成品总量；
- 连续刷新后的内存变化；
- 网络失败、文件损坏和断电后的恢复结果。

专用测试设备首次实验允许把尚无安全读取接口的项目标为“待真机测量”，并按
[`固件制作规范`](../固件制作规范.md) 第 7.1 节逐次授权进入安装；压力测试一旦出现持续增长、
越界、看门狗重启或界面卡死，立即停止后续安装。没有测量值的项目不得进入发布成品。

### 10.2 历史首次真机观察成品

`ap01-1.0.2_0031-agents-observation.bin` 的当前保留文件 SHA-256 为
`2ef4305bd3f29873d7817a495097b074e06f62ba0189e4f35f0be65b77c55813`。它使用旧页面注册
方案，曾通过当时限定的一级与详情交互验收；该验收没有覆盖后来暴露问题的全部原厂确认路径。
该成品不符合第 5.5、6.1 节，保留作历史对照，不作为当前设计输入且不得再次安装。

### 10.3 历史四页后台同步实验成品

`ap01-1.0.2_0031-agents-sync-experimental.bin` 曾验证本机完整包、流式接收、三组 `/tmp`
结果槽、五分钟刷新和四页显示链路。首次安装文件 SHA-256 为
`110d59ae41d8ad7dff40d7829f3fde389478e4c6b331d3092153f212bc82535f`；当前同名保留文件
SHA-256 为 `a0f43b8d8214d6649846618098bc13c71815d44642b31c9f949e16765fde2616`，两者不是同一
冻结成品。

首次安装证据只见
[`四页同步实验成品首次安装记录`](../../knowledge/AP01-官方固件分析/cases/2026-07-28-AGENTS四页同步实验成品首次安装.md)。
数据传输和结果槽经验可以在新实现中重新验证后复用；旧页面注册与键值挂接不得复用。两个
同名不同指纹的文件均不作为当前设计输入，后续成品不得再复用文件名覆盖既有冻结成品。

### 10.4 历史一级页面开关失败成品

`ap01-1.0.2_0031-opt.bin` 的 SHA-256 为
`780445a29121e74f187f324a1edb873f190f5b91f98dec838ac152130bece136`，文件尾恢复校验值为
`0x70a6c7d5`。该成品同时增加设置创建循环、设置行和覆盖页，真机卡在开机动画并已回刷，
禁止再次安装。过程与未知边界只见
[`一级页面开关组合成品首次安装记录`](../../knowledge/AP01-官方固件分析/cases/2026-07-28-一级页面开关组合成品首次安装.md)。

### 10.5 历史设置列表空挂接观察成品

`ap01-1.0.2_0031-page-settings-hook-observation.bin` 的 SHA-256 为
`622e1e87f8f38a3bf1e079ea589d12d75bfc042dc66930b903563231ab380720`。它在旧四页成品上
只加入设置列表原路径空挂接，曾通过安装、重启、联网和主动取包；没有实现第八项，也没有
证明增加设置项所需的容量与生命周期。过程证据只见
[`设置列表空挂接首次安装记录`](../../knowledge/AP01-官方固件分析/cases/2026-07-28-设置列表空挂接首次安装.md)。
该成品不作为当前设计输入且不得再次安装。

### 10.6 原厂详情序号兼容观察成品

`ap01-1.0.2_0031-agents-detail-compat-observation.bin` 的 SHA-256 为
`748d29837a8bf94eb83b263f4ab1d26b13556466498ba44365d5be82b2f78b6a`。真机出现普通
页面确认误入时间设定、菜单“返回”触发重启，禁止再次安装。该节只保留成品身份与结果；
过程证据只见
[`原厂详情序号兼容观察成品首次安装`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-原厂详情序号兼容观察成品首次安装.md)。

### 10.7 原厂确认键边界兼容观察成品

`ap01-1.0.2_0031-agents-confirm-compat-observation.bin` 的 SHA-256 为
`aea891f64449ada67991ca4349b6b1c603548e7d81d717b9d0d0a7da922fd1ad`。真机确认菜单
“返回”仍触发重启，禁止再次安装。该节不再承载字段改写方案；过程证据只见
[`原厂确认键边界兼容观察成品首次安装`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-原厂确认键边界兼容观察成品首次安装.md)。

### 10.8 九对象覆盖层观察成品

`ap01-1.0.2_0031-agents-pet-overlay-observation.bin` 的 SHA-256 为
`e5e1f39db1491307f5639e993c6d6507fe46ee91f5799adbcd19c04464e0e909`。真机确认系统
设置菜单“返回”触发重启，禁止再次安装。对象新增的失败边界只见
[`AGENTS 九对象覆盖层静态设计`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-AGENTS九对象覆盖层静态设计.md)。

### 10.9 原厂萌宠控件复用观察成品

`ap01-1.0.2_0031-agents-stock-pet-reuse-observation.bin` 的 SHA-256 为
`c4c0db992ba2f9ae8bfe26f3211824b0c19d66600d25ce1b4dd3e7a03de84326`，文件尾恢复
校验值为 `0xc713d982`。62 项自动测试通过，但真机确认日历详情无法返回一级页面，禁止再次
安装；设备已回刷 `opt-setting.bin`。过程与根因只见
[`日历详情返回回归定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-日历详情返回回归定位.md)。

### 10.10 原厂交互分派兼容观察成品

`ap01-1.0.2_0031-agents-stock-dispatch-observation.bin` 尚未安装。其 SHA-256 为
`348d2192072917d4284c412443b94b48a97065070d6a6f637a5debadbca81ec8`，文件尾恢复
校验值为 `0x72e1fc2e`。它通过了当时的 62 项自动测试，但设计复核发现：

1. 真实页面切换仍调用 `0xa00b06f4`，没有沿原厂一级回调使用 `0xa00bfa4e`；
2. AGENTS 状态仍写入只有创建清零证据的萌宠状态偏移 `12` 高 3 位。

两项均违反当前第 6.1 节，故该冻结成品在安装前即判定失效并禁止安装。离线测试通过不构成
原厂调用链正确或原厂字段可写的证据。

### 10.11 原厂精确调用链观察成品

本观察成品固定命名为
`ap01-1.0.2_0031-agents-stock-callchain-observation.bin`，只能从已验收的
`opt-setting.bin` 重新生成。构建前必须先完成第 6.1、7.1.1 节实现，并满足：

1. 页面注册点、旧跳板和原厂对象树保持输入原字节或运行时原值；萌宠状态原有偏移
   `0`、`4`、`8`、`12` 保持原值；
2. 键值包装只使用 `0xa00be388`、`0xa00be3ca`、`0xa00bfa4e` 这一条原厂实际调用链；
3. 文件偏移 `0x0b502c` 只把申请长度从 16 改为 20，原厂申请失败与删除路径不变；除该处、
   两个键值回调地址、后台同步回调和新增载荷外，不修改萌宠创建路径；
4. 载荷不存在对 `0xa00b0290`、`0xa00b06f4` 的调用，也不存在容器偏移 `52`、`56`、`60`
   和萌宠状态偏移 `0`、`4`、`8`、`12` 的写入；
5. 反汇编证明非序号 `7` 的确认键和原厂内部详情的三种键值在读取独立尾部或萌宠状态前进入
   原厂回调；
6. 对交互分派序号 `0` 至 `8` 与键值 19、20、10 的全部组合做自动路由测试；
7. 对尾部标记错误、互补值错误、状态越界、页面销毁重建、进入、四页切换、退出、动图切源
   失败和真实页面切换核对失败做关闭恢复测试；
8. 六个原厂页面与 AGENTS 在观察成品中全部可达；逐页验证确认、详情进入、详情返回和左右
   切换，页面开关记录与设置第八项均不得进入本成品；
9. 构建报告逐项列出上述门禁的反汇编地址、指令顺序和测试结果，不能只统计入口出现次数。

该观察成品已从代码提交 `022596f48724096307d254383c20ecd8c64690bf` 冻结生成。
SHA-256 为 `e57b8d7a91d69b038004cb8ac5d1620bd0351248c46333d329ceac3e446be40d`，
MD5 为 `20ab4fe1c80720d09c090c4b545223d2`，文件尾恢复校验值为 `0xd83774a9`。
载荷为 32,536 字节，固定候选空间剩余 28,398 字节，单函数最大静态栈为 640 字节；
64 项自动测试通过。构建清单位于
`artifacts/build/agents-stock-callchain/manifest.json`。

该成品已在专用测试设备完成同文件上传回读、仅下载校验、正式安装、重启、重新上线和主动
取数自动验收，但真机确认原厂功率页按下并松开旋钮后设备重启，禁止再次安装；设备已回刷
`opt-setting.bin`。真机过程只见
[`AGENTS 原厂精确调用链观察成品首次安装`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-AGENTS原厂精确调用链观察成品首次安装.md)。

### 10.12 原厂确认键无栈透传观察成品

本观察成品固定命名为
`ap01-1.0.2_0031-agents-stock-enter-gate-observation.bin`，只能从已验收的
`opt-setting.bin` 重新生成。除第 10.11 节全部离线门禁外，还必须满足：

1. 一级键值回调的第一段代码不得调整栈指针、保存寄存器、调用函数或写内存；
2. 事件类型不是 17、键值不是 19、20、10、交互分派序号为 `1、2、8` 的全部组合，都在
   第一条栈修改指令之前直接进入原厂回调；
3. 序号 `0、3、4、5、6` 的确认键在第一条栈修改指令之前直接进入原厂回调；
4. 只有序号 `7` 的确认键，以及序号 `0、3、4、5、6、7` 的左右键进入完整包装；
5. 无栈入口只允许读取事件偏移 `0`、`8`、`16`，键值指针偏移 `0` 和非空窗口对象偏移
   `52`；不得出现其他读取、任何写入或对 AGENTS、萌宠状态的访问；
6. 构建报告分别列出无栈入口、第一条栈修改、完整包装和原厂回调跳转地址，并验证 27 组
   交互分派与键值组合的入口路由；
7. 进入完整包装的两条跳转必须解析到 `ap01_agents_wrapped_key_event`，不得解析到页面注册
   入口或载荷起点；无栈原厂跳板只能包含地址装入和尾跳两条有效指令，允许其后存在一条
   对齐空指令；
8. 真机逐项验证功率、时间、日期、天气、设置、萌宠的确认路径，原厂内部详情返回，以及
   AGENTS 概览和三个详情交互；任一原厂页面重启、卡死或错误跳转都立即回刷
   `opt-setting.bin`。

该观察成品已从代码提交 `ae1e8d5f226a93150fd2d7a21135425a96790fb3` 冻结生成。
SHA-256 为 `dd3ded32015e722464e434ca89e19bff3cfc47b585b4a2ce73a6d9a9ff95a036`，
MD5 为 `16a3d579ac76db401c09e3d5ba7ed3eb`，文件尾恢复校验值为 `0xad0b7e6d`。
载荷为 32,636 字节，固定候选空间剩余 28,298 字节，单函数最大静态栈为 640 字节；
64 项自动测试通过。构建清单位于
`artifacts/build/agents-stock-enter-gate/manifest.json`。

该成品已在专用测试设备完成同文件上传回读、仅下载校验、正式安装、重启、重新上线和主动
取数自动验收。真机确认功率页按下并松开后界面线程卡死，但设备运行计时和后台取数持续
增长；该成品禁止再次安装，设备已回刷 `opt-setting.bin`。真机过程只见
[`AGENTS 原厂确认键无栈透传观察成品首次安装`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-AGENTS原厂确认键无栈透传观察成品首次安装.md)。

### 10.13 原厂键值回调局部分支观察成品

本观察成品固定命名为
`ap01-1.0.2_0031-agents-stock-local-branches-observation.bin`，只能从已验收的
`opt-setting.bin` 重新生成，并满足：

1. 一级窗口回调地址装入位置 `0x0b3838`、`0x0b3842` 与输入逐字节一致；
2. 原厂界面定时回调地址装入位置 `0x0b37e4`、`0x0b37ee` 与输入逐字节一致；
3. 只在 `0x0bd336`、`0x0bd460`、`0x0bd712`、`0x0bea64` 替换各四字节入口；
4. 四个入口分别跳到 `0x1c0bc`、`0x1c0c4`、`0x1c0cc`、`0x1c0d4` 的八字节近跳板；
5. 每个近跳板只尾跳到一一对应的载荷入口，不得共享错误入口；
6. 功率确认序号 0 的原厂路径、原厂键值回调入口和四个局部目标以外的回调字节保持不变；
7. 原厂恢复路径分别从 `0xa00bc33a`、`0xa00bc464`、`0xa00bc716`、`0xa00bda68`
   继续，消费事件只从 `0xa00bc1d2` 退出；
8. 不挂接原厂界面定时回调；进入或操作 AGENTS 时应用最新已提交数据；
9. 先逐项真机验证功率确认、日历返回、设置返回和原厂萌宠确认，再验证 AGENTS 左右、详情
   和返回；任一异常立即回刷 `opt-setting.bin`。

构建器必须把四个原字节断言、跳转目标、近跳板、原厂恢复地址、功率确认隔离和两个全局
回调地址未修改写入清单。完成离线冻结前不得安装。

该观察成品已从代码提交 `0453a0d6d04455011cd684eb2bb542f140d63f8c` 冻结生成。
SHA-256 为 `1958bf4796635684c5e1b004472665daf11404faab2f204184169b7138c4dc48`，
MD5 为 `a6935dac9d0d8fcc2dd320a0e5391636`，文件尾恢复校验值为 `0xab199ac7`。
载荷为 33,072 字节，固定候选空间剩余 27,862 字节，单函数最大静态栈为 640 字节；
64 项自动测试通过。构建清单位于
`artifacts/build/agents-stock-local-branches/manifest.json`。独立逐字节复核确认两个全局回调
地址、功率确认区间，以及四个局部目标以外的原厂键值回调均与 `opt-setting.bin` 一致。

该成品已在专用测试设备完成同文件上传回读、仅下载校验、正式安装、重启、重新上线和主动
取数自动验收。换机换网后的物理验收中，用户从可见的 AGENTS 看板向左旋转一格，观察到
萌宠页短暂出现并最终落到功率页；该结果不符合本节第 9 条，当前物理验收判定不通过，功率
确认和其余物理操作全部暂停。现场没有键值、相位和状态证据，不在本 DESIGN 中指定根因。
过程只见
[`AGENTS 原厂局部分支观察成品首次安装`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-AGENTS原厂局部分支观察成品首次安装.md)。

### 10.14 完整离线组合候选

`FW-INTEGRATION-002` 已从代码提交 `d1e920d` 生成
`artifacts/firmware/ap01-1.0.2_0031-opt-integration-candidate.bin`。文件长度为
6,804,520 字节，SHA-256 为
`76e330a970482b261e930bd83f57535444e1ac822ae5ec486703f83d2cd67e51`，MD5 为
`1bd0cffd3f17046819aeeda9622300e4`，文件尾恢复校验值为 `0xb92429fb`。

载荷为 42,192 字节，固定候选空间剩余 18,742 字节。两次独立构建逐字节一致；73 项自动
测试通过。独立比较阶段输入与冻结成品后，实际变化 361,984 字节，全部落在清单的 33 个
允许区间内，区间外变化为 0。一级总键值回调注册地址、原厂界面定时回调注册地址、功率
确认路径和页面注册路径保持不变；第五个局部挂接只替换运行地址 `0xa00bc092` 的实际切页
调用。

构建清单位于 `artifacts/build/opt-integration-candidate/manifest.json`，类型为
`opt-integration-candidate-firmware`，状态为 `built-not-approved-for-installation`，
安装权限为关闭。固件包含设备专属授权材料，只保存在被精确忽略的本机交付路径；清单已核对
不包含设备授权值。

上述离线结果是建立唯一旋钮事实原文前的历史结果。由于一级窗口与系统设置必须按实际回调
分别解释方向，且链接进成品的旧导航实现尚未排除相似窗口入口误用，本候选当前审计失效。
重新完成第 6 节要求的 DESIGN、代码、反汇编和逐页物理验证前，不得复用该文件进行安装。

### 10.15 低栈原厂局部分支最小候选

`FW-AGENTS-006` 固定文件名为
`ap01-1.0.2_0031-agents-low-stack-local-branches-candidate.bin`。它从 SHA-256 为
`348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1` 的只读
`opt-setting.bin` 构建，代码提交为 `6e618bb59bc5cdd2cb92a482d1e4aec0590f7539`。

冻结成品长度为 6,804,520 字节，SHA-256 为
`09c5c77b77e464249c0f018fc7f34cbd624965e1b6aa6670430dd0baa63a9fa8`，MD5 为
`12abac7b56b5ff9e63423206c6666750`。载荷为 30,576 字节，固定候选空间剩余
30,358 字节；下载状态为 136 字节，单函数最大静态栈为 208 字节。两次独立构建逐字节一致，
73 项全项目测试通过，允许区间外变化为 0。

该成品仍是设备专属实验候选，安装许可保持关闭。完成云端上传并把同一对象完整回读到本机、
逐字节一致后，必须展示文件名、长度、上述两个指纹、目标设备状态、启动失败风险和已验收
回退材料，取得一次即时确认后才能下发正式安装。

当前云端上传对象已经完整回读 6,804,520 字节，回读文件与冻结成品逐字节一致，SHA-256 与
MD5 均匹配本节固定值。随后只下发一次正式安装，设备完成下载、安装、重启并重新上线，运行
计时重新开始。自动刷机检查到此结束；立即由用户肉眼确认是否离开开机动画，再单独测试看板
通信和旋钮，不用等待额外的所谓稳定观察时间。

`FW-AGENTS-006` 在功率一级页确认时出现整机重启，已否定并禁止再次安装。

### 10.16 功率路径隔离的局部界面版

`FW-AGENTS-007` 固定文件名为
`ap01-1.0.2_0031-agents-local-ui-power-safe.bin`。它从第 10.15 节相同的已验收
`opt-setting.bin` 构建，并遵守
[`2026-07-31 FW-AGENTS-006 功率页确认重启`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-006功率页确认重启.md)
第 4 节的隔离限制。

本版的 AGENTS 概览在用户交互上是位于萌宠与功率之间的一级页；按下概览后
进入周报、今日和近 30 天三个二级页，二级页旋转循环，按下返回概览。技术上继续
复用原厂萌宠对象、已筛选的萌宠三个局部分支和原厂切页函数，不新建一级窗口对象。
该复用边界的原厂事实只见
[`AGENTS 原厂键值回调局部分支静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-07-29-原厂键值回调局部分支静态定位.md)
与 [`原厂各页面物理旋钮交互实现`](../../knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md)。

本版明确不修改以下原厂内容：

1. 天气后台下载回调地址；
2. 天气后台网络调用；
3. 成功定时、随机周期和失败退避；
4. 天气请求地址与参数区；
5. 功率确认路径、一级总键值回调地址和界面定时回调地址。

因为本版不执行后台同步，四页先显示经指纹固定的内置页面；它不声称实现
`SPEC-DASHBOARD-002` 至 `SPEC-DASHBOARD-005` 的在线取数。只有功率确认、功率详情返回、
功率页左右导航以及 AGENTS 一级和二级交互逐项真机通过后，才能在下一轮 DESIGN
中重新引入后台同步。

构建时不链接 `result_loader.c` 和设备授权数据，改为只链接
`local_ui_loader.c`。该文件只向已存在的原厂萌宠对象确认使用内置页面，不打开文件、
不发起网络请求、不读取或写入后台同步状态。反汇编门禁必须证明载荷不包含
后台下载入口、网络包装入口、界面同步定时入口和设备授权符号；载荷二进制字符串门禁
必须另外证明不包含临时文件路径和设备授权值。检查器不得把位于 `/tmp/` 下的外部构建目录名
误当为载荷内容。

已由代码提交 `8b1e4e6` 完成构建。成品长度为 6,804,520 字节，SHA-256 为
`6de04bd13750ef4f8c6d076f01ade4cf1ac62bf4aeaf6ac648be6f26c0825f5e`，MD5 为
`24f4b8fa380b1fc74bfe14c9801ed986`。载荷为 27,772 字节，固定空间剩余 33,162 字节，
重定位为 0，局部界面帮助函数的静态栈为 0 字节。全项目 73 项自动测试通过；
两次独立构建的成品指纹一致并逐字节比较通过。清单证明功率确认路径、后台网络
全部指令与地址区、一级总键值回调地址和界面定时回调地址均与阶段输入一致；
载荷不包含后台三个入口、临时文件路径和设备授权数据。

首次安装时，云端对象已完整回读且与本节成品逐字节一致。设备接受第一次安装任务；下载期间
一次连接错误没有终止任务，后续进度继续增长。设备最终回到空闲状态，进度值为 101，运行
计时从 3,205 秒重置为 15 秒并重新上线。自动刷机检查到此结束。该结果只解除“尚未安装”
状态，不替代功率页和物理旋钮验收；完整过程只见
[`FW-AGENTS-007 首次安装`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-007首次安装.md)。

真机随后在功率一级页确认时卡住。设备仍在线，运行计时没有重置。该成品的 AGENTS 概览
右旋路径在恢复萌宠后直接调用原厂切页函数，没有回到原厂萌宠右旋继续地址；本方案已否定，
禁止再次安装。失败证据只见
[`FW-AGENTS-007 功率页确认卡住`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-007功率页确认卡住.md)。

### 10.17 恢复后交还原厂萌宠右旋分支

`FW-AGENTS-008` 保持第 10.16 节的内置页面、后台网络隔离、原厂萌宠对象复用和三个局部
挂接范围不变，只纠正 AGENTS 概览右旋离开路径。

当前方案按以下顺序实现：

1. 读取并确认当前独立状态为 AGENTS 概览；
2. 调用既有原厂萌宠恢复路径，把独立状态关闭并恢复原厂萌宠动图；
3. 恢复局部入口保存的寄存器和栈；
4. 恢复 `a0=一级窗口容器`、`a1=7`；
5. 直接返回原厂萌宠右旋继续地址 `0xa00bc716`，由原厂代码完成内部状态处理和后续切换；
6. 删除该路径对 `0xa00bfa4e` 的直接调用、切换结果复读和自定义公共退出。

实现落点固定为 `features/agents_dashboard_firmware/page_registration.S`、
`features/agents_dashboard_firmware/sync_build.py` 和同模块测试。反汇编门禁必须证明
AGENTS 概览右旋路径调用萌宠恢复后只能到达 `0xa00bc716`，且该路径不存在直接
`0xa00bfa4e` 调用。成功条件是自动测试、两次构建逐字节一致、安装重启上线及功率确认通过；
功率确认继续卡住时立即否定本方案，不继续测试其他页面。

当前代码已删除 AGENTS 概览右旋路径中的直接切页、切换结果复读和自定义公共退出，并复用
既有未消费事件恢复路径返回 `0xa00bc716`。旧 `FW-AGENTS-007` 构建入口已改为强制拒绝；
当前构建入口固定生成
`ap01-1.0.2_0031-agents-local-ui-stock-resume.bin`。局部 11 项和全项目 74 项自动测试通过，
反汇编门禁确认非组合载荷没有直接切页调用。

已由代码提交 `fac7ccf` 完成两次独立构建，成品逐字节一致。冻结文件长度为 6,804,520
字节，SHA-256 为 `8c5641c57eb5ade918450b535f11d8ab5f2a0d4527d9d7015c43a5fbc069ff6a`，
MD5 为 `ae4f9b764a5afd0908d9fe292739b91a`；载荷为 27,748 字节，固定空间剩余 33,186
字节。该结果只证明离线实现与本节方案一致，不替代真机功率确认。

首次安装未完成的证据只见
[`FW-AGENTS-008 首次安装未完成`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-008首次安装未完成.md)。
人工断电重启恢复后，同一冻结成品重新上传并完成全量回读，回读文件与冻结成品逐字节一致；
设备只接受一次安装任务，状态依次进入下载、安装中和已安装，运行时间从 777 秒重置为 8 秒
并重新上线。该结果证明 `FW-AGENTS-008` 已刷入，不替代启动画面和功率确认真机验收。完整
记录只见
[`FW-AGENTS-008 重新安装`](../../knowledge/AP01-官方固件分析/cases/2026-07-31-FW-AGENTS-008重新安装.md)。
用户随后肉眼确认设备已经离开开机动画并进入一级页面，默认显示时钟页；启动画面验收通过，
但继续旋转时无法找到原厂功率页。当前方案的物理导航验收失败，禁止再次安装；功率确认与
功率详情返回没有进入可执行状态。

### 10.18 刷前连续事件模拟门禁

`FW-SIM-001` 位于 `features/agents_dashboard_firmware/`。它只表达本功能已经由 SPEC 和原厂
旋钮事实确定的交互规则，不重新定义原厂键值方向。原厂页面顺序、各回调分支及继续地址只引用
[`原厂各页面物理旋钮交互实现`](../../knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md)。

模拟输入固定包括：原厂分派序号、AGENTS 独立状态、可见页面、页面启用组合、已经筛选后的
局部旋钮事件，以及构建器从最终成品反汇编和构建清单取得的实际分支合同。每一步输出事件序号、
事件、前后分派序号、前后 AGENTS 状态、实际动作、继续地址和最终可见页面。

刷前必须完成下列场景：

1. 从每个原厂一级页面分别连续左旋和右旋，证明全部启用的原厂页面均可到达并回到起点；
2. 萌宠、AGENTS 概览和功率之间的两个方向，以及 AGENTS 关闭和萌宠关闭的组合；
3. 概览确认进入周剩余额度，三个详情双向首尾循环，任一详情确认返回概览；
4. 基座在线且功率数据有效时，功率确认必须回到原厂功率流程；基座离线、功率数据失效或
   功率页残留时，必须拒绝进入详情并复用原厂离线切页入口回到时钟；
5. 连续同向、快速反转、损坏状态尾和切换失败恢复；
6. 对限定长度内的全部左旋、右旋和确认序列穷举，任何一步不得出现未知状态或一次事件产生
   两次可见页面迁移。

凡是自定义分支只返回原厂继续地址，但现有静态证据和模拟合同不能确定该地址执行后的最终可见
页面，报告必须写明“最终页面未解析”并失败。不得用预期页面、相邻页或过去真机现象补值。
构建入口必须先运行严格模拟；任一断言失败时，在创建成品和构建清单之前停止。

本门禁不执行 AP01 处理器指令，不模拟界面库内存、线程、定时、显示硬件或真实机械波形，不能
替代真机验收。当前环境没有已经验证的 AP01 处理器与整板执行模型，因此指令级和整板级模拟仍
是明确阻塞项；报告不得把本层通过写成固件可刷、物理旋钮通过或功率详情通过。

实现落点为 `features/agents_dashboard_firmware/interaction_simulator.py`、同模块测试和
`app/ap01_firmware.py` 的 `agents-interaction-simulate` 命令。构建器在载荷及调用链检查完成
后、成品写出前调用同一模拟核心。当前冻结清单的模拟报告执行 16 种页面启用组合、38 个定向
场景和 3,630 条深度 5 穷举序列；概览进入详情、三详情双向循环、详情返回和功率确认隔离轨迹
可解析，但概览右旋返回 `0xa00bc716` 后的最终页面无法解析，反向进入 AGENTS 及萌宠关闭组合
也缺少当前成品挂接证据，所以报告结论为失败，禁止构建。

### 10.19 原厂收尾顺序的双向局部候选

`FW-AGENTS-009` 只纠正第 10.17 节已被真机否定的页面生命周期顺序。它不修改键值
映射、底层方向过滤、功率确认、一级总回调注册、页面创建和后台网络路径。原厂
直接证据只见
[`2026-08-01 原厂功率与萌宠退出收尾静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-原厂功率与萌宠退出收尾静态定位.md)，
键值、回调和原厂入口的唯一原文继续只见
[`AP01 原厂各页面物理旋钮交互实现`](../../knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md)。

候选固定使用四个局部点：

| 局部点 | 用途 | 未消费时的原厂出口 |
| --- | --- | --- |
| `0xa00bc460` | 萌宠左旋：AGENTS 概览恢复萌宠，详情反向循环 | `0xa00bc464` |
| `0xa00bc712` | 萌宠右旋：萌宠进入 AGENTS，概览离开时只关闭新增状态 | `0xa00bc716` |
| `0xa00bda64` | 萌宠确认：概览与三个详情互相进入和返回 | `0xa00bda68` |
| `0xa00bc092` | 原厂各页面专用处理完成后，在共享序号 `7` 上选择萌宠或 AGENTS 画面 | 原参数调用 `0xa00bfa4e` |

实现顺序固定为：

1. 萌宠右旋且 AGENTS 关闭时，复用既有动图对象显示概览并消费事件；
2. AGENTS 概览右旋时，只把新增状态尾写为关闭，不调用动图数据源恢复；
3. 恢复 `a0=一级容器`、`a1=7` 后返回 `0xa00bc716`，使原厂萌宠收尾面对事件
   开始时实际显示的动图状态；
4. 功率页向共享序号 `7` 移动时，必须先完成 `0xa00bc33a` 之后的全部原厂处理；
5. 只在原厂到达 `0xa00bc092` 时，对“目标序号 `7` 且方向 `2`”显示 AGENTS 概览；
6. 对“目标序号 `7` 且方向 `1`”恢复原厂萌宠画面；其他目标和方向不改参数；
7. 最终始终调用 `0xa00bfa4e`；显示 AGENTS 后还要用 `0xa00be388` 确认结果为
   序号 `7`，失败时恢复原厂萌宠并关闭新增状态。

本版只使用固定全开的萌宠与 AGENTS 组合验证导航生命周期，不链接页面开关设置对象、
持久化和后台同步。因此严格模拟只能对本成品实际固定配置放行；页面关闭组合继续属于
`FW-PAGE-003` 和最终组合的阻塞项，不能由本版冒充已实现。

构建和模拟必须从成品反汇编提取以下合同，不允许手写预期值代替：

1. 三个萌宠局部挂接和一个统一切页挂接全部存在；
2. 概览右旋路径调用“只关闭状态”帮助入口，且在返回 `0xa00bc716` 前不调用
   `0xa00cf8d8`；
3. 切页过滤只在目标 `7` 上可改显示状态，并只调用原厂 `0xa00bfa4e`；
4. `0xa00bc336` 及功率确认区间与 `opt-setting.bin` 逐字节一致；
5. 严格模拟至少覆盖时钟、日期、天气、设置、萌宠、AGENTS 概览、三个详情、功率确认、
   连续同向和快速反转；每一步都有唯一最终页面。

上述模拟失败时不生成成品。模拟通过后按已确定的刷机门禁直接完成上传全量回读
和一次安装，不再请求第二次确认。真机只验启动画面、原厂功率到达与确认、双向进出
AGENTS 和详情循环；任一项失败时否定 `FW-AGENTS-009`，不重复安装。

实现提交 `7381d7a` 已完成上述代码、反汇编门禁和一次安装放行。成品载荷为
`27,928` 字节，固定空间剩余 `33,006` 字节；全项目 `87` 项自动测试通过，
严格交互模拟失败为 `0`，两次独立构建逐字节一致。完整证据只见
[`FW-AGENTS-009 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-009离线构建.md)。
上传、完整回读、一次安装、重启和重新上线的实测证据只见
[`FW-AGENTS-009 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-009安装.md)。
用户物理验收仍找不到功率页，因此本方案已否定并禁止再次安装；失败证据只见
[`FW-AGENTS-009 功率页仍未找到`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-009功率页仍未找到.md)。

### 10.20 基座生命周期与功率确认局部保护

`FW-AGENTS-010` 复用第 10.19 节已经实现的三个萌宠局部分支和统一切页点，不恢复任何
已否定的总回调、直接切页或后台网络改写。新增保护的原厂事实只见
[`2026-08-01 基座离线功率页残留与确认路径静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-基座离线功率页残留与确认路径静态定位.md)，
旋钮入口和页面分派继续只引用
[`原厂各页面物理旋钮交互实现`](../../knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md)。

新增挂接固定为：

| 项目 | 固定值 |
| --- | --- |
| 已筛选局部入口 | `0xa00bc8c6`，文件偏移 `0x0bd8c6` |
| 原字节 | `e3 16 0a 90` |
| 近跳板 | 文件偏移 `0x01c0dc`，原始 8 字节全零 |
| 有效状态原厂继续地址 | `0xa00bc8ca` |
| 无效状态公共出口 | `0xa00bc1d2` |
| 原厂设备端口数 | `0x62fc4cd8` |
| 原厂功率汇总数据指针 | `0x62fcaa00` |
| 原厂离线切页入口 | `0xa0193862`，目标序号固定为时钟 `3` |

局部保护必须按以下顺序执行：

1. 如果进入时的原厂交互分派序号不是 `0`，直接返回 `0xa00bc1d2`，复现被替换指令原行为；
2. 读取 `0x62fc4cd8`；值为零时进入无效状态处理；
3. 读取 `0x62fcaa00`；指针为空时进入无效状态处理；
4. 两项均有效时不修改原厂对象、状态或数据，直接返回 `0xa00bc8ca`；
5. 无效状态先复用 `0xa00b16a4` 把设备端口数写为零，再以一级容器和目标序号 `3` 调用
   `0xa0193862`，最后返回 `0xa00bc1d2`；
6. 保护代码保存并恢复返回地址，不写原厂功率对象、萌宠状态尾或一级窗口字段。

构建器必须逐字节断言挂接原字节和近跳板全零；最终成品中，功率确认区间只允许
`0x0bd8c6..0x0bd8ca` 发生变化，其余字节保持阶段输入一致。载荷反汇编必须同时证明保护入口
只调用 `0xa00b16a4` 与 `0xa0193862`，有效分支只返回 `0xa00bc8ca`，无效分支只返回
`0xa00bc1d2`。

严格模拟在现有一级循环、AGENTS 概览和三个详情场景外，新增四类状态：

1. 基座在线、功率数据有效：功率页参与一级循环，按下进入原厂功率详情；
2. 基座离线：功率页不参与一级循环，时钟为默认模拟起点；
3. 基座刚离线但功率页仍残留：按下只回时钟，不进入原厂功率详情；
4. 端口数非零但功率数据已经失效：按下只回时钟，并把后续模拟状态改为离线。

输出名固定为 `ap01-1.0.2_0031-agents-base-safe.bin`，清单类型固定为
`agents-local-ui-base-safe-firmware`。在代码、测试、反汇编、两次确定性构建和上传对象完整
回读全部通过前，不得安装。当前提交前离线验证已经通过，但临时审计文件不作为刷机输入；必须
从已提交代码完成两次正式冻结构建。正式离线门禁通过后只取得专用测试设备的一次自动实验安装
资格，不取得发布资格。

### 10.21 真实数据后台同步恢复

`FW-AGENTS-011` 只解决 `SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-005` 在当前真机版未接入的
真实数据刷新。它完整继承第 10.20 节的三个萌宠局部分支、统一切页点、
独立状态尾和功率确认连接保护。复用证据与能力边界只见
[`2026-08-01 AGENTS 真实数据同步复用复查`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-AGENTS真实数据同步复用复查.md)。

本轮只允许以下新增修改：

1. 链接 `result_loader.c`，恢复 64 字节包头、逐页 CRC-32 损坏检查和三组
   `/tmp` 临时槽；
2. 把原厂天气下载回调、网络执行调用与五分钟成功计时指向既有
   低内存后台入口；
3. 两个原厂天气地址都固定为 `http://192.168.31.174:18765/a`，两个格式区
   只输出该地址；
4. 服务启动时直接采集、渲染和发布四页包；服务和固件均不创建、读取或复制
   设备共享配置；
5. 进入 AGENTS 概览或切换三个详情时，先显示内置等待页，再读取已完整
   提交的当前槽；没有完整包或页面加载失败时继续显示内置页，不修改导航状态。

禁止修改一级总回调、页面注册点、设置列表、原厂萌宠字段、功率对象、
功率详情与旋钮事件内同步网络请求。后台下载仍由原厂天气任务执行，
界面事件只读取已提交结果。

构建门禁必须新增证明：

- 新成品同时包含后台流式入口和第 10.20 节功率保护；
- 下载状态固定为 136 字节，单函数静态栈不超过 320 字节；
- 载荷不含设备代号、访问标识、秘密值、共享配置路径或配置对象；
- 原厂键值总回调和页面注册保持阶段输入原字节；
- 功率确认区除已批准的局部挂接外不得增加差异；
- 严格连续事件模拟必须继续通过第 10.20 节四类基座状态；
- 服务端新包、旧包、截断包、损坏页和无包全部通过自动测试。

输出名固定为 `ap01-1.0.2_0031-agents-live-data-base-safe.bin`，清单类型固定为
`agents-live-data-base-safe-firmware`。代码提交并同步远端后，从该提交进行两次独立构建。
全量测试、严格模拟、两次确定性构建、服务端实时取包、云端全量回读和
设备空闲全部通过后，按既定门禁自动安装一次，不再向用户请求二次确认。

本方案安装后设备运行 `369` 秒仍没有向服务发起请求，已由真机证据否定。上述文件名和清单
类型禁止再次生成或安装；失败原文只见
[`FW-AGENTS-011 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-011安装后未取数.md)。

### 10.22 完整复用成功参考路径

`FW-AGENTS-012` 继承第 10.20 节全部页面、旋钮局部分支、共享序号切页过滤和功率确认保护，
并继承第 10.21 节的 64 字节四页包、三槽提交、固定局域网地址和低内存后台入口。只允许以下
纠偏：

1. 两个原厂天气格式区都严格写入 `%s`，与参考项目成功实现逐字节同义；
2. 把原厂界面定时回调地址的高低两段装入指令改到 `ap01_agents_ui_timer_wrapper`；
3. 包装入口必须先调用原厂界面定时回调，再读取当前分派序号；只有共享序号为 7、萌宠对象
   链有效且 AGENTS 独立状态有效时，才读取并应用已经完整提交的结果；
4. 原厂页面或萌宠状态下只执行原厂回调，不能读取结果包或改变页面；
5. 进入概览和三个详情时继续保留主动应用入口，因此定时包装失败不阻塞旋钮事件返回。

两个天气格式和界面定时包装作为参考项目已成功路径整体复用；由于当前没有设备端后台日志，
不得把其中任一处单独写成 `FW-AGENTS-011` 的已证实根因。

构建门禁必须证明：

- 两个格式区有效内容均严格等于 `%s`；
- 界面定时回调地址精确指向新包装入口，反汇编证明原厂回调调用位于所有 AGENTS 判断之前；
- 一级键值总回调和页面注册保持阶段输入原字节；
- 后台传输、功率确认保护、共享序号过滤和连续事件模拟同时通过；
- 失败的 `FW-AGENTS-011` 文件名和清单类型不能由新入口生成。

输出名固定为 `ap01-1.0.2_0031-agents-live-data-reference-complete.bin`，清单类型固定为
`agents-live-data-reference-complete-firmware`，交互合同固定为 `FW-AGENTS-012`。完成代码提交、
全量测试、独立模拟、两次确定性构建、服务端验证和云端全量回读后，自动安装一次。安装后只
等待一个固定 300 秒取包周期；请求仍未到达则立即否定，不继续增加等待时间。

本方案安装后设备运行到 `328` 秒仍没有向服务发起请求；本地单播和 AP01 到电脑的受控出站
数据包均通过，因此不是当次局域网出站阻断。该方案已经否定，禁止再次生成或安装；失败原文
只见 [`FW-AGENTS-012 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-012安装后未取数.md)。

### 10.23 后台下载状态移出原厂任务栈

`FW-AGENTS-013` 完整继承第 10.22 节的请求格式、原厂调用点、界面定时包装、64 字节四页包、
三槽提交、页面交互和功率保护，不改变服务协议。当前仅能确认成功参考实现的后台包装入口静态
栈为 `96` 字节，而 `FW-AGENTS-012` 为 `208` 字节；没有异常地址或原厂任务栈余量记录，
因此这只是需要隔离验证的剩余差异。

本方案只允许以下修改：

1. 通过原厂已经链接并用于萌宠页面对象的 `malloc` 和 `free` 入口申请、释放 `136` 字节
   下载状态；
2. 后台包装入口只在申请成功后读取三槽记录并调用原厂网络执行入口，申请失败时直接返回且不
   发起请求；
3. 网络执行返回、文件关闭失败、响应状态失败、包校验失败和提交成功的所有出口都必须清空
   原上下文参数并恰好释放一次；
4. 下载回调的状态字段、逐页校验、临时文件路径和提交顺序保持不变；
5. 不增加静态可写地址，不改变一级回调、旋钮局部分支、页面注册、功率保护或界面定时包装。

构建门禁必须证明后台包装入口静态栈不高于参考实现的 `96` 字节，并用主机替身覆盖申请失败、
网络失败、包损坏和成功提交四类释放路径。输出名固定为
`ap01-1.0.2_0031-agents-live-data-low-stack.bin`，清单类型固定为
`agents-live-data-low-stack-firmware`，交互合同固定为 `FW-AGENTS-013`。旧
`FW-AGENTS-012` 入口必须拒绝构建。代码提交、全量测试、独立模拟、两次确定性构建、服务端
验证和云端全量回读通过后自动安装一次；安装后仍只等待一个固定 `300` 秒取数周期。

本方案的后台包装入口静态栈实测为 `64` 字节，但安装后设备运行到 `307` 秒仍没有请求。
因此方案已经否定，禁止再次生成或安装；失败原文只见
[`FW-AGENTS-013 安装后未取数`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-013安装后未取数.md)。

### 10.24 参考项目原版传输探针

`FW-TRANSPORT-PROBE-001` 只回答“参考项目已经成功的原厂天气任务复用，在当前设备和当前
网络上是否仍会发起请求”。它直接使用 `/Users/mac/Desktop/cuktech-screen-controller` 当前
远端 `main` 的 `ap01_custom_ota.py`、`ap01_realtime_patch.py` 和 `ap01_wifi_bridge.py`，不得
把实现复制到本项目后再改写。

探针必须满足：

1. 从实时查询得到的官方 `1.0.2_0031` 生成单张兼容动图基线，再注入参考项目原版单页
   加载器；
2. 使用电脑当前地址和独立端口，服务只提供符合参考加载器实际校验的 `320×240` 动图；
3. 不链接本项目四页解析、AGENTS 页面、旋钮局部分支、共享序号过滤或功率确认保护；
4. 参考项目测试、两次确定性构建、全量回读和设备空闲通过后自动安装一次；
5. 安装后只以服务日志中来自 AP01 地址的真实请求为成功，不以电脑访问自身代替；
6. 探针成功后立即先更新 DESIGN，再把其已执行载荷和触发字节作为下一 AGENTS 方案基线；
   探针失败则先检查当前原厂天气任务前置条件，不再改四页解析器。

探针不是功能候选，不执行 AGENTS 屏幕和旋钮验收，也不能写成真实看板已完成。

该探针已完成上传全量回读、单次安装、重启和固定请求窗口，服务没有收到
来自 AP01 的请求。因此本项目四页解析和界面代码已排除为“请求根本没有创建”的
原因；安装原文只见
[`FW-TRANSPORT-PROBE-001 安装后未请求`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-TRANSPORT-PROBE-001安装后未请求.md)。

### 10.25 解除看板取包对天气位置的依赖

`FW-AGENTS-014` 完整继承第 10.23 节的四页包、三槽提交、低栈包装、界面定时包装、
局部旋钮分支、共享序号过滤和功率确认保护。本节新增的原厂直接证据只见
[`2026-08-01 原厂天气任务首次取数前置条件静态定位`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-原厂天气任务首次取数前置条件静态定位.md)。

当前设备没有可用的位置编号，城市后备字段也为空；原厂天气任务因此在网络上下文
创建之前持续进入失败重试。AGENTS 局域网包本身不使用天气位置，因此实现只允许：

1. 保留 `0xa00b13fe -> 0xa01aa812 -> 0xa00b6ca6` 的原厂首次调度和自调度链；
2. 把 `0xa00b6dd4` 的位置取得调用指向新增占位入口；
3. 占位入口对原参数写入九位数字文本和终止字节，然后返回原厂成功值 `1`；
4. 位置取得挂接使用独立近跳板，不与网络执行、旋钮、页面或功率保护共用改写区间；
5. 两个原厂请求格式继续严格等于 `%s`，因此占位文本不会发送到服务；
6. 原厂网络上下文、执行入口、成功周期和失败重试不重写。

构建门禁必须逐字节证明原调用、近跳板和新入口地址；反汇编必须证明新入口只写入
`10` 个字节并返回 `1`，不调用其他固件入口。主机测试必须覆盖空指针和正常缓冲区；
空指针返回 `0`，不允许进入原厂成功分支。

输出名固定为 `ap01-1.0.2_0031-agents-live-data-location-independent.bin`，清单类型固定为
`agents-live-data-location-independent-firmware`，交互合同固定为 `FW-AGENTS-014`。代码提交并
同步远程后，从该提交完成全量测试、严格交互模拟、两次确定性构建、服务端四页包校验和
云端全量回读，随后直接安装一次。设备请求成功标准固定为服务收到来自 AP01 的 `/a`
请求，并且四页包全部发送完整；不以本机自读或仅安装重启代替。

该成品已完成云端全量回读和一次安装，服务随后两次收到来自 AP01 的 `/a` 请求，
两次精确相隔 `300` 秒，且四个数据源全部正常。首次取包和定时刷新门禁已通过；
屏幕显示和物理旋钮仍按第 11 节独立验收。
安装原文只见
[`FW-AGENTS-014 安装与真实取包`](../../knowledge/AP01-官方固件分析/cases/2026-08-01-FW-AGENTS-014安装与真实取包.md)。

## 11. 构建与验证

### 11.1 离线验证

每个成品必须通过：

1. 原厂基线身份检查；
2. 文件头安全字段和文件尾结构未发生非预期变化；
3. 修改前旧字节检查；
4. 新增载荷符号、重定位、跳转范围和调用约定检查；
5. 页面表和设置表边界测试；
6. 旋钮唤醒、一级循环、详情循环和设置循环测试；
7. 两份设置记录的损坏、旧版本和断电模拟测试；
8. 四页结果缺一、截断、串代、指纹错误和无查询字段取包测试；
9. 服务端顺序、超时、恢复优先级和全部失败测试；
10. 文件头、总长度、型号、版本和文件尾恢复校验；
11. 修改区间外逐字节一致；
12. 禁止修改区域无差异；
13. 构建清单完整；
14. 冻结成品重新读取后的完整文件指纹一致。
15. 第 10.18 节连续事件模拟通过，且报告不存在“最终页面未解析”；报告随构建清单保存。
16. 第 10.20 节的功率确认挂接、有效继续、无效切页、基座状态和功率数据状态全部通过。

### 11.2 恢复门禁验证

进入任何优化固件安装前，必须按
[`AP01 分级恢复与救砖操作规程`](../SOP/AP01-分级恢复与救砖.md) 完成：

1. 原厂状态下同机 R0 回刷；
2. AP01 主板、芯片、测试点和信号电平记录；
3. 同机两份逐字节一致的全闪存备份；
4. AP01 真实分区图；
5. 应用没有运行时进入 R2、R3 或 R4 中至少一条通道；
6. 只对已确认应用槽恢复同机原厂内容；
7. 写后读回一致、原厂启动和全部基础能力验收。

上述证据必须指向具体设备、硬件批次、工具版本、备份文件指纹和案例记录。只有只读备份而
没有写回启动演练，仍判定不通过。

### 11.3 分阶段实验成品

恢复门禁通过后，真机实验仍按最小变化拆分：

1. 先安装匹配原厂应用更新包，固定 R0 基线；
2. 第一份实验成品只加入一个不涉及后台网络、持久化和启动早期的可见变化；
3. 验证启动、联网、米家通信、官方更新和该变化后，回刷原厂；
4. 后续每份成品只新增一个可独立验收的模块；
5. 页面开关、持久化、看板页面、后台刷新和设置循环分别进入不同实验阶段；
6. 任一阶段出现重启、持续内存增长、联网异常或更新异常，停止合并后续模块并回到原厂；
7. 最终组合成品重新执行全部离线检查、独立恢复门禁、安装验收和原厂回刷。

不得用一份同时引入全部未知能力的成品完成首次实验。

### 11.4 真机验证顺序

真机阶段固定顺序如下：

1. 核对第 11.2 节恢复证据仍与当前设备和工具匹配；
2. 查询并保存当前槽、更新槽和启动成功状态；
3. 运行与冻结成品绑定的严格刷前交互模拟；
4. 模拟通过后自动上传冻结成品并完整下载回本机，逐字节比对；
5. 客观门禁保持匹配时自动在指定测试设备安装一次；
6. 验证重启、重新上线、实际启动槽和原厂基础能力；
7. 功率验收前记录基座已经吸附并供电，且功率页正在显示实时数据；随后验证功率详情进入与
   返回；再取下基座，验证离线后功率页不可达，残留窗口内按下只回时钟；
8. 完成 `SPEC-FIRMWARE-004`、`SPEC-PAGE-001` 至 `SPEC-PAGE-007`、
   `SPEC-DASHBOARD-001` 至 `SPEC-DASHBOARD-033`、`SPEC-ROTARY-001` 至
   `SPEC-ROTARY-008` 的全部功能验收；
9. 上传冻结的匹配原厂应用更新包并完整回读；
10. 取得回退即时确认后回刷原厂应用更新包；
11. 验证重启、重新上线、原厂页面、米家通信和官方更新；
12. 再决定是否允许发布。

第 1～2 步和第 8～10 步没有完整证据时，不得声称本固件满足“不变砖、可刷回原厂”。

## 12. 恢复实现边界

### 12.1 R0 回刷

R0 复用已经真机证明可到达设备的米家在线更新通道，不另造传输协议。实现必须补齐第 4.3
节的完整文件回读、全文件指纹、槽证据、单次安装和完整验收门禁。

R0 依赖原厂应用仍能启动、联网并响应更新，不能作为首次实验安装所需的独立恢复通道。

### 12.2 R1 至 R4

R1 至 R4 当前只进入证据记录和门禁模块，不生成自动写入命令：

- R1 记录 AP01 真机实际回退条件、次数、槽和启动成功标记；
- R2 记录 AP01 下载、复位、串口、电平和只读识别结果；
- R3 记录 AP01 调试焊盘、映射、锁定状态和只读结果；
- R4 记录实体闪存、在电路读取条件、拆焊要求和同机备份。

只有对应层级在专用测试设备上完成恢复演练后，才允许把冻结的接线、工具、地址范围和停止
条件写入操作规程。工具界面提供写入或擦除功能，不代表本项目可以调用。

### 12.3 故障处理

安装状态未知时，安装器只查询，不重复下发。设备离线后，安装器停止所有在线写入并生成
故障现场记录，不自动尝试断电、切槽、硬件连接或整片恢复。

恢复成功标准与准确发布措辞只见
[`AP01 离线恢复与救砖证据`](../../knowledge/AP01-官方固件分析/离线恢复与救砖证据.md)
第 8、9 节。

## 13. 当前实现阻塞项

| 阻塞项 | 需要的事实证据 | 解除后维护位置 |
| --- | --- | --- |
| AP01 下载与调试接口 | 主板焊盘、信号、电平、进入和退出方法 | 知识文档、SOP |
| 同机全闪存备份 | 真实容量、两次一致读取和两个独立副本 | 知识文档、真机案例 |
| 应用包到闪存的映射 | `BFNP` 内部组成、文件偏移到物理地址的转换和文件尾处理 | 知识文档、本文第 4、12 节 |
| AP01 分区与槽 | 两份分区表、当前槽、更新槽、应用槽和保留区地址 | 本文第 4、11、12 节 |
| 第二阶段启动与自动回退 | AP01 使用版本、配置、重试次数、回退和成功标记 | 知识文档、本文第 12 节 |
| 一次性配置状态 | 只读结果及安全启动、防降级和调试状态 | 知识文档 |
| 独立恢复通道 | 应用不运行时写回同机原厂并成功启动 | `固件制作规范.md`、SOP、真机案例 |
| 独立持久化记录 | `/data` 空间、原子改名、断电测试 | 本文第 5、9 节 |
| AGENTS 独立界面状态 | 20 字节申请、偏移 16 状态尾部、销毁重建和申请失败的真机结果 | 本文第 6.1、10.11 节 |
| 功率数据并发释放边界 | 端口离线、功率汇总数据释放与界面确认之间的线程互斥证据 | 知识文档、本文第 10.20 节、真机案例 |
| 四页结果存储 | 空间、读写寿命、解码峰值 | 本文第 7～10 节 |
| 新增加密通信 | 请求签名和服务器证书校验实测 | 本文第 8 节 |
| 最终成品名称冲突 | 历史失败成品改名、固件清单更新和全部引用核对 | 本文第 4.1、10.4 节 |
| 原厂回刷 | 同一指定设备完整回刷证据 | `固件制作规范.md`、SOP、真机案例 |
| 同版本与降级策略 | 云端、应用和启动链对匹配原厂包的实测接受结果 | 知识文档、SOP、真机案例 |

解除任一阻塞项时，必须先补证据文档，再更新本 DESIGN，随后才能写对应实现。

## 14. 下一轮实现顺序

本节是当前允许继续实现的顺序。任一步失败都按
[`design.md`](../design.md) 第 2 节先更新纠偏记录，再拟定下一方案。

### 14.1 当前材料复核

1. 核对仓库跟踪的原厂基线、`opt-setting.bin`、原厂四个局部分支观察成品和各自构建清单；
2. 在本机私有工作目录为三种固件分别生成只读副本，复制前后重新计算长度和两个完整文件指纹；
3. 构建与真实材料测试只接收只读副本；仓库内版本化来源保持可写时仍必须被现有门禁拒绝；
4. 按当前提交重新运行全部现有自动测试；
5. 重建阶段成品与局部分支观察成品，比较完整文件指纹、修改区间和修改区间外字节；
6. 核对编译工具、字体、动图整理工具和独立解码工具仍满足 `requirements.md`；
7. 任一结果与已记录指纹不同，先把差异写入本 DESIGN，不进入真机。

只读副本准备能力归入 `core/firmware_image/`：它只负责复制、权限、长度、指纹和来源记录，不
包含 AP01 业务规则。具体固件身份仍由现有基线定义和各功能构建门禁判断。测试使用临时只读
副本，不再假设版本控制可以保存跨系统只读权限。

实现合同固定为：

1. `core/firmware_image/material.py` 提供 `prepare_read_only_copy` 公开能力；
2. 调用方必须给出预期字节数、SHA-256 和可选 MD5，复制前后分别核对；
3. 默认工作目录为被版本控制忽略的
   `env/firmware-inputs/<SHA-256>/<原文件名>`，目录只允许当前用户访问；
4. 先写同目录临时文件，写完同步到磁盘、复核指纹、设为只读，再以原子替换形成目标；
5. 目标已存在时，只有身份和只读状态全部匹配才复用；任何不一致都停止，不覆盖未知文件；
6. `app/ap01_firmware.py` 只增加参数转换和结果打印，不保存第二套身份规则；
7. 自动测试分别覆盖正确复制、错误指纹、可写目标、已存在未知文件和复制后只读。

冻结构建还必须从当前提交建立独立干净工作区。该工作区只接收当前提交跟踪的代码与固定资源，
以及从原工作区显式提供的被忽略字体；不得把原工作区未提交文件复制进去。构建
前后分别核对原工作区六个看板成品的状态，证明没有读取后改写、暂存或覆盖它们。

### 14.2 `FW-AGENTS-010` 基座生命周期保护候选

当前先按第 10.20 节完成唯一新增的功率确认局部保护：

1. 扩展载荷、链接入口和构建器，只允许固定原字节、固定近跳板、两个原厂继续地址和两个原厂
   复用调用；
2. 扩展连续事件模拟，把基座连接与功率数据有效性作为独立状态，离线一级循环不得要求功率页；
3. 新增损坏原字节、占用近跳板、错误继续地址、错误调用地址、功率区间额外变化和模拟缺失的
   拒绝测试；
4. 运行全项目测试并完成两次独立构建；两份成品和清单中的载荷、修改区间与完整文件指纹必须
   一致；
5. 全部门禁通过后，按《固件制作规范》第 7.1 节对当前唯一测试设备自动上传、完整回读并安装
   一次；不再请求第二次确认；
6. 安装后只做重启、上线和更新空闲核对，不进行长时间观察；物理验收按第 11.4 节记录基座
   状态后执行。

任一离线门禁失败时不得生成成品；真机仍出现功率不可达、功率确认卡住或原厂页面回归时，先
写入本 DESIGN 的纠偏记录，再进入下一节观测候选，不在同一成品继续叠加猜测性修改。

### 14.3 `FW-AGENTS-005` 观测候选

写代码前必须在本节补齐观测载荷的精确挂接地址、原字节、缓冲区位置、长度上限、导出方式和
清理时机。当前拟定的单次事件记录只包含：

1. 单调递增的事件序号；
2. 原厂回调收到的原始键值；
3. 当前方向过滤结果；
4. 当前原厂一级页面对象或可证明的页面状态；
5. AGENTS 状态尾部当前值；
6. 实际进入的局部分支；
7. 分支调用前后的可证明目标状态。

先做主机侧边界和编码测试，再生成只增加观测、不改变分支目标的专用候选。没有独立恢复门禁
和当次安装授权时，只完成离线构建，不安装。

### 14.4 物理证据到分支修正

只有同一冻结观测成品取得下列一组完整记录，才允许拟定分支修正方案：

1. 从可见 AGENTS 概览开始的单格左旋；
2. 从可见 AGENTS 概览开始的单格右旋；
3. 从功率、时钟或萌宠原厂页分别进入相邻页；
4. 同一方向连续两格；
5. 快速反转后下一完整格；
6. 每组操作的事件记录、可见画面、时间和设备身份。

证据只证明键值解释错误时，修改键值到局部分支的映射；证据只证明活动状态不同步时，修改状态
同步；两者都不能证明时继续观测，不同时改两个变量。

### 14.5 `FW-PAGE-003` 四段候选

AGENTS 导航通过后，页面开关按下列四个独立成品推进：

1. 只读挂接：设置列表增加入口但不创建开关对象、不保存、不改变导航；
2. 设置对象：进入开关页、七项首尾循环和复选显示，但退出后丢弃变化；
3. 独立保存：两份记录轮换、断电、损坏、旧版本和首次默认值；
4. 导航生效：保存成功后立即更新候选过滤，关闭当前页时只在下一次旋转寻找目标。

每一段分别执行启动、联网、米家通信、官方更新、全部原厂页面和设置二级菜单回归。前一段没有
真机通过，不得进入后一段或组合完整页面开关。

#### 14.5.1 已否定方案 `FW-PAGE-003-A`

本轮只实施第一段“只读挂接”。该名称只表示新增入口不能读取、修改或保存六个页面开关值；
固件仍会在原厂设置列表中创建一行普通项目。

原厂创建入口、标签表容量、行对象所有权、状态编号和回调边界只见
[`设置列表第八项容量与生命周期静态复查`](../../knowledge/AP01-官方固件分析/cases/2026-07-30-设置列表第八项容量与生命周期静态复查.md)
第 2 至 6 节；物理键值和设置回调方向只见
[`原厂各页面物理旋钮交互实现`](../../knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md)
第 5、6 节。历史空挂接只作为原分支可跳出并返回的真机证据，不能作为本候选代码输入。

输入固定为本机私有只读 `opt-setting.bin`：长度 `6,804,520` 字节，SHA-256 为
`348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1`，MD5 为
`13a7286f4824b1ad87d9bc32f1d3d39c`。输出固定为
`ap01-1.0.2_0031-page-settings-read-only-entry.bin`。

当前允许修改的实现只有：

1. 先按第 10.1 节固定工具、参数和双解码门禁无损整理原厂第一张动图，只使用
   `0x2475d0` 起的已证明连续载荷空间；原资源指针、帧像素、时序和其他资源不变；
2. 把文件偏移 `0x1999b4` 的设置循环上限从原厂七项改为八项；
3. 把文件偏移 `0x1999dc` 的原厂返回判断挂到新增分发入口；序号 0～5 原样回到
   `0xa01989e0`，序号 6 只调用一次原厂普通行创建入口 `0xa0192fb4`，序号 7 回到原厂返回行
   入口 `0xa0198ad0`；
4. 只在文件偏移 `0x0be8cc`、`0x0be8d4` 把系统设置已注册回调指向本候选包装；包装内只补
   状态 `54→55`、`55→54` 两条相邻选择，复用 `0xa00c6dfe` 和 `0xa00f3d5a`；
5. 状态 `54` 确认时消费事件但不创建对象、不读写文件、不改变一级导航；状态 `55` 确认时
   先恢复原厂返回状态 `54`，再以原事件尾跳 `0xa00f8096`；其余事件全部原参数尾跳原厂；
6. 新增载荷只包含普通行分发、最小设置回调包装、实际列表查找和“开关一级页面”标签；禁止
   链接对象创建与删除、内存申请与释放、页面开关资源、持久化、一级页面过滤、AGENTS 或网络
   入口。

实现归入 `features/primary_page_settings/`，应用命令只转换路径、工具和结果。该功能依赖
`core/firmware_image/` 的固件身份与差异校验，以及 `core/firmware_payload_space/` 的固定载荷
空间，不在底层模块写入页面业务规则。当前实现文件固定为 `read_only_entry.S`、
`read_only_entry.ld`、`read_only_entry.py` 和 `test_read_only_entry.py`；应用命令为
`page-settings-read-only-entry-build`。

刷前门禁固定为：

1. 输入身份和只读权限完全匹配；无损动图固定指纹、逐帧像素与时序、独立解码结果全部匹配；
   四个原字节位置逐项匹配；
2. 反汇编证明序号 0～5 不变、序号 6 不读六项标签表、序号 7 仍进入原厂返回行；
3. 反汇编证明设置回调只调用本文批准的原厂入口，未处理事件尾跳原厂回调；
4. 严格设置事件模拟覆盖八个位置的左右边界、连续同向、快速反转后的下一格、在新增行重复
   确认、从返回行退出和全部长度五的事件序列；任何状态越出 `48..55` 都失败；
5. 两次独立构建逐字节一致，修改只落在固定动图长度与数据区、四个挂接位置、载荷区和文件尾
   恢复校验；
6. 上述全部通过后，按当前自动安装门禁上传同一冻结成品、完整回读并在唯一测试设备安装一次，
   不再次询问用户。

当前冻结结果：提交 `ea169dab06c405cfe151a4c52ed84b7368f4bd71` 的两次完整固件构建逐字节
一致；成品长度 `6,804,520` 字节，SHA-256 为
`456008470df3cad464a60bb7432013686ad33c0f1f4abb9697cdb1badb3f697e`，MD5 为
`7f75f58e649c822e765bfa18f36c1a72`；设备端载荷 `435` 字节，固定空间剩余 `60,499` 字节。
页面开关模块 `7` 项、全项目 `98` 项、安装策略 `6` 项测试通过；`8` 个定向场景和全部长度五
的 `1,944` 条连续事件序列失败为 `0`。完整证据只见
[`FW-PAGE-003-A 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-003-A离线构建.md)。

当前真机安装结果：云端完整回读与冻结成品逐字节一致；设备只接受一次安装命令，在 `140`
秒完成安装、重启和重新上线。即时复查设备在线、更新空闲且没有最近更新错误。自动证据只见
[`FW-PAGE-003-A 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-003-A安装.md)，
物理验收仍按下段条件执行。

用户随后肉眼确认设备仍卡在开机动画，因此本方案已经否定，禁止再次安装。云端在线与运行
时间重置不能替代屏幕启动完成；失败事实只见
[`FW-PAGE-003-A 开机动画回归`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-003-A开机动画回归.md)。
回退后必须先把设置循环上限、序号 6 普通行创建、两个回调替换和载荷空间整理拆为更小的启动
实验并逐项更新 DESIGN，不能直接进入第二段。

该已否定方案原定的真机成功条件是：设备正常启动；设置页出现八项；新增行文字正确；左右都能
到达新增行和原厂返回行；确认新增行不打开页面、不退出设置、不死机；确认返回行仍退出；原厂
七项功能、一级页面、米家通信和官方更新不回归。实际在第一项即失败，后续条件均未执行。

#### 14.5.2 当前有效方案 `FW-PAGE-004-A`

启动修改的重新反汇编、可确认结论和不能指定单一根因的边界只见
[`FW-PAGE-003-A 启动修改分层复查`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-003-A启动修改分层复查.md)。
本阶段只验证当前已验收 `opt-setting.bin` 上的启动基础，不实现任何新的页面开关用户行为。

输入固定为只读 `opt-setting.bin`：长度 `6,804,520` 字节，SHA-256 为
`348d0843ac3f3f380eb155170c4104fd8467a018ddfd13670d67be998f269dc1`，MD5 为
`13a7286f4824b1ad87d9bc32f1d3d39c`。输出固定为
`ap01-1.0.2_0031-page-settings-startup-passthrough.bin`。

允许修改范围只有：

1. 按第 10.1 节固定工具、参数和双解码门禁无损整理原厂第一张动图，释放并使用
   `0x2475d0` 起的固定载荷空间；
2. 在固定载荷空间写入只包含序号分发的透传入口：序号 `6` 回到原厂返回行入口
   `0xa0198ad0`，其余原厂可达序号回到 `0xa01989e0`；
3. 只把文件偏移 `0x1999dc` 的原厂返回判断改为跳到该透传入口；
4. 按原格式重算文件尾恢复校验。

明确禁止修改：文件偏移 `0x1999b4` 的七项循环上限、`0x0be8cc` 与 `0x0be8d4` 的原厂设置
回调地址、任何设置行、设置对象、一级导航、AGENTS、网络、持久化和原厂页面交互。

实现归入 `features/primary_page_settings/`，文件固定为 `startup_passthrough.S`、
`startup_passthrough.ld`、`startup_passthrough.py` 和 `test_startup_passthrough.py`；应用薄入口
固定为 `page-settings-startup-passthrough-build`。该模块继续只依赖
`core/firmware_image/` 与 `core/firmware_payload_space/`。

刷前门禁固定为：输入身份和只读权限匹配；动图逐帧像素、时序和双解码一致；反汇编只有两个
原厂继续地址且没有调用；循环上限与设置回调四个原字节逐字节不变；七项创建分发模拟与原厂
逐序号等价；两次完整构建逐字节一致；修改区间外一致；文件尾恢复校验通过。全部通过后按现有
自动门禁上传、完整回读并单次安装，不再次询问用户。

真机成功条件只有：首次安装后正常进入一级页面、设备在线且更新空闲。设置列表和旋钮行为应与
当前 `opt-setting.bin` 完全相同。用户确认启动通过前不得制作 `FW-PAGE-004-B`；失败时立即
否定并回刷 `opt-setting.bin`。

当前离线实现已经在提交 `f4c5b6016525f9107dfc8b4910afcc0d7d234e49` 冻结，成品身份、修改
范围、测试、模拟和两次构建证据只见
[`FW-PAGE-004-A 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-A离线构建.md)。
单次安装、重启与重新上线证据只见
[`FW-PAGE-004-A 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-A安装.md)；
该自动证据不替代用户对正常一级页面的即时确认。

用户随后确认已经进入一级页面，A 阶段完成物理验收。该结论只批准进入第 14.5.3 节，不向
设置行创建或设置回调包装外推。

#### 14.5.3 当前有效方案 `FW-PAGE-004-B`

第八项容量、对象所有权、序号 6 普通行和序号 7 原厂返回行的唯一事实只见
[`设置列表第八项容量与生命周期静态复查`](../../knowledge/AP01-官方固件分析/cases/2026-07-30-设置列表第八项容量与生命周期静态复查.md)，
设置页实际注册回调和旋钮边界只见
[`原厂各页面物理旋钮交互实现`](../../knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md)
第 5 节。

输入固定为只读 A 阶段成品：长度 `6,804,520` 字节，SHA-256 为
`8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b`，MD5 为
`a066fab43085e8ab4c8b8aa33ff8af6f`。输出固定为
`ap01-1.0.2_0031-page-settings-row-creation.bin`。

本阶段只允许在 A 阶段上增加三项修改：

1. 把文件偏移 `0x1999b4` 的设置循环上限从 7 改为 8；
2. 替换 `0x2475d0` 起的 A 阶段透传载荷：序号 1 至 5 仍回到 `0xa01989e0`；序号 6 以原父
   列表 `x9`、固定标签“开关一级页面”和原参数调用原厂普通行入口 `0xa0192fb4`，完成后回到
   `0xa01989b2`；序号 7 回到 `0xa0198ad0`，继续由原厂入口创建返回行；
3. 按原格式重算文件尾恢复校验。

A 阶段在 `0x1999dc` 的局部分发挂接保持不变。`0x0be8cc`、`0x0be8d4` 的原厂设置回调地址
必须逐字节不变；一级窗口、九个一级对象、一级回调、网络、AGENTS、持久化与其他原厂页面均
禁止修改。

实现归入 `features/primary_page_settings/`，新增 `row_creation.S`、`row_creation.ld`、
`row_creation.py` 和 `test_row_creation.py`；应用薄入口固定为
`page-settings-row-creation-build`。模块只依赖 `core/firmware_image/` 与
`core/firmware_payload_space/` 已有常量。

刷前门禁固定为：A 阶段输入身份和只读权限匹配；新载荷只调用一次 `0xa0192fb4`；序号 6、7
不读取原厂六项标签表；序号 0 至 5 原创建路径不变；原厂回调地址不变；一级窗口与九个一级
对象区间不变；两次载荷链接和两次完整构建逐字节一致；修改区间外一致；文件尾恢复校验通过。

本阶段的临时交互边界是：原厂回调仍把状态 54 当成原厂返回项，因此旋转到新增行后按下会按
原厂行为退出设置，序号 7 的返回行暂时不能由原厂回调选中。这不是页面开关成品行为，只用于
隔离创建阶段，C 阶段才补两项相邻选择和新增行确认。

真机成功条件只有：正常进入一级页面；进入设置后存在八项，“开关一级页面”位于原厂返回行
之前；旋转可选中新增行；不要求按下新增行。用户确认这三项前不得制作 `FW-PAGE-004-C`；启动
失败时回刷 A 阶段成品，行创建或布局失败时记录现场后回刷 A 阶段成品。

当前 B 阶段已经在提交 `d9ab9238d282308227fd51feeb052338e6abf0d2` 冻结；成品身份、反汇编、
测试、模拟、修改范围和两次构建证据只见
[`FW-PAGE-004-B 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-B离线构建.md)。
单次安装、重启与重新上线证据只见
[`FW-PAGE-004-B 安装`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-B安装.md)；
该自动证据不替代新增行物理确认。

用户随后确认 B 阶段卡在开机动画。失败与回退事实只见
[`FW-PAGE-004-B 开机动画回归`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-B开机动画回归.md)。
本节方案已否定，B 阶段成品禁止再次安装。

#### 14.5.4 当前有效方案 `FW-PAGE-005-A`

本方案继续遵守
[`原厂各页面物理旋钮交互实现`](../../knowledge/AP01-官方固件分析/原厂各页面物理旋钮交互实现.md)
第 5 节，并以
[`FW-PAGE-004-B 开机动画回归`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-004-B开机动画回归.md)
第 3 节的原厂循环复查为直接依据。

输入重新固定为只读 A 阶段成品：长度 `6,804,520` 字节，SHA-256 为
`8986ceb31b7d26802bb06fc62a23f5c29eb00f089dc86a5a40d33d95b4dd345b`，MD5 为
`a066fab43085e8ab4c8b8aa33ff8af6f`。输出固定为
`ap01-1.0.2_0031-page-settings-single-iteration-append.bin`。

允许修改只有：

1. 替换 `0x2475d0` 起的 A 阶段透传载荷；
2. 按原格式更新文件尾恢复校验。

文件偏移 `0x1999b4` 必须保持原厂七项循环字节 `9d47`，`0x1999dc` 的 A 阶段分发挂接保持
不变。载荷对序号 1 至 5 仍返回 `0xa01989e0`；序号 6 在同一次分支内：

1. 以原父列表 `x9`、固定标签“开关一级页面”和原参数调用一次 `0xa0192fb4`；
2. 再以同一父列表和原参数调用一次原厂返回行入口 `0xa0193058`；
3. 回到 `0xa01989b2`，让原厂序号加到 7 后按原循环上限退出；
4. 不存在序号 7 的载荷分支。

`0x0be8cc`、`0x0be8d4` 的原厂设置回调地址、一级窗口、九个一级对象、一级回调、AGENTS、
网络、持久化和其他原厂页面均禁止修改。

实现归入 `features/primary_page_settings/`，新增 `single_iteration_append.S`、
`single_iteration_append.ld`、`single_iteration_append.py` 和
`test_single_iteration_append.py`；应用薄入口固定为
`page-settings-single-iteration-append-build`。模块只依赖现有固件镜像与载荷空间能力。

刷前门禁固定为：A 阶段输入身份与只读权限匹配；循环上限逐字节不变；载荷只调用一次普通行
入口和一次原厂返回行入口；没有序号 7 分支；设置回调与一级页面区间不变；两次载荷链接和两次
完整构建逐字节一致；修改区间外一致；文件尾恢复校验通过。

本阶段已按上述门禁冻结构建，成品指纹、修改范围、测试与刷前设备状态只见
[`FW-PAGE-005-A 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-005-A离线构建.md)。

真机成功条件是：正常进入一级页面；设置列表存在八个项目；“开关一级页面”位于原厂返回行
之前且可选中。本阶段仍保持原厂设置回调，暂不要求按下新增行。任一启动条件失败时立即否定本
方案、回刷 A 阶段，并停止继续使用启动期原厂列表扩展结构；通过后才允许设计设置回调包装。

#### 14.5.5 当前有效方案 `FW-PAGE-006-A`

本方案以
[`FW-PAGE-005-A 开机动画回归与双调用者定位`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-005-A开机动画回归与双调用者定位.md)
为唯一新增原厂证据。`0xa01988d4` 的开机调用位于 `0xa00b3ffe`，调用后地址为
`0xa00b4002`；用户确认进入设置时的第二次调用位于 `0xa00bd9e6`，调用后地址为
`0xa00bd9ea`。创建函数在当前栈偏移 `+124` 保存调用者返回地址。

输入继续固定为只读 A 阶段成品。输出固定为
`ap01-1.0.2_0031-page-settings-delayed-row-creation.bin`。文件偏移 `0x1999b4` 的原厂七次
循环字节、`0x1999dc` 的 A 阶段分发挂接和两个原厂设置回调地址均保持不变。只允许替换
`0x2475d0` 起的 A 阶段载荷并更新文件尾恢复校验。

新载荷对序号 1 至 5 仍返回 `0xa01989e0`。序号 6 必须先读取当前栈偏移 `+124`：

1. 返回地址不是 `0xa00bd9ea` 时直接跳回原厂返回行路径 `0xa0198ad0`；开机调用的
   `0xa00b4002` 必须走该路径，不调用普通行创建入口；
2. 返回地址是 `0xa00bd9ea` 时，以原父列表 `x9`、固定标签“开关一级页面”和原参数调用一次
   `0xa0192fb4`，再调用一次 `0xa0193058`，最后回到 `0xa01989b2`；
3. 不修改循环上限，不存在序号 7 分支，不替换调用点或设置回调。

实现仍归入 `features/primary_page_settings/`，新增 `delayed_row_creation.S`、
`delayed_row_creation.ld`、`delayed_row_creation.py` 和
`test_delayed_row_creation.py`；应用入口固定为
`page-settings-delayed-row-creation-build`。模块只依赖现有固件镜像与载荷空间能力。

刷前模拟必须把两个调用者分开：开机调用只创建七个原厂项目，普通行新增调用数为零；设置进入
调用创建八个项目，新增普通行和原厂返回行各调用一次。反汇编还必须证明保存返回地址读取偏移为
`124`、两个固定调用后地址存在、开机路径可直接到达 `0xa0198ad0`、设置回调与循环上限不变、
两次完整构建逐字节一致且修改区间外一致。

真机分两步验收：先确认正常进入一级页面；再进入设置，确认八项和“开关一级页面”位于原厂
返回行之前。本阶段不按下新增行。若开机失败，否定返回地址门并回刷 A 阶段；若开机通过但进入
设置失败，停止调用原厂普通行创建入口，不再制作设置列表追加候选。只有两步都通过，才允许先写
下一阶段设置回调 DESIGN。

本阶段已经按上述门禁冻结构建，成品指纹、反汇编、双调用者模拟与 A 阶段回刷状态只见
[`FW-PAGE-006-A 离线构建`](../../knowledge/AP01-官方固件分析/cases/2026-08-03-FW-PAGE-006-A离线构建.md)。

### 14.6 服务端与最终组合

设备交互通过后，再补：

1. 四类主机的同一服务入口、系统重启恢复和最后成功结果；
2. 设备端多个服务位置按顺序切换；
3. 字段级“无法获取”、断网和不完整结果保持；
4. 四页真机画质、五分钟刷新和全部详情旋钮路径；
5. 从同一原厂基线确定性组合完整成品；
6. 第 11 节全部验证、原厂回刷、独立恢复和发布验收。

完整优化版之前的任何候选都必须保持实验名称，不得使用最终成品名称。

完成第 6 节逐回调审计与代码纠偏后，才允许按 `FW-INTEGRATION-003` 生成一份先完成离线
验证、随后只能按《固件制作规范》第 7.1 节持续实验授权进行自动单次安装验证的完整集成候选。该候选
必须同时满足：

1. 输入为 `FW-MATERIAL-001` 生成的只读 `opt-setting.bin`；
2. 只挂接原厂萌宠左旋、右旋、确认三个局部分支，不恢复已经否定的新增对象、总分派、总回调替换或重复功率左键方案；
3. 页面开关只使用当前已记录的四个固定挂接点和独立双记录保存实现；
4. 清单类型固定为 `opt-rewrite-candidate-firmware`，状态固定为
   `built-not-approved-for-installation`；
5. 清单逐项列出已组合范围、待真机验证范围和全部修改区间；
6. 构建入口和清单都把 `installation_allowed` 固定为 `false`，用于阻止普通安装和发布；
   专用测试设备只有在持续实验授权、唯一设备、唯一指纹和严格模拟全部匹配时才能自动安装；
7. 输出名固定为 `ap01-1.0.2_0031-opt-rewrite-candidate.bin`，不覆盖当前被历史失败成品占用的最终文件名；
8. 分别核对一级窗口与系统设置回调的键值分支，不存在跨回调共用的全局方向解释；
9. 一级窗口只调用唯一事实原文列出的实际状态与切换入口，链接进成品的旧导航实现不可达且
   不得被清单批准。

逐次实验必须先完成同一上传对象的完整回读，再即时展示设备、成品、已通过检查、
未完成恢复条件和回退材料。只有用户在展示后明确确认这一次安装，执行端才能人工下发一次；
确认不写回冻结构建清单，也不得解除普通安装与发布门禁。

该候选即使全部离线检查通过，也不能自动变为 `FW-FINAL-001`。只有第 14.2 至 14.6 节要求的
真机交互、原厂回归、回刷和恢复证据齐全，先更新本 DESIGN 后，才能启用最终名称和安装权限。
